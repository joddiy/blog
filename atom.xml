<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>Jz Blog</title>
  
  <subtitle>Trivias</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="https://joddiy.github.io/"/>
  <updated>2020-05-09T13:00:32.825Z</updated>
  <id>https://joddiy.github.io/</id>
  
  <author>
    <name>Joddiy Zhang</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>TensorFlow graph optimization with Grappler</title>
    <link href="https://joddiy.github.io/2020/05/09/TensorFlow-graph-optimization-with-Grappler/"/>
    <id>https://joddiy.github.io/2020/05/09/TensorFlow-graph-optimization-with-Grappler/</id>
    <published>2020-05-09T09:51:56.000Z</published>
    <updated>2020-05-09T13:00:32.825Z</updated>
    
    <summary type="html">
    
      Study notes of Tensorflow 2.0
    
    </summary>
    
    
      <category term="Study Notes" scheme="https://joddiy.github.io/categories/Study-Notes/"/>
    
    
      <category term="Tensorflow" scheme="https://joddiy.github.io/tags/Tensorflow/"/>
    
      <category term="Deep Learning" scheme="https://joddiy.github.io/tags/Deep-Learning/"/>
    
      <category term="Tools" scheme="https://joddiy.github.io/tags/Tools/"/>
    
  </entry>
  
  <entry>
    <title>Better performance with tf.function</title>
    <link href="https://joddiy.github.io/2020/05/09/Better-performance-with-tf-function/"/>
    <id>https://joddiy.github.io/2020/05/09/Better-performance-with-tf-function/</id>
    <published>2020-05-09T09:51:39.000Z</published>
    <updated>2020-05-09T13:00:21.409Z</updated>
    
    <summary type="html">
    
      Study notes of Tensorflow 2.0
    
    </summary>
    
    
      <category term="Study Notes" scheme="https://joddiy.github.io/categories/Study-Notes/"/>
    
    
      <category term="Tensorflow" scheme="https://joddiy.github.io/tags/Tensorflow/"/>
    
      <category term="Deep Learning" scheme="https://joddiy.github.io/tags/Deep-Learning/"/>
    
      <category term="Tools" scheme="https://joddiy.github.io/tags/Tools/"/>
    
  </entry>
  
  <entry>
    <title>Distributed training with TensorFlow</title>
    <link href="https://joddiy.github.io/2020/05/09/Distributed-training-with-TensorFlow/"/>
    <id>https://joddiy.github.io/2020/05/09/Distributed-training-with-TensorFlow/</id>
    <published>2020-05-09T09:22:15.000Z</published>
    <updated>2020-05-09T21:03:44.187Z</updated>
    
    <content type="html"><![CDATA[<h1 id="Distributed-training-with-TensorFlow"><a href="#Distributed-training-with-TensorFlow" class="headerlink" title="Distributed training with TensorFlow"></a>Distributed training with TensorFlow</h1><p><a href="https://www.tensorflow.org/guide/distributed_training" target="_blank" rel="noopener">doc link</a></p><h2 id="Overview"><a href="#Overview" class="headerlink" title="Overview"></a>Overview</h2><p><code>tf.distribute.Strategy</code> is a TensorFlow API to distribute training across multiple GPUs, multiple machines or TPUs. Using this API, you can distribute your existing models and training code with minimal code changes.</p><hr><h2 id="Types-of-strategies"><a href="#Types-of-strategies" class="headerlink" title="Types of strategies"></a>Types of strategies</h2><p>tf.distribute.Strategy intends to cover a number of use cases along different axes. Some of these combinations are currently supported and others will be added in the future. Some of these axes are:</p><ul><li><p>Synchronous vs asynchronous training: These are two common ways of distributing training with data parallelism. In sync training, all workers train over different slices of input data in sync, and aggregating gradients at each step. In async training, all workers are independently training over the input data and updating variables asynchronously. Typically sync training is supported via all-reduce and async through parameter server architecture.</p></li><li><p>Hardware platform: You may want to scale your training onto multiple GPUs on one machine, or multiple machines in a network (with 0 or more GPUs each), or on Cloud TPUs.</p></li></ul><table><thead><tr><th>Training API</th><th>MirroredStrategy</th><th>TPUStrategy</th><th>MultiWorkerMirroredStrategy</th></tr></thead><tbody><tr><td>Keras API</td><td>Supported</td><td>Experimental support</td><td>Experimental support</td></tr><tr><td>Custom training loop</td><td>Experimental support</td><td>Experimental support</td><td>Support planned post 2.0</td></tr><tr><td>Estimator API</td><td>Limited Support</td><td>Not supported</td><td>Limited Support</td></tr></tbody></table><table><thead><tr><th>CentralStorageStrategy</th><th>ParameterServerStrategy</th><th>OneDeviceStrategy</th></tr></thead><tbody><tr><td>Experimental support</td><td>Supported planned post 2.0</td><td>Supported</td></tr><tr><td>Support</td><td>planned post 2.0</td><td>No support yet</td></tr><tr><td>Limited Support</td><td>Limited Support</td><td>Limited Support</td></tr></tbody></table><h3 id="MirroredStrategy"><a href="#MirroredStrategy" class="headerlink" title="MirroredStrategy"></a>MirroredStrategy</h3><p>tf.distribute.MirroredStrategy supports synchronous distributed training on multiple GPUs on one machine. It creates one replica per GPU device. Each variable in the model is mirrored across all the replicas. Together, these variables form a single conceptual variable called MirroredVariable. These variables are kept in sync with each other by applying identical updates.</p><p>Efficient <strong>all-reduce</strong> algorithms are used to communicate the variable updates across the devices.</p><p>All-reduce aggregates tensors across all the devices by adding them up, and makes them available on each device. It’s a fused algorithm that is very efficient and can reduce the overhead of synchronization significantly. </p><p>There are many all-reduce algorithms and implementations available, depending on the type of communication available between devices. By default, it uses NVIDIA NCCL as the all-reduce implementation. You can choose from a few other options we provide, or write your own.</p><p>Here is the simplest way of creating MirroredStrategy:</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">mirrored_strategy = tf.distribute.MirroredStrategy()</span><br></pre></td></tr></table></figure><p>This will create a MirroredStrategy instance which will use all the GPUs that are visible to TensorFlow, and use NCCL as the cross device communication.</p><p>If you wish to use only some of the GPUs on your machine, you can do so like this:</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">mirrored_strategy = tf.distribute.MirroredStrategy(devices=[<span class="string">"/gpu:0"</span>, <span class="string">"/gpu:1"</span>])</span><br></pre></td></tr></table></figure><p>Currently, tf.distribute.HierarchicalCopyAllReduce and tf.distribute.ReductionToOneDevice are two options other than tf.distribute.NcclAllReduce which is the default.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">mirrored_strategy = tf.distribute.MirroredStrategy(</span><br><span class="line">    cross_device_ops=tf.distribute.HierarchicalCopyAllReduce())</span><br></pre></td></tr></table></figure><h3 id="CentralStorageStrategy"><a href="#CentralStorageStrategy" class="headerlink" title="CentralStorageStrategy"></a>CentralStorageStrategy</h3><p>tf.distribute.experimental.CentralStorageStrategy does synchronous training as well. Variables are not mirrored, instead they are placed on the CPU and operations are replicated across all local GPUs. If there is only one GPU, all variables and operations will be placed on that GPU.</p><p>Create an instance of CentralStorageStrategy by:</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">central_storage_strategy = tf.distribute.experimental.CentralStorageStrategy()</span><br></pre></td></tr></table></figure><h3 id="MultiWorkerMirroredStrategy"><a href="#MultiWorkerMirroredStrategy" class="headerlink" title="MultiWorkerMirroredStrategy"></a>MultiWorkerMirroredStrategy</h3><p>tf.distribute.experimental.MultiWorkerMirroredStrategy is very similar to MirroredStrategy. It implements synchronous distributed training across multiple workers, each with potentially multiple GPUs. Similar to MirroredStrategy, it creates copies of all variables in the model on each device across all workers.</p><p>It uses CollectiveOps as the multi-worker all-reduce communication method used to keep variables in sync. </p><p>A collective op is a single op in the TensorFlow graph which can automatically choose an all-reduce algorithm in the TensorFlow runtime according to hardware, network topology and tensor sizes.</p><p>It also implements additional performance optimizations. For example, it includes a static optimization that converts multiple all-reductions on small tensors into fewer all-reductions on larger tensors.</p><div class="alert warning"><p>How?</p></div><p>MultiWorkerMirroredStrategy currently allows you to choose between two different implementations of collective ops. CollectiveCommunication.RING implements ring-based collectives using gRPC as the communication layer. CollectiveCommunication.NCCL uses Nvidia’s NCCL to implement collectives.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">multiworker_strategy = tf.distribute.experimental.MultiWorkerMirroredStrategy(</span><br><span class="line">    tf.distribute.experimental.CollectiveCommunication.NCCL)</span><br></pre></td></tr></table></figure><h3 id="TPUStrategy"><a href="#TPUStrategy" class="headerlink" title="TPUStrategy"></a>TPUStrategy</h3><p>tf.distribute.experimental.TPUStrategy lets you run your TensorFlow training on Tensor Processing Units (TPUs). </p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">cluster_resolver = tf.distribute.cluster_resolver.TPUClusterResolver(</span><br><span class="line">    tpu=tpu_address)</span><br><span class="line">tf.config.experimental_connect_to_cluster(cluster_resolver)</span><br><span class="line">tf.tpu.experimental.initialize_tpu_system(cluster_resolver)</span><br><span class="line">tpu_strategy = tf.distribute.experimental.TPUStrategy(cluster_resolver)</span><br></pre></td></tr></table></figure><h3 id="ParameterServerStrategy"><a href="#ParameterServerStrategy" class="headerlink" title="ParameterServerStrategy"></a>ParameterServerStrategy</h3><p>tf.distribute.experimental.ParameterServerStrategy supports parameter servers training on multiple machines. In this setup, some machines are designated as workers and some as parameter servers. Each variable of the model is placed on one parameter server. Computation is replicated across all GPUs of all the workers.</p><h3 id="OneDeviceStrategy"><a href="#OneDeviceStrategy" class="headerlink" title="OneDeviceStrategy"></a>OneDeviceStrategy</h3><p>tf.distribute.OneDeviceStrategy runs on a single device. This strategy will place any variables created in its scope on the specified device. Input distributed through this strategy will be prefetched to the specified device. Moreover, any functions called via strategy.run will also be placed on the specified device.</p><p>You can use this strategy to test your code before switching to other strategies which actually distributes to multiple devices/machines.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">strategy = tf.distribute.OneDeviceStrategy(device=<span class="string">"/gpu:0"</span>)</span><br></pre></td></tr></table></figure><hr><h2 id="Using-tf-distribute-Strategy-with-Keras"><a href="#Using-tf-distribute-Strategy-with-Keras" class="headerlink" title="Using tf.distribute.Strategy with Keras"></a>Using tf.distribute.Strategy with Keras</h2><p>By integrating into tf.keras backend, we’ve made it seamless for you to distribute your training written in the Keras training framework.</p><p>Here’s what you need to change in your code:</p><ul><li>Create an instance of the appropriate tf.distribute.Strategy</li><li>Move the creation and compiling of Keras model inside strategy.scope.</li></ul><p>We support all types of Keras models - sequential, functional and subclassed.</p><p>Here is a snippet of code to do this for a very simple Keras model with one dense layer:</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">mirrored_strategy = tf.distribute.MirroredStrategy()</span><br><span class="line"><span class="keyword">with</span> mirrored_strategy.scope():</span><br><span class="line">  model = tf.keras.Sequential([tf.keras.layers.Dense(<span class="number">1</span>, input_shape=(<span class="number">1</span>,))])</span><br><span class="line">  model.compile(loss=<span class="string">'mse'</span>, optimizer=<span class="string">'sgd'</span>)</span><br></pre></td></tr></table></figure><p>In this example we used MirroredStrategy so we can run this on a machine with multiple GPUs. </p><p>strategy.scope() indicated which parts of the code to run distributed. Creating a model inside this scope allows us to create mirrored variables instead of regular variables. Compiling under the scope allows us to know that the user intends to train this model using this strategy. Once this is set up, you can fit your model like you would normally. </p><p>MirroredStrategy takes care of replicating the model’s training on the available GPUs, aggregating gradients, and more.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># tf.data.Dataset as dataset</span></span><br><span class="line">dataset = tf.data.Dataset.from_tensors(([<span class="number">1.</span>], [<span class="number">1.</span>])).repeat(<span class="number">100</span>).batch(<span class="number">10</span>)</span><br><span class="line">model.fit(dataset, epochs=<span class="number">2</span>)</span><br><span class="line">model.evaluate(dataset)</span><br><span class="line"></span><br><span class="line"><span class="comment"># np array as dataset</span></span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line">inputs, targets = np.ones((<span class="number">100</span>, <span class="number">1</span>)), np.ones((<span class="number">100</span>, <span class="number">1</span>))</span><br><span class="line">model.fit(inputs, targets, epochs=<span class="number">2</span>, batch_size=<span class="number">10</span>)</span><br></pre></td></tr></table></figure><p>In both cases (dataset or numpy), each batch of the given input is divided equally among the multiple replicas. For instance, if using MirroredStrategy with 2 GPUs, each batch of size 10 will get divided among the 2 GPUs, with each receiving 5 input examples in each step. </p><p>Each epoch will then train faster as you add more GPUs. Typically, you would want to increase your batch size as you add more accelerators so as to make effective use of the extra computing power. You will also need to re-tune your learning rate, depending on the model. </p><div class="alert warning"><p>How to merge these replicated models?</p></div><p>You can use strategy.num_replicas_in_sync to get the number of replicas.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Compute global batch size using number of replicas.</span></span><br><span class="line">BATCH_SIZE_PER_REPLICA = <span class="number">5</span></span><br><span class="line">global_batch_size = (BATCH_SIZE_PER_REPLICA *</span><br><span class="line">                     mirrored_strategy.num_replicas_in_sync)</span><br><span class="line">dataset = tf.data.Dataset.from_tensors(([<span class="number">1.</span>], [<span class="number">1.</span>])).repeat(<span class="number">100</span>)</span><br><span class="line">dataset = dataset.batch(global_batch_size)</span><br><span class="line"></span><br><span class="line">LEARNING_RATES_BY_BATCH_SIZE = &#123;<span class="number">5</span>: <span class="number">0.1</span>, <span class="number">10</span>: <span class="number">0.15</span>&#125;</span><br><span class="line">learning_rate = LEARNING_RATES_BY_BATCH_SIZE[global_batch_size]</span><br></pre></td></tr></table></figure><hr><h2 id="Using-tf-distribute-Strategy-with-custom-training-loops"><a href="#Using-tf-distribute-Strategy-with-custom-training-loops" class="headerlink" title="Using tf.distribute.Strategy with custom training loops"></a>Using tf.distribute.Strategy with custom training loops</h2><p>If you need more flexibility and control over your training loops than is possible with Estimator or Keras, you can write custom training loops. </p><p>For instance, when using a GAN, you may want to take a different number of generator or discriminator steps each round. Similarly, the high level frameworks are not very suitable for Reinforcement Learning training.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">with</span> mirrored_strategy.scope():</span><br><span class="line">  model = tf.keras.Sequential([tf.keras.layers.Dense(<span class="number">1</span>, input_shape=(<span class="number">1</span>,))])</span><br><span class="line">  optimizer = tf.keras.optimizers.SGD()</span><br><span class="line"></span><br><span class="line">dataset = tf.data.Dataset.from_tensors(([<span class="number">1.</span>], [<span class="number">1.</span>])).repeat(<span class="number">1000</span>).batch(</span><br><span class="line">    global_batch_size)</span><br><span class="line">dist_dataset = mirrored_strategy.experimental_distribute_dataset(dataset)</span><br><span class="line"></span><br><span class="line"><span class="meta">@tf.function</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">train_step</span><span class="params">(dist_inputs)</span>:</span></span><br><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">step_fn</span><span class="params">(inputs)</span>:</span></span><br><span class="line">    features, labels = inputs</span><br><span class="line"></span><br><span class="line">    <span class="keyword">with</span> tf.GradientTape() <span class="keyword">as</span> tape:</span><br><span class="line">      <span class="comment"># training=True is only needed if there are layers with different</span></span><br><span class="line">      <span class="comment"># behavior during training versus inference (e.g. Dropout).</span></span><br><span class="line">      logits = model(features, training=<span class="literal">True</span>)</span><br><span class="line">      cross_entropy = tf.nn.softmax_cross_entropy_with_logits(</span><br><span class="line">          logits=logits, labels=labels)</span><br><span class="line">      <span class="comment"># scaled the total loss by the global batch size</span></span><br><span class="line">      loss = tf.reduce_sum(cross_entropy) * (<span class="number">1.0</span> / global_batch_size)</span><br><span class="line"></span><br><span class="line">    grads = tape.gradient(loss, model.trainable_variables)</span><br><span class="line">    optimizer.apply_gradients(list(zip(grads, model.trainable_variables)))</span><br><span class="line">    <span class="keyword">return</span> cross_entropy</span><br><span class="line"></span><br><span class="line">  per_example_losses = mirrored_strategy.run(step_fn, args=(dist_inputs,))</span><br><span class="line">  mean_loss = mirrored_strategy.reduce(</span><br><span class="line">      tf.distribute.ReduceOp.MEAN, per_example_losses, axis=<span class="number">0</span>)</span><br><span class="line">  <span class="keyword">return</span> mean_loss</span><br><span class="line"></span><br><span class="line"> <span class="keyword">with</span> mirrored_strategy.scope():</span><br><span class="line">  <span class="keyword">for</span> inputs <span class="keyword">in</span> dist_dataset:</span><br><span class="line">    print(train_step(inputs))</span><br></pre></td></tr></table></figure><hr><h2 id="Using-tf-distribute-Strategy-with-Estimator-Limited-support"><a href="#Using-tf-distribute-Strategy-with-Estimator-Limited-support" class="headerlink" title="Using tf.distribute.Strategy with Estimator (Limited support)"></a>Using tf.distribute.Strategy with Estimator (Limited support)</h2><p>tf.estimator is a distributed training TensorFlow API that originally supported the async parameter server approach. </p><p>We pass the strategy object into the RunConfig for the Estimator.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">mirrored_strategy = tf.distribute.MirroredStrategy()</span><br><span class="line">config = tf.estimator.RunConfig(</span><br><span class="line">    train_distribute=mirrored_strategy, eval_distribute=mirrored_strategy)</span><br><span class="line">regressor = tf.estimator.LinearRegressor(</span><br><span class="line">    feature_columns=[tf.feature_column.numeric_column(<span class="string">'feats'</span>)],</span><br><span class="line">    optimizer=<span class="string">'SGD'</span>,</span><br><span class="line">    config=config)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">input_fn</span><span class="params">()</span>:</span></span><br><span class="line">  dataset = tf.data.Dataset.from_tensors((&#123;<span class="string">"feats"</span>:[<span class="number">1.</span>]&#125;, [<span class="number">1.</span>]))</span><br><span class="line">  <span class="keyword">return</span> dataset.repeat(<span class="number">1000</span>).batch(<span class="number">10</span>)</span><br><span class="line">regressor.train(input_fn=input_fn, steps=<span class="number">10</span>)</span><br><span class="line">regressor.evaluate(input_fn=input_fn, steps=<span class="number">10</span>)</span><br></pre></td></tr></table></figure><hr><p>Another difference to highlight here between Estimator and Keras is the input handling. In Estimator, we do not do automatic splitting of batch, nor automatically shard the data across different workers. You have full control over how you want your data to be distributed across workers and devices, and you must provide an input_fn to specify how to distribute your data.</p><p>Your input_fn is called once per worker, thus giving one dataset per worker. Then one batch from that dataset is fed to one replica on that worker, thereby consuming N batches for N replicas on 1 worker. In other words, the dataset returned by the input_fn should provide batches of size PER_REPLICA_BATCH_SIZE. And the global batch size for a step can be obtained as PER_REPLICA_BATCH_SIZE * strategy.num_replicas_in_sync.</p><h2 id="Use-a-GPU"><a href="#Use-a-GPU" class="headerlink" title="Use a GPU"></a>Use a GPU</h2><p>This guide is for users who have tried above approaches and found that they need fine-grained control of how TensorFlow uses the GPU.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> tensorflow <span class="keyword">as</span> tf</span><br><span class="line">print(<span class="string">"Num GPUs Available: "</span>, len(tf.config.experimental.list_physical_devices(<span class="string">'GPU'</span>)))</span><br></pre></td></tr></table></figure><h3 id="Logging-device-placement"><a href="#Logging-device-placement" class="headerlink" title="Logging device placement"></a>Logging device placement</h3><p>To find out which devices your operations and tensors are assigned to, put tf.debugging.set_log_device_placement(True) as the first statement of your program. Enabling device placement logging causes any Tensor allocations or operations to be printed.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">tf.debugging.set_log_device_placement(<span class="literal">True</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># Create some tensors</span></span><br><span class="line">a = tf.constant([[<span class="number">1.0</span>, <span class="number">2.0</span>, <span class="number">3.0</span>], [<span class="number">4.0</span>, <span class="number">5.0</span>, <span class="number">6.0</span>]])</span><br><span class="line">b = tf.constant([[<span class="number">1.0</span>, <span class="number">2.0</span>], [<span class="number">3.0</span>, <span class="number">4.0</span>], [<span class="number">5.0</span>, <span class="number">6.0</span>]])</span><br><span class="line">c = tf.matmul(a, b)</span><br><span class="line"></span><br><span class="line">print(c)</span><br></pre></td></tr></table></figure><h3 id="Manual-device-placement"><a href="#Manual-device-placement" class="headerlink" title="Manual device placement"></a>Manual device placement</h3><p>If you would like a particular operation to run on a device of your choice instead of what’s automatically selected for you, you can use with tf.device to create a device context, and all the operations within that context will run on the same designated device.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">tf.debugging.set_log_device_placement(<span class="literal">True</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># Place tensors on the CPU</span></span><br><span class="line"><span class="keyword">with</span> tf.device(<span class="string">'/CPU:0'</span>):</span><br><span class="line">  a = tf.constant([[<span class="number">1.0</span>, <span class="number">2.0</span>, <span class="number">3.0</span>], [<span class="number">4.0</span>, <span class="number">5.0</span>, <span class="number">6.0</span>]])</span><br><span class="line">  b = tf.constant([[<span class="number">1.0</span>, <span class="number">2.0</span>], [<span class="number">3.0</span>, <span class="number">4.0</span>], [<span class="number">5.0</span>, <span class="number">6.0</span>]])</span><br><span class="line"></span><br><span class="line">c = tf.matmul(a, b)</span><br><span class="line">print(c)</span><br></pre></td></tr></table></figure><h3 id="Limiting-GPU-memory-growth"><a href="#Limiting-GPU-memory-growth" class="headerlink" title="Limiting GPU memory growth"></a>Limiting GPU memory growth</h3><p>By default, TensorFlow maps nearly all of the GPU memory of all GPUs (subject to CUDA_VISIBLE_DEVICES) visible to the process. This is done to more efficiently use the relatively precious GPU memory resources on the devices by reducing memory fragmentation. To limit TensorFlow to a specific set of GPUs we use the tf.config.experimental.set_visible_devices method.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">gpus = tf.config.experimental.list_physical_devices(<span class="string">'GPU'</span>)</span><br><span class="line"><span class="keyword">if</span> gpus:</span><br><span class="line">  <span class="comment"># Restrict TensorFlow to only use the first GPU</span></span><br><span class="line">  <span class="keyword">try</span>:</span><br><span class="line">    tf.config.experimental.set_visible_devices(gpus[<span class="number">0</span>], <span class="string">'GPU'</span>)</span><br><span class="line">    logical_gpus = tf.config.experimental.list_logical_devices(<span class="string">'GPU'</span>)</span><br><span class="line">    print(len(gpus), <span class="string">"Physical GPUs,"</span>, len(logical_gpus), <span class="string">"Logical GPU"</span>)</span><br><span class="line">  <span class="keyword">except</span> RuntimeError <span class="keyword">as</span> e:</span><br><span class="line">    <span class="comment"># Visible devices must be set before GPUs have been initialized</span></span><br><span class="line">    print(e)</span><br></pre></td></tr></table></figure><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">gpus = tf.config.experimental.list_physical_devices(<span class="string">'GPU'</span>)</span><br><span class="line"><span class="keyword">if</span> gpus:</span><br><span class="line">  <span class="keyword">try</span>:</span><br><span class="line">    <span class="comment"># Currently, memory growth needs to be the same across GPUs</span></span><br><span class="line">    <span class="keyword">for</span> gpu <span class="keyword">in</span> gpus:</span><br><span class="line">      tf.config.experimental.set_memory_growth(gpu, <span class="literal">True</span>)</span><br><span class="line">    logical_gpus = tf.config.experimental.list_logical_devices(<span class="string">'GPU'</span>)</span><br><span class="line">    print(len(gpus), <span class="string">"Physical GPUs,"</span>, len(logical_gpus), <span class="string">"Logical GPUs"</span>)</span><br><span class="line">  <span class="keyword">except</span> RuntimeError <span class="keyword">as</span> e:</span><br><span class="line">    <span class="comment"># Memory growth must be set before GPUs have been initialized</span></span><br><span class="line">    print(e)</span><br></pre></td></tr></table></figure><p>The second method is to configure a virtual GPU device with tf.config.experimental.set_virtual_device_configuration and set a hard limit on the total memory to allocate on the GPU.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">gpus = tf.config.experimental.list_physical_devices(<span class="string">'GPU'</span>)</span><br><span class="line"><span class="keyword">if</span> gpus:</span><br><span class="line">  <span class="comment"># Restrict TensorFlow to only allocate 1GB of memory on the first GPU</span></span><br><span class="line">  <span class="keyword">try</span>:</span><br><span class="line">    tf.config.experimental.set_virtual_device_configuration(</span><br><span class="line">        gpus[<span class="number">0</span>],</span><br><span class="line">        [tf.config.experimental.VirtualDeviceConfiguration(memory_limit=<span class="number">1024</span>)])</span><br><span class="line">    logical_gpus = tf.config.experimental.list_logical_devices(<span class="string">'GPU'</span>)</span><br><span class="line">    print(len(gpus), <span class="string">"Physical GPUs,"</span>, len(logical_gpus), <span class="string">"Logical GPUs"</span>)</span><br><span class="line">  <span class="keyword">except</span> RuntimeError <span class="keyword">as</span> e:</span><br><span class="line">    <span class="comment"># Virtual devices must be set before GPUs have been initialized</span></span><br><span class="line">    print(e)</span><br></pre></td></tr></table></figure><h3 id="Using-a-single-GPU-on-a-multi-GPU-system"><a href="#Using-a-single-GPU-on-a-multi-GPU-system" class="headerlink" title="Using a single GPU on a multi-GPU system"></a>Using a single GPU on a multi-GPU system</h3><p>If you have more than one GPU in your system, the GPU with the lowest ID will be selected by default. If you would like to run on a different GPU, you will need to specify the preference explicitly:</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">tf.debugging.set_log_device_placement(<span class="literal">True</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">try</span>:</span><br><span class="line">  <span class="comment"># Specify an invalid GPU device</span></span><br><span class="line">  <span class="keyword">with</span> tf.device(<span class="string">'/device:GPU:2'</span>):</span><br><span class="line">    a = tf.constant([[<span class="number">1.0</span>, <span class="number">2.0</span>, <span class="number">3.0</span>], [<span class="number">4.0</span>, <span class="number">5.0</span>, <span class="number">6.0</span>]])</span><br><span class="line">    b = tf.constant([[<span class="number">1.0</span>, <span class="number">2.0</span>], [<span class="number">3.0</span>, <span class="number">4.0</span>], [<span class="number">5.0</span>, <span class="number">6.0</span>]])</span><br><span class="line">    c = tf.matmul(a, b)</span><br><span class="line"><span class="keyword">except</span> RuntimeError <span class="keyword">as</span> e:</span><br><span class="line">  print(e)</span><br></pre></td></tr></table></figure><p>If you would like TensorFlow to automatically choose an existing and supported device to run the operations in case the specified one doesn’t exist, you can call tf.config.set_soft_device_placement(True).</p><h3 id="Using-multiple-GPUs"><a href="#Using-multiple-GPUs" class="headerlink" title="Using multiple GPUs"></a>Using multiple GPUs</h3>]]></content>
    
    <summary type="html">
    
      Study notes of Tensorflow 2.0
    
    </summary>
    
    
      <category term="Study Notes" scheme="https://joddiy.github.io/categories/Study-Notes/"/>
    
    
      <category term="Tensorflow" scheme="https://joddiy.github.io/tags/Tensorflow/"/>
    
      <category term="Deep Learning" scheme="https://joddiy.github.io/tags/Deep-Learning/"/>
    
      <category term="Tools" scheme="https://joddiy.github.io/tags/Tools/"/>
    
  </entry>
  
  <entry>
    <title>TensorFlow 2.0 Functions, not Sessions.</title>
    <link href="https://joddiy.github.io/2020/05/07/Tensorflow-2-0/"/>
    <id>https://joddiy.github.io/2020/05/07/Tensorflow-2-0/</id>
    <published>2020-05-07T13:03:33.000Z</published>
    <updated>2020-05-09T14:33:34.716Z</updated>
    
    <content type="html"><![CDATA[<h1 id="TensorFlow-2-0-Functions-not-Sessions"><a href="#TensorFlow-2-0-Functions-not-Sessions" class="headerlink" title="TensorFlow 2.0: Functions, not Sessions."></a>TensorFlow 2.0: Functions, not Sessions.</h1><p><a href="https://github.com/tensorflow/community/blob/49c9f13500ddedddf60144690b9be744a3654b11/rfcs/20180918-functions-not-sessions-20.md" target="_blank" rel="noopener">doc link</a></p><h2 id="Design-Proposal"><a href="#Design-Proposal" class="headerlink" title="Design Proposal"></a>Design Proposal</h2><h3 id="Basic-idea-Python-functions-as-Graphs"><a href="#Basic-idea-Python-functions-as-Graphs" class="headerlink" title="Basic idea: Python functions as Graphs"></a>Basic idea: Python functions as Graphs</h3><p>Where tf.function is a decorator that “defines a TensorFlow function”. A “TensorFlow function” defines a computation as a graph of TensorFlow operations, with named arguments and explicit return values. </p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> tensorflow <span class="keyword">as</span> tf</span><br><span class="line"></span><br><span class="line"><span class="meta">@tf.function</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">compute_z1</span><span class="params">(x, y)</span>:</span></span><br><span class="line">  <span class="keyword">return</span> tf.add(x, y)</span><br><span class="line"></span><br><span class="line"><span class="meta">@tf.function</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">compute_z0</span><span class="params">(x)</span>:</span></span><br><span class="line">  <span class="keyword">return</span> compute_z1(x, tf.square(x))</span><br><span class="line"></span><br><span class="line">z0 = compute_z0(<span class="number">2.</span>)</span><br><span class="line">z1 = compute_z1(<span class="number">2.</span>, <span class="number">2.</span>)</span><br></pre></td></tr></table></figure><p>Having the Python function correspond to what the runtime will execute reduces conceptual complexity in translating between the two domains. </p><h3 id="Referencing-state-Variables-tables-etc"><a href="#Referencing-state-Variables-tables-etc" class="headerlink" title="Referencing state: Variables, tables etc."></a>Referencing state: Variables, tables etc.</h3><p>A function decorated Python function encapsulates a graph and its execution. The Python function may reference stateful objects (i.e., state backed by DT_RESOURCE tensors in the runtime, e.g., tf.Variable) by referencing the corresponding Python object, and these will be captured as implicit inputs to the function.</p><p>Comparing TensorFlow code today with how we propose it looks in 2.x:</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># TF 1.x</span></span><br><span class="line">W = tf.Variable(</span><br><span class="line">  tf.glorot_uniform_initializer()(</span><br><span class="line">    (<span class="number">10</span>, <span class="number">10</span>)))</span><br><span class="line">b = tf.Variable(tf.zeros(<span class="number">10</span>))</span><br><span class="line">c = tf.Variable(<span class="number">0</span>)</span><br><span class="line"></span><br><span class="line">x = tf.placeholder(tf.float32)</span><br><span class="line">ctr = c.assign_add(<span class="number">1</span>)</span><br><span class="line"><span class="keyword">with</span> tf.control_dependencies([ctr]):</span><br><span class="line">  y = tf.matmul(x, W) + b</span><br><span class="line">init = </span><br><span class="line">  tf.global_variables_initializer()</span><br><span class="line"></span><br><span class="line"><span class="keyword">with</span> tf.Session() <span class="keyword">as</span> sess:</span><br><span class="line">  sess.run(init)</span><br><span class="line">  print(sess.run(y,</span><br><span class="line">  feed_dict=&#123;x: make_input_value()&#125;))</span><br><span class="line">  <span class="keyword">assert</span> int(sess.run(c)) == <span class="number">1</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># TF 2.0</span></span><br><span class="line">W = tf.Variable(</span><br><span class="line">  tf.glorot_uniform_initializer()(</span><br><span class="line">    (<span class="number">10</span>, <span class="number">10</span>)))</span><br><span class="line">b = tf.Variable(tf.zeros(<span class="number">10</span>))</span><br><span class="line">c = tf.Variable(<span class="number">0</span>)</span><br><span class="line"></span><br><span class="line"><span class="meta">@tf.function</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">f</span><span class="params">(x)</span>:</span></span><br><span class="line">  c.assign_add(<span class="number">1</span>)</span><br><span class="line">  <span class="keyword">return</span> tf.matmul(x, W) + b</span><br><span class="line"></span><br><span class="line">print(f(make_input_value())</span><br><span class="line"><span class="keyword">assert</span> int(c) == <span class="number">1</span></span><br></pre></td></tr></table></figure><p>Worthy of note here - in TensorFlow 1.x, the memory underlying the variables W and b in the runtime lives for the lifetime of the Session - unrelated to the lifetime of the Python objects. In 2.x, the lifetime of the Python objects and the runtime state are tied together.</p><h3 id="Control-dependencies"><a href="#Control-dependencies" class="headerlink" title="Control dependencies"></a>Control dependencies</h3><p>In TensorFlow graphs today, control dependencies are sometimes needed to ensure correct evaluation order. </p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># TF 1.x</span></span><br><span class="line">v = tf.Variable(<span class="number">1.0</span>)</span><br><span class="line">init_op = tf.global_variables_initializer()</span><br><span class="line">assign_op = v.assign(<span class="number">2.0</span>)</span><br><span class="line">read = v.read_value()</span><br><span class="line"></span><br><span class="line"><span class="keyword">with</span> tf.Session() <span class="keyword">as</span> sess:</span><br><span class="line">  sess.run(init_op)</span><br><span class="line">  val = sess.run(read)</span><br><span class="line">  print(val) <span class="comment"># Will print 1.0, the assign is ignored</span></span><br><span class="line">  val = sess.run([read, assign_op])[<span class="number">0</span>]</span><br><span class="line">  print(val)   <span class="comment"># Non-deterministically prints 1.0 or 2.0,</span></span><br><span class="line"></span><br><span class="line"> <span class="comment"># TF2.0</span></span><br><span class="line"> v = tf.Variable(<span class="number">1.0</span>)</span><br><span class="line"><span class="meta">@tf.function</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">f</span><span class="params">()</span>:</span></span><br><span class="line">  v.assign(<span class="number">2.0</span>)</span><br><span class="line">  <span class="keyword">return</span> v.read_value()</span><br><span class="line"></span><br><span class="line">print(f()) <span class="comment"># Always prints 2.0.</span></span><br></pre></td></tr></table></figure><p>Note that the intention here is to avoid observable differences from program order. For example:</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">a = tf.Variable(<span class="number">1.0</span>)</span><br><span class="line">b = tf.Variable(<span class="number">1.0</span>)</span><br><span class="line"><span class="meta">@tf.function</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">f</span><span class="params">()</span>:</span></span><br><span class="line">  a.assign(<span class="number">2.0</span>)</span><br><span class="line">  b.assign(<span class="number">3.0</span>)</span><br><span class="line">  <span class="keyword">return</span> a + b</span><br><span class="line">print(f())</span><br></pre></td></tr></table></figure><p>Will always print 5.0 since the assignments will occur before the read. However, there is no guaranteed ordering between the assignment of a and b (as any difference in that is not observable).</p><h3 id="Functions-that-create-state"><a href="#Functions-that-create-state" class="headerlink" title="Functions that create state"></a>Functions that create state</h3><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">v = <span class="literal">None</span></span><br><span class="line"></span><br><span class="line"><span class="meta">@tf.function</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">f</span><span class="params">(x)</span>:</span></span><br><span class="line">  <span class="keyword">global</span> v</span><br><span class="line">  <span class="keyword">if</span> v <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">    v = tf.Variable(<span class="number">1.0</span>)</span><br><span class="line">  <span class="keyword">return</span> tf.cast(x, tf.float32) + v</span><br><span class="line"></span><br><span class="line">f(tf.constant(<span class="number">1</span>, dtype=tf.float32)) <span class="comment"># Creates the variable, returns 2.0</span></span><br><span class="line">f(tf.constant(<span class="number">2</span>, dtype=tf.int32))   <span class="comment"># Reuses the variable, returns 3.0</span></span><br></pre></td></tr></table></figure><ol><li>State (like tf.Variable objects) are only created the first time the function f is called. If any variables are created in the first execution of f, then @tf.function will trace f again the second time it is invoked in order to record the behavior that will be used from then on. </li><li>The caller must make sure that any variable referenced by the function still exists whenever the function is evaluated. @tf.function itself will keep only weak references to these created variables. </li></ol><h3 id="Trace-Caches"><a href="#Trace-Caches" class="headerlink" title="Trace Caches"></a>Trace Caches</h3><p>Since new graphs are traced when new input signatures are encountered, a function can encapsulate multiple graphs. For example, considering the following, there are two graphs created here:</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@tf.function</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">f</span><span class="params">(x)</span>:</span></span><br><span class="line">  <span class="keyword">return</span> tf.square(x)</span><br><span class="line"></span><br><span class="line">f(tf.constant(<span class="number">1</span>, dtype=tf.int32))</span><br><span class="line">f(tf.constant(<span class="number">1.0</span>, dtype=tf.float32))</span><br></pre></td></tr></table></figure><p>Note the use of tf.constant to ensure that the argument is a Tensor. If the argument were a Python value, then additional graphs will be traced for each such value. For example, the following two calls will result in two additional graphs being traced:</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">f(<span class="number">1.0</span>)</span><br><span class="line">f(<span class="number">2.0</span>)</span><br></pre></td></tr></table></figure><p>Where arguments are not Tensors, the “value” of the argument is used to compute the trace_cache_key. For example:</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@tf.function</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">f</span><span class="params">(x, use_multiply)</span>:</span></span><br><span class="line">  <span class="keyword">return</span> tf.multiply(x, x) <span class="keyword">if</span> use_multiply <span class="keyword">else</span> tf.square(x)</span><br><span class="line"></span><br><span class="line">f(tf.constant(<span class="number">2.0</span>), <span class="literal">True</span>)</span><br><span class="line">f(tf.constant(<span class="number">2.0</span>), <span class="literal">False</span>)</span><br></pre></td></tr></table></figure><p>will result in 2 graphs being created, since the two calls result in two different cache keys because the value of the Python object (the second argument) changes between the two.</p><p>Note that the “type” of Tensor inputs to the function also incorporates the shape. For example:</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@tf.function</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">f</span><span class="params">(x)</span>:</span> </span><br><span class="line"><span class="keyword">return</span> tf.add(x, <span class="number">1.</span>)</span><br><span class="line"></span><br><span class="line">f(tf.constant([<span class="number">2.0</span>]))</span><br><span class="line">f(tf.constant([<span class="number">2.0</span>, <span class="number">3.0</span>]))</span><br><span class="line">f(tf.constant([[<span class="number">2.0</span>]]))</span><br><span class="line">f(tf.constant([<span class="number">3.0</span>]))</span><br><span class="line">f(tf.constant([<span class="number">4.0</span>, <span class="number">5.0</span>]))</span><br></pre></td></tr></table></figure><p>will result in 3 graphs being created.</p><p>The trace_cache_key also incorporates the “context” in which the call was made. For example:</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@tf.function</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">f</span><span class="params">(x)</span>:</span> <span class="keyword">return</span> tf.add(x, <span class="number">1.</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">with</span> tf.device(<span class="string">"/device:CPU:0"</span>):</span><br><span class="line">  f(tf.constant(<span class="number">2.0</span>))</span><br><span class="line"><span class="keyword">with</span> tf.device(<span class="string">"/device:GPU:0"</span>):</span><br><span class="line">  f(tf.constant(<span class="number">2.0</span>))</span><br></pre></td></tr></table></figure><p>Will create 2 graphs.</p><p><strong>CAUTION: Too many traces</strong></p><p><strong>CAUTION: Mutable non-Tensor arguments</strong></p><p>The trace_cache_key includes the Python object for non-Tensor arguments. Mutations of these arguments might not be detected. For example:</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># non-Tensor object</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Params</span><span class="params">(object)</span>:</span></span><br><span class="line">  multiply = <span class="literal">True</span></span><br><span class="line"></span><br><span class="line">p = Params()</span><br><span class="line"><span class="meta">@tf.function</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">f</span><span class="params">(x, y)</span>:</span></span><br><span class="line">  <span class="keyword">return</span> tf.multiply(x, <span class="number">2.</span>) <span class="keyword">if</span> y.multiply <span class="keyword">else</span> tf.add(x, <span class="number">2.</span>)</span><br><span class="line"></span><br><span class="line">f(<span class="number">3.</span>, p) <span class="comment"># Returns 6.0</span></span><br><span class="line">p.multiply = <span class="literal">False</span></span><br><span class="line">f(<span class="number">3.</span>, p)  <span class="comment"># Mutations to `p` may not trigger a retrace, so might still return 6.0</span></span><br></pre></td></tr></table></figure><h3 id="Input-Signatures"><a href="#Input-Signatures" class="headerlink" title="Input Signatures"></a>Input Signatures</h3><p>An “input signature” can be explicitly specified to control the trace_cache_key computation based on the type and shape of Tensor (and list of Tensor) arguments to f.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@tf.function(input_signature=((tf.float32, [None]))</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">f</span><span class="params">(x)</span>:</span> </span><br><span class="line"><span class="keyword">return</span> tf.add(x, <span class="number">1.</span>)</span><br><span class="line"></span><br><span class="line">f(tf.constant([<span class="number">2.0</span>]))      <span class="comment"># Returns [3.0]</span></span><br><span class="line">f(tf.constant([<span class="number">2.0</span>, <span class="number">3.0</span>])) <span class="comment"># Matches the input signature as [None]</span></span><br><span class="line">                           <span class="comment"># matches the actual shape [2]</span></span><br><span class="line">f(tf.constant([[<span class="number">2.0</span>]]))    <span class="comment"># Raises an error as the arguments don't match the</span></span><br><span class="line">                           <span class="comment"># input signature.</span></span><br><span class="line">f(tf.constant([<span class="number">2</span>], dtype=tf.int32)) <span class="comment"># Raises an error as the dtype of the argument</span></span><br><span class="line">                                    <span class="comment"># does not match the input signature</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># f is backed by a single Graph since the input signature specification allowed</span></span><br><span class="line"><span class="comment"># for the same graph to be used when the input shape is (1,) or (2,).</span></span><br></pre></td></tr></table></figure><ol><li>For a Tensor argument, it specifies a (dtype, shape pattern).<ul><li>(tf.float32, [None]) means the argument must be a float32 vector (with any number of elements).</li><li>(tf.int32, []) means that the argument must be an int32 scalar.</li></ul></li><li>For a list of Tensor objects, it specifies an optional list length and the signature for elements in the list (i.e., the dtype and shape pattern for all elements in the list).</li><li>For non-Tensor arguments: tf.PYTHON_VALUE</li></ol><p>You can use the tf.TRACE_ON_NEW_VALUE to release the restriction of dtype:</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@tf.function(input_signature=((tf.TRACE_ON_NEW_VALUE, [None]))</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">f</span><span class="params">(x)</span>:</span> </span><br><span class="line"><span class="keyword">return</span> tf.square(x)</span><br><span class="line"></span><br><span class="line">f(tf.constant([<span class="number">2.0</span>])) <span class="comment"># Returns 4.0</span></span><br><span class="line">f(tf.constant([<span class="number">2</span>, <span class="number">2</span>], dtype=tf.int32) <span class="comment"># Returns [4, 4] after tracing a new graph</span></span><br></pre></td></tr></table></figure><h3 id="Classes"><a href="#Classes" class="headerlink" title="Classes"></a>Classes</h3><p>If a member function of a class does not create variables, it may be decorated with @tf.function and it will work:</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">AnyShapeModel</span><span class="params">(object)</span>:</span></span><br><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self)</span>:</span></span><br><span class="line">    self.v = <span class="literal">None</span></span><br><span class="line"></span><br><span class="line"><span class="meta">  @tf.function</span></span><br><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">increment</span><span class="params">(self, amount)</span>:</span></span><br><span class="line">    <span class="keyword">if</span> self.v <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">      self.v = tf.Variable(tf.zeros_like(amount))</span><br><span class="line">    self.v.assign_add(amount)</span><br><span class="line"></span><br><span class="line">model1 = AnyShapeModel()</span><br><span class="line">model1.increment(tf.constant(<span class="number">3</span>))</span><br><span class="line"><span class="keyword">assert</span> int(model1.v) == <span class="number">3</span></span><br><span class="line">model1.increment(tf.constant(<span class="number">4</span>))</span><br><span class="line"><span class="keyword">assert</span> int(model1.v) == <span class="number">7</span></span><br><span class="line">model2 = AnyShapeModel()</span><br><span class="line">model2.increment(tf.constant([<span class="number">4</span>, <span class="number">5</span>]))</span><br><span class="line"><span class="keyword">assert</span> model2.v.numpy() == [<span class="number">4</span>, <span class="number">5</span>]</span><br></pre></td></tr></table></figure><p>The semantics here are that each new instance is allowed to create variables in each @tf.function once.</p><h3 id="function-ing-Python-control-flow"><a href="#function-ing-Python-control-flow" class="headerlink" title="function-ing Python control flow"></a>function-ing Python control flow</h3><p>If the function has <strong>data-dependent control flow</strong> then though the function will execute fine with eager execution enabled, function decorating it will fail. For example:</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">f</span><span class="params">(x, y)</span>:</span></span><br><span class="line">  <span class="keyword">if</span> tf.equal(y, <span class="number">0.0</span>):</span><br><span class="line">    <span class="keyword">return</span> y</span><br><span class="line">  <span class="keyword">return</span> x / y</span><br><span class="line"></span><br><span class="line">x = tf.constant(<span class="number">2.0</span>)</span><br><span class="line">y = tf.constant(<span class="number">2.0</span>)</span><br><span class="line"></span><br><span class="line">f(x, y) <span class="comment"># Will be 1.0</span></span><br><span class="line"></span><br><span class="line">df = tf.function(f)</span><br><span class="line">df(x, y)  <span class="comment"># Will raise an error complaining about the data-dependent control flow</span></span><br></pre></td></tr></table></figure><p>To fix this, one would have to use the graph construction APIs for control flow (tf.cond, tf.while_loop):</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">f</span><span class="params">(x, y)</span>:</span></span><br><span class="line">  <span class="keyword">return</span> tf.cond(tf.equal(y, <span class="number">0.0</span>), <span class="keyword">lambda</span>: y, <span class="keyword">lambda</span>: x/y)</span><br><span class="line"></span><br><span class="line">x = tf.constant(<span class="number">2.0</span>)</span><br><span class="line">y = tf.constant(<span class="number">2.0</span>)</span><br><span class="line"></span><br><span class="line">f(x, y) <span class="comment"># Will be 1.0</span></span><br><span class="line"></span><br><span class="line">df = tf.function(f)</span><br><span class="line">df(x, y)  <span class="comment"># Will be 1.0</span></span><br></pre></td></tr></table></figure><p>This situation can be improved with the help of autograph to allow expression of control flow in Python. </p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">df = tf.function(autograph=<span class="literal">True</span>)(f)</span><br><span class="line">f(x, y) <span class="comment"># Will be 1.0</span></span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      Study notes of Tensorflow 2.0
    
    </summary>
    
    
      <category term="Study Notes" scheme="https://joddiy.github.io/categories/Study-Notes/"/>
    
    
      <category term="Tensorflow" scheme="https://joddiy.github.io/tags/Tensorflow/"/>
    
      <category term="Deep Learning" scheme="https://joddiy.github.io/tags/Deep-Learning/"/>
    
      <category term="Tools" scheme="https://joddiy.github.io/tags/Tools/"/>
    
  </entry>
  
  <entry>
    <title>Understanding and Optimizing Packed Neural Network Training for Hyper-Parameter Tuning</title>
    <link href="https://joddiy.github.io/2020/05/01/Understanding-and-Optimizing-Packed-Neural-Network-Training-for-Hyper-Parameter-Tuning/"/>
    <id>https://joddiy.github.io/2020/05/01/Understanding-and-Optimizing-Packed-Neural-Network-Training-for-Hyper-Parameter-Tuning/</id>
    <published>2020-04-30T16:00:00.000Z</published>
    <updated>2020-05-09T09:55:44.954Z</updated>
    
    <content type="html"><![CDATA[<h1 id="Understanding-and-Optimizing-Packed-Neural-Network-Training-for-Hyper-Parameter-Tuning"><a href="#Understanding-and-Optimizing-Packed-Neural-Network-Training-for-Hyper-Parameter-Tuning" class="headerlink" title="Understanding and Optimizing Packed Neural Network Training for Hyper-Parameter Tuning"></a>Understanding and Optimizing Packed Neural Network Training for Hyper-Parameter Tuning</h1><h2 id="Subject"><a href="#Subject" class="headerlink" title="Subject"></a>Subject</h2><p>This paper studies jointly training multiple neural network models on a single GPU. Our research prototype is in TensorFlow, and we evaluate performance across different models (ResNet, MobileNet, DenseNet, and MLP) and training scenarios. </p><h2 id="Contribution"><a href="#Contribution" class="headerlink" title="Contribution"></a>Contribution</h2><ol><li>packing two models can bring up to 40% performance improvement over unpacked setups for a single training step and the improvement increases when packing more models; </li><li>the benefit of a pack primitive largely depends on a number of factors including memory capacity, chip architecture, neural network structure, and batch size; </li><li>there exists a trade-off between packing and unpacking when training multiple neural network models on limited resources; (4) a pack-based Hyperband is up to 2.7× faster than the original Hyperband training method in our experiment setting, with this improvement growing as memory size increases and subsequently the density of models packed. </li></ol><h2 id="Introduction"><a href="#Introduction" class="headerlink" title="Introduction"></a>Introduction</h2><h3 id="Background"><a href="#Background" class="headerlink" title="Background"></a>Background</h3><ol><li>It is increasingly the case that a diverse set of model training tasks share limited training resources. The long-running nature of these tasks and the large variation in their size and complexity makes efficient resource sharing a crucial concern. </li><li>These concerns are compounded by an extensive trial-and-error development process where parameters are tuned and architectures are tweaked that result in a large number of trial models to train. </li></ol><h3 id="Multi-tenant-training"><a href="#Multi-tenant-training" class="headerlink" title="Multi-tenant training"></a>Multi-tenant training</h3><h4 id="granularity-of-full-devices"><a href="#granularity-of-full-devices" class="headerlink" title="granularity of full devices"></a>granularity of full devices</h4><p>Such a policy can result in poor resource utilization due to its coarse granularity. If a training workload consists of a large number of small neural networks, allocating entire devices to these training tasks is wasteful and significantly delays any large model training.</p><h4 id="fine-grained-resource-sharing"><a href="#fine-grained-resource-sharing" class="headerlink" title="fine-grained resource sharing"></a>fine-grained resource sharing</h4><p>Unlike CPUs, the full virtualization of GPU resources is very nascent. In this setting, those parallel kernels would transfer and store multiple copies of the same training data on the device. </p><h3 id="Pack-models"><a href="#Pack-models" class="headerlink" title="Pack models"></a>Pack models</h3><p>A multiple static neural network architectures (e.g., ones that are typically used in Computer Vision) can be rewritten as a single concatenated network that preserves the input, output, and backpropagation semantics.</p><ol><li>facilitate partitioning of a single device</li><li>synchronize data processing threads on GPUs</li><li>collapse common variables in the computation graph.</li></ol><p>It is often the case during hyperparameter or neural architecture search that multiple similar networks are trained, and packed configurations can feed a single I/O stream of training examples to each variant of the network. In contrast, an isolated sharing configuration may lead to duplicated work and wasted resources.</p><h3 id="Conclusion"><a href="#Conclusion" class="headerlink" title="Conclusion"></a>Conclusion</h3><ol><li>packing models together is not strictly beneficial, that packing models that are too different can result in excessive blocking and wasted work.</li><li>this paper studies the range of possible improvements (and/or overheads) for using packed configurations. </li><li>design an algorithm that heuristically finds performant packing configurations</li></ol><h2 id="Background-1"><a href="#Background-1" class="headerlink" title="Background"></a>Background</h2><h3 id="Motivation"><a href="#Motivation" class="headerlink" title="Motivation"></a>Motivation</h3><p><img src="https://res.cloudinary.com/dzarsr3se/image/upload/v1588964038/blog/Understanding-and-Optimizing-Packed-Neural-Network-Training-for-Hyper-Parameter-Tuning/1_mfq4g7.png" alt="P1"></p><p>Suppose, we are training two models on the same dataset to test if a small tweak in network architecture will improve performance. If the system could pack together models when compatible in size, then these redundant data streams can be fused together to improve performance.</p><h3 id="Basic-Framework-API"><a href="#Basic-Framework-API" class="headerlink" title="Basic Framework API"></a>Basic Framework API</h3><p>We desire a framework that can pack models together when possible and jointly optimize their respective computation graphs to reduce redundant work. </p><p>Each training task is identified by four inputs.</p><ol><li>Model.</li><li>Optimizer.</li><li>Batch Size.</li><li>Steps. </li></ol><p>Such a system requires three basic primitives load, pack, and free. </p><ol><li>load<figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">load(model, device)</span><br></pre></td></tr></table></figure></li><li>free<figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">checkpt &#x3D; free(model, device)</span><br></pre></td></tr></table></figure></li><li>pack<figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">output1 &#x3D; nn1(input1)</span><br><span class="line">output2 &#x3D; nn2(input2)</span><br><span class="line">&#x3D;&#x3D;&gt;</span><br><span class="line">[output1 output2] &#x3D; nn12([input1, input2])</span><br></pre></td></tr></table></figure></li></ol><p>This pack operation is fully differentiable and preserves the correctness semantics of the two original networks. The models can be jointly trained.</p><p>If the models are too different the device may waste effort stalling on one model.</p><h2 id="Implementation"><a href="#Implementation" class="headerlink" title="Implementation"></a>Implementation</h2><h3 id="Basic-Packing"><a href="#Basic-Packing" class="headerlink" title="Basic Packing"></a>Basic Packing</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">resnet_out &#x3D; ... #reference to resnet output </span><br><span class="line">mobilenet_out &#x3D; ..#reference to mobilenet output </span><br><span class="line">packed_output &#x3D; stack([resnet_out, mobilenet_out])</span><br></pre></td></tr></table></figure><p><img src="https://res.cloudinary.com/dzarsr3se/image/upload/v1588964039/blog/Understanding-and-Optimizing-Packed-Neural-Network-Training-for-Hyper-Parameter-Tuning/2_yjjdme.png" alt="P2"></p><p>If one of the models is significantly more complex than the others, it will block progress.</p><ol><li>Misaligned Batch Sizes</li></ol><p>Our method is to rewrite the packed model to include a dummy operation that pads models with the smaller batch size to match the larger ones in dimension. </p><p><img src="https://res.cloudinary.com/dzarsr3se/image/upload/v1588964038/blog/Understanding-and-Optimizing-Packed-Neural-Network-Training-for-Hyper-Parameter-Tuning/3_mozcet.png" alt="P3"></p><ol start="2"><li>Misaligned Step Counts</li></ol><p>when the earliest model finishes, the packed model has to be check-pointed and reconfigured to only include the remaining models. This introduces a switching overhead of loading and freeing the device memory.</p><h3 id="Eliminating-Redundancy"><a href="#Eliminating-Redundancy" class="headerlink" title="Eliminating Redundancy"></a>Eliminating Redundancy</h3><p>Dimensional differences between the models or training differences between the models can lead to wasted work. </p><p>In this case, input1 and input2 refer to the same dataset. We can avoid transferring the batch multiple times by symbolically rewriting the network description to refer to the same input:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">output1 &#x3D; nn_relu(input1)</span><br><span class="line">output2 &#x3D; nn_sigmoid(input2)</span><br><span class="line">&#x3D;&#x3D;&gt;</span><br><span class="line">[output1 output2] &#x3D; nn12([input, input])</span><br></pre></td></tr></table></figure><p>When packing models that use the same preprocessing tasks, the pack primitive fuse the steam processing accordingly.</p><h2 id="Profiling-Model-Packing"><a href="#Profiling-Model-Packing" class="headerlink" title="Profiling Model Packing"></a>Profiling Model Packing</h2><p>As it stands, model packing leads to the following tradeoffs. Potential performance improvements include: </p><ol><li>performing computations (inference and backpropagation) in parallel if and when possible, </li><li>eliminating redundant data transfers when models are trained on the same dataset, </li><li>combining redudant computations including preprocessing. </li></ol><p>On the other hand, the potential overheads include: </p><ol><li>models that dominate the device resources and block the progress of the others, </li><li>padding misaligned batch sizes,</li><li>loading and unloading models with a differing number of training steps.</li></ol><p>In particular, we find that packing is most beneficial when the models jointly trained are similar and train on the same dataset.</p><h3 id="Metrics"><a href="#Metrics" class="headerlink" title="Metrics"></a>Metrics</h3><p>we define the improvement metric as follows:</p><p>[IMPV]</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">Ts(Seq) &#x3D; Ts(Model 1)+···+Ts(Model n)</span><br><span class="line">Ts(Pack) &#x3D; Ts(Pack(Model 1,··· ,n))</span><br><span class="line"></span><br><span class="line">IMPV &#x3D; (Ts(Seq)−Ts(Pack))&#x2F;Ts(Seq)</span><br></pre></td></tr></table></figure><p>The switching overhead of training n models is defined as:</p><p>[SwOH]</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">SwOH &#x3D; Te(Seq)−Te(Model 1)···−Te(Model n)</span><br></pre></td></tr></table></figure><h3 id="Micro-Benchmarks"><a href="#Micro-Benchmarks" class="headerlink" title="Micro-Benchmarks"></a>Micro-Benchmarks</h3><p>Improvement of packing models when increasing number of models and batch size on GPU.</p><p><img src="https://res.cloudinary.com/dzarsr3se/image/upload/v1588964038/blog/Understanding-and-Optimizing-Packed-Neural-Network-Training-for-Hyper-Parameter-Tuning/4_aeswjs.png" alt="P4"></p><h3 id="Ablation-Study"><a href="#Ablation-Study" class="headerlink" title="Ablation Study"></a>Ablation Study</h3><p>We test more cases based on the five factors:</p><p><img src="https://res.cloudinary.com/dzarsr3se/image/upload/v1588964038/blog/Understanding-and-Optimizing-Packed-Neural-Network-Training-for-Hyper-Parameter-Tuning/T1_u9m0tn.png" alt="T1"></p><p>The best scenarios are where the same training data and same batch size are used. Over all the configurations, the pack primitive always brings benefits when we training models with same data or same batch size since it will reduces the data transfer. </p><p><img src="https://res.cloudinary.com/dzarsr3se/image/upload/v1588964038/blog/Understanding-and-Optimizing-Packed-Neural-Network-Training-for-Hyper-Parameter-Tuning/5_a4nf4v.png" alt="P5"></p><h3 id="Memory-Usage"><a href="#Memory-Usage" class="headerlink" title="Memory Usage"></a>Memory Usage</h3><p>MLP-3 model maintains the same as batch size goes up. For convolutional neural networks like ResNet, MobileNet and DenseNet, the GPU memory usage is proportional to the batch size.</p><p><img src="https://res.cloudinary.com/dzarsr3se/image/upload/v1588964038/blog/Understanding-and-Optimizing-Packed-Neural-Network-Training-for-Hyper-Parameter-Tuning/6_otfjyk.png" alt="P6"></p><h3 id="Switching-Overheads"><a href="#Switching-Overheads" class="headerlink" title="Switching Overheads"></a>Switching Overheads</h3><p>The switching overhead is minor compared to the overall training time.</p><p><img src="https://res.cloudinary.com/dzarsr3se/image/upload/v1588964037/blog/Understanding-and-Optimizing-Packed-Neural-Network-Training-for-Hyper-Parameter-Tuning/T2_yivti7.png" alt="T2"></p><h3 id="Packing-vs-CUDA-Parallelism"><a href="#Packing-vs-CUDA-Parallelism" class="headerlink" title="Packing vs CUDA Parallelism"></a>Packing vs CUDA Parallelism</h3><p>Although CUDA supports it, our results show that it is an inefficient technique. We find that in all but the simplest cases lead to an out-of-memory error: “failed to allocateXXXfromdevice:CUDA_ERROR_OUT_OF_MEMORY: out of memory”. </p><h2 id="Packing-Aware-Hyperparameter-Search"><a href="#Packing-Aware-Hyperparameter-Search" class="headerlink" title="Packing-Aware Hyperparameter Search"></a>Packing-Aware Hyperparameter Search</h2><h3 id="Hyperband-for-Hyperparameter-Search"><a href="#Hyperband-for-Hyperparameter-Search" class="headerlink" title="Hyperband for Hyperparameter Search"></a>Hyperband for Hyperparameter Search</h3><p>Hyperband works by repeatedly sampling random parameter configurations, partially training models with those configurations and discarding those configurations that do not seem promising. </p><p>Hyperband is very effective for parallel hyperparameter search in comparison to sequential algorithms such as Bayesian Optimization.</p><p>The algorithm is long-running since it consists of a large number of trial models/configurations to run and each of them will occupy the entire GPU resource when running.</p><h3 id="Packed-Hyperband"><a href="#Packed-Hyperband" class="headerlink" title="Packed Hyperband"></a>Packed Hyperband</h3><p>Our pack primitive allows Hyperband to jointly train configurations when possible thereby reducing the overall training time. </p><p>The optimization problem is to search over all packable partitions to find the best possible configuratio. We call this primitive pack_opt(), which solves the search problem to produce feasible packing configuration. </p><h3 id="Heuristic-Solution"><a href="#Heuristic-Solution" class="headerlink" title="Heuristic Solution"></a>Heuristic Solution</h3><p>We design a nearest-neighbor based heuristic. The method randomly selects a single configuration as the centroid and packs all other configurations similar to it until the device runs out of memory. </p><p>For calculating the similarity, we map hyperparameter configurations to multidimensional feature space and measure the pairwise Euclid distance among all the configurations. </p><p>We take standard distance unit as 1, and compute the distance between any two configurations. For categorical hyperparameters like optimizer and activation, the distance is 0 if same and 1 if different, for numeric hyperparameters, we use the index to compute distance. </p><p><img src="https://res.cloudinary.com/dzarsr3se/image/upload/v1588964037/blog/Understanding-and-Optimizing-Packed-Neural-Network-Training-for-Hyper-Parameter-Tuning/T3_efcra4.png" alt="T3"></p><h3 id="Evaluation"><a href="#Evaluation" class="headerlink" title="Evaluation"></a>Evaluation</h3><p>A Random Pack Hyperband can save more time than Batch-size Pack Hyperband since it achieves a better GPU resource utilization. Our kNN strategy gets the best of both worlds: it finds the most beneficial packing opportunities while completely utilizing the available resources, and benefits are scalable when deployed in an environment with larger GPU resource.</p><p><img src="https://res.cloudinary.com/dzarsr3se/image/upload/v1588964037/blog/Understanding-and-Optimizing-Packed-Neural-Network-Training-for-Hyper-Parameter-Tuning/T4_gzjwyr.png" alt="T4"></p><h2 id="Related-Work"><a href="#Related-Work" class="headerlink" title="Related Work"></a>Related Work</h2><p>We focus on related work that studies placement in machine learning and hyperparameter tuning systems.</p><h3 id="Systems-for-Hyperparameter-Tuning"><a href="#Systems-for-Hyperparameter-Tuning" class="headerlink" title="Systems for Hyperparameter Tuning"></a>Systems for Hyperparameter Tuning</h3><ol><li>Google Vizier [3] exposes hyperparamter search as a service to its organization’s data scientists. Aggressive “scale-out” has been the main design principle of Vizier and similar systems.</li><li>A more controlled resource usage. CHOPT proposes a novel stop-and-go scheme that can take advantage of transient cloud based resources. </li><li>HyperSched, proposes a scheduling framework for hyperparameter tuning tasks when there are contended specialized resources. And, some work has been done on resource management [33] and pipeline re-use [18] in the non-deep learning setting. </li></ol><h3 id="Coarse-grained-Multi-tenancy"><a href="#Coarse-grained-Multi-tenancy" class="headerlink" title="Coarse-grained Multi-tenancy"></a>Coarse-grained Multi-tenancy</h3><p>dividing resources at the granularity of full devices</p><ol><li>One of the earliest works in this space was the TetriSched project, which considered how to specify and optimize locality constraints during process scheduling.</li><li>Project Philly analyzed a trace of machine learning workloads run on a large cluster of GPUs in Microsoft and indicated the trade-off between locality and GPU utilization. </li></ol><h3 id="Fine-grained-Multi-tenancy"><a href="#Fine-grained-Multi-tenancy" class="headerlink" title="Fine-grained Multi-tenancy"></a>Fine-grained Multi-tenancy</h3><p>There are a number of projects which attempt to achieve the fine granularity. This idea called packing.</p><ol><li>Gandiva is a cluster scheduling framework for deep learning jobs that provides primitives such as time-slicing and migration to schedule different jobs, it supports packing as well, implementing packing arrangements using a greedy algorithm without decent optimizations.</li><li>Narayanan et al. [26] discuss a packing idea that tries to combine various models and trains them together.</li></ol><p>However, this proposed packing method of layer fusion has limited application scenarios as it only focuses on models with the same batch size.</p><h3 id="Multi-tenancy-Systems"><a href="#Multi-tenancy-Systems" class="headerlink" title="Multi-tenancy Systems"></a>Multi-tenancy Systems</h3><ol><li>CROSSBOW</li><li>PipeDream</li><li>Ease.ml</li></ol><h2 id="Discussion-and-Limitations"><a href="#Discussion-and-Limitations" class="headerlink" title="Discussion and Limitations"></a>Discussion and Limitations</h2><p>Any distributed training and the regarding optimization is out of the scope of the paper. </p>]]></content>
    
    <summary type="html">
    
      Study notes of paper https://arxiv.org/abs/2002.02885
    
    </summary>
    
    
      <category term="Research" scheme="https://joddiy.github.io/categories/Research/"/>
    
    
      <category term="Deep Learning" scheme="https://joddiy.github.io/tags/Deep-Learning/"/>
    
      <category term="Hyper-Parameter Tuning" scheme="https://joddiy.github.io/tags/Hyper-Parameter-Tuning/"/>
    
      <category term="Graph Optimization" scheme="https://joddiy.github.io/tags/Graph-Optimization/"/>
    
  </entry>
  
  <entry>
    <title>Dynamic Malware Detection Research</title>
    <link href="https://joddiy.github.io/2018/12/18/dynamic-malware-research/"/>
    <id>https://joddiy.github.io/2018/12/18/dynamic-malware-research/</id>
    <published>2018-12-18T03:20:01.000Z</published>
    <updated>2020-05-07T13:38:30.705Z</updated>
    
    <content type="html"><![CDATA[<h2 id="Using-Spatio-Temporal-Information-in-API-Calls-with-Machine-Learning-Algorithms-for-Malware-Detection（2009）"><a href="#Using-Spatio-Temporal-Information-in-API-Calls-with-Machine-Learning-Algorithms-for-Malware-Detection（2009）" class="headerlink" title="Using Spatio-Temporal Information in API Calls with Machine Learning Algorithms for Malware Detection（2009）"></a>Using Spatio-Temporal Information in API Calls with Machine Learning Algorithms for Malware Detection（2009）</h2><p>This paper uses statistical features which are extracted from both spatial (arguments) and temporal (sequences) information available in Windows API calls. </p><p>The Windows API provides thousands of distinct API calls serving diverse functionalities. However, this paper chooses short-listed 237 API calls which are widely used by both benign Windows applications and malware programs. They refer to the short-listed calls as the core-functionality API calls. They further divide the core-functionality API calls into seven different categories: (1) registry, (2) network management, (3) memory management, (4) file I/O, (5) socket, (6) processor and threads, and (7) dynamically linked libraries (DLLs). </p><p><img src="1.png" alt="Figure1"></p><h3 id="spatial-information"><a href="#spatial-information" class="headerlink" title="spatial information"></a>spatial information</h3><p>The spatial information refers to the set of arguments which always are generally address pointers or variables that hold crisp numerical values. They use fundamental statistical and information theoretic measures to quantify the spatial information. These measures include mean, variance, entropy, minimum, and maximum of given values. For example, they have observed that malware writers suffice on lean and mean implementations of malware programs with low memory usage so that they can remain unnoticed for as long as possible.</p><h3 id="Temporal-information"><a href="#Temporal-information" class="headerlink" title="Temporal information"></a>Temporal information</h3><p>Temporal information refers to represents the order of invoked API calls where call sequences and control flow graphs are the most popular techniques. By some auto-correlation analysis, this paper finds that 3rd order dependence has a high correlation for both benign and malware programs, so it finally uses 3-gram API call sequence. Then they model API call sequence using a discrete time Markov chain. The order of a Markov chain represents the extent to which past states determine the present state. They uses 3rd order Markov chain to represent the probability of transitions, which means the next possible API call depends on previous three API calls. The transition matrix T looks like:</p><p><img src="2.png" alt="Figure2"></p><p>Each element in this matrix indicates the probability of a new API call when they have observed the first 3 API calls. Then the paper information gain to select the most discriminative features:</p><p><img src="3.png" alt="Figure3"></p><p>Specifically, for each element in the matrix, the paper uses a to count its appearance times in benign training samples and b to count in malware samples. Then this element’s information gain can be computed by:</p><p><img src="4.png" alt="Figure4"></p><h3 id="Experiment"><a href="#Experiment" class="headerlink" title="Experiment"></a>Experiment</h3><p>The paper designs an experiment using spatio-temporal features’ set over standalone spatial or temporal features set.  </p><p><img src="5.png" alt="Figure5"></p><p>The table shows that that spatial and temporal features alone can provide on the average detection accuracies of approximately 0.898 and 0.952 respectively. However, the combined spatio-temporal features’ set provide an average detection accuracy of 0.963 – approximately 5% improvement over standalone features’ set.</p><p>NB with spatio-temporal features’ set provides the best detection accuracy of 0.980. It is closely followed by SMO which provides the detection accuracy of 0.968. On the other hand, J48 provides the lowest accuracy of 0.947.</p><h2 id="Discovering-Similar-Malware-Samples-Using-API-Call-Topics（2015）"><a href="#Discovering-Similar-Malware-Samples-Using-API-Call-Topics（2015）" class="headerlink" title="Discovering Similar Malware Samples Using API Call Topics（2015）"></a>Discovering Similar Malware Samples Using API Call Topics（2015）</h2><p>This paper aims to extract similar malware samples automatically using the concept of “API call topics,” which represents a set of API calls that are intrinsic to a specific group of malware samples. They present an unsupervised approach to extract API call topics from a large corpus of API calls. Through analysis of the API call logs collected from thousands of malware samples, they demonstrate that the extracted API call topics can detect similar malware samples. The proposed approach is expected to be useful for automating the process of analyzing a huge volume of logs collected from dynamic malware analysis systems.</p><p>The key idea of this paper is that they present an unsupervised approach to extract API call topics which can detect similar malware samples. </p><h3 id="Dataset"><a href="#Dataset" class="headerlink" title="Dataset"></a>Dataset</h3><p>The data of this paper consists of Cuckoo Sandbox logs of 2,641 malware samples collected from September 2012 to March 2013. The format of all malware samples was the Portable Executable (PE) format used in Windows operating systems. These malware samples were executed on Cuckoo Sandbox in 90 seconds and generated 1.7 GB of dynamic analysis logs in JSON format.</p><p>Some interesting they found:</p><ul><li>The majority of the samples had more than 400 API calls, a few samples had only a small number of API calls.</li><li>Malware samples that exhibited a very low number of API calls likely failed to execute correctly because of the anti-VM techniques. Therefore, they pruned malware samples with fewer than 10 API calls. </li></ul><h3 id="Methodologies"><a href="#Methodologies" class="headerlink" title="Methodologies"></a>Methodologies</h3><h4 id="API-words-features"><a href="#API-words-features" class="headerlink" title="API words features"></a>API words features</h4><p>This paper parsed the JSON formatted Cuckoo Sandbox log, then they used three variables(API function names, arguments names, argument values) to convert an API call into an “API word”， and they omitted return values and thread IDs.</p><p>They used level-3 approach to compress the API logs. Its definition is <em>api:n1:v1:n2:v2:…</em>, for example, <em>“LdrGetDllHandle”:”FileName”:”C:nnWINDOWSnnsystem32nnrpcss.dll”:”ModuleHandle”:0x00000000</em>. Using the API words, they generated a feature vector for each malware sample (By BoW approach with TF-IDF word weighting). </p><h4 id="Feature-selection"><a href="#Feature-selection" class="headerlink" title="Feature selection"></a>Feature selection</h4><p>The paper applied DF-thresholding to eliminate very popular API words that appear in a majority of the malware samples. These API words are too generic to characterize a cluster of malware samples. </p><h4 id="Non-negative-Matrix-Factorization-NMF"><a href="#Non-negative-Matrix-Factorization-NMF" class="headerlink" title="Non-negative Matrix Factorization (NMF)"></a>Non-negative Matrix Factorization (NMF)</h4><p>NMF is an algorithm that factorizes a matrix into two matrices with the constraint that all three matrices have no negative elements. The advantage of NMF is that it can extract topics that can then be used as signatures for the task. </p><p>The key idea of NMF is to factorize the feature matrix X, i.e., X = TV.</p><p><img src="6.png" alt="Figure6"></p><p>K is a parameter that controls the number of basis, which corresponds to clusters. The matrices T and V can be interpreted as the topic (basis) and the clustering results, respectively. This factorization can be obtained by minimizing the distance between the matrices X and TV: </p><p><img src="7.png" alt="Figure7"></p><p>The paper adopt Kullback-Leibler(KL) divergence to compute the distance d:</p><p><img src="8.png" alt="Figure8"></p><h3 id="Result"><a href="#Result" class="headerlink" title="Result"></a>Result</h3><p>The API call topics successfully extracted malware samples that had the similar intrinsic API calls. In addition, the detected malware samples were actually similar to each other because the majority of the detected samples have a single primary label in each cluster.</p><p><img src="9.png" alt="Figure9"></p><h2 id="DeepSign-Deep-Learning-for-Automatic-Malware-Signature-Generation-and-Classification（2015）"><a href="#DeepSign-Deep-Learning-for-Automatic-Malware-Signature-Generation-and-Classification（2015）" class="headerlink" title="DeepSign: Deep Learning for Automatic Malware Signature Generation and Classification（2015）"></a>DeepSign: Deep Learning for Automatic Malware Signature Generation and Classification（2015）</h2><p>The method uses a deep belief network (DBN), implemented with a deep stack of denoising autoencoders, generating an invariant compact representation of the malware behavior.</p><h3 id="Feature"><a href="#Feature" class="headerlink" title="Feature"></a>Feature</h3><p>This paper used one of the simplest method of NLP to approach the sandbox files, that is, using unigram(1-gram) extraction. Furthermore, this paper in order to remain as domain agnostic as possible, they propose to treat the sandbox file as a simple text file, and extract unigrams without any preprocessing. That means, they omits all meaningless symbols and only pay attention to the words, for example, given “api”: “CreateFileW”, the terms extracted are “api”: and “CreateFileW”, completely ignoring what each part means. </p><p>In order to get the fixed size features, they proposed that:</p><ol><li>For each sandbox file in the dataset, extract all unigrams.</li><li>Remove the unigrams which appear in all files (contain no information).</li><li>For each unigram count the number of files in which it appears.</li><li>Select top 20,000 with highest frequency.</li><li>Convert each sandbox file to a 20,000 sized bit string, by checking whether each of the 20,000 unigrams appeared in it.</li></ol><p>In their approach, they trained a deep denoising autoencoder consisting of eight layer: 20,000–5,000–2,500–1,000–500–250–100–30. At each step only one layer is trained, then the weights are “frozen”, and the subsequent layer is trained. </p><p>At the end of this training phase, they have a deep network which is capable of converting the 20,000 input vector into 30 floating point values. They regard these 30-sized vector as the “signature” of the program.</p><p>The process of DBN training looks like:</p><p><img src="10.png" alt="Figure10"></p><p>(I) Train the first autoencoder layer. (II) Use the weights from the previous layer, and build a new autoencoder on top of them. The weights taken from the first autoencoder are frozen. (III) Similarly, take the weights from the previous layer and add a new layer. (IV) Proceed with layer-wise training until training all the eight layers for the final DBN.</p><p>The all steps of this paper:</p><p><img src="11.png" alt="Figure11"></p><p>Some configuration:</p><ul><li>Each layer-wise training with a 0.5 dropout.</li><li>Using ReLU as the activation function.</li><li>0.2 noise ratio, 0.001 learning rate then linearly decays to 0.000001, batch size of 20, and no momentum.</li></ul><h3 id="Dataset-1"><a href="#Dataset-1" class="headerlink" title="Dataset"></a>Dataset</h3><p>Their dataset consists of six major categories of malware, and 300 variants in each category, for a total of 1,800 samples. The six malware categories used are Zeus, Carberp, Spy-Eye, Cidox, Andromeda, and DarkComet.</p><h3 id="Experimental-Results"><a href="#Experimental-Results" class="headerlink" title="Experimental Results"></a>Experimental Results</h3><ul><li>SVM with 96.4% accuracy.</li><li>KNN with 95.3% accuracy.</li><li>This paper’s DBN with 98.6% accuracy.</li></ul><h2 id="A-Novel-Approach-to-Detect-Malware-Based-on-API-Call-Sequence-Analysis（2015"><a href="#A-Novel-Approach-to-Detect-Malware-Based-on-API-Call-Sequence-Analysis（2015" class="headerlink" title="A Novel Approach to Detect Malware Based on API Call Sequence Analysis（2015)"></a>A Novel Approach to Detect Malware Based on API Call Sequence Analysis（2015)</h2><p>This paper adopted DNA sequence alignment algorithms and extracted common API call sequence patterns of malicious function from malware in different categories. They indicated that two major approaches in dynamic analysis are control flow analysis and API call analysis. However, malware authors try to circumvent those techniques through inserting meaningless codes or shuffling the sequence of programs.</p><h3 id="DNA-Sequence-Alignment"><a href="#DNA-Sequence-Alignment" class="headerlink" title="DNA Sequence Alignment"></a>DNA Sequence Alignment</h3><p>Sequence alignment algorithms are most widely used in the area of biometrics to calculate the similarity between two or more DNA sequences or to find certain DNA subsequences from full DNA.</p><p>Sequence alignment algorithms fall into two categories, which are global and local alignment. Global alignment aligns entire sequence. It is useful when they attempt to find the sequence of most similar length and strings. Local alignment finds the highly similar subsequences between given sequences. </p><p>Pairwise sequence alignment methods find global alignments or the locally similar subsequences of two sequences. Multiple sequence alignment algorithm extends pairwise alignment to handle multiple sequences at the same time. This paper applied multiple sequence alignment algorithm.</p><h3 id="Environment-Setup"><a href="#Environment-Setup" class="headerlink" title="Environment Setup"></a>Environment Setup</h3><p>In this study, they used the Detours hooking library supported byMicrosoft to trace API call sequences from Windows executable programs. The Detours hooking process proceeds as shown in the following. Unlike the original process without Detours, the hooking process goes through the Detour function and the Trampoline function. This allows them to trace API call sequence.</p><p><img src="12.png" alt="Figure12"></p><p>For DNA sequence alignment, they used the ClustalX. ClustalX is widely used freeware in genome sequence analysis, such as DNA, RNA, or protein sequences. The program supports multiple sequence alignment (MSA) and also provides visualization.</p><p>In their dataset, 2,727 kinds of API were found; they categorized them into 26 groups according to MSDN library:<br>In our dataset, 2,727 kinds of API were found; they categorized them into 26 groups according to MSDN library:</p><p><img src="13.png" alt="Figure13"></p><p>To extract the common API call sequence pattern among malware, the longest common subsequences (LCSs) were used. LCS shows the longest malware API call sequence pattern, it can be treated as a malware’s signature. For example, the LCSs of ABCD and ACB are AB and AC:</p><p>Furthermore, they excluded these typical LCSs reduce false positive error since some benign programs share same API calls sequence. They employed an additional feature. They assumed that there exists a specific API call sequence pattern that matches specific malicious activity. If such an API call sequence pattern exists, they can detect unknown attacks by checking whether a critical API call sequence pattern exists. For example:</p><p><img src="14.png" alt="Figure14"></p><p>It should be noted that round brackets represent the OR relation. For example, in the case of IAT hooking, one possible critical API call sequence pattern is [LoadLibrary, strcmp, VirtualProtect]. They believe that this behavior-based malware classification can be effective in the dynamic analysis of API call sequences.</p><p>The overall process as following:</p><p><img src="15.png" alt="Figure15"></p><p>In the analysis process, the extracted API call sequence of a newly inserted program is compared with the API signatures in the database. If any LCS ismatched with the signatures, the newly inserted program is classified as malware.</p><h3 id="Experiment-result"><a href="#Experiment-result" class="headerlink" title="Experiment result"></a>Experiment result</h3><p>To summarize, the precision is 1, FPR is 0, and recall is 0.998. The 𝐹1 score is 0.999.</p><p><img src="16.png" alt="Figure16"></p><h2 id="MALWARE-CLASSIFICATION-WITH-RECURRENT-NETWORKS-2015"><a href="#MALWARE-CLASSIFICATION-WITH-RECURRENT-NETWORKS-2015" class="headerlink" title="MALWARE CLASSIFICATION WITH RECURRENT NETWORKS(2015)"></a>MALWARE CLASSIFICATION WITH RECURRENT NETWORKS(2015)</h2><p>This paper proposed a different approach, which, similar to natural language modeling, learns the language of malware spoken through the executed instructions and extracts robust, time domain features. Echo state networks (ESNs) and recurrent neural networks (RNNs) are used for the projection stage that extracts the features.</p><p>Specifically, using the recurrent model to directly classify the files is not efficient (there is only a single bit of information for every sequence). Instead, they use a recurrent model trained to predict next API call, and use the hidden state of the model (that encodes the history of past events) as the fixed-length feature vector that is given to a separate classifier (logistic regression or MLP).</p><p>The overall process as following:</p><p><img src="18.png" alt="Figure18"></p><p>ut and ht demote the input and state vectors. At each state, they apply a recurrent neural network operation to the input and get a hidden vector ht. Then they apply a max-pooling to get a feature vector, then, applying a MLP or logistic regression to do the classification on the feature vector</p><p>Finally, they got TPR 71.71% at FPR 0.1%.</p><h2 id="Malware-Characterization-using-Windows-API-Call-Sequences-2016"><a href="#Malware-Characterization-using-Windows-API-Call-Sequences-2016" class="headerlink" title="Malware Characterization using Windows API Call Sequences(2016)"></a>Malware Characterization using Windows API Call Sequences(2016)</h2><p>This paper proposed a hashed method to identify the category of malware. First, they chose a total set of 534 important Win-APIs have been hooked and mapped to 26categories (A…Z). Then they used Fuzzy hashing algorithm ssdeep to generate fuzzy hash based signature. These signatures were matched to quantify the API call sequence homologies between test samples and training samples.</p><p>The overall process as following:</p><p><img src="19.png" alt="Figure19"></p><h3 id="Categorization-of-Win-API-Calls"><a href="#Categorization-of-Win-API-Calls" class="headerlink" title="Categorization of Win-API Calls"></a>Categorization of Win-API Calls</h3><p>This paper have categorized the total set of 534 Win-API calls into 26 Categories based upon the function these APIs are performing.</p><p><img src="20.png" alt="Figure20"></p><h3 id="Fuzzy-Hash-Signatures"><a href="#Fuzzy-Hash-Signatures" class="headerlink" title="Fuzzy Hash Signatures"></a>Fuzzy Hash Signatures</h3><p>This paper applied ssdeep program to compute Context Triggered Piecewise Hash (CTPH), also called fuzzy hash, on the categorized API call sequences.</p><p>The concept of fuzzy hashing has been used as it has the capability to compare two different samples and determine the level of similarity between them. Instead of generating a single hash for a file, piecewise hashing generates many hashes for a file based on different sections of the file. CTPH algorithm uses the rolling hash to determine the start and stop of each segment. CTPH Signature generation algorithm combines these section hashes in a particular way to generate the fuzzy hash of the file.</p><p>They selected this technique because CTPH can match inputs that have homologies and these sequences may be different in both content and length.</p><p><img src="21.png" alt="Figure21"></p><p>Ssdeep matching algorithm calculates the matching between fuzzy hashes of two different samples. This score is based on the edit distance algorithm: Damerau Levenshtein Distance between two strings. After matching a comparison score is generated between 0 and 100. they have used this matching score as malware classification criteria.</p><h3 id="Result-1"><a href="#Result-1" class="headerlink" title="Result"></a>Result</h3><p>Following table gives the classification accuracy and FPR for these five 2-class problems.</p><p><img src="22.png" alt="Figure22"></p><p>In addition, this table gives the accuracy &amp; FPR for 2 class classifier problems (Malware vs Benign).</p><p><img src="23.png" alt="Figure23"></p><h2 id="Deep-Learning-LSTM-based-Ransomeware-Detection-2017"><a href="#Deep-Learning-LSTM-based-Ransomeware-Detection-2017" class="headerlink" title="Deep Learning LSTM based Ransomeware Detection(2017)"></a>Deep Learning LSTM based Ransomeware Detection(2017)</h2><p>This paper aims to detect ransomware behavior by employing Long-Short Term Memory (LSTM) networks for binary sequence classification of API calls. They present an automated approach to extract API calls from the log of modified sandbox environment and detect ransomware behavior.</p><p>Ransomware are malicious software designed to deny access to a computer system or computer resource. There are two categories of ransomware: Crypto ransomware and locker ransomware. Locker ransomware try to lock access to the system user interface. Crypto ransomware on the other hand, deny access to user ata using strong encryption algorithms.</p><h3 id="API-call-extraction"><a href="#API-call-extraction" class="headerlink" title="API call extraction"></a>API call extraction</h3><p>This paper used a cuckoo-modified since it has provided more anti-anti sandbox features than the standard version. Also, the timeout value for analysis has been extended to 20 minutes to defeat delayed execution strategies employed by some malwares authors.</p><p>The overall process as following:</p><p><img src="17.png" alt="Figure17"></p><h3 id="LSTM"><a href="#LSTM" class="headerlink" title="LSTM"></a>LSTM</h3><p>This paper picked up 38 APIs which were common in all the samples. And then labeled them as an integer between 1 and 39. Each process had a different length of API sequence. After the conversion to integer, 0s were appended to the sequences to match the length of the longest sequence of length 779980.</p><p>Finally, they received an experimental accuracy of 96.67%.</p><h2 id="NEURAL-SEQUENTIAL-MALWARE-DETECTION-WITH-PARAMETERS-2018"><a href="#NEURAL-SEQUENTIAL-MALWARE-DETECTION-WITH-PARAMETERS-2018" class="headerlink" title="NEURAL SEQUENTIAL MALWARE DETECTION WITH PARAMETERS(2018)"></a>NEURAL SEQUENTIAL MALWARE DETECTION WITH PARAMETERS(2018)</h2><p>This paper extend previous work, they said the previous LSTM model only considered the API calls sequence but ignored its parameters. So they extend this model to include each system API call’s two most input parameters.</p><p>Due to the variation in the structure and size of the vocabulary, this paper used these parameters in a limited character space. They used these parameters by learning character grams from them. They built n-grams over these characters in the corpus, and only extract the top-K by the frequency distribution.</p><p>The process of constructing character grams as following:</p><p><img src="24.png" alt="Figure24"></p><p>Then by using the following two algorithms, they converted the n-grams to indexed vector.</p><p><img src="25.png" alt="Figure25"></p><p><img src="26.png" alt="Figure26"></p><p>Then they applied tanh to each vectored parameter, finally concatenate each API calls name with its parameters together.</p><p>The overall process as following:</p><p><img src="28.png" alt="Figure28"></p><p>The LSTM process looks like the above paper, which used a maxpooling to extract feature from each stage of LSTM, then applied a logistic regression followed by a ReLU and sigmoid.</p><h3 id="Result-2"><a href="#Result-2" class="headerlink" title="Result"></a>Result</h3><p>They collected the system API calls and their parameters from 75,000 files, which were evenly split between malware and benign files, in September 2017. The data from all of the files is unique, and there is a total of 158 event types in our data.</p><p>Comparison of the ROC curves for two versions of the Events Only LSTM+NN model, the Events Only CNN+LSTM model, and the proposed Events + Parameters model as following:</p><p><img src="29.png" alt="Figure29"></p>]]></content>
    
    <summary type="html">
    
      This post summaries 8 papers on dynamic malware detection domain.
    
    </summary>
    
    
      <category term="Research" scheme="https://joddiy.github.io/categories/Research/"/>
    
    
      <category term="Malware Detection" scheme="https://joddiy.github.io/tags/Malware-Detection/"/>
    
  </entry>
  
  <entry>
    <title>Hello World</title>
    <link href="https://joddiy.github.io/2000/01/01/hello-world/"/>
    <id>https://joddiy.github.io/2000/01/01/hello-world/</id>
    <published>1999-12-31T16:00:00.000Z</published>
    <updated>2020-05-07T12:28:14.976Z</updated>
    
    <content type="html"><![CDATA[<p>Welcome to <a href="https://hexo.io/" target="_blank" rel="noopener">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/" target="_blank" rel="noopener">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html" target="_blank" rel="noopener">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues" target="_blank" rel="noopener">GitHub</a>.</p><h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo new <span class="string">"My New Post"</span></span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/writing.html" target="_blank" rel="noopener">Writing</a></p><h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/server.html" target="_blank" rel="noopener">Server</a></p><h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/generating.html" target="_blank" rel="noopener">Generating</a></p><h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/one-command-deployment.html" target="_blank" rel="noopener">Deployment</a></p>]]></content>
    
    <summary type="html">
    
      Welcome to Hexo.
    
    </summary>
    
    
    
  </entry>
  
</feed>
