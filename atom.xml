<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>Jz Blog</title>
  
  <subtitle>Trivias</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="https://joddiy.github.io/"/>
  <updated>2020-09-08T22:54:18.138Z</updated>
  <id>https://joddiy.github.io/</id>
  
  <author>
    <name>Joddiy Zhang</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>Serverless Computing</title>
    <link href="https://joddiy.github.io/2020/09/09/Serverless-Computing/"/>
    <id>https://joddiy.github.io/2020/09/09/Serverless-Computing/</id>
    <published>2020-09-08T20:13:11.000Z</published>
    <updated>2020-09-08T22:54:18.138Z</updated>
    
    <content type="html"><![CDATA[<h1 id="Serverless-Computing-One-Step-Forward-Two-Steps-Back"><a href="#Serverless-Computing-One-Step-Forward-Two-Steps-Back" class="headerlink" title="Serverless Computing: One Step Forward, Two Steps Back"></a>Serverless Computing: One Step Forward, Two Steps Back</h1><p>A FaaS offering by itself is of little value, since each function execution is isolated and ephemeral. Building applications on FaaS requires data management in both persistent and temporary storage.</p><p>As a result, cloud providers are quick to emphasize that serverless is not only FaaS. It is FaaS supported by a <strong>“standard library”: the various multitenanted, autoscaling services provided by the vendor.</strong></p><!-- ### ForwardIt is autoscaling. The workload automatically drives the allocation and deallocation of resources.## Two Major Backward.First, they painfully ignore the importance of efficient data processing. Second, they stymie the development of distributed systems. --><h2 id="Three-Design-Patterns"><a href="#Three-Design-Patterns" class="headerlink" title="Three Design Patterns"></a>Three Design Patterns</h2><p>Embarrassingly parallel functions</p><blockquote><p>In some applications, each function invocation is an independent task and never needs to communicate with other functions.</p></blockquote><p>Orchestration functions</p><blockquote><p>A second class of use cases leverages serverless functions simply to orchestrate calls to proprietary autoscaling services, such as large-scale analytics.</p></blockquote><p>Function Composition</p><blockquote><p>The third category consists of collections of functions that are composed to build applications and thus need to pass along outputs and inputs.</p></blockquote><h2 id="Serverless-Is-Too-Less"><a href="#Serverless-Is-Too-Less" class="headerlink" title="Serverless Is Too Less"></a>Serverless Is Too Less</h2><p>Serverless computing today is at best a simple and powerful way to run embarrassingly parallel computations or harness proprietary services.</p><p><strong>The limitations in today’s FaaS offerings:</strong></p><p>(1). Limited Lifetimes</p><p>After 15 minutes, function invocations are shut down by the Lambda infrastructure. </p><p>There is no way to ensure that subsequent invocations are run on the same VM. Hence functions must be written assuming that state will not be recoverable across invocations.</p><p>(2). I/O Bottlenecks</p><p>Recent studies show that a single Lambda function can achieve on average 538Mbps network bandwidth.</p><p>Worse, AWS appears to attempt to pack Lambda functions from the same user together on a single VM, so the limited bandwidth is shared by multiple functions.</p><p>(3). Communication Through Slow Storage</p><p>Two Lambda functions can only communicate through an autoscaling intermediary service.</p><p>Hence maintaining state across client calls requires writing the state out to slow storage, and reading it back on every subsequent call.</p><p><strong>A number of corollaries follow directly.</strong></p><p>FaaS is a Data-Shipping Architecture</p><blockquote><p>FaaS routinely “ships data to code” rather than “shipping code to data.”</p></blockquote><p>FaaS Stymies Distributed Computing</p><blockquote><p>Two functions can work together serverlessly only by passing data through slow and expensive storage.</p></blockquote><h2 id="Case-Studies-Model-Training"><a href="#Case-Studies-Model-Training" class="headerlink" title="Case Studies: Model Training"></a>Case Studies: Model Training</h2><p>(1). Lambda:</p><p>Each Lambda is allocated the maximum lifetime (15 min) and 640MB RAM and runs as many training iterations as possible.</p><p>Each iteration in Lambda took <strong>3.08 seconds</strong>: 2.49 to fetch a 100MB batch from S3 and 0.59 seconds to run the AdamOptimizer.</p><p>We trained the model over 10 full passes of the training data, which translates to <strong>31 sequential lambda executions</strong>, each of which runs for 15 minutes, or 465 minutes total latency. This costs $0.29.</p><p>(2). Tensorflow</p><p>For comparison, we trained the same model on an m4.large EC2 instance, which has 8GB of RAM and 2vCPUs. In this setting, each iteration is significantly faster <strong>(0.14 seconds)</strong>: 0.04 seconds to fetch data from an EBS volume and 0.1 seconds to run the optimizer.</p><p>The same training process takes about 1300 seconds (just under 22 minutes), which translates to a cost of $0.04.</p><p>Lambda’s limited resources and data-shipping architecture mean that running this algorithm on Lambda is 21× slower and 7.3× more expensive than running on EC2.</p><h2 id="Case-Studies-Prediction-Serving"><a href="#Case-Studies-Prediction-Serving" class="headerlink" title="Case Studies: Prediction Serving"></a>Case Studies: Prediction Serving</h2><p>In practice, prediction serving relies on access to specialized hardware like GPUs, which are not available through AWS Lambda.</p><p>To that end, we exercised Lambda’s favored service for batching inputs: AWS Simple Queueing Service (SQS). Our model in this experiment is a simple blacklist of dirty words. SQS only allows batches of 10 messages at a time, so we limited all experiments here to 10-message batches.</p><p>The average latency over 1,000 batch invocations for the Lambda application was <strong>559ms</strong> per batch if the model was retrieved on every invocation and results written back to S3 and was <strong>447ms</strong> when results were placed back into an SQS queue.</p><p>The first replaced Lambda’s role in the application with an EC2 machine to receive SQS message batches—this showed a latency of <strong>13ms</strong> per batch averaged over 1,000 batches—27× faster than our “optimized” Lambda implementation. </p><p>The second experiment used ZeroMQ to replace SQS’s role in the application, and receive messages directly on the EC2 machine. This “serverful” version had a per batch latency of <strong>2.8ms</strong>—127× faster than the optimized Lambda implementation.</p><p>SQS request rate alone would cost $1,584 per hour. </p><h2 id="Case-Studies-Distributed-Computing"><a href="#Case-Studies-Distributed-Computing" class="headerlink" title="Case Studies: Distributed Computing"></a>Case Studies: Distributed Computing</h2><p>try alternative solutions to achieve distributed computation.</p><img src="1.png" style="width:800px;"/><p>First we measure the pure functional, event-driven programming cost of a 1KB object being handled by a Lambda function invocation—this incurs both I/O and function overheads and is exorbitantly expensive5. Next we measure the cost of explicit I/O from Lambda, namely an average write+read from a long-running function—this is still over an order of magnitude slower than one would like.</p><p>We see that latencies from EC2 are almost identical, so the overhead is in the storage service costs, not in Lambda.</p><p>Finally we show the latency of (acked) messaging using Python functions directly addressing each other via ZeroMQ. This last cost is close to typical intra-rack datacenter network measurements; studies from even a few years ago report average inter-rack measurements around 1.26ms</p><p>In sum, communicating via cloud storage is not a reasonable replacement for directly-addressed networking, even with direct I/O.</p><p>DynamoDB as a fine-grained communication medium is incredibly expensive: Supporting a cluster of 1,000 nodes costs at minimum $450 per hour.</p><h1 id="Cirrus-a-Serverless-Framework-for-End-to-end-MLWorkflows"><a href="#Cirrus-a-Serverless-Framework-for-End-to-end-MLWorkflows" class="headerlink" title="Cirrus: a Serverless Framework for End-to-end MLWorkflows"></a>Cirrus: a Serverless Framework for End-to-end MLWorkflows</h1><p>This work proposes Cirrus, a distributed ML training framework that addresses these challenges by leveraging serverless computing.</p><h2 id="End-to-end-MLWorkflow-Challenges"><a href="#End-to-end-MLWorkflow-Challenges" class="headerlink" title="End-to-end MLWorkflow Challenges"></a>End-to-end MLWorkflow Challenges</h2><p><strong>Over-provisioning</strong></p><blockquote><p>The heterogeneity of the different tasks in an MLworkflowleads to a significant resource imbalance during the execution of a training workflow.</p></blockquote><p><strong>Explicit resource management</strong></p><blockquote><p>The established approach of exposing low-level VM resources, such as storage and CPUs, puts a significant burden on ML developers who are faced with the challenge of provisioning, configuring, and managing these resources for each of their ML workloads.</p></blockquote><h2 id="Serverless-Computing"><a href="#Serverless-Computing" class="headerlink" title="Serverless Computing"></a>Serverless Computing</h2><p>major limitations of existing serverless environments:</p><p><strong>Small local memory and storage</strong></p><blockquote><p>AWS lambdas can only access at most 3GB of local RAM and 512MB of local disk</p></blockquote><p><strong>Lowbandwidth and lack of P2P communication</strong></p><blockquote><p>We find that the largest AWS Lambda can only sustain 60MB/s of bandwidth. AWS Lambdas do not allow peer-to-peer communication. Thus, common communication strategies used for datacenter ML, such as tree-structured or ring-structured AllReduce communication [43], become impossible to implement efficiently in such environments.</p></blockquote><p><strong>Short-lived and unpredictable launch times</strong></p><blockquote><p>AWS lambdas can take up to several minutes to start after being launched. This means that during training, lambdas start at unpredictable times and can finish in the middle of training. This requires ML runtimes for lambdas to tolerate the frequent departure and arrival of workers.</p></blockquote><p><strong>Lack of fast shared storage</strong></p><blockquote><p>shared storage needs to be low-latency, high-throughput, and optimized for the type of communications in ML workloads.</p></blockquote><h2 id="CIRRUS-DESIGN"><a href="#CIRRUS-DESIGN" class="headerlink" title="CIRRUS DESIGN"></a>CIRRUS DESIGN</h2><img src="2.png" style="width:800px;"/><p>Design Principles:</p><ul><li>Adaptive, fine-grained resource allocation.</li><li>Stateless server-side backend.</li><li>End-to-end serverless API.</li><li>High scalability.</li></ul><p>(1) Worker runtime</p><p>The system runtime meets two goals: 1) lightweight, and 2) high-performance.</p><p>The worker runtime provides two interfaces. </p><p>First, it provides a smart iterator for training datasets stored in S3. This iterator prefetches and buffers minibatches in the lambda’s local memory in parallel with the worker’s computations to mitigate the high latency (&gt;10ms) of accessing S3.</p><p>Second, it provides an API for the distributed data store. This API implements: data compression, sparse transfers of data, asynchronous communication and sharding across multiple nodes.</p><p>(2) Distributed data store</p><p>Cirrus’s data store serves the purpose of storing intermediate data to be shared by all workers.</p><p>To achieve low-latency, we deploy our data store in cloud VMs. It achieves latencies as low as 300μs versus ≈ 10ms for AWS S3.</p><h2 id="API-Example"><a href="#API-Example" class="headerlink" title="API Example"></a>API Example</h2><img src="3.png" style="width:800px;"/><p>The first step in the workflow with Cirrus is to load the dataset and upload it to S3. </p><p>Cirrus parses the data, automatically creates partitions for it and then uploads it to S3. The front-end partitions datasets in blocks of roughly 10MB. </p><p>We chose this size because data partitions in Cirrus are the granularity of data workers transfer from S3. We have found this size allows lambda workers to achieve good network transfer bandwidth. In addition, this keeps the size of each worker’s minibatch cache small.</p><h2 id="Cirrus-Implementation"><a href="#Cirrus-Implementation" class="headerlink" title="Cirrus Implementation"></a>Cirrus Implementation</h2><p>(1) Client backend</p><p>Lambdas that are launched during training are relaunched automatically when their lifetime terminates (every 15 minutes).</p><p>The backend keeps a pool of threads that can be used for responding to requests for new lambda tasks.</p><p>(2) Distributed data store</p><p>Cirrus’s distributed data store provides an interface supporting a key-value store interface (set/get) and a parameter-server interface (send gradient / get model).</p><p>several optimizations:</p><ul><li>multithreaded server that distributes work across many cores</li><li>data compression for the gradientand models</li><li>sparse gradient and model data structures</li></ul><p>(3) Worker runtime</p><img src="4.png" style="width:800px;"/><p>For data access, the runtime provides a minibatch-based iterator backed by a local memory ring-buffer that allows workers to access training minibatches with low latency. </p><p>In addition, it provides an efficient API to communicate with the distributed data store.</p><h2 id="EVALUATION"><a href="#EVALUATION" class="headerlink" title="EVALUATION"></a>EVALUATION</h2><img src="5.png" style="width:800px;"/>]]></content>
    
    <summary type="html">
    
      Paper summary for Serverless Computing
    
    </summary>
    
    
      <category term="Study Notes" scheme="https://joddiy.github.io/categories/Study-Notes/"/>
    
    
      <category term="Tools" scheme="https://joddiy.github.io/tags/Tools/"/>
    
      <category term="Deep Learning" scheme="https://joddiy.github.io/tags/Deep-Learning/"/>
    
  </entry>
  
  <entry>
    <title>Model Serving</title>
    <link href="https://joddiy.github.io/2020/09/02/Model-Serving/"/>
    <id>https://joddiy.github.io/2020/09/02/Model-Serving/</id>
    <published>2020-09-01T23:31:18.000Z</published>
    <updated>2020-09-08T20:29:45.767Z</updated>
    
    <content type="html"><![CDATA[<h1 id="From-the-Edge-to-the-Cloud-Model-Serving-in-ML-NET"><a href="#From-the-Edge-to-the-Cloud-Model-Serving-in-ML-NET" class="headerlink" title="From the Edge to the Cloud: Model Serving in ML.NET"></a>From the Edge to the Cloud: Model Serving in ML.NET</h1><p>In this paper, they:</p><ul><li>give an overview of current state of the art practices for surfacing ML pipelines predictions into applications</li><li>highlight the limitations of using containers to operationalize models for application consumption. </li></ul><h2 id="Model-Serving-An-Overview"><a href="#Model-Serving-An-Overview" class="headerlink" title="Model Serving: An Overview"></a>Model Serving: An Overview</h2><p><img src="1.png" alt=""></p><h2 id="Deploying-Models-into-Containers"><a href="#Deploying-Models-into-Containers" class="headerlink" title="Deploying Models into Containers"></a>Deploying Models into Containers</h2><p>The first option, depicted in Figure 2(a) is to ship models into containers (e.g., Docker [4]) wired with proper Remote Procedure Calls(RPCs) to a Web Server. With this approach, predictions have to go through the network and be rendered on the cloud: low latency or edge scenarios are therefore out of scope.</p><p>Pros:</p><ul><li>decoupling models from serving system development.</li><li>eases the implementation of mechanisms and policies for fault tolerance and scalability. </li><li>hardware acceleration can be exploited when available.</li></ul><p>Cons:</p><ul><li>each container comes with its own runtime and set of processes, thus introducing memory overheads that can possibly be higher than the actual model size.</li><li>the RPC layer and REST API introduce network communication costs.</li><li>only a restricted set of optimizations are available, no knowledge of the internals of the pipelines.</li></ul><h2 id="Importing-Models-Directly-into-Applications"><a href="#Importing-Models-Directly-into-Applications" class="headerlink" title="Importing Models Directly into Applications"></a>Importing Models Directly into Applications</h2><p>The second option (Figure 2(b) is to integrate the model logic directly into the application(ML.NET: the model is a dynamic library the application can link). This approach is suitable for the cloud as well as for edge devices and it unlocks low latency scenarios.</p><p>Pros:</p><ul><li>removes the overhead of managing containers and implementing RPC functionalities to communicate with the Serving System.</li></ul><p>Cons:</p><ul><li>sub-optimal from a performance perspective, DAGs with high chance of overlapping structure and similar parameters, but these similarities cannot be recognized nor exploited using a black-box approach.</li></ul><h2 id="White-Box-Model-Serving"><a href="#White-Box-Model-Serving" class="headerlink" title="White Box Model Serving"></a>White Box Model Serving</h2><p>Models are registered to a Runtime that considers them not as mere executable code but as DAGs of operators.</p><p>Pros:</p><ul><li>apply optimizations over the models such as operator reordering to improve latency or operator and sub-graph sharing to improve memory consumption and computation reuse.</li></ul><p>End-to-end Optimizations:</p><ul><li>avoid memory allocation on the data path</li><li>avoid creating separate routines per operator when possible</li><li>avoid reflection and JIT compilation at prediction time</li></ul><p>Multi-model Optimizations:</p><ul><li>shareable components have to be uniquely stored in memory and reused as much as possible to achieve optimal memory usage</li></ul><h2 id="PRETZEL"><a href="#PRETZEL" class="headerlink" title="PRETZEL"></a>PRETZEL</h2><p>PRETZEL views models as database queries and employs database techniques to optimize DAGs and to improve end-to-end performance.</p><p><img src="2.png" alt=""></p><p>off-line phase:</p><ul><li>pre-trained ML.NET pipelines are translated into Flour transformations. </li><li>Oven optimizer re-arranges and fuses transformations into model plans composed of parameterized logical units called stages. </li><li>Each logical stage is then Ahead-Of-Time(AOT)-compiled into physical computation units. </li><li>Logical and physical stages together with model parameters and training statistics form a model plan. </li><li>Model plans are registered for prediction serving in the Runtime where physical stages and parameters are shared among pipelines with similar model plans.</li></ul><p>on-line phase:</p><ul><li>physical stages are parameterized dynamically with the proper values maintained in the ObjectStore. </li><li>The Scheduler is in charge of binding physical stages to shared execution units.</li></ul><h1 id="Cloudburst-Stateful-Functions-as-a-Service"><a href="#Cloudburst-Stateful-Functions-as-a-Service" class="headerlink" title="Cloudburst: Stateful Functions-as-a-Service"></a>Cloudburst: Stateful Functions-as-a-Service</h1><p>Today’s popular FaaS platforms only work well for isolated, stateless functions. </p><blockquote><p>The hallmark autoscaling feature of serverless platforms is enabled by an increasingly popular design principle: the disaggregation of storage and compute services.<br>Unfortunately, today’s FaaS platforms take disaggregation to an extreme. </p></blockquote><p>We are interested in exploring designs that preserve the autoscaling and operational benefits of current offerings(FaaS), while adding performant, cost-efficient, and consistent shared state and communication(Stateful).</p><p>Stateful Serverless via logical disaggregation with physical colocation(LDPC):</p><ul><li>deploy resources to diffierent services in close physical proximity.</li></ul><p>This paper presents a new Function-as-a-Service platform called Cloudburst. Cloudburst achieves this via a combination of an autoscaling key-value store (providing state sharing and overlay routing) and mutable caches co-located with function executors (providing data locality).</p><h2 id="Today’s-Serverless-Functions"><a href="#Today’s-Serverless-Functions" class="headerlink" title="Today’s Serverless Functions:"></a>Today’s Serverless Functions:</h2><p>Function Composition: AWS Lambda imposes a latency overhead of up to 20ms for a single function<br>invocation.</p><p>Direct Communication: point-to-point communication may seem tricky in a system with dynamic membership, distributed hashtables (DHTs) or lightweight key-value stores (KVSs) can provide a lower-latency solution.</p><p>Low-Latency Access to Shared Mutable State: worse than shared memory, weak data consistency guarantees.</p><h2 id="Stateful-Serverless"><a href="#Stateful-Serverless" class="headerlink" title="Stateful Serverless:"></a>Stateful Serverless:</h2><p>distributed storage(KVS server) and local caching(KVS cache).</p><p>Coordination-free consistency.</p><h2 id="Programming-Interface"><a href="#Programming-Interface" class="headerlink" title="Programming Interface"></a>Programming Interface</h2><p>To enable stateful functions, Cloudburst allows programmers to put and get Python objects via the Anna KVS API.</p><p>For repeated execution, Cloudburst allows users to register arbitrary compositions of functions as DAGs.</p><p><img src="3.png" alt=""></p><h2 id="Architecture"><a href="#Architecture" class="headerlink" title="Architecture"></a>Architecture</h2><p><img src="4.png" alt=""></p><p>User requests are received by a scheduler, which routes them to function executors. Each scheduler operates independently, and the system relies on a standard stateless cloud load balancer(AWS Elastic Load Balancer).</p><p> Function executors run in individual processes that are packed into VMs along with a local cache per VM. The cache on each VM intermediates between the local executors and the remote KVS.</p><p>All Cloudburst components are run in individual Docker containers. Cloudburst uses Kubernetes [49] simply to start containers and redeploy them on failure.</p><p><strong>Function Executors:</strong> Before each invocation, the executor retrieves and deserializes the requested function and transparently resolves all KVS reference function arguments in parallel.</p><p><strong>Caches:</strong> To ensure that frequently-used data is locally available, every function execution VM has a local cache, which executors contact via IPC.</p><p><strong>Function Schedulers</strong>: prioritize data locality when scheduling both single functions and DAGs. picks the executor with the most data cached locally.</p><p><strong>Monitoring and Resource Management</strong>: For each DAG, If the incoming request rate is significantly higher than the request completion rate of the system, the monitoring engine will increase the resources allocated to that DAG function.</p><p><strong>Fault Tolerance:</strong> If a machine fails while executing a function, the whole DAG is re-executed after a configurable timeout. </p><h1 id="Optimizing-Prediction-Serving-on-Low-Latency-Serverless-Dataflow"><a href="#Optimizing-Prediction-Serving-on-Low-Latency-Serverless-Dataflow" class="headerlink" title="Optimizing Prediction Serving on Low-Latency Serverless Dataflow"></a>Optimizing Prediction Serving on Low-Latency Serverless Dataflow</h1><p>prediction serving three key properties:</p><ul><li>computationally intensive</li><li>low latency requirements</li><li>compositional, meaning a single request passes through multiple stages.</li><li>(for system) must operate in the presence of bursty and unpredictable workloads.</li></ul><p>AWS Sagemaker and Azure ML deploy individual models as separate microservices, However, this approach:</p><ul><li>no visibility into the structure</li><li>how the individual microservices relate to each other, which significantly complicates debugging and limits end-to-end performance optimization.</li></ul><p>Pretzel:<br>requires the developer to rewrite their individual models (i.e., pipeline stages) which is cumbersome and limits model development.</p><p>In this paper, (black-box) operators, which are typically models trained by users in their library of choice. A graph of familiar dataflow operators (e.g., map, filter, join) can be used to wrap black-box models.</p><p>They present Cloudflow, a dataflow system for prediction serving pipelines. Cloudflow is built on top of Cloudburst.</p><h2 id="Architecture-and-API"><a href="#Architecture-and-API" class="headerlink" title="Architecture and API"></a>Architecture and API</h2><p><img src="5.png" alt=""></p><p>Dataflow API:</p><p>anyof passes exactly one of its input tables to the output.</p><p>fuse is an internal operator that executes multiple other operators over a Table.</p><p><img src="6.png" alt=""></p><p>Cloudflow also has an extend method. extend takes in another valid flow as an argument and appends its DAG to the existing flow, creating a dataflow that chains the pair.</p><h2 id="Optimizing-Dataflows"><a href="#Optimizing-Dataflows" class="headerlink" title="Optimizing Dataflows"></a>Optimizing Dataflows</h2><p>All the techniques described in this section are automatic optimizations; the user only needs to select which optimizations to enable.</p><p><strong>Operator Fusion:</strong> compiled into a single Cloudburst function. greedily fuse operators with same resource requirements.</p><p><strong>Competitive Execution:</strong> reduce the tail latency, redundant parallel replicas of the operator in question and add an anyof to consume the results.</p><p><strong>Operator Autoscaling and Placement:</strong> Cloudburst natively autoscales function individually. Then we extended the Cloudburst API to allow functions to be annotated with different resource class labels.</p><p><strong>Data Locality via Dynamic Dispatch:</strong> </p><ul><li>Cloudflow fuses each lookup operator with the operator downstream from it.</li><li>Cloudburst to perform dynamic dispatch of the (fused) operator at a machine that has cached the column value.</li></ul><p><strong>Batching:</strong> Cloudflow’s API provides a flag for the function arguments to map and filter to declare batch-awareness. Cloudburst dequeues multiple execution requests and executes the entire batch in a single.</p><h1 id="Notes"><a href="#Notes" class="headerlink" title="Notes"></a>Notes</h1><p>FasS</p><ul><li>low latency</li><li>cost efficieny</li><li>workload burst from 0 to most</li><li>can do training?</li></ul>]]></content>
    
    <summary type="html">
    
      Paper summary for Model Serving
    
    </summary>
    
    
      <category term="Study Notes" scheme="https://joddiy.github.io/categories/Study-Notes/"/>
    
    
      <category term="Tools" scheme="https://joddiy.github.io/tags/Tools/"/>
    
      <category term="Deep Learning" scheme="https://joddiy.github.io/tags/Deep-Learning/"/>
    
  </entry>
  
  <entry>
    <title>DNN Inference Accelerator</title>
    <link href="https://joddiy.github.io/2020/08/18/DNN-Computation-Optimization/"/>
    <id>https://joddiy.github.io/2020/08/18/DNN-Computation-Optimization/</id>
    <published>2020-08-18T13:39:59.000Z</published>
    <updated>2020-08-25T17:05:12.629Z</updated>
    
    <content type="html"><![CDATA[<h2 id="A-typical-DNN-Inference-Accelerator-stack"><a href="#A-typical-DNN-Inference-Accelerator-stack" class="headerlink" title="A typical DNN Inference Accelerator stack"></a>A typical DNN Inference Accelerator stack</h2><p><img src="1.jpg" alt="1"></p><p>TensorRT for CUDA and OpenVINO for CPU.</p><p>NVIDIA TensorRT is an SDK for high-performance deep learning inference. It includes a deep learning inference optimizer and runtime that delivers low latency and high-throughput for deep learning inference applications.<br>*(ONNX suporrted)</p><blockquote><p>The build phase performs the following optimizations on the layer graph:</p><ul><li>Elimination of layers whose outputs are not used</li><li>Elimination of operations which are equivalent to no-op</li><li>The fusion of convolution, bias and ReLU operations</li><li>Aggregation of operations with sufficiently similar parameters and the same source tensor (for example, the 1x1 convolutions in GoogleNet v5’s inception module)</li></ul></blockquote><p>Merging of concatenation layers by directing layer outputs to the correct eventual destination.</p><p>The OpenVINO toolkit is a comprehensive toolkit for quickly developing applications and solutions that emulate human vision. Based on Convolutional Neural Networks (CNNs), the toolkit extends CV workloads across Intel® hardware, maximizing performance.</p><h2 id="Steps-for-Inference-Accelerator"><a href="#Steps-for-Inference-Accelerator" class="headerlink" title="Steps for Inference Accelerator"></a>Steps for Inference Accelerator</h2><p><img src="2.png" alt="2"></p><ul><li>Operator optimization: lossless optimize a sepcific operator by using expert-designed algorithms.</li><li>Graph optimization: lossless optimize the grph by pruning, fusing or replacing the graph.</li><li>Model compression: lossy optimization by using low precision storage. </li><li>Deploy optimization: resource arrangement and schedule.</li></ul><h2 id="Summary"><a href="#Summary" class="headerlink" title="Summary"></a>Summary</h2><p>An ADMM Based Framework for AutoML Pipeline Configuration:(hyper-parameter tuning)</p><ul><li>automatically configuring machine learning pipelines(functional modules: preprocessor, transformer, estimator).</li><li>proposes some math notions to represent this problem(pipelines, hyper-parameters, and constraints).</li><li>decomposes the problem into sub-problems.</li></ul><p><img src="8.png" alt="8"></p><p><img src="7.png" alt="7"></p><p>Beyond Data and Model Parallelism for Deep Neural Networks(Graph optimization, Deploy optimization)</p><ul><li>proposes parallelism dimensions SOAP(Sample, Operation, Attribute, and Parameter dimensions), e.g, Data Parallelism is S, and Model Parallelism is O, P.</li><li>splits a node to Multi-GPUs by SOAP, each as a task, and data transfer is also a task.</li><li>SOAP as a search space, uses MCMC to find efficient task placement.</li><li>uses predicted node excution time as model total execution time.</li></ul><p><img src="3.png" alt="3"></p><p><img src="4.png" alt="4"></p><p>TASO: Optimizing Deep Learning Computation with Automatic Generation of Graph Substitutions(Graph optimization)</p><ul><li>optimizes the graph by using graph substitution.</li><li>DFS algorithm generates all acyclic computation graphs as a substitution if its output is same.</li><li>analyzes the above graph substitution to get operator properties.</li><li>joint optimization with data layout(data stores along which dimension) and graph transformation. TASO measures the execution time and estimates the performance of a graph.</li></ul><p><img src="5.png" alt="5"></p><p><img src="6.png" alt="6"></p><h2 id="Open-problem"><a href="#Open-problem" class="headerlink" title="Open problem"></a>Open problem</h2><ul><li>jointly optimize DNN computation at both graph-level and deploy-level(hyper-parameter tuning).<ul><li>AutoML Pipeline Configuration with Reinforcement Learning</li><li>(uses the predicted node excution time to compute the model total execution time)</li><li>contribution 1: modules, continuous/integer hyper-parameters, constraints as Reinforcement Learning policy(refer the first paper).</li><li>contribution 2: joint optimization with graph-level and deploy-level(graph optimization on multi-GPUs associate with saveral configuration)(refer the second and third paper).</li><li>contribution 3: ?</li><li>Graph Neural Networks，GNN ?</li></ul></li></ul><h2 id="Draft"><a href="#Draft" class="headerlink" title="Draft"></a>Draft</h2><ul><li>Motivition: accelerate automatically hyperparameter tuning</li><li>Methodology: reinforcement learning</li><li></li></ul><h2 id="Challenges-in-hyperparameter-search"><a href="#Challenges-in-hyperparameter-search" class="headerlink" title="Challenges in hyperparameter search"></a>Challenges in hyperparameter search</h2><h3 id="Costly-objective-function-evaluations"><a href="#Costly-objective-function-evaluations" class="headerlink" title="Costly objective function evaluations"></a>Costly objective function evaluations</h3><h3 id="Randomness"><a href="#Randomness" class="headerlink" title="Randomness"></a>Randomness</h3><h3 id="Complex-search-spaces"><a href="#Complex-search-spaces" class="headerlink" title="Complex search spaces"></a>Complex search spaces</h3><h2 id="Related-Work"><a href="#Related-Work" class="headerlink" title="Related Work"></a>Related Work</h2><ul><li>Reinforcement Learning, optimize device placement for one model, search space, which device the operator should be placed.</li><li></li></ul>]]></content>
    
    <summary type="html">
    
      Paper summary for DNN Inference Accelerator
    
    </summary>
    
    
      <category term="Study Notes" scheme="https://joddiy.github.io/categories/Study-Notes/"/>
    
    
      <category term="Tools" scheme="https://joddiy.github.io/tags/Tools/"/>
    
      <category term="Deep Learning" scheme="https://joddiy.github.io/tags/Deep-Learning/"/>
    
  </entry>
  
  <entry>
    <title>linux-script</title>
    <link href="https://joddiy.github.io/2020/07/10/linux-script/"/>
    <id>https://joddiy.github.io/2020/07/10/linux-script/</id>
    <published>2020-07-09T21:08:06.000Z</published>
    <updated>2020-07-09T21:08:06.000Z</updated>
    
    <summary type="html">
    
    </summary>
    
    
    
  </entry>
  
  <entry>
    <title>Big Data Interview</title>
    <link href="https://joddiy.github.io/2020/07/03/Big-Data-Interview/"/>
    <id>https://joddiy.github.io/2020/07/03/Big-Data-Interview/</id>
    <published>2020-07-03T05:32:07.000Z</published>
    <updated>2020-07-20T19:05:19.000Z</updated>
    
    <content type="html"><![CDATA[<h1 id="Hadoop"><a href="#Hadoop" class="headerlink" title="Hadoop"></a>Hadoop</h1><h2 id="关键技术"><a href="#关键技术" class="headerlink" title="关键技术"></a>关键技术</h2><ol><li><p>数据分布在多台机器</p><p> 可靠性：每个数据块都复制到多个节点</p><p> 性能：多个节点同时处理数据</p></li><li><p>计算随数据走</p><p> 网络IO速度 &lt;&lt; 本地磁盘IO速度，大数据系统会尽量地将任务分配到离数据最近的机器上运行（程序运行时，将程序及其依赖包都复制到数据所在的机器运行）</p><p> 代码向数据迁移，避免大规模数据时，造成大量数据迁移的情况，尽量让一段数据的计算发生在同一台机器上</p></li><li><p>串行IO取代随机IO</p><p> 传输时间 &lt;&lt; 寻道时间，一般数据写入后不再修改</p></li><li><p>Hadoop可运行于一般的商用服务器上，具有高容错、高可靠性、高扩展性等特点</p></li><li><p>特别适合写一次，读多次的场景</p><p> 适合: 大规模数据, 流式数据（写一次，读多次）, 商用硬件（一般硬件）<br> 不适合: 低延时的数据访问, 大量的小文件, 频繁修改文件（基本就是写1次）</p></li></ol><p><img src="https://atts.w3cschool.cn/attachments/image/wk/hadoop/architecture.png" alt=""></p><h2 id="列出Hadoop集群的Hadoop守护进程和相关的角色。"><a href="#列出Hadoop集群的Hadoop守护进程和相关的角色。" class="headerlink" title="列出Hadoop集群的Hadoop守护进程和相关的角色。"></a>列出Hadoop集群的Hadoop守护进程和相关的角色。</h2><p><img src="https://atts.w3cschool.cn/attachments/image/20190627/1561603936683523.png" alt=""></p><ul><li><p>Block数据块;</p><p>  基本存储单位，一般大小为64M（配置大的块主要是因为：1）减少搜寻时间，一般硬盘传输速率比寻道时间要快，大的块可以减少寻道时间；2）减少管理块的数据开销，每个块都需要在NameNode上有对应的记录；3）对数据块进行读写，减少建立网络的连接成本）</p><p>  一个大文件会被拆分成一个个的块，然后存储于不同的机器。如果一个文件少于Block大小，那么实际占用的空间为其文件的大小</p><p>  基本的读写单位，类似于磁盘的页，每次都是读写一个块</p><p>  每个块都会被复制到多台机器，默认复制3份</p></li><li><p>NameNode:</p><p>  存储文件的metadata，运行时所有数据都保存到内存，整个HDFS可存储的文件数受限于NameNode的内存大小</p><p>  一个Block在NameNode中对应一条记录（一般一个block占用150字节），如果是大量的小文件，会消耗大量内存。同时map task的数量是由splits来决定的，所以用MapReduce处理大量的小文件时，就会产生过多的map task，线程管理开销将会增加作业时间。处理大量小文件的速度远远小于处理同等大小的大文件的速度。因此Hadoop建议存储大文件</p><p>  数据会定时保存到本地磁盘，但不保存block的位置信息，而是由DataNode注册时上报和运行时维护（NameNode中与DataNode相关的信息并不保存到NameNode的文件系统中，而是NameNode每次重启后，动态重建）</p><p>  NameNode失效则整个HDFS都失效了，所以要保证NameNode的可用性</p></li><li><p>Secondary NameNode：</p><p>  定时与NameNode进行同步（定期合并文件系统镜像和编辑日志，然后把合并后的传给NameNode，替换其镜像，并清空编辑日志，类似于CheckPoint机制），但NameNode失效后仍需要手工将其设置成主机</p></li><li><p>DataNode：</p><p>  保存具体的block数据</p><p>  负责数据的读写操作和复制操作</p><p>  DataNode启动时会向NameNode报告当前存储的数据块信息，后续也会定时报告修改信息</p><p>  DataNode之间会进行通信，复制数据块，保证数据的冗余性</p></li></ul><h2 id="Hadoop-新旧架构对比"><a href="#Hadoop-新旧架构对比" class="headerlink" title="Hadoop 新旧架构对比"></a>Hadoop 新旧架构对比</h2><p><img src="https://atts.w3cschool.cn/attachments/image/20170808/1502171952954287.jpg" alt=""></p><p>旧的MapReduce架构</p><ul><li><p>JobTracker：</p><p>  这是运行在Namenode上，负责提交和跟踪MapReduce Job的守护程序。负责资源管理，跟踪资源消耗和可用性，作业生命周期管理（调度作业任务，跟踪进度，为任务提供容错）</p></li><li><p>TaskTracker：</p><p>  这是Datanode上运行的守护进程。它在Slave节点上负责具体任务的运行。</p></li><li><p>旧框架存在问题：</p><p>  JobTracker是MapReduce的集中处理点，存在单点故障</p><p>  JobTracker完成了太多的任务，造成了过多的资源消耗，当MapReduce job 非常多的时候，会造成很大的内存开销。这也是业界普遍总结出老Hadoop的MapReduce只能支持4000 节点主机的上限</p><p>  在TaskTracker端，以map/reduce task的数目作为资源的表示过于简单，没有考虑到cpu/ 内存的占用情况，如果两个大内存消耗的task被调度到了一块，很容易出现OOM</p><p>  在TaskTracker端，把资源强制划分为map task slot和reduce task slot, 如果当系统中只有map task或者只有reduce task的时候，会造成资源的浪费，也就集群资源利用的问题</p></li></ul><p><img src="https://atts.w3cschool.cn/attachments/image/20170808/1502172142576437.jpg" alt=""></p><p>YARN就是将JobTracker的职责进行拆分，将资源管理和任务调度监控拆分成独立的进程：一个全局的资源管理和一个每个作业的管理（ApplicationMaster） ResourceManager和NodeManager提供了计算资源的分配和管理，而ApplicationMaster则完成应用程序的运行</p><ul><li><p>ResourceManager: 全局资源管理和任务调度</p></li><li><p>NodeManager: 单个节点的资源管理和监控</p></li><li><p>ApplicationMaster: 单个作业的资源管理和任务监控</p></li><li><p>Container: 资源申请的单位和任务运行的容器</p></li></ul><p>旧架构 基本流程</p><p><img src="1.png" alt=""></p><p><img src="2.png" alt=""></p><p>YARN 基本流程</p><p><img src="https://atts.w3cschool.cn/attachments/image/20170808/1502172265232242.jpg" alt=""></p><p><img src="https://atts.w3cschool.cn/attachments/image/20170808/1502172265232242.jpg" alt=""></p><ol><li><p>Job submission</p><p> 从ResourceManager中获取一个Application ID 检查作业输出配置，计算输入分片 拷贝作业资源（job jar、配置文件、分片信息）到HDFS，以便后面任务的执行</p></li><li><p>Job initialization</p><p> ResourceManager 将作业递交给 Scheduler（有很多调度算法，一般是根据优先级）Scheduler 为作业分配一个 Container， ResourceManager 就加载一个 application master process 并交给 NodeManager 管理, ApplicationMaster 主要是创建一系列的监控进程来跟踪作业的进度，同时获取输入分片，为每一个分片创建一个 Map task 和相应的 reduce task 。 Application Master 还决定如何运行作业，如果作业很小（可配置），则直接在同一个JVM下运行</p></li><li><p>Task assignment</p><p> ApplicationMaster 向Resource Manager 申请资源（一个个的Container，指定任务分配的资源要求）一般是根据 data locality 来分配资源</p></li><li><p>Task execution</p><p> Application Master 根据 ResourceManager 的分配情况，在对应的 NodeManager 中启动 Container 。从 HDFS 读取任务所需资源（job jar，配置文件等），然后执行该任务</p></li><li><p>Progress and status update</p><p> 定时将任务的进度和状态报告给 ApplicationMaster， Client 定时向 ApplicationMaster 获取整个任务的进度和状态</p></li><li><p>Job completion</p><p> Client 定时检查整个作业是否完成 作业完成后，会清空临时文件、目录等</p></li></ol><h2 id="Yarn-Failover"><a href="#Yarn-Failover" class="headerlink" title="Yarn Failover"></a>Yarn Failover</h2><ol><li><p>任务失败</p><p> 运行时异常或者JVM退出都会报告给 ApplicationMaster</p><p> 通过心跳来检查挂住的任务(timeout)，会检查多次（可配置）才判断该任务是否失效</p><p> 一个作业的任务失败率超过配置，则认为该作业失败</p><p> 失败的任务或作业都会有 ApplicationMaster 重新运行</p></li><li><p>ApplicationMaster 失败</p><p> ApplicationMaster 定时发送心跳信号到 ResourceManager，通常一旦 ApplicationMaster 失败，则认为失败，但也可以通过配置多次后才失败</p><p> 一旦 ApplicationMaster 失败，ResourceManager 会启动一个新的 ApplicationMaster</p><p> 新的 ApplicationMaster 负责恢复之前错误的 ApplicationMaster 的状态 (yarn.app.mapreduce.am.job.recovery.enable=true)，这一步是通过将应用运行状态保存到共享的存储上来实现的，ResourceManager 不会负责任务状态的保存和恢复</p><p> Client也会定时向 ApplicationMaster 查询进度和状态，一旦发现其失败，则向 ResouceManager 询问新的 ApplicationMaster</p></li><li><p>NodeManager失败</p><p> NodeManager 定时发送心跳到 ResourceManager，如果超过一段时间没有收到心跳消息，ResourceManager 就会将其移除</p><p> 任何运行在该 NodeManager 上的任务和 ApplicationMaster 都会在其他 NodeManager 上进行恢复</p><p> 如果某个 NodeManager 失败的次数太多，ApplicationMaster 会将其加入黑名单（ResourceManager没有），任务调度时不在其上运行任务</p></li><li><p>ResourceManager失败</p><p> 通过 checkpoint 机制，定时将其状态保存到磁盘，然后失败的时候，重新运行</p><p> 通过 zookeeper 同步状态和实现透明的 High Available</p></li></ol><p>可以看出，一般的错误处理都是由当前模块的父模块进行监控（心跳）和恢复。而最顶端的模块则通过定时保存、同步状态和 zookeeper 来ֹ实现 HA</p><h2 id="Zookeeper"><a href="#Zookeeper" class="headerlink" title="Zookeeper"></a>Zookeeper</h2><p>Zookeeper： 为分布式应用提供分布式协作（协调）服务。使用类似文件系统的树形结构。目的是分布式服务不再需要由于协作冲突而另外实现协作服务。</p><p>Zookeeper集群统一为其他分布式应用（也是集群，例如HDFS集群、Yarn集群等等）集群提供协作服务。</p><p><img src="3.jpg" alt=""></p><p>Zookeeper 的节点名称叫做 znode，以路径标注， 每个节点存储上限是 1MB。数据访问是原子的。</p><p>znode 有临时性和永久性之分。临时性 znode 在客户端会话结束后删除。临时性 znode 对所有客户端也是可见的。</p><p>一个顺序的 znode 由 Zookeeper 给定一个序号作为其名称一部分，如’/a/b-3’，3 即为指定的序号。可用来实现分布式锁。</p><p>Watch 在 znode 变更时通知客户端。客户端调用 znode 读操作时会同时获得一个 Watch，当 znode 发生写操作时，此 Watch 会被通知一次且仅一次。</p><p>更新 znode 必须声明版本号（乐观锁），如果版本号错误则更新失败，可指定版本号为 -1 进行强制更新。</p><p>Zookeeper 抛弃了文件系统的操作原语，因为文件非常小且整体读写，所以不需要打开、关闭、寻址操作。</p><p>支持 同步 和 异步 两种 API。</p><p>znode 维护一个 ACL 列表，决定谁可以访问那些操作。</p><p>Zookeeper 运行在一组叫做 ensemble 的集群上，服务器数量是奇数个，2N+1， N代表允许宕机的个数。</p><p><img src="4.jpg" alt=""></p><p>Zookeeper角色：Leader， Follower， Client</p><p>Zookeeper 采用 Zab 协议，分两个阶段：</p><ol><li><p>领导者选举，且要求大部分更随者同领导者同步状态。</p></li><li><p>原子广播，所有写操作被传送给领导者，并通过广播将更新信息告诉跟随者，当大部分跟随者都同步了写操作，则返回成功给客户端。</p></li></ol><p>每次写操作都有一个唯一全局标识，zxid，按先后顺序排序。所有的更新都是，顺序的，原子的，单系统镜像的，容错的，合时的（不过太过时）。</p><p>读操作由内存提供，可能读到过时信息，可调用 sync 强制同领导者同步。</p><p>tick time 是基本时长，其他时间均为该时间倍数。</p><p>案例：</p><ol><li><p>配置服务：每次读操作获得一个 Watch，在更新时被通知，重新读即可。</p></li><li><p>分布式锁：指明一个lock znode，所有客户端在该 znode下创建临时 znode，拥有最小序列号的 znode 获得锁。避免链接丢失而重复创建锁，获得锁时，应检查属于该客户会话的锁是否存在。</p></li></ol><h2 id="Hadoop-读取数据"><a href="#Hadoop-读取数据" class="headerlink" title="Hadoop 读取数据"></a>Hadoop 读取数据</h2><p>通过 InputFormat 决定读取的数据的类型，然后拆分成一个个 InputSplit ，每个 InputSplit 对应一个 Map 处理，RecordReader 读取 InputSplit 的内容给 Map。</p><p>InputSplit 代表一个个逻辑分片，并没有真正存储数据，只是提供了一个如何将数据分片的方法，Split 内有 获取长度和位置的方法，长度可排序大的先执行，位置利于数据局部化。</p><p>RecordReader 将 InputSplit 拆分成一个个 key, value 对给 Map 处理</p><p>大量小文件如何处理：CombineFileInputFormat 可以将若干个 Split 打包成一个</p><p>通常一个 split 就是一个 block，这样做的好处是使得 Map 可以在存储有当前数据的节点上运行本地的任务，而不需要通过网络进行跨节点的任务调度</p><p>通过 mapred.min.split.size， mapred.max.split.size, block.size 来控制拆分的大小</p><p><img src="https://atts.w3cschool.cn/attachments/image/wk/hadoop/mapreduce-split.png" alt=""></p><p>分片间的数据如何处理：忽略最后一条记录，后一个split远程读该条记录</p><h2 id="HDFS"><a href="#HDFS" class="headerlink" title="HDFS"></a>HDFS</h2><p>不适用场景：低延迟访问，大量小文件，多用户写入。HDFS文件只有一个写入者，且总是在文件末尾。</p><p>HDFS中小于 64M 时不会占用整块空间，大容量块是为了减少寻址开销。</p><p>Namenode高可用：写入本地同时写入一个 NFS 挂载，第二Namenode</p><p>JAVA 接口：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// URL</span></span><br><span class="line">URL.setURLStreamHandlerFactory(<span class="keyword">new</span> FsUrlStreamHandlerFactory());</span><br><span class="line">InputStream in  = <span class="keyword">new</span> URL(<span class="string">".."</span>).openStream();</span><br><span class="line">IOUtils.copyBytes(in, System.out, <span class="number">4096</span>, <span class="keyword">false</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">//FileSystem API</span></span><br><span class="line">FileSystem fs = FileSystem.get(URI.create(<span class="string">".."</span>), conf);</span><br><span class="line">fs.oepn(<span class="keyword">new</span> Path(<span class="string">".."</span>)) <span class="comment">// return a FSDataInputStream, 支持随机访问和获取当前位置</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//写入</span></span><br><span class="line">FileSystem fs = FileSystem.get(URI.create(<span class="string">".."</span>), conf);</span><br><span class="line">OutputStream out = fs.create(<span class="keyword">new</span> Path(<span class="string">".."</span>), <span class="keyword">new</span> Progressable()&#123;&#125;); <span class="comment">// return a FSDataOutputStream, 支持获取当前位置</span></span><br><span class="line">IOUtils.copyBytes(in, out, <span class="number">4096</span>, <span class="keyword">true</span>);</span><br><span class="line"></span><br><span class="line">fs.mkdirs(Path);</span><br><span class="line"></span><br><span class="line">fs.exists(Path);</span><br><span class="line"></span><br><span class="line">fs.getFileStatus(Path);</span><br><span class="line"></span><br><span class="line">fs.listStatus(Path);</span><br><span class="line"></span><br><span class="line">fs.globStatus(Path, PathFilter); <span class="comment">// 返回路径上匹配的文件</span></span><br><span class="line"></span><br><span class="line">fs.delete(Path, <span class="keyword">boolean</span> recursive);</span><br></pre></td></tr></table></figure><p>HDFS 读取数据</p><p><img src="5.png" alt=""></p><p>Namenode 只提供索引，不提供数据。错误时跳过，损坏时读副本。检查校验和。</p><p>网络拓扑</p><p><img src="7.png" alt=""></p><p>HDFS 写入数据</p><p><img src="6.png" alt=""></p><p>如果某datanode失败，会重新执行确认队列中的数据，且向namenode注册新身份，跳过故障节点。</p><p>写入副本的放置</p><p><img src="8.png" alt=""></p><p>一致模型：写入一个块即可看到该块，如果要同步读取，调用 sync() 方法。</p><p>distcp 分布式复制，多线程，两个 HDFS 之间传输。以 MapReduce 方式进行，只有 Map。</p><p>Hadoop Archives: 多个小文件合并成 .har 存储。以副本方式归档、归档后不可修改。</p><h2 id="Hadoop-I-O"><a href="#Hadoop-I-O" class="headerlink" title="Hadoop I/O"></a>Hadoop I/O</h2><p>CRC-32 校验和，每512字节计算32字节。</p><p>压缩Gzip，ZIP，bzip2，LZO。CompressionCodec 编码器，使用 native library 加速。CodecPool 编码器池。</p><p>压缩后无法分割，造成一个 map 处理多个块，无法数据本地化。</p><p>序列划：</p><ol><li>Writable 接口，write(DataOutput) 和 readFields(DataInput) 方法。</li><li>两个比较器，Writable 接口和RawComparator 接口。前者对象比较 CompareTo(A, B)，后者字节比较 compare(byte[], int, int, byte[], int, int)，默认的 compare 的实现会调用 compareTo， 因此也会反序列化成对象。</li></ol><p>Writable 类：BooleanWritable, ByteWritable, IntWritable, VIntWritable, FloatWritable, LongWritable, VLongWritable, DoubleWritable.</p><p>Text: 严格按照字节寻址，unicode会拆成两个地址。</p><p>NullWritable: 单实例，除此之外，其他 Writable 都可以修改。</p><p>ObjectWritable：可封装多种类型。</p><p>GenericWritable：可封装多种类型，但通过数组索引指明类型。</p><p>Writable集合：ArrayWritbale，TwoDArrayWritable，MapWritable，SortedMapWritable。数组，二维数字，Map，sortedMap。</p><p>HashPartitioner 使用 writable 中的 hashCode() 方法来选择分区。</p><p>SequenceFile：用来打包多个小文件，读取提供，next()，getCurrentValue() 方法。seek() 可定位，但不是记录边界时会出错，可用 sync() 定位到下一个同步点。文件为 name.seq。</p><p>MapFile：经过排序带有索引的 SequenceFile。文件为 name.map。其中包含一个 index 和一个 data 文件。index 中只保留部分索引，提供 get() 和 getClost() 读取方法。可用 fix() 方法重建索引，或者从 SequenceFile 转为 MapFile。</p><h2 id="Hadoop-测试"><a href="#Hadoop-测试" class="headerlink" title="Hadoop 测试"></a>Hadoop 测试</h2><p>对 Map 和 Reduce 单元测试</p><p>本地作业上测试驱动程序，MiniDFSCluster 和 MiniMRCluster 测试。</p><p>远程调试：设置 keep.failed.task.files 为 true，保存失败任务输入，登录目标节点，用 IsolationRunner 再次执行。</p><h2 id="Shuffle-和-排序"><a href="#Shuffle-和-排序" class="headerlink" title="Shuffle 和 排序"></a>Shuffle 和 排序</h2><p>Map 端：缓冲区 100M，超过 80% 开始内存溢写，分区，且分区内排序，排序后执行combiner，多个溢写文件会合并，少于3个不合并。可压缩。</p><p>Reduce 端：</p><ul><li>复制阶段：5个线程复制，先复制到内存，超出阈值溢写到磁盘。需解压。</li><li>排序阶段：根据合并系数进行合并，可能最后有多个文件。</li><li>reduce 阶段：多个文件输入 reduce 函数。</li></ul><p>优化原则：为 shuffle 指定更多空间，避免多次磁盘溢写。对于Map，主要就是避免把文件写入磁盘，例如使用 Combiner，增大 io.sort.mb 的值。对于 Reduce ，主要是把 Map 的结果尽可能地保存到内存中，同样也是要避免把中间结果写入磁盘。默认情况下，所有的内存都是分配给 Reduce 方法的。使用压缩，增大buffer size。</p><h2 id="任务执行"><a href="#任务执行" class="headerlink" title="任务执行"></a>任务执行</h2><p>speculative execution， 某个任务过慢，开启相同任务作为备份，谁先完成用谁，另外一个则终止。</p><p>JVM 重用</p><p>跳过坏记录：skipping 模式，失败两次后，第三次失败则记录并跳过。</p><p>多个实例向同一个文件写：先写入临时文件夹，先完成的允许修改。</p><h2 id="Hadoop-流格式"><a href="#Hadoop-流格式" class="headerlink" title="Hadoop 流格式"></a>Hadoop 流格式</h2><p>Input:</p><ul><li>FileInputFormat，过滤隐藏文件，分割原则：max(min_split, min(max_split, block_size))</li><li>CombineFileInputFormat，把多个小文件打包到一个输入中。</li><li>TextInputForamt</li><li>KeyValueTextInputForamt，第一个 tab 分开 key 和 value</li><li>NLineInputForamt，指定每个 map 收到多少行</li><li>SteamInputForamt, 处理 XML</li><li>SequenceFileInputForamt，可处理 SequenceFile</li><li>SequenceFileAsTextInputForamt，键值变成 Text</li><li>SequenceFileAsBinaryInputForamt，键值变成 Binary</li><li>MultipleInputs，在每一个路径上规定 Format</li><li>DBInputFormat</li></ul><p>Output:</p><ul><li>TextOutputForamt</li><li>SequenceFileOutputForamt</li><li>SequenceFileAsBinaryOutputForamt</li><li>MapFileOutputForamt</li><li>MltipleOutputFormat, 可控制文件名</li><li>MultipleOutpus，文件名后跟’-r-分区索引’，但灵活，功能多</li><li>LazyOutputForamt，有记录时才写文件，避免空文件</li></ul><h2 id="计数器"><a href="#计数器" class="headerlink" title="计数器"></a>计数器</h2><p>任务失败计数器可能减少。枚举定义计数器，也可以用组名定义动态计数器。可通过 Web 界面，命令行 和 JAVA API 获取计数器。</p><h2 id="排序"><a href="#排序" class="headerlink" title="排序"></a>排序</h2><ul><li>部分排序：利用 MapReduce，</li><li>部分排序查找：输出变为 MapFile</li><li>全局排序：partition 对 key 从小到大分区，利用采样器 Sampler 均匀分区，partition 文件写入分布式缓存全局共享。</li><li>二次排序：复合键，单键分区（到同一个reduce），单键分组（到同一组，setOutputValueGroupingComparator），自定义 comparator，按照双键排序（setOutputKeyComparatorClass）。</li></ul><p>python 流中，可用 KeyFieldBasedPartitioner 进行单键分割，用 KeyFiledBasedComparator 比较双键比较。</p><h2 id="链接"><a href="#链接" class="headerlink" title="链接"></a>链接</h2><p>速度 全内存链接 &gt; 部分内存链接 &gt; MapReduce</p><ul><li>Map 端：要求严格，用来链接多个作业输出，相同数量reducer，相同键，输出文件不可分。</li><li>Reduce 端：比如 1对多 链接，左半部分 key 为（链接键，0），右半部分 key 为（链接键，1），分区分组都按照连接键进行，0 和 1 保证了到达的先后顺序。</li></ul><h2 id="分布式缓存"><a href="#分布式缓存" class="headerlink" title="分布式缓存"></a>分布式缓存</h2><p>使用 -files -archives，让APPlicationMaster直接去拷贝。引用计数和缓存清理。</p><p>或者通过API，addCacheFile() 和 addCacheArchive() 去指定拷贝。添加 symlink 符号链接。</p><h2 id="增加和移除节点"><a href="#增加和移除节点" class="headerlink" title="增加和移除节点"></a>增加和移除节点</h2><p><img src="9.png" alt=""></p><p><img src="10.png" alt=""></p><h2 id="Pig"><a href="#Pig" class="headerlink" title="Pig"></a>Pig</h2><p><img src="11.png" alt=""></p><h2 id="Hbase"><a href="#Hbase" class="headerlink" title="Hbase"></a>Hbase</h2><p>Hbase 是一个分布的，面向列的，排序的 map 存储。</p><p>基于Google Bigtable的开源实现，是一个具有高可靠性、高性能、面向列、可伸缩性、典型的key/value分布式存储的nosql数据库系统，主要用于海量结构化和半结构化数据存储。它介于nosql和RDBMS之间，仅能通过行键（row key）和行键的range来检索数据，行数据存储是原子性的，仅支持单行事务（可通过hive支持来实现多表join等复杂操作）。HBase查询数据功能很简单，不支持join等复杂操作，不支持跨行和跨表事务</p><p>单元格内容为无解释的字节数组，行键也是字节数组，表以行键排序。面向列族的存储。</p><p>把表按照行切分成区域，区域分散在 HBase 集群上。</p><p>行级事务，行的更新时原子的。</p><p>两个目录表，-ROOT- 表包含 .META. 表所在区域。.META. 表包含所有用户空间区域列表。</p><p>区域自动平衡。</p><p><img src="12.png" alt=""></p><h2 id="Hive"><a href="#Hive" class="headerlink" title="Hive"></a>Hive</h2><p>Hive 中存放是表，是和 hdfs 的映射关系，hive 是逻辑上的数据仓库，实际操作的都是 hdfs 上的文件，HQL 就是用 sql 语法来写的 mr 程序。</p><p>Hive是基于Hadoop的一个数据仓库工具，可以将结构化的数据文件映射为一张数据库表，并提供HQL语句（类SQL语言）查询功能，存储依赖于HDFS。支持多种计算引擎，如Spark、MapReduce（默认）、Tez；支持多种存储格式，如TextFile、SequenceFile、RCFile、ORC、Parquet（常用）；支持多种压缩格式，如gzip、lzo、snappy（常用）、bzip2</p><h2 id="Sqoop"><a href="#Sqoop" class="headerlink" title="Sqoop"></a>Sqoop</h2><p>hadoop生态圈上的数据传输工具。</p><p>可以将关系型数据库的数据导入非结构化的hdfs、hive或者bbase中，也可以将hdfs中的数据导出到关系型数据库或者文本文件中。</p><p>使用的是mr程序来执行任务，使用jdbc和关系型数据库进行交互。</p><h2 id="Flume"><a href="#Flume" class="headerlink" title="Flume"></a>Flume</h2><p>Flume 一个分布式、可靠的、高可用的用于数据采集、聚合和传输的系统。常用于日志采集系统中，支持定制各类数据发送方用于收集数据、通过自定义拦截器对数据进行简单的预处理并传输到各种数据接收方如HDFS、HBase、Kafka中。之前由Cloudera开发，后纳入Apache</p><h2 id="ALS-canopy-朴素贝叶斯分类-apriori"><a href="#ALS-canopy-朴素贝叶斯分类-apriori" class="headerlink" title="ALS, canopy, 朴素贝叶斯分类, apriori"></a>ALS, canopy, 朴素贝叶斯分类, apriori</h2><h1 id="Spark"><a href="#Spark" class="headerlink" title="Spark"></a>Spark</h1><p>Spark是一个快速、通用、可扩展、可容错的、内存迭代式计算的大数据分析引擎。目前生态体系主要包括用于批数据处理的SparkRDD、SparkSQL，用于流数据处理的SparkStreaming、Structured-Streaming，用于机器学习的Spark MLLib，用于图计算的Graphx以及用于统计分析的SparkR，支持Java、Scala、Python、R多种数据语言</p>]]></content>
    
    <summary type="html">
    
      Study notes of C++
    
    </summary>
    
    
      <category term="Study Notes" scheme="https://joddiy.github.io/categories/Study-Notes/"/>
    
    
      <category term="Big data" scheme="https://joddiy.github.io/tags/Big-data/"/>
    
      <category term="Hadoop" scheme="https://joddiy.github.io/tags/Hadoop/"/>
    
  </entry>
  
  <entry>
    <title>CTR Summary</title>
    <link href="https://joddiy.github.io/2020/06/12/CTR-Summary/"/>
    <id>https://joddiy.github.io/2020/06/12/CTR-Summary/</id>
    <published>2020-06-12T12:28:35.000Z</published>
    <updated>2020-06-12T12:44:05.000Z</updated>
    
    <content type="html"><![CDATA[<h1 id="CTR"><a href="#CTR" class="headerlink" title="CTR"></a>CTR</h1><h1 id="FTRL"><a href="#FTRL" class="headerlink" title="FTRL"></a>FTRL</h1><h1 id="FM"><a href="#FM" class="headerlink" title="FM"></a>FM</h1><h1 id="FFM"><a href="#FFM" class="headerlink" title="FFM"></a>FFM</h1><h1 id="Deep-FM"><a href="#Deep-FM" class="headerlink" title="Deep FM"></a>Deep FM</h1>]]></content>
    
    <summary type="html">
    
      Paper summary for CTR, FTRL, FM, FFM, Deep FM.
    
    </summary>
    
    
      <category term="Research" scheme="https://joddiy.github.io/categories/Research/"/>
    
    
      <category term="CTR" scheme="https://joddiy.github.io/tags/CTR/"/>
    
  </entry>
  
  <entry>
    <title>NLP Summary</title>
    <link href="https://joddiy.github.io/2020/06/10/NLP-Summary/"/>
    <id>https://joddiy.github.io/2020/06/10/NLP-Summary/</id>
    <published>2020-06-10T07:42:33.000Z</published>
    <updated>2020-06-18T18:06:48.000Z</updated>
    
    <content type="html"><![CDATA[<h1 id="ELMo"><a href="#ELMo" class="headerlink" title="ELMo"></a>ELMo</h1><h2 id="Introduction"><a href="#Introduction" class="headerlink" title="Introduction"></a>Introduction</h2><p>Our word vectors are learned functions of the internal states of a deep bidirectional language model (biLM), which is pretrained on a large text corpus. </p><p>learning high quality representations can be challenging. They should ideally model both: </p><ol><li>complex characteristics of word use (e.g., syntax and semantics), and</li><li>how these uses vary across linguistic contexts (i.e., to model polysemy).</li></ol><p>ELMo (Embeddings from Language Models). ELMo representations are deep, in the sense that they are a function of all of the internal layers of the biLM.</p><p>Using intrinsic evaluations, we show that the higher-level LSTM states capture context-dependent aspects of word meaning (e.g., they can be used without modification to perform well on supervised word sense disambiguation tasks) while lowerlevel states model aspects of syntax (e.g., they can be used to do part-of-speech tagging). </p><h2 id="Related-work"><a href="#Related-work" class="headerlink" title="Related work"></a>Related work</h2><p>Due to their ability to capture syntactic and semantic information of words from large scale unlabeled text, pretrained word vectors are a standard component of most state-of-the-art NLP architectures. However, these approaches for learning word vectors only allow <strong>a single contextindependent representation for each word</strong>.</p><p>Previously proposed methods overcome some of the shortcomings of traditional word vectors by either enriching them with subword information or learning separate vectors for each word sense. Our approach also benefits from subword units through the use of character convolutions, and we seamlessly incorporate multi-sense information into downstream tasks without explicitly training to predict predefined sense classes.</p><p>Other recent work has also focused on learning <strong>context-dependent representations</strong>. context2vec uses a bidirectional LSTM to encode the <strong>context around a pivot word</strong>. Other approaches for learning contextual embeddings include the pivot word itself in the representation and are computed with the encoder of either <strong>a supervised neural machine translation (MT) system</strong> or <strong>an unsupervised language model</strong>.</p><blockquote><p>what?</p></blockquote><p>Previous work has also shown that different layers of deep biRNNs encode different types of information. </p><h2 id="ELMo-Embeddings-from-Language-Models"><a href="#ELMo-Embeddings-from-Language-Models" class="headerlink" title="ELMo: Embeddings from Language Models"></a>ELMo: Embeddings from Language Models</h2><p>ELMo word representations are functions of the entire input sentence. They are computed on top of two-layer biLMs with character convolutions, as a linear function of the internal network states. This setup allows us to do semi-supervised learning, where the biLM is pretrained at a large scale and easily incorporated into a wide range of existing neural NLP architectures.</p><h3 id="Bidirectional-language-models"><a href="#Bidirectional-language-models" class="headerlink" title="Bidirectional language models"></a>Bidirectional language models</h3><p>At each position k, each LSTM layer outputs a context-dependent representation <code>h(LM, k, j)</code> where j = 1,…,L. The top layer LSTM output, <code>h(LM, k, L)</code> , is used to predict the next token tk+1 with a Softmax layer.</p><p>A backward LM can be implemented in an analogous way to a forward LM, with each backward LSTM layer j in a L layer deep model producing representations <code>h(LM, k, j)</code> of tk given (tk+1,…,tN).</p><p>A biLM combines both a forward and backward LM. Our formulation jointly maximizes the log likelihood of the forward and backward directions:</p><p><img src="1.png" alt=""></p><p>We tie the parameters for both the token representation (Θx) and Softmax layer (Θs) in the forward and backward direction while maintaining separate parameters for the LSTMs in each direction.</p><h3 id="ELMo-1"><a href="#ELMo-1" class="headerlink" title="ELMo"></a>ELMo</h3><p>ELMo is a task specific combination of the intermediate layer representations in the biLM. For each token tk, a L-layer biLM computes a set of 2L + 1 representations.</p><p><img src="2.png" alt=""></p><p><code>h(LM, k, j)</code> is the token layer.</p><p>ELMo collapses all layers in R into a single vector, ELMok = E (Rk; Θe). More generally, we compute a task specific weighting of all biLM layers:</p><p>stask are softmax-normalized weights and the scalar parameter γtask allows the task model to scale the entire ELMo vector. γ is of practical importance to aid the optimization process.</p><p>Considering that the activations of each biLM layer have a different distribution, in some cases it also helped to apply layer normalization (Ba et al., 2016) to each biLM layer before weighting.</p><h3 id="Using-biLMs-for-supervised-NLP-tasks"><a href="#Using-biLMs-for-supervised-NLP-tasks" class="headerlink" title="Using biLMs for supervised NLP tasks"></a>Using biLMs for supervised NLP tasks</h3><p>To add ELMo to the supervised model, we first freeze the weights of the biLM and then concatenate the ELMo vector <code>(ELMo, task, k)</code> with xk and pass the ELMo enhanced representation [x; <code>(ELMo, task, k)</code>] into the task RNN.</p><p>Finally, we found it beneficial to add a moderate amount of dropout to ELMo and in some cases to regularize the ELMo weights by adding λ∥w∥2 to the loss. This imposes an inductive bias on the ELMo weights to stay close to an average of all biLM layers.</p><h3 id="Pre-trained-bidirectional-language-model-architecture"><a href="#Pre-trained-bidirectional-language-model-architecture" class="headerlink" title="Pre-trained bidirectional language model architecture"></a>Pre-trained bidirectional language model architecture</h3><p>For a purely character-based input representation, we halved all embedding and hidden dimensions from the single best model CNN-BIG-LSTM in Jozefowicz et al. (2016). The final model uses L = 2 biLSTM layers with 4096 units and 512 dimension projections and a residual connection from the first to second layer. The context insensitive type representation uses 2048 character n-gram convolutional filters followed by two highway layers (Srivastava et al., 2015) and a linear projection down to a 512 representation.</p><p>The context insensitive type representation uses 2048 character n-gram convolutional filters followed by two highway layers and a linear projection down to a 512 representation. </p><p>As a result, the biLM provides three layers of representations for each input token, including those outside the training set due to the purely character input.</p><p>Once pretrained, the biLM can compute representations for any task. </p><h1 id="ULMFiT"><a href="#ULMFiT" class="headerlink" title="ULMFiT"></a>ULMFiT</h1><h2 id="Introduction-1"><a href="#Introduction-1" class="headerlink" title="Introduction"></a>Introduction</h2><p>We propose Universal Language Model Fine-tuning (ULMFiT), an effective transfer learning method that can be applied to any task in NLP, and introduce techniques that are key for fine-tuning a language model.</p><p>transductive transfer vs. inductive transfer</p><blockquote><p>what?</p></blockquote><p>Language models (LM) overfit to small datasets and suffered catastrophic forgetting when fine-tuned with a classifier. Compared to CV, NLP models are typically more shallow and thus require different fine-tuning methods.</p><p>We propose a new method, Universal Language Model Fine-tuning (ULMFiT) that addresses these issues and enables robust inductive transfer learning for any NLP task.</p><p>The same 3-layer LSTM architecture— with the same hyperparameters and no additions other than tuned dropout hyperparameters— outperforms highly engineered models and trans-fer learning approaches on six widely studied text classification tasks.</p><p>Our contributions are the following: </p><ol><li>We propose Universal Language Model Fine-tuning (ULMFiT), a method that can be used to achieve CV-like transfer learning for any task for NLP. </li><li>We propose <em>discriminative fine-tuning, slanted triangular learning rates, and gradual unfreezing</em>, novel techniques to retain previous knowledge and avoid catastrophic forgetting during fine-tuning. </li><li>We significantly outperform the state-of-the-art on six representative text classification datasets, with an error reduction of 18-24% on the majority of datasets. </li><li>We show that our method enables extremely sample-efficient transfer learning and perform an extensive ablation analysis.</li></ol><h2 id="Related-work-1"><a href="#Related-work-1" class="headerlink" title="Related work"></a>Related work</h2><h3 id="Hypercolumns"><a href="#Hypercolumns" class="headerlink" title="Hypercolumns"></a>Hypercolumns</h3><p>The prevailing approach is to pretrain embeddings that capture additional context via other tasks. Embeddings at different levels are then used as features, concatenated either with the word embeddings or with the inputs at intermediate layers. This method is known as hypercolumns.</p><h2 id="Universal-Language-Model-Fine-tuning"><a href="#Universal-Language-Model-Fine-tuning" class="headerlink" title="Universal Language Model Fine-tuning"></a>Universal Language Model Fine-tuning</h2><p>We propose Universal Language Model Fine-tuning (ULMFiT), which pretrains a language model (LM) on a large general-domain corpus and fine-tunes it on the target task using novel techniques. The method is universal in the sense that it meets these practical criteria: </p><ol><li>It works across tasks varying in document size, number, and label type; </li><li>it uses a single architecture and training process; </li><li>it requires no custom feature engineering or preprocessing; and </li><li>it does not require additional in-domain documents or labels.</li></ol><p>ULMFiT consists of the following steps, which we show in Figure 1: a) General-domain LM pretraining; b) target task LM fine-tuning; and c) target task classifier fine-tuning.</p><p><img src="3.png" alt=""></p><h3 id="General-domain-LM-pretraining"><a href="#General-domain-LM-pretraining" class="headerlink" title="General-domain LM pretraining"></a>General-domain LM pretraining</h3><p>We pretrain the language model on Wikitext-103 (Merity et al., 2017b) consisting of 28,595 preprocessed Wikipedia articles and 103 million words.</p><h3 id="Target-task-LM-fine-tuning"><a href="#Target-task-LM-fine-tuning" class="headerlink" title="Target task LM fine-tuning"></a>Target task LM fine-tuning</h3><p>We thus fine-tune the LM on data of the target task. Given a pretrained general-domain LM, this stage converges faster as it only needs to adapt to the idiosyncrasies of the target data, and it allows us to train a robust LM even for small datasets. We propose discriminative fine-tuning and slanted triangular learning rates.</p><h4 id="Discriminative-fine-tuning"><a href="#Discriminative-fine-tuning" class="headerlink" title="Discriminative fine-tuning"></a>Discriminative fine-tuning</h4><p>For discriminative fine-tuning, we split the parameters θ into {θ1, . . . , θL} where θl contains the parameters of the model at the l-th layer and L is the number of layers of the model. Similarly, we obtain {η1,…,ηL} where ηl is the learning rate of the l-th layer.</p><p>The SGD update with discriminative fine-tuning is then the following:</p><p><img src="4.png" alt=""></p><p>We empirically found it to work well to first choose the learning rate ηL of the last layer by fine-tuning only the last layer and using ηl−1 = ηl/2.6 as the learning rate for lower layers.</p><h4 id="Slanted-triangular-learning-rates"><a href="#Slanted-triangular-learning-rates" class="headerlink" title="Slanted triangular learning rates"></a>Slanted triangular learning rates</h4><p>we propose slanted triangular learning rates (STLR), which first linearly increases the learning rate and then linearly decays it according to the following update schedule, which can be seen in:</p><p><img src="5.png" alt=""></p><p><img src="6.png" alt=""></p><p>where T is the number of training iterations, cut_frac is the fraction of iterations we increase the LR, cut is the iteration when we switch from increasing to decreasing the LR, p is the fraction of the number of iterations we have increased or will decrease the LR respectively, ratio specifies how much smaller the lowest LR is from the maximum LR ηmax, and ηt is the learning rate at iteration t.</p><p>We generally use cut_frac = 0.1, ratio = 32 and ηmax = 0.01.</p><h3 id="Target-task-classifier-fine-tuning"><a href="#Target-task-classifier-fine-tuning" class="headerlink" title="Target task classifier fine-tuning"></a>Target task classifier fine-tuning</h3><p>Finally, for fine-tuning the classifier, we augment the pretrained language model with two additional linear blocks.</p><h4 id="Concat-pooling"><a href="#Concat-pooling" class="headerlink" title="Concat pooling"></a>Concat pooling</h4><p>As input documents can consist of hundreds of words, information may get lost if we only consider the last hidden state of the model. For this reason, we concatenate the hidden state at the last time step hT of the document with both the max-pooled and the mean-pooled representation of the hidden states over as many time steps as fit in GPU memory H = {h1,…,hT}:</p><p><img src="7.png" alt=""></p><h4 id="Gradual-unfreezing"><a href="#Gradual-unfreezing" class="headerlink" title="Gradual unfreezing"></a>Gradual unfreezing</h4><p>We first unfreeze the last layer and fine-tune all unfrozen layers for one epoch. We then unfreeze the next lower frozen layer and repeat, until we finetune all layers until convergence at the last iteration.</p><h4 id="BPTT-for-Text-Classification-BPT3C"><a href="#BPTT-for-Text-Classification-BPT3C" class="headerlink" title="BPTT for Text Classification (BPT3C)"></a>BPTT for Text Classification (BPT3C)</h4><p>In order to make fine-tuning a classifier for large documents feasible, we propose BPTT for Text Classification (BPT3C): We divide the document into fixedlength batches of size b. At the beginning of each batch, the model is initialized with the final state of the previous batch. </p><h4 id="Bidirectional-language-model"><a href="#Bidirectional-language-model" class="headerlink" title="Bidirectional language model"></a>Bidirectional language model</h4><p>For all our experiments, we pretrain both a forward and a backward LM. </p><h1 id="Transformer"><a href="#Transformer" class="headerlink" title="Transformer"></a>Transformer</h1><h2 id="Introduction-2"><a href="#Introduction-2" class="headerlink" title="Introduction"></a>Introduction</h2><p>Recurrent models typically factor computation along the symbol positions of the input and output sequences. Aligning the positions to steps in computation time, they generate a sequence of hidden states ht, as a function of the previous hidden state ht−1 and the input for position t.</p><p>This inherently sequential nature precludes parallelization within training examples, which becomes critical at longer sequence lengths, as memory constraints limit batching across examples.</p><p>Attention mechanisms have become an integral part of compelling sequence modeling and transduction models in various tasks, allowing modeling of dependencies without regard to their distance in the input or output sequences</p><p>In this work we propose the Transformer, a model architecture eschewing recurrence and instead relying entirely on an attention mechanism to draw global dependencies between input and output.</p><h2 id="Background"><a href="#Background" class="headerlink" title="Background"></a>Background</h2><p>The goal of reducing sequential computation also forms the foundation of the Extended Neural GPU [20], ByteNet [15] and ConvS2S [8], all of which use convolutional neural networks as basic building block, computing hidden representations in parallel for all input and output positions.</p><p>In these models, the number of operations required to relate signals from two arbitrary input or output positions grows in the distance between positions, linearly for ConvS2S and logarithmically for ByteNet. This makes it more difficult to learn dependencies between distant positions.</p><p>In the Transformer this is reduced to a constant number of operations, albeit at the cost of reduced effective resolution due to averaging attention-weighted positions, an effect we counteract with <strong>Multi-Head Attention</strong>.</p><p>Self-attention, sometimes called intra-attention is an attention mechanism relating different positions of a single sequence in order to compute a representation of the sequence.</p><p>To the best of our knowledge, however, the Transformer is the first transduction model relying<br>entirely on self-attention to compute representations of its input and output without using sequencealigned<br>RNNs or convolution.</p><h2 id="Model-Architecture"><a href="#Model-Architecture" class="headerlink" title="Model Architecture"></a>Model Architecture</h2><p>The Transformer follows this overall architecture using stacked self-attention and point-wise, fully connected layers for both the encoder and decoder, shown in the left and right halves of Figure:</p><p><img src="8.png" alt=""></p><h3 id="Encoder-and-Decoder-Stacks"><a href="#Encoder-and-Decoder-Stacks" class="headerlink" title="Encoder and Decoder Stacks"></a>Encoder and Decoder Stacks</h3><p><strong>Encoder:</strong></p><p>The encoder is composed of a stack of N = 6 identical layers. Each layer has two sub-layers. The first is a multi-head self-attention mechanism, and the second is a simple, position-wise fully connected feed-forward network. </p><p>We employ a residual connection around each of the two sub-layers, followed by layer normalization. That is, the output of each sub-layer is LayerNorm(x + Sublayer(x)), where Sublayer(x) is the function implemented by the sub-layer itself. </p><p>To facilitate these residual connections, all sub-layers in the model, as well as the embedding layers, produce outputs of dimension dmodel = 512.</p><p><strong>Decoder:</strong></p><p>The decoder is also composed of a stack of N = 6 identical layers. In addition to the two sub-layers in each encoder layer, the decoder inserts a third sub-layer, which performs multi-head attention over the output of the encoder stack.</p><p>Similar to the encoder, we employ residual connections around each of the sub-layers, followed by layer normalization. </p><p>We also modify the self-attention sub-layer(Masked Multi-Head Attention) in the decoder stack to ensure that the predictions for position i can depend only on the known outputs at positions less than i.</p><h3 id="Attention"><a href="#Attention" class="headerlink" title="Attention"></a>Attention</h3><p>An attention function can be described as mapping a query and a set of key-value pairs to an output, where the query, keys, values, and output are all vectors. The output is computed as a weighted sum of the values, where the weight assigned to each value is computed by a compatibility function of the query with the corresponding key. </p><blockquote><p>Attention(Q, K, V) = softmax(Q * K.T / √dk) * V</p></blockquote><p><img src="9.png" alt=""></p><p><strong>Scaled Dot-Product Attention:</strong></p><p>The input consists of queries and keys of dimension dk, and values of dimension dv. We compute the dot products of the query with all keys, divide each by pdk, and apply a softmax function to obtain the weights on the values.</p><p>In practice, we compute the attention function on a set of queries simultaneously, packed together into a matrix Q. The keys and values are also packed together into matrices K and V . We compute the matrix of outputs as:</p><p><img src="10.png" alt=""></p><blockquote><p>X ∈ (len, dmodel), WQ ∈ (dmodel, dk), WK ∈ (dmodel, dk), WV ∈ (dmodel, dv)<br>Q = X * WQ ∈ (len, dk)<br>K = X * WK ∈ (len, dk)<br>V = X * WV ∈ (len, dv)<br>Q * K.T ∈ (len, len)<br>softmax(Q * K.T / √dk) ∈ (len, len)<br>softmax(Q * K.T / √dk) * V ∈ (len, dv)</p></blockquote><blockquote><p>two types of attention: additive attention and dot-product attention?</p></blockquote><p>Why divide √dk?</p><ul><li>And the author suspect for large values of dk, the dot products grow large in magnitude, pushing the softmax function into regions where it has extremely small gradients</li><li>q · k = sum(qi * ki) for i from 1 to dk, has mean 0 and variance dk. </li></ul><p><strong>Multi-Head Attention:</strong></p><p>We found it beneficial to linearly project the queries, keys and values h times with different, learned<br>linear projections to dk, dk and dv dimensions, respectively.</p><p>On each of these projected versions of queries, keys and values we then perform the attention function in parallel, yielding dv-dimensional output values(len*dv). These are concatenated and once again projected, resulting in the final values, as depicted in Figure:</p><p><img src="11.png" alt=""></p><blockquote><p>The Q, K, V should be the output of previous layer, the shape is (len, dmodel)<br>headi ∈ (len, dv), WO ∈ (h * dv, dmodel)<br>MultiHead(Q, K, V) ∈ (len, dmodel)<br>the author uses h = 8, dk = dv = dmodel/h = 64, dmodel = 512</p></blockquote><blockquote><p>In implemention, we combine the h head in one linear layer as:<br>X ∈ (len, dmodel), WQ ∈ (dmodel, h * dk), WK ∈ (dmodel, h * dk), WV ∈ (dmodel, h * dv)<br>h * dk = dmodel<br>Q = X * WQ ∈ (len, h * dk)<br>K = X * WK ∈ (len, h * dk)<br>V = X * WV ∈ (len, h * dv)<br>Q * K.T ∈ (len, len)<br>softmax(Q * K.T / √dk) ∈ (len, len)<br>softmax(Q * K.T / √dk) * V ∈ (len, h * dv)</p></blockquote><p><strong>Applications of Attention in our Model:</strong></p><ul><li>In a self-attention in encoder, the Q, K, V comes from previous layer’s output</li><li>In the attention in decoder, the Q comes from the previous decoder, and K, V comes from encoder.</li><li>In decoder, when we predict word i, we mask out (setting to −∞) all work i+1 to len in K, V from encoder.</li></ul><h3 id="Position-wise-Feed-Forward-Networks"><a href="#Position-wise-Feed-Forward-Networks" class="headerlink" title="Position-wise Feed-Forward Networks"></a>Position-wise Feed-Forward Networks</h3><p><img src="12.png" alt=""></p><blockquote><p>X ∈ (len, dmodel), W1 ∈ (dmodel, dff), W1 ∈ (dff, dmodel), b ∈ (len, dmodel)<br>X * W1 ∈ (len, dff)<br>FFN(x) ∈ (len, dmodel)<br>dmodel = 512, dff = 2048</p></blockquote><h3 id="Embeddings-and-Softmax"><a href="#Embeddings-and-Softmax" class="headerlink" title="Embeddings and Softmax"></a>Embeddings and Softmax</h3><p>In our model, we share the same weight matrix between the two embedding layers and the pre-softmax linear transformation(convert the decoder output to predicted next-token probabilities). In the embedding layers, we multiply those weights by √dmodel.</p><blockquote><p>WHY?</p></blockquote><h3 id="Positional-Encoding"><a href="#Positional-Encoding" class="headerlink" title="Positional Encoding"></a>Positional Encoding</h3><p>In order for the model to make use of the order of the sequence, we must inject some information about the relative or absolute position of the tokens in the sequence.</p><p>We add “positional encodings” to the input embeddings at the bottoms of the encoder and decoder stacks. In this work, we use sine and cosine functions of different frequencies:</p><p><img src="13.png" alt=""></p><p>where pos is the position and i is the dimension, and any fixed offset k, PE(pos+k) can be represented as a linear function of PE(pos).</p><h2 id="Why-Self-Attention"><a href="#Why-Self-Attention" class="headerlink" title="Why Self-Attention"></a>Why Self-Attention</h2><p>We compare various aspects of self-attention layers to the recurrent and convolutional layers.</p><p><img src="14.png" alt=""></p><p><strong>One is the total computational complexity per layer.</strong></p><p>In terms of computational complexity, self-attention layers are faster than recurrent layers when the sequence length n is smaller than the representation dimensionality d.</p><p>To improve computational performance for tasks involving very long sequences, self-attention could be restricted to considering only a neighborhood of size r in the input sequence centered around the respective output position. This would increase the maximum path length to O(n/r).</p><p>A single convolutional layer with kernel width k &lt; n does not connect all pairs of input and output positions. Doing so requires a stack of O(n/k) convolutional layers in the case of contiguous kernels, or O(logk(n)) in the case of dilated convolutions. Convolutional layers are generally more expensive than recurrent layers, by a factor of k.</p><p><strong>Another is the amount of computation that can be parallelized.</strong></p><p><strong>The third is the path length between long-range dependencies in the network.</strong></p><p>Learning long-range dependencies is a key challenge in many sequence transduction tasks. One key factor affecting the ability to learn such dependencies is the length of the paths forward and backward signals have to traverse in the network. The shorter these paths between any combination of positions in the input and output sequences, the easier it is to learn long-range dependencies.</p><h1 id="GPT"><a href="#GPT" class="headerlink" title="GPT"></a>GPT</h1><p>Labeled data for learning these specific tasks is scarce, so it is challenging for discriminatively trained models to perform adequately.</p><p>We demonstrate that large gains on these tasks can be realized by <span style="color:blue">generative pre-training</span> of a language model on a diverse corpus of unlabeled text, followed by discriminative fine-tuning on each specific task.</p><h2 id="Introduction-3"><a href="#Introduction-3" class="headerlink" title="Introduction"></a>Introduction</h2><p>Models that can leverage linguistic information from unlabeled data provide a valuable alternative to gathering more annotation, which can be time-consuming and expensive. Further, even in cases where considerable supervision is available, learning good representations in an unsupervised fashion can provide a significant performance boost. </p><p>Leveraging more than word-level information from unlabeled text, however, is challenging for two main reasons. </p><ol><li>It is unclear what type of optimization objectives are most effective at learning text representations that are useful for transfer. </li><li>There is no consensus on the most effective way to transfer these learned representations to the target task. </li></ol><p>In this paper, we explore a semi-supervised approach for language understanding tasks using <span style="color:blue">a combination of unsupervised pre-training and supervised fine-tuning. </span></p><p>Our goal is to learn a universal representation that transfers with little adaptation to a wide range of tasks.</p><p>We employ a two-stage training procedure:</p><ol><li>First, we use a language modeling objective on the unlabeled data to learn the initial parameters of a neural network model. </li><li>Subsequently, we adapt these parameters to a target task using the corresponding supervised objective.</li></ol><p>For our model architecture, we use the Transformer.</p><p>During transfer, we utilize task-specific input adaptations derived from traversal-style approaches, which process structured text input as a single contiguous sequence of tokens.</p><h2 id="Related-Work"><a href="#Related-Work" class="headerlink" title="Related Work"></a>Related Work</h2><p><strong>Semi-supervised learning for NLP</strong></p><p>The earliest approaches used unlabeled data to compute word-level or phrase-level statistics. Over the last few years, researchers have demonstrated the benefits of using word embeddings.</p><p>These approaches, however, mainly transfer word-level information, whereas we aim to capture higher-level semantics.</p><p><strong>Unsupervised pre-training</strong></p><p>Research demonstrated that pre-training acts as a regularization scheme, enabling better generalization in deep neural networks.</p><p>The usage of LSTM models in other approaches restricts their prediction ability to a short range. Our choice of transformer networks allows us to capture longerrange linguistic structure.</p><p>Other approaches use hidden representations from a pre-trained model involving a substantial amount of new parameters for each separate target task, whereas we require minimal changes to our model architecture during transfer.</p><p><strong>Auxiliary training objectives</strong></p><p>POS tagging, chunking, named entity recognition, and language modeling.</p><h2 id="Framework"><a href="#Framework" class="headerlink" title="Framework"></a>Framework</h2><p>Our training procedure consists of two stages. The first stage is learning a high-capacity language<br>model on a large corpus of text. This is followed by a fine-tuning stage, where we adapt the model to<br>a discriminative task with labeled data.</p><h3 id="Unsupervised-pre-training"><a href="#Unsupervised-pre-training" class="headerlink" title="Unsupervised pre-training"></a>Unsupervised pre-training</h3><p>We use a standard language modeling objective to maximize the following likelihood:</p><p><img src="19.png" alt=""></p><p>where k is the size of the context window, and the conditional probability P is modeled using a neural network with parameters Θ. These parameters are trained using stochastic gradient descent.</p><p>In our experiments, we use a multi-layer Transformer decoder or the language model, which is a variant of the transformer. This model applies a multi-headed self-attention operation over the input context tokens followed by position-wise feedforward layers to produce an output distribution over target tokens:</p><p><img src="20.png" alt=""></p><p>where U = (u−k, … , u−1) is the context vector of tokens, n is the number of layers, We is the token<br>embedding matrix, and Wp is the position embedding matrix.</p><h3 id="Supervised-fine-tuning"><a href="#Supervised-fine-tuning" class="headerlink" title="Supervised fine-tuning"></a>Supervised fine-tuning</h3><p>We assume a labeled dataset C, where each instance consists of a sequence of input tokens, x1, … , xm, along with a label y.</p><p>The inputs are passed through our pre-trained model to obtain the final transformer block’s activation h(m,l) , which is then fed into an added linear output layer with parameters Wy to predict y:</p><p><img src="21.png" alt=""></p><p>This gives us the following objective to maximize:</p><p><img src="22.png" alt=""></p><p>Specifically, we optimize the following objective (with weight λ):</p><p><img src="23.png" alt=""></p><p>Overall, the only extra parameters we require during fine-tuning are Wy , and embeddings for delimiter tokens.</p><h3 id="Task-specific-input-transformations"><a href="#Task-specific-input-transformations" class="headerlink" title="Task-specific input transformations"></a>Task-specific input transformations</h3><p>Certain other tasks, like question answering or textual entailment, have structured inputs such as ordered sentence pairs, or triplets of document, question, and answers.</p><p>Since our pre-trained model was trained on contiguous sequences of text, we require some modifications to apply it to these tasks.</p><p>we use a traversal-style approach [52], where we convert structured inputs into an ordered sequence that our pre-trained model can process. These input transformations allow us to avoid making extensive changes to the architecture across tasks. </p><p><img src="18.png" alt=""></p><p>All transformations include adding randomly initialized start and end tokens (⟨s⟩, ⟨e⟩).</p><p><strong>Textual entailment</strong></p><p><strong>Similarity</strong></p><p><strong>Question Answering and Commonsense Reasoning</strong></p><h1 id="Bert"><a href="#Bert" class="headerlink" title="Bert"></a>Bert</h1><p>BERT, which stands for Bidirectional Encoder Representations from Transformers.</p><p>BERT is designed to pretrain deep bidirectional representations from unlabeled text by jointly conditioning on both left and right context in all layers.</p><h2 id="Introduction-4"><a href="#Introduction-4" class="headerlink" title="Introduction"></a>Introduction</h2><p>Language model pre-training has been shown to be effective for improving many natural language processing tasks.</p><p>There are two existing strategies for applying pre-trained language representations to downstream tasks: feature-based and fine-tuning.</p><p><img src="17.png" alt=""></p><p>The feature-based approach, such as ELMo, uses task-specific architectures that include the pre-trained representations as additional features.</p><p>The fine-tuning approach, such as the Generative Pre-trained Transformer (OpenAI GPT) (Radford et al., 2018), introduces minimal task-specific parameters, and is trained on the downstream tasks by simply fine-tuning all pretrained parameters.</p><p>We argue that current techniques restrict the power of the pre-trained representations, especially for the fine-tuning approaches.</p><p>The major limitation is that standard language models are unidirectional, and this limits the choice of architectures that can be used during pre-training. For example, in OpenAI GPT, the authors use a left-to-right architecture, where every token can only attend to previous tokens in the self-attention layers of the Transformer.</p><p>In this paper, we improve the fine-tuning based approaches by proposing BERT: Bidirectional Encoder Representations from Transformers. BERT alleviates the previously mentioned unidirectionality constraint by using a <span style="color:blue">“masked language model” (MLM)</span> pre-training objective.</p><p>The masked language model randomly masks some of the tokens from the input, and the objective is to predict the original vocabulary id of the masked word based only on its context.</p><p>Unlike left-to-right language model pre-training, <span style="color:blue">the MLM objective enables the representation to fuse the left and the right context, which allows us to pretrain a deep bidirectional Transformer.</span></p><p>In addition to the masked language model, we also use a <span style="color:blue">“next sentence prediction”</span> task that jointly pretrains text-pair representations. </p><h2 id="Related-Work-1"><a href="#Related-Work-1" class="headerlink" title="Related Work"></a>Related Work</h2><h3 id="Unsupervised-Feature-based-Approaches"><a href="#Unsupervised-Feature-based-Approaches" class="headerlink" title="Unsupervised Feature-based Approaches"></a>Unsupervised Feature-based Approaches</h3><p>Pre-trained word embeddings are an integral part of modern NLP systems, offering significant improvements over embeddings learned from scratch. </p><p>ELMo and its predecessor generalize traditional word embedding research along a different dimension. They extract context-sensitive features from a left-to-right and a right-to-left language model.</p><h3 id="Unsupervised-Fine-tuning-Approaches"><a href="#Unsupervised-Fine-tuning-Approaches" class="headerlink" title="Unsupervised Fine-tuning Approaches"></a>Unsupervised Fine-tuning Approaches</h3><p>More recently, sentence or document encoders which produce contextual token representations have been pre-trained from unlabeled text and fine-tuned for a supervised downstream task.</p><p>The advantage of these approaches is that few parameters need to be learned from scratch.</p><p>At least partly due to this advantage, OpenAI GPT (Radford et al., 2018) achieved previously state-of-the-art results on many sentence-level tasks from the GLUE benchmark.</p><h3 id="Transfer-Learning-from-Supervised-Data"><a href="#Transfer-Learning-from-Supervised-Data" class="headerlink" title="Transfer Learning from Supervised Data"></a>Transfer Learning from Supervised Data</h3><h2 id="BERT"><a href="#BERT" class="headerlink" title="BERT"></a>BERT</h2><p>There are two steps in our framework: pre-training and fine-tuning.</p><p>During pre-training, the model is trained on unlabeled data over different pre-training tasks. For fine-tuning, the BERT model is first initialized with the pre-trained parameters, and all of the parameters are fine-tuned using labeled data from the downstream tasks.</p><p>A distinctive feature of BERT is its unified architecture across different tasks. There is minimal difference between the pre-trained architecture and the final downstream architecture.</p><p><strong>Model Architecture</strong></p><p><img src="15.png" alt=""></p><p>BERT’s model architecture is a multi-layer bidirectional Transformer encoder.</p><p>In this work, we denote the number of layers (i.e., Transformer blocks) as L, the hidden size as H, and the number of self-attention heads as A.</p><p>We primarily report results on two model sizes: BERT(BASE) (L=12, H=768, A=12, Total Parameters=110M) and BERT(LARGE) (L=24, H=1024, A=16, Total Parameters=340M).</p><p>In all cases we set the feed-forward/filter size to be 4H, i.e., 3072 for the H = 768 and 4096 for the H = 1024.</p><p>BERT(BASE) was chosen to have the same model size as OpenAI GPT for comparison purposes. Critically, however, the BERT Transformer uses <span style="color:blue">bidirectional self-attention</span>, while the GPT Transformer uses constrained self-attention where every token can only attend to context to its left.</p><p><strong>Input/Output Representations</strong></p><p>To make BERT handle a variety of down-stream tasks, our input representation is able to unambiguously represent both a single sentence and a pair of sentences (e.g., ⟨ Question, Answer ⟩) in one token sequence.</p><p>A “sequence” refers to the input token sequence to BERT, which may be a single sentence or two sentences packed together.</p><p>We use WordPiece embeddings (Wu et al., 2016) with a 30,000 token vocabulary.</p><p>The first token of every sequence is always a special classification token ([CLS]). </p><p>Sentence pairs are packed together into a single sequence. We differentiate the sentences in two ways. First, we separate them with a special token ([SEP]). Second, we add a learned embedding to every token indicating whether it belongs to sentence A or sentence B. </p><p><img src="16.png" alt=""></p><blockquote><p>C ∈ (H,), Ti ∈ (H,)</p></blockquote><h3 id="Pre-training-BERT"><a href="#Pre-training-BERT" class="headerlink" title="Pre-training BERT"></a>Pre-training BERT</h3><p>We pre-train BERT using two unsupervised tasks, described in this section.</p><p><strong>Task #1: Masked LM</strong></p><p>Intuitively, it is reasonable to believe that a deep bidirectional model is strictly more powerful than either a left-to-right model or the shallow concatenation of a left-to-right and a right-to-left model.</p><p>Unfortunately, standard conditional language models can only be trained left-to-right or right-to-left, since bidirectional conditioning would allow each word to indirectly “see itself”, and the model could trivially predict the target word in a multi-layered context.</p><p>In order to train a deep bidirectional representation, we simply mask some percentage of the input tokens at random, and then predict those masked tokens.</p><p>In this case, the final hidden vectors corresponding to the mask tokens are fed into an output softmax over the vocabulary, as in a standard LM. We refer to this procedure as a <span style="color:blue">“masked LM” (MLM).</span></p><p>In all of our experiments, we mask 15% of all WordPiece tokens in each sequence at random. In contrast to denoising auto-encoders, we only predict the masked words rather than reconstructing the entire input.</p><p><span style="color:blue">A downside</span> is that we are creating a mismatch between pre-training and fine-tuning, since the [MASK] token does not appear during fine-tuning. </p><p>To mitigate this, we chooses 15% of the token positions at random for prediction. If the i-th token is chosen, we replace the i-th token with (1) the [MASK] token 80% of the time (2) a random token 10% of the time (3) the unchanged i-th token 10% of the time.</p><p>Then, Ti will be used to predict the original token with cross entropy loss. </p><p><strong>Task #2: Next Sentence Prediction (NSP)</strong></p><p>In order to train a model that understands sentence relationships, we pre-train for a <strong>binarized next sentence prediction task</strong> that can be trivially generated from any monolingual corpus.</p><p>Specifically, when choosing the sentences A and B for each pre- training example, 50% of the time B is the actual next sentence that follows A (labeled as IsNext), and 50% of the time it is a random sentence from the corpus (labeled as NotNext).</p><p>As we show in Figure 1, C is used for next sentence prediction (NSP).</p><p><strong>Pre-training data</strong></p><p>For the pre-training corpus we use the BooksCorpus (800M words) and English Wikipedia (2,500M words). It is criti- cal to use a document-level corpus rather than a shuffled sentence-level corpus in order to extract long contiguous sequences.</p><h3 id="Fine-tuning-BERT"><a href="#Fine-tuning-BERT" class="headerlink" title="Fine-tuning BERT"></a>Fine-tuning BERT</h3><p>For each task, we simply plug in the task-specific inputs and outputs into BERT and fine-tune all the parameters end-to-end.</p><p>At the output, the token representations are fed into an output layer for token-level tasks, such as sequence tagging or question answering, and the [CLS] representation is fed into an output layer for classification, such as entailment or sentiment analysis.</p><p>Compared to pre-training, fine-tuning is relatively inexpensive.</p><h1 id="GPT2"><a href="#GPT2" class="headerlink" title="GPT2"></a>GPT2</h1>]]></content>
    
    <summary type="html">
    
      Paper summary for elmo, transformer, bet, and gpt.
    
    </summary>
    
    
      <category term="Research" scheme="https://joddiy.github.io/categories/Research/"/>
    
    
      <category term="NLP" scheme="https://joddiy.github.io/tags/NLP/"/>
    
  </entry>
  
  <entry>
    <title>Leetcode Top 100</title>
    <link href="https://joddiy.github.io/2020/06/04/Leetcode/"/>
    <id>https://joddiy.github.io/2020/06/04/Leetcode/</id>
    <published>2020-06-03T19:08:18.000Z</published>
    <updated>2020-07-15T17:32:35.000Z</updated>
    
    <content type="html"><![CDATA[<h1 id="lengthOfLongestSubstring-cache-O-n"><a href="#lengthOfLongestSubstring-cache-O-n" class="headerlink" title="lengthOfLongestSubstring (cache) O(n)"></a>lengthOfLongestSubstring (cache) O(n)</h1><img src="1.png" style="width:800px;"/><p>We set two indexes, i and j, i to indicate the start of slide window, and j for end.<br>First we move j to the right, once we find a duplicated value, we stop, and calculate the length.<br>Then, we move i to right to remove the first duplicated value.<br>We also use a dict to store where we store the value.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># O(kn)</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(a)</span>:</span></span><br><span class="line">    i, j = <span class="number">0</span>, <span class="number">0</span></span><br><span class="line">    indice = &#123;&#125;</span><br><span class="line">    ret = <span class="number">0</span></span><br><span class="line">    <span class="keyword">while</span> j &lt; len(a):</span><br><span class="line">        <span class="keyword">if</span> a[j] <span class="keyword">in</span> indice:</span><br><span class="line">            i = indice[a[j]]</span><br><span class="line">            <span class="comment"># reconstruct the indice map</span></span><br><span class="line">            indice = &#123;a[k]: k <span class="keyword">for</span> k <span class="keyword">in</span> range(i+<span class="number">1</span>, j+<span class="number">1</span>)&#125;</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="comment"># only add the current element</span></span><br><span class="line">            indice[a[j]] = j</span><br><span class="line">        ret = max(ret, len(indice))</span><br><span class="line">        j += <span class="number">1</span></span><br><span class="line">    <span class="keyword">return</span> ret</span><br><span class="line"></span><br><span class="line"><span class="comment"># O(n)</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution2</span><span class="params">(a)</span>:</span></span><br><span class="line">    i, j = <span class="number">0</span>, <span class="number">0</span></span><br><span class="line">    indice = &#123;&#125;</span><br><span class="line">    ret = <span class="number">0</span></span><br><span class="line">    <span class="keyword">while</span> j &lt; len(a):</span><br><span class="line">        <span class="keyword">if</span> a[j] <span class="keyword">in</span> indice:</span><br><span class="line">            <span class="comment"># once we find a duplicate key</span></span><br><span class="line">            <span class="comment"># then valid path must be the length between these two key minus 1</span></span><br><span class="line">            i = max(indice[a[j]], i)</span><br><span class="line">        ret = max(ret, j-i+<span class="number">1</span>)</span><br><span class="line">        indice[a[j]] = j+<span class="number">1</span></span><br><span class="line">        j += <span class="number">1</span></span><br><span class="line">    <span class="keyword">return</span> ret</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">print(solution2(<span class="string">"abcabcbb"</span>))</span><br></pre></td></tr></table></figure><h1 id="findMedianSortedArrays-binary-search-O-log-m-n"><a href="#findMedianSortedArrays-binary-search-O-log-m-n" class="headerlink" title="findMedianSortedArrays (binary search) O(log(m+n))"></a>findMedianSortedArrays (binary search) O(log(m+n))</h1><!-- ![2.png](2.png) --><img src="2.png" style="width:800px;"/><pre><code>      left_part          |        right_partA[0], A[1], ..., A[i-1]  |  A[i], A[i+1], ..., A[m-1]B[0], B[1], ..., B[j-1]  |  B[j], B[j+1], ..., B[n-1]</code></pre><p>We use i and j to divide the list A and B. Once we reach following conditions, we can get the median.</p><ul><li>len(left_part)=len(right_part)</li><li>max(left_part)≤min(right_part)</li><li>median = (max(left_part) + min(right_part)) / 2</li></ul><p>We can simplify the condition to:</p><ul><li>B[j−1]≤A[i] and A[i−1]≤B[j]</li><li>j=(m+n+1)/2-i</li></ul><p>We do binary search to find the i:</p><ul><li>if B[j−1]≤A[i] and A[i−1]≤B[j], we found the median</li><li>if B[j−1]&gt;A[i], we need to increase i using binary search</li><li>if A[i−1]&gt;B[j], we need to decrease i using binary search</li></ul><p>Note: </p><ul><li>imin, imax, half_len = 0, m, (m+n+1)//2</li><li>i = (imin+imax)//2, j = half_len – i</li><li>if i &lt; m and nums2[j-1] &gt; nums1[i]: imin = i + 1  # binary search</li><li>elif i &gt; 0 and nums1[i-1] &gt; nums2[j]: imax = i - 1  # binary search</li></ul><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(nums1, nums2)</span>:</span></span><br><span class="line">    m, n = len(nums1), len(nums2)</span><br><span class="line">    <span class="keyword">if</span> m &gt; n:</span><br><span class="line">        nums1, nums2, m, n = nums2, nums1, n, m</span><br><span class="line">    imin, imax, half_len = <span class="number">0</span>, m, (m + n + <span class="number">1</span>) // <span class="number">2</span></span><br><span class="line">    <span class="keyword">while</span> imin &lt;= imax:</span><br><span class="line">        i = (imin + imax) // <span class="number">2</span></span><br><span class="line">        j = half_len - i</span><br><span class="line">        <span class="keyword">if</span> i &gt; <span class="number">0</span> <span class="keyword">and</span> nums1[i - <span class="number">1</span>] &gt; nums2[j]:</span><br><span class="line">            imax = i - <span class="number">1</span></span><br><span class="line">        <span class="keyword">elif</span> i &lt; m <span class="keyword">and</span> nums1[i] &lt; nums2[j - <span class="number">1</span>]:</span><br><span class="line">            imin = i + <span class="number">1</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="keyword">if</span> i == <span class="number">0</span>:</span><br><span class="line">                max_of_left = nums2[j - <span class="number">1</span>]</span><br><span class="line">            <span class="keyword">elif</span> j == <span class="number">0</span>:</span><br><span class="line">                max_of_left = nums1[i - <span class="number">1</span>]</span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                max_of_left = max(nums1[i - <span class="number">1</span>], nums2[j - <span class="number">1</span>])</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (m + n) % <span class="number">2</span> == <span class="number">1</span>:</span><br><span class="line">                <span class="keyword">return</span> max_of_left</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> i == m:</span><br><span class="line">                min_of_right = nums2[j]</span><br><span class="line">            <span class="keyword">elif</span> j == n:</span><br><span class="line">                min_of_right = nums1[i]</span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                min_of_right = min(nums1[i], nums2[j])</span><br><span class="line"></span><br><span class="line">            <span class="keyword">return</span> (max_of_left + min_of_right) / <span class="number">2.</span></span><br></pre></td></tr></table></figure><h1 id="longestPalindrome-dp-O-N-2"><a href="#longestPalindrome-dp-O-N-2" class="headerlink" title="longestPalindrome (dp) O(N^2)"></a>longestPalindrome (dp) O(N^2)</h1><img src="3.png" style="width:800px;"/><p>We set two indexes,’i’ and ‘j’, i to indicate the length of slide window, j to indicate the start of slide window, the dp formular is:</p><blockquote><p>P(i, j) = P(i+1, j-1) + 2, if s[i] == s[j].</p></blockquote><p>Note: </p><ul><li>P(i, i) = 1</li><li>since ‘aa’ is also palindrome, so for P(i, i+1) = 2 if s[i] = s[i+1]</li></ul><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># O(n^2)</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(s)</span>:</span></span><br><span class="line">    n = len(s)</span><br><span class="line">    dp = [[<span class="number">0</span>] * n <span class="keyword">for</span> i <span class="keyword">in</span> range(n+<span class="number">1</span>)]</span><br><span class="line">    ret = <span class="string">""</span></span><br><span class="line">    max_len = <span class="number">0</span></span><br><span class="line">    <span class="comment"># i is the length, j is the start position</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>, len(s)+<span class="number">1</span>):</span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> range(len(s)-i+<span class="number">1</span>):</span><br><span class="line">            <span class="comment"># case 1, single char</span></span><br><span class="line">            <span class="keyword">if</span> i == <span class="number">1</span>:</span><br><span class="line">                dp[i][j] = <span class="number">1</span></span><br><span class="line">            <span class="comment"># case 2, continuous smae char</span></span><br><span class="line">            <span class="keyword">elif</span> i == <span class="number">2</span>:</span><br><span class="line">                <span class="keyword">if</span> s[j] == s[j+<span class="number">1</span>]:</span><br><span class="line">                    dp[i][j] = <span class="number">2</span></span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                <span class="keyword">if</span> dp[i<span class="number">-2</span>][j+<span class="number">1</span>] &gt; <span class="number">0</span> <span class="keyword">and</span> s[j] == s[j+i<span class="number">-1</span>]:</span><br><span class="line">                    dp[i][j] = dp[i<span class="number">-2</span>][j+<span class="number">1</span>] + <span class="number">2</span></span><br><span class="line">            <span class="keyword">if</span> dp[i][j] &gt; max_len:</span><br><span class="line">                max_len = dp[i][j]</span><br><span class="line">                ret = s[j:j+i]</span><br><span class="line">    <span class="keyword">return</span> ret</span><br></pre></td></tr></table></figure><h1 id="isMatch-recursive-with-memo-O-MN"><a href="#isMatch-recursive-with-memo-O-MN" class="headerlink" title="isMatch (recursive with memo) O(MN)"></a>isMatch (recursive with memo) O(MN)</h1><img src="4.png" style="width:800px;"/><p>We can add memory to avoid repeated check.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(s, p)</span>:</span></span><br><span class="line">    memo = &#123;&#125;</span><br><span class="line">    m, n = len(s), len(p)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">recursive</span><span class="params">(i, j)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> (i, j) <span class="keyword">not</span> <span class="keyword">in</span> memo:</span><br><span class="line">            <span class="keyword">if</span> j == n:</span><br><span class="line">                ans = i == m</span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                first_match = i &lt; m <span class="keyword">and</span> p[j] <span class="keyword">in</span> (s[i], <span class="string">'.'</span>)</span><br><span class="line">                <span class="keyword">if</span> j + <span class="number">1</span> &lt; n <span class="keyword">and</span> p[j + <span class="number">1</span>] == <span class="string">'*'</span>:</span><br><span class="line">                    ans = recursive(</span><br><span class="line">                        i, j + <span class="number">2</span>) <span class="keyword">or</span> first_match <span class="keyword">and</span> recursive(i + <span class="number">1</span>, j)</span><br><span class="line">                <span class="keyword">else</span>:</span><br><span class="line">                    ans = first_match <span class="keyword">and</span> recursive(i + <span class="number">1</span>, j + <span class="number">1</span>)</span><br><span class="line">            memo[i, j] = ans</span><br><span class="line">        <span class="keyword">return</span> memo[i, j]</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> recursive(<span class="number">0</span>, <span class="number">0</span>)</span><br></pre></td></tr></table></figure><h1 id="maxArea-trick-O-n"><a href="#maxArea-trick-O-n" class="headerlink" title="maxArea (trick) O(n)"></a>maxArea (trick) O(n)</h1><img src="5.png" style="width:800px;"/><p>We set i = 0, and j = len(heights), each time, we move the shorter height to find a higher height, and recalculate the max area. Beceuase if the next height is shorter than current height, it also has a short length on x-axis, it cannot have a larger volumn.</p><p>Note: we cannot use dp, since this problem doesn’t have the optimal sub-structure.</p><p>This trick works, because we fasten the i and j from the outside to inside, the width will decrease, only if the height increase, there may exist a larger area. And why we move the shorter height, because, if we fix the shorter height, and move the higher height, the min height still is the shorter height, the max area will only decrease with the width.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># O(n)</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(height)</span>:</span></span><br><span class="line">    i, j = <span class="number">0</span>, len(height) - <span class="number">1</span></span><br><span class="line">    ret = min(height[j], height[i]) * (j-i)</span><br><span class="line">    <span class="keyword">while</span> i &lt; j:</span><br><span class="line">        ret = max(ret, min(height[i], height[j]) * (j-i))</span><br><span class="line">        <span class="keyword">if</span> height[i] &lt; height[j]:</span><br><span class="line">            i += <span class="number">1</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            j -= <span class="number">1</span></span><br><span class="line">    <span class="keyword">return</span> ret</span><br></pre></td></tr></table></figure><h1 id="mark-threeSum-trick-O-n-2"><a href="#mark-threeSum-trick-O-n-2" class="headerlink" title="(mark) threeSum (trick) O(n^2)"></a>(mark) threeSum (trick) O(n^2)</h1><img src="6.png" style="width:800px;"/><p>We firstly sort the array, and then set three indexes, the first one is i, after we fix the i, we set j at the i+1, and k at the n-1, then we want the sum(s[j], s[k]) is -s[i], if the sum is less, we move the j=j+1, else if the sum is larger, we move the k=k-1(since the array is sorted).</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># O(n^2) sort</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(nums)</span>:</span></span><br><span class="line">    n = len(nums)</span><br><span class="line">    nums.sort()</span><br><span class="line">    ret = []</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(n<span class="number">-2</span>):</span><br><span class="line">        <span class="keyword">if</span> i &gt; <span class="number">0</span> <span class="keyword">and</span> nums[i] == nums[i<span class="number">-1</span>]: <span class="comment"># avoid repeat</span></span><br><span class="line">            <span class="keyword">continue</span></span><br><span class="line">        j, k = i+<span class="number">1</span>, n<span class="number">-1</span></span><br><span class="line">        <span class="keyword">while</span> j &lt; k:</span><br><span class="line">            s = nums[i] + nums[j] + nums[k]</span><br><span class="line">            <span class="keyword">if</span> s &lt; <span class="number">0</span>:</span><br><span class="line">                j += <span class="number">1</span></span><br><span class="line">            <span class="keyword">elif</span> s &gt; <span class="number">0</span>:</span><br><span class="line">                k -= <span class="number">1</span></span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                ret.append([nums[i], nums[j], nums[k]])</span><br><span class="line">                <span class="keyword">while</span> j &lt; k <span class="keyword">and</span> nums[j] == nums[j+<span class="number">1</span>]: <span class="comment"># avoid repeat</span></span><br><span class="line">                    j += <span class="number">1</span></span><br><span class="line">                <span class="keyword">while</span> j &lt; k <span class="keyword">and</span> nums[k] == nums[k<span class="number">-1</span>]: <span class="comment"># avoid repeat</span></span><br><span class="line">                    k -= <span class="number">1</span></span><br><span class="line">                j += <span class="number">1</span></span><br><span class="line">                k -= <span class="number">1</span></span><br><span class="line">    <span class="keyword">return</span> ret</span><br></pre></td></tr></table></figure><h1 id="letterCombinations-full-permutation-O-3-N-4-M"><a href="#letterCombinations-full-permutation-O-3-N-4-M" class="headerlink" title="letterCombinations (full permutation), O(3^N * 4^M)"></a>letterCombinations (full permutation), O(3^N * 4^M)</h1><img src="7.png" style="width:800px;"/><p>Just use recursive, within the function, use while to iterate all possible chars to call recursive function.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line">phone = &#123;<span class="string">'2'</span>: [<span class="string">'a'</span>, <span class="string">'b'</span>, <span class="string">'c'</span>],</span><br><span class="line">         <span class="string">'3'</span>: [<span class="string">'d'</span>, <span class="string">'e'</span>, <span class="string">'f'</span>],</span><br><span class="line">         <span class="string">'4'</span>: [<span class="string">'g'</span>, <span class="string">'h'</span>, <span class="string">'i'</span>],</span><br><span class="line">         <span class="string">'5'</span>: [<span class="string">'j'</span>, <span class="string">'k'</span>, <span class="string">'l'</span>],</span><br><span class="line">         <span class="string">'6'</span>: [<span class="string">'m'</span>, <span class="string">'n'</span>, <span class="string">'o'</span>],</span><br><span class="line">         <span class="string">'7'</span>: [<span class="string">'p'</span>, <span class="string">'q'</span>, <span class="string">'r'</span>, <span class="string">'s'</span>],</span><br><span class="line">         <span class="string">'8'</span>: [<span class="string">'t'</span>, <span class="string">'u'</span>, <span class="string">'v'</span>],</span><br><span class="line">         <span class="string">'9'</span>: [<span class="string">'w'</span>, <span class="string">'x'</span>, <span class="string">'y'</span>, <span class="string">'z'</span>]&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(digits)</span>:</span></span><br><span class="line">    ret = []</span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> digits:</span><br><span class="line">        <span class="keyword">return</span> ret</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">recursion</span><span class="params">(prefix, digits)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> digits:</span><br><span class="line">            ret.append(prefix)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="keyword">for</span> c <span class="keyword">in</span> phone[digits[<span class="number">0</span>]]:</span><br><span class="line">                recursion(prefix+c, digits[<span class="number">1</span>:])</span><br><span class="line">    recursion(<span class="string">""</span>, digits)</span><br><span class="line">    <span class="keyword">return</span> ret</span><br></pre></td></tr></table></figure><h1 id="removeNthFromEnd-trick-O-n"><a href="#removeNthFromEnd-trick-O-n" class="headerlink" title="removeNthFromEnd (trick) O(n)"></a>removeNthFromEnd (trick) O(n)</h1><img src="8.png" style="width:800px;"/><p>We use two indexes, I and j, and we let the I firstly move forward n steps, and then we move these two indexes together, once the I reach the end, the j is at last n-th node.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(head, n)</span>:</span></span><br><span class="line">    t = ListNode(<span class="literal">None</span>)</span><br><span class="line">    t.next = head</span><br><span class="line">    p = t</span><br><span class="line">    q = t</span><br><span class="line">    <span class="keyword">for</span> _ <span class="keyword">in</span> range(n):</span><br><span class="line">        p = p.next</span><br><span class="line">    <span class="keyword">while</span> p.next:</span><br><span class="line">        q = q.next</span><br><span class="line">        p = p.next</span><br><span class="line">    q.next = q.next.next</span><br><span class="line">    <span class="keyword">return</span> t.next</span><br></pre></td></tr></table></figure><h1 id="generateParenthesis-recursive-O-4-n-√2"><a href="#generateParenthesis-recursive-O-4-n-√2" class="headerlink" title="generateParenthesis (recursive) O(4^n/√2)"></a>generateParenthesis (recursive) O(4^n/√2)</h1><img src="9.png" style="width:800px;"/><p>We use a count n to record the remaining available left parentheses, and another count k to record the assigned left parentheses.</p><ul><li>if n == 0 and k == 0, output</li><li>If n == 0 and k&gt;0, recursive(n, k-1), add ‘)’</li><li>If n &gt; 0 and k == 0, recursive(n-1, k+1), add ‘(’</li><li>else we should try two directions: recursive(n-1, k+1), add ‘(’, and recursive(n, k-1), add ‘)’</li></ul><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(n)</span>:</span></span><br><span class="line">    ret = []</span><br><span class="line">    <span class="keyword">if</span> n == <span class="number">0</span>:</span><br><span class="line">        <span class="keyword">return</span> ret</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">recursion</span><span class="params">(prefix, i, j)</span>:</span></span><br><span class="line">        <span class="comment"># i means the assigned number of "(" and don't have ")"</span></span><br><span class="line">        <span class="comment"># j means the available number of "("</span></span><br><span class="line">        <span class="keyword">if</span> i==<span class="number">0</span> <span class="keyword">and</span> j == <span class="number">0</span>:</span><br><span class="line">            ret.append(prefix)</span><br><span class="line">        <span class="keyword">elif</span> i == <span class="number">0</span>:</span><br><span class="line">            recursion(prefix+<span class="string">"("</span>, i+<span class="number">1</span>, j<span class="number">-1</span>)</span><br><span class="line">        <span class="keyword">elif</span> j == <span class="number">0</span>:</span><br><span class="line">            recursion(prefix+<span class="string">")"</span>, i<span class="number">-1</span>, j)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            recursion(prefix+<span class="string">"("</span>, i+<span class="number">1</span>, j<span class="number">-1</span>)</span><br><span class="line">            recursion(prefix+<span class="string">")"</span>, i<span class="number">-1</span>, j)</span><br><span class="line"></span><br><span class="line">    recursion(<span class="string">""</span>, <span class="number">0</span>, n)</span><br><span class="line">    <span class="keyword">return</span> ret</span><br></pre></td></tr></table></figure><h1 id="nextPermutation-trick-O-n"><a href="#nextPermutation-trick-O-n" class="headerlink" title="nextPermutation (trick), O(n)"></a>nextPermutation (trick), O(n)</h1><img src="10.png" style="width:800px;"/><p>We set j=len(n)-1, and set i=len(n)-2, we search the i from end to start, once we found n[i] &lt; n[j], we swop n[i] and n[j], then we reverse n[i:j+1]</p><p>Note, from the last value, once we find a smaller value, we reverse all values between these two values.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(nums)</span>:</span></span><br><span class="line">    n = len(nums)</span><br><span class="line">    i = n - <span class="number">2</span></span><br><span class="line">    <span class="keyword">while</span> i &gt;= <span class="number">0</span> <span class="keyword">and</span> nums[i + <span class="number">1</span>] &lt;= nums[i]:</span><br><span class="line">        i -= <span class="number">1</span></span><br><span class="line">    <span class="keyword">if</span> i &gt;= <span class="number">0</span>:</span><br><span class="line">        j = n - <span class="number">1</span></span><br><span class="line">        <span class="keyword">while</span> j &gt;= <span class="number">0</span> <span class="keyword">and</span> nums[j] &lt;= nums[i]:</span><br><span class="line">            j -= <span class="number">1</span></span><br><span class="line">        nums[i], nums[j] = nums[j], nums[i]</span><br><span class="line">        nums[i + <span class="number">1</span>:] = nums[:i:<span class="number">-1</span>]</span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        nums[::] = nums[::<span class="number">-1</span>]</span><br></pre></td></tr></table></figure><h1 id="longestValidParentheses"><a href="#longestValidParentheses" class="headerlink" title="longestValidParentheses"></a>longestValidParentheses</h1><img src="11.png" style="width:800px;"/><h2 id="longestValidParentheses-stack-trick-O-n"><a href="#longestValidParentheses-stack-trick-O-n" class="headerlink" title="longestValidParentheses (stack trick) O(n)"></a>longestValidParentheses (stack trick) O(n)</h2><p>The idea behind this problem is, when we find a ‘)’ (stack is not empty), we need to check where does this start. So we record the position in the stack.</p><p>Once we encounter an invalid char(still need pop), or we has a ‘(’ now, we push its position into the stack, after that, when we encounter a “)”, we firstly pop a value from the stack, then we can always found the start point of the valid sub-path at the stack[-1].</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># dp O(n)</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(s)</span>:</span></span><br><span class="line">    max_ret = <span class="number">0</span></span><br><span class="line">    stack = [<span class="number">-1</span>]</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(len(s)):</span><br><span class="line">        <span class="keyword">if</span> s[i] == <span class="string">'('</span>:</span><br><span class="line">            stack.append(i)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            stack.pop()</span><br><span class="line">            <span class="keyword">if</span> len(stack) == <span class="number">0</span>:</span><br><span class="line">                stack.append(i) <span class="comment"># last invalid point</span></span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                max_ret = max(max_ret, i - stack[<span class="number">-1</span>]) <span class="comment"># i-stack[-1] is current valid length</span></span><br><span class="line">    <span class="keyword">return</span> max_ret</span><br><span class="line"></span><br><span class="line"><span class="comment"># dp O(n)</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(s)</span>:</span></span><br><span class="line">    ret = <span class="number">0</span></span><br><span class="line">    dp = [<span class="number">0</span>] * len(s)</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>, len(s)):</span><br><span class="line">        <span class="keyword">if</span> s[i] == <span class="string">")"</span>:</span><br><span class="line">            <span class="keyword">if</span> s[i - <span class="number">1</span>] == <span class="string">"("</span>:</span><br><span class="line">                dp[i] = (dp[i - <span class="number">2</span>] <span class="keyword">if</span> i &gt; <span class="number">1</span> <span class="keyword">else</span> <span class="number">0</span>) + <span class="number">2</span></span><br><span class="line">            <span class="keyword">elif</span> i - dp[i - <span class="number">1</span>] &gt; <span class="number">0</span> <span class="keyword">and</span> s[i - dp[i - <span class="number">1</span>] - <span class="number">1</span>] == <span class="string">"("</span>:</span><br><span class="line">                dp[i] = dp[i<span class="number">-1</span>] + <span class="number">2</span> + \</span><br><span class="line">                    (dp[i-dp[i<span class="number">-1</span>]<span class="number">-2</span>] <span class="keyword">if</span> i-dp[i<span class="number">-1</span>]<span class="number">-2</span> &gt; <span class="number">0</span> <span class="keyword">else</span> <span class="number">0</span>)</span><br><span class="line">        ret = max(ret, dp[i])</span><br><span class="line">    <span class="keyword">return</span> ret</span><br></pre></td></tr></table></figure><h2 id="mark-longestValidParentheses-dp-O-n"><a href="#mark-longestValidParentheses-dp-O-n" class="headerlink" title="(mark) longestValidParentheses (dp) O(n)"></a>(mark) longestValidParentheses (dp) O(n)</h2><p>the i-th element of dp arary indicates the length of the longest valid substring ending at i-th index. And because the valid substing always ends with ‘)’, we will have:</p><ul><li>s[i-1,i] = ‘()’, dp[i] = dp[i-2]+2, (dp[i-1]=0)</li><li>s[i-1,i] = ‘))’, we need to check s[i-dp[i-1]-1], the last char before the longest substring ending at [i-1] whether is ‘(’, if it is, dp[i] = dp[i-1] + dp[i-dp[i-1]-2]+2, the longest substing a [i-1] + substring before last valid substring + 2.</li></ul><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># dp O(n)</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(s)</span>:</span></span><br><span class="line">    ret = <span class="number">0</span></span><br><span class="line">    dp = [<span class="number">0</span>] * len(s)</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>, len(s)):</span><br><span class="line">        <span class="keyword">if</span> s[i] == <span class="string">")"</span>:</span><br><span class="line">            print(s[i], i, dp[i<span class="number">-1</span>])</span><br><span class="line">            <span class="keyword">if</span> s[i<span class="number">-1</span>] == <span class="string">"("</span>:</span><br><span class="line">                dp[i] = (dp[i<span class="number">-2</span>] <span class="keyword">if</span> i &gt; <span class="number">1</span> <span class="keyword">else</span> <span class="number">0</span>) + <span class="number">2</span></span><br><span class="line">            <span class="keyword">elif</span> i-dp[i<span class="number">-1</span>] &gt; <span class="number">0</span> <span class="keyword">and</span> s[i-dp[i<span class="number">-1</span>]<span class="number">-1</span>] == <span class="string">"("</span>:</span><br><span class="line">                dp[i] = dp[i<span class="number">-1</span>] + <span class="number">2</span> + \</span><br><span class="line">                    (dp[i-dp[i<span class="number">-1</span>]<span class="number">-2</span>] <span class="keyword">if</span> i-dp[i<span class="number">-1</span>]<span class="number">-2</span> &gt; <span class="number">0</span> <span class="keyword">else</span> <span class="number">0</span>)</span><br><span class="line">        ret = max(ret, dp[i])</span><br><span class="line">    <span class="keyword">return</span> ret</span><br></pre></td></tr></table></figure><h1 id="mark-search-binary-search-trick-O-log-n"><a href="#mark-search-binary-search-trick-O-log-n" class="headerlink" title="(mark) search (binary search trick) O(log n)"></a>(mark) search (binary search trick) O(log n)</h1><img src="12.png" style="width:800px;"/><p>For example, we take [4, 5, 6, 7, 0, 1, 2] as input, and s=0, e=n-1, m=(s+e)//2</p><p>We make these four assumptions:</p><ul><li>the axis locates at right, and target is at left: nums[s] &lt;= target &lt; nums[m] -&gt; recursive(s, m-1)</li><li>the axis locates at left, and target is at right: nums[m] &lt; target &lt;= nums[e] -&gt; recursive(s+1, m)</li><li>the axis locates at right, and target is at right: nums[m] &gt; nums[e]  -&gt; recursive(m+1, e)</li><li>the axis locates at left, and target is at left: nums[s] &gt; nums[m] -&gt; recursive(s, m-1)</li></ul><p>For third case, there is a hidden condition, , if nums[m] &gt; nums[e], this means the axis locates at right, so the nums[s] must be less than nums[m], at this condition, we only need to check the target if it locates at the right, since if the target locates at left, this case will be the first case. The same idea for fourth case.</p><p>Besides, the recursion doesn’t break the rules of rotated list.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(nums, target)</span>:</span></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> nums:</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">recursion</span><span class="params">(i, j)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> j &lt; i:</span><br><span class="line">            <span class="keyword">return</span> <span class="number">-1</span></span><br><span class="line">        m = (i+j)//<span class="number">2</span></span><br><span class="line">        <span class="keyword">if</span> nums[m] == target:</span><br><span class="line">            <span class="keyword">return</span> m</span><br><span class="line">        <span class="comment"># target locates at non-axis side (and is left side)</span></span><br><span class="line">        <span class="keyword">elif</span> nums[i] &lt;= target &lt; nums[m]:</span><br><span class="line">            <span class="keyword">return</span> recursion(i, m<span class="number">-1</span>)</span><br><span class="line">        <span class="comment"># target locates at non-axis side (and is right side)</span></span><br><span class="line">        <span class="keyword">elif</span> nums[m] &lt; target &lt;= nums[j]:</span><br><span class="line">            <span class="keyword">return</span> recursion(m+<span class="number">1</span>, j)</span><br><span class="line">        <span class="comment"># target locates at axis side (and is right side)</span></span><br><span class="line">        <span class="keyword">elif</span> nums[m] &gt; nums[j]:</span><br><span class="line">            <span class="keyword">return</span> recursion(m+<span class="number">1</span>, j)</span><br><span class="line">        <span class="comment"># target locates at axis side (and is left side)</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="keyword">return</span> recursion(i, m<span class="number">-1</span>)</span><br><span class="line">    <span class="keyword">return</span> recursion(<span class="number">0</span>, len(nums)<span class="number">-1</span>)</span><br></pre></td></tr></table></figure><h1 id="searchRange-Binary-Search-trick-O-log-n"><a href="#searchRange-Binary-Search-trick-O-log-n" class="headerlink" title="searchRange (Binary Search trick) O(log n)"></a>searchRange (Binary Search trick) O(log n)</h1><img src="13.png" style="width:800px;"/><p>we use two binary search, one search the left boundary and another for the right boundary.</p><p>We modify the original binary search, make it to continue search when we found the target value. Also, once right_index <code>&lt;</code>left_index, we return left_index for the left boundary search, and return right_index for the right one.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(nums, target)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">recursiveLeft</span><span class="params">(s, e)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> e &lt; s:</span><br><span class="line">            <span class="keyword">return</span> s</span><br><span class="line">        m = (s+e)//<span class="number">2</span></span><br><span class="line">        <span class="keyword">if</span> nums[m] &lt; target:</span><br><span class="line">            <span class="keyword">return</span> recursiveLeft(m+<span class="number">1</span>, e)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="keyword">return</span> recursiveLeft(s, m<span class="number">-1</span>)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">recursiveRight</span><span class="params">(s, e)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> e &lt; s:</span><br><span class="line">            <span class="keyword">return</span> e</span><br><span class="line">        m = (s+e)//<span class="number">2</span></span><br><span class="line">        <span class="keyword">if</span> nums[m] &lt;= target:</span><br><span class="line">            <span class="keyword">return</span> recursiveRight(m+<span class="number">1</span>, e)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="keyword">return</span> recursiveRight(s, m<span class="number">-1</span>)</span><br><span class="line"></span><br><span class="line">    left, right = recursiveLeft(<span class="number">0</span>, len(nums)<span class="number">-1</span>), recursiveRight(<span class="number">0</span>, len(nums)<span class="number">-1</span>)</span><br><span class="line">    <span class="comment"># once we cannot found the value, the left will right-1</span></span><br><span class="line">    <span class="keyword">return</span> [left, right] <span class="keyword">if</span> left &lt;= right <span class="keyword">else</span> [<span class="number">-1</span>, <span class="number">-1</span>]</span><br></pre></td></tr></table></figure><h1 id="combinationSum-dfs-O-exponential"><a href="#combinationSum-dfs-O-exponential" class="headerlink" title="combinationSum (dfs) O(exponential)"></a>combinationSum (dfs) O(exponential)</h1><img src="14.png" style="width:800px;"/><p>just add a loop to call the recursion function.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(candidates, target)</span>:</span></span><br><span class="line">    ret = []</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">recursion</span><span class="params">(prefix, i, target)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> target == <span class="number">0</span>:</span><br><span class="line">            ret.append(prefix)</span><br><span class="line">        <span class="keyword">for</span> i <span class="keyword">in</span> range(i, len(candidates)):</span><br><span class="line">            num = candidates[i]</span><br><span class="line">            <span class="keyword">if</span> target - num &gt;= <span class="number">0</span>:</span><br><span class="line">                recursion(prefix+[num], i, target-num)</span><br><span class="line">    recursion([], <span class="number">0</span>, target)</span><br><span class="line">    <span class="keyword">return</span> ret</span><br></pre></td></tr></table></figure><h1 id="firstMissingPositive-trick-hash-with-mod-position-O-n"><a href="#firstMissingPositive-trick-hash-with-mod-position-O-n" class="headerlink" title="firstMissingPositive (trick: hash with mod position) O(n)"></a>firstMissingPositive (trick: hash with mod position) O(n)</h1><img src="15.png" style="width:800px;"/><p>The biggest challenge is, we can only use extra constant space which means we cannot set up a hash table with O(n) space. So the solution use the input array as the has table. </p><p>Using: nums[nums[i] % n] += n(first to handle all &lt;0 or &gt;=n to 0), this to mark a bin has been covered, after iterating all values, we can check the value which less than n, the first value we found is the final result.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(nums)</span>:</span></span><br><span class="line">    nums.append(<span class="number">0</span>) <span class="comment"># very important, for example, [1]</span></span><br><span class="line">    n = len(nums)</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(n):  <span class="comment"># delete those useless elements</span></span><br><span class="line">        <span class="keyword">if</span> nums[i] &lt; <span class="number">0</span> <span class="keyword">or</span> nums[i] &gt;= n:</span><br><span class="line">            nums[i] = <span class="number">0</span></span><br><span class="line">    <span class="comment"># use the index as the hash to record the frequency of each number</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(n):</span><br><span class="line">        nums[nums[i] % n] += n <span class="comment"># +n and %n to make the original value do not be overlaped</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>, n):</span><br><span class="line">        <span class="keyword">if</span> nums[i] &lt; n:</span><br><span class="line">            <span class="keyword">return</span> i</span><br><span class="line">    <span class="keyword">return</span> n</span><br></pre></td></tr></table></figure><h1 id="Trap-two-dp-left-and-right-O-n"><a href="#Trap-two-dp-left-and-right-O-n" class="headerlink" title="Trap (two dp, left and right) O(n)"></a>Trap (two dp, left and right) O(n)</h1><img src="16.png" style="width:800px;"/><p>We use two dp, to record both max heights from left and right respectively.</p><ul><li>dp_l[i] = max(dp_l[i-1], height[i])</li><li>dp_r[i] = max(dp_r[i+1], height[i])</li></ul><p>Then, each bin’s water volume is: min(dp_l[i], dp_r[i]) - height[i].</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(height)</span>:</span></span><br><span class="line">    height = [<span class="number">0</span>] + height + [<span class="number">0</span>]</span><br><span class="line">    n = len(height)</span><br><span class="line">    ret = <span class="number">0</span></span><br><span class="line">    max_left = [<span class="number">0</span>] * n  <span class="comment"># record the max height from left</span></span><br><span class="line">    max_right = [<span class="number">0</span>] * n  <span class="comment"># record the max height from right</span></span><br><span class="line">    cur_max = height[<span class="number">0</span>]</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(n):</span><br><span class="line">        cur_max = max(cur_max, height[i])</span><br><span class="line">        max_left[i] = cur_max</span><br><span class="line">    cur_max = height[<span class="number">-1</span>]</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(n<span class="number">-1</span>, <span class="number">-1</span>, <span class="number">-1</span>):</span><br><span class="line">        cur_max = max(cur_max, height[i])</span><br><span class="line">        max_right[i] = cur_max</span><br><span class="line">        ret += min(max_left[i], max_right[i]) - height[i]</span><br><span class="line">    <span class="keyword">return</span> ret</span><br></pre></td></tr></table></figure><h1 id="Permute-loop-call-recusive-O-2-n"><a href="#Permute-loop-call-recusive-O-2-n" class="headerlink" title="Permute (loop call recusive) O(2^n)"></a>Permute (loop call recusive) O(2^n)</h1><img src="17.png" style="width:800px;"/><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(nums)</span>:</span></span><br><span class="line">    ret = []</span><br><span class="line">    n = len(nums)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">recursion</span><span class="params">(prefix, remaining)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> remaining:</span><br><span class="line">            ret.append(prefix)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="keyword">for</span> k <span class="keyword">in</span> range(len(remaining)):</span><br><span class="line">                recursion(prefix+[remaining[k]], remaining[:k] + remaining[k+<span class="number">1</span>:])</span><br><span class="line">    recursion([], nums)</span><br><span class="line">    <span class="keyword">return</span> ret</span><br></pre></td></tr></table></figure><h1 id="Rotate-trick-O-n-2"><a href="#Rotate-trick-O-n-2" class="headerlink" title="Rotate (trick) O(n^2)"></a>Rotate (trick) O(n^2)</h1><img src="18.png" style="width:800px;"/><p>First reverse and then transpose</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(matrix)</span>:</span></span><br><span class="line">    n = len(matrix)</span><br><span class="line">    i, j = <span class="number">0</span>, n - <span class="number">1</span></span><br><span class="line">    <span class="comment"># swap</span></span><br><span class="line">    <span class="keyword">while</span> i &lt; j:</span><br><span class="line">        matrix[i], matrix[j] = matrix[j], matrix[i]</span><br><span class="line">        i += <span class="number">1</span></span><br><span class="line">        j -= <span class="number">1</span></span><br><span class="line">    <span class="comment"># transpose</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(n):</span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> range(i, n):</span><br><span class="line">            matrix[i][j], matrix[j][i] = matrix[j][i], matrix[i][j]</span><br></pre></td></tr></table></figure><h1 id="mark-groupAnagrams-hash-O-m"><a href="#mark-groupAnagrams-hash-O-m" class="headerlink" title="(mark) groupAnagrams (hash) O(m)"></a>(mark) groupAnagrams (hash) O(m)</h1><img src="19.png" style="width:800px;"/><p>using collections.defaultdict(list), take the tuple(sorted(s)) as the key of hash table.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> collections <span class="keyword">import</span> defaultdict</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(strs)</span>:</span></span><br><span class="line">    ret = defaultdict(list)</span><br><span class="line">    <span class="keyword">for</span> s <span class="keyword">in</span> strs:</span><br><span class="line">        ret[tuple(sorted(s))].append(s)</span><br><span class="line">    <span class="keyword">return</span> list(ret.values())</span><br></pre></td></tr></table></figure><p>** sorted(s) is ok but set(s) cannot.</p><h1 id="mark-maxSubArray-trick-O-n"><a href="#mark-maxSubArray-trick-O-n" class="headerlink" title="(mark) maxSubArray (trick) O(n)"></a>(mark) maxSubArray (trick) O(n)</h1><img src="20.png" style="width:800px;"/><p>We scan the array from the left to right, we use a list to record the accumulated sum, only if the left sum is positive, we use it, otherwise, this negative value should be discarded. </p><p>So the sum is: nums[i] = max(0, nums[i-1]) + nums[i]</p><p>For each continue positve sub-list, we compare to record the maximum sum: ret = max(ret, nums[i]).</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(nums)</span>:</span></span><br><span class="line">    cur_sum = <span class="number">0</span></span><br><span class="line">    max_v = - sys.maxsize</span><br><span class="line">    <span class="keyword">for</span> num <span class="keyword">in</span> nums:</span><br><span class="line">        cur_sum = max(<span class="number">0</span>, cur_sum) + num</span><br><span class="line">        max_v = max(max_v, cur_sum)</span><br><span class="line">    <span class="keyword">return</span> max_v</span><br></pre></td></tr></table></figure><h1 id="canJump-greedy-O-n"><a href="#canJump-greedy-O-n" class="headerlink" title="canJump (greedy) O(n)"></a>canJump (greedy) O(n)</h1><img src="21.png" style="width:800px;"/><p>We scan the list from the left to right up to the farest value we can reach, each time once we get a new value, we modify the farest value, so if we can attend the final position, return true, otherwise return false.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(nums)</span>:</span></span><br><span class="line">    m = <span class="number">0</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">0</span>, len(nums)):</span><br><span class="line">        <span class="keyword">if</span> i &gt; m:</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line">        m = max(m, i+nums[i])</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(nums)</span>:</span></span><br><span class="line">    i, max_i = <span class="number">0</span>, <span class="number">0</span></span><br><span class="line">    <span class="keyword">while</span> i &lt;= max_i <span class="keyword">and</span> i &lt; len(nums) <span class="keyword">and</span> max_i &lt; len(nums)<span class="number">-1</span>:</span><br><span class="line">        max_i = max(max_i, i+nums[i])</span><br><span class="line">        i += <span class="number">1</span></span><br><span class="line">    <span class="keyword">return</span> max_i &gt;= len(nums) - <span class="number">1</span></span><br></pre></td></tr></table></figure><h1 id="merge-greedy-O-n"><a href="#merge-greedy-O-n" class="headerlink" title="merge (greedy) O(n)"></a>merge (greedy) O(n)</h1><img src="22.png" style="width:800px;"/><p>We firstly sort the input, then using a list, add the first tuple into the list, each time check whether the last tuple’s right value is larger than the second tuple’s first value, if yes, we merge this two, else only insert the tuple into the list.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(intervals)</span>:</span></span><br><span class="line">    intervals.sort(key=<span class="keyword">lambda</span> x: x[<span class="number">0</span>])</span><br><span class="line">    merged = []</span><br><span class="line">    <span class="keyword">for</span> interval <span class="keyword">in</span> intervals:</span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> merged <span class="keyword">or</span> merged[<span class="number">-1</span>][<span class="number">1</span>] &lt; interval[<span class="number">0</span>]:</span><br><span class="line">            merged.append(interval)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            merged[<span class="number">-1</span>][<span class="number">1</span>] = max(merged[<span class="number">-1</span>][<span class="number">1</span>], interval[<span class="number">1</span>])</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> merged</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(intervals)</span>:</span></span><br><span class="line">    intervals.sort(key=<span class="keyword">lambda</span> x: x[<span class="number">0</span>])</span><br><span class="line">    ret = []</span><br><span class="line">    <span class="keyword">while</span> intervals:</span><br><span class="line">        interval_1 = intervals.pop(<span class="number">0</span>)</span><br><span class="line">        <span class="keyword">if</span> intervals <span class="keyword">and</span> interval_1[<span class="number">1</span>] &gt;= intervals[<span class="number">0</span>][<span class="number">0</span>]:</span><br><span class="line">            intervals[<span class="number">0</span>][<span class="number">0</span>] = interval_1[<span class="number">0</span>]</span><br><span class="line">            intervals[<span class="number">0</span>][<span class="number">1</span>] = max(intervals[<span class="number">0</span>][<span class="number">1</span>], interval_1[<span class="number">1</span>])</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            ret.append(interval_1)</span><br><span class="line">    <span class="keyword">return</span> ret</span><br></pre></td></tr></table></figure><h1 id="uniquePaths-dp-O-m-n"><a href="#uniquePaths-dp-O-m-n" class="headerlink" title="uniquePaths (dp) O(m+n)"></a>uniquePaths (dp) O(m+n)</h1><img src="23.png" style="width:800px;"/><p><code>dp[i][j] = dp[i][j-1] + dp[i-1][j]</code></p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(m, n)</span>:</span></span><br><span class="line">    dp = [[<span class="number">0</span>] * n <span class="keyword">for</span> _ <span class="keyword">in</span> range(m)]</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(m):</span><br><span class="line">        dp[i][<span class="number">0</span>] = <span class="number">1</span></span><br><span class="line">    <span class="keyword">for</span> j <span class="keyword">in</span> range(n):</span><br><span class="line">        dp[<span class="number">0</span>][j] = <span class="number">1</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>, m):</span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> range(<span class="number">1</span>, n):</span><br><span class="line">            dp[i][j] = dp[i][j<span class="number">-1</span>] + dp[i<span class="number">-1</span>][j]</span><br><span class="line">    <span class="keyword">return</span> dp[<span class="number">-1</span>][<span class="number">-1</span>]</span><br></pre></td></tr></table></figure><h1 id="minPathSum-dp-O-mn"><a href="#minPathSum-dp-O-mn" class="headerlink" title="minPathSum (dp) O(mn)"></a>minPathSum (dp) O(mn)</h1><img src="24.png" style="width:800px;"/><p><code>dp[i][j] = min(dp[i][j-1], dp[i-1][j]) + m[i][j]</code></p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(grid)</span>:</span></span><br><span class="line">    m, n = len(grid), len(grid[<span class="number">0</span>])</span><br><span class="line">    dp = [[<span class="number">0</span>]*n <span class="keyword">for</span> _ <span class="keyword">in</span> range(m)]</span><br><span class="line">    dp[<span class="number">0</span>][<span class="number">0</span>] = grid[<span class="number">0</span>][<span class="number">0</span>]</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>, m):</span><br><span class="line">        dp[i][<span class="number">0</span>] = dp[i<span class="number">-1</span>][<span class="number">0</span>] + grid[i][<span class="number">0</span>]</span><br><span class="line">    <span class="keyword">for</span> j <span class="keyword">in</span> range(<span class="number">1</span>, n):</span><br><span class="line">        dp[<span class="number">0</span>][j] = dp[<span class="number">0</span>][j<span class="number">-1</span>] + grid[<span class="number">0</span>][j]</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>, m):</span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> range(<span class="number">1</span>, n):</span><br><span class="line">            dp[i][j] = min(dp[i<span class="number">-1</span>][j], dp[i][j<span class="number">-1</span>]) + grid[i][j]</span><br><span class="line">    <span class="keyword">return</span> dp[<span class="number">-1</span>][<span class="number">-1</span>]</span><br></pre></td></tr></table></figure><h1 id="climbStairs-dp-O-n"><a href="#climbStairs-dp-O-n" class="headerlink" title="climbStairs (dp) O(n)"></a>climbStairs (dp) O(n)</h1><img src="25.png" style="width:800px;"/><p><code>dp[i] = dp[i-1] + dp[i-2], dp[0]=1, dp[1] = 1</code></p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(n)</span>:</span></span><br><span class="line">    <span class="keyword">if</span> n == <span class="number">1</span>:</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span></span><br><span class="line">    dp = [<span class="number">0</span>] * n</span><br><span class="line">    dp[<span class="number">0</span>], dp[<span class="number">1</span>] = <span class="number">1</span>, <span class="number">2</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">2</span>, n):</span><br><span class="line">        dp[i] = dp[i<span class="number">-1</span>] + dp[i<span class="number">-2</span>]</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> dp[<span class="number">-1</span>]</span><br></pre></td></tr></table></figure><h1 id="mark-minDistance-dc-O-mn"><a href="#mark-minDistance-dc-O-mn" class="headerlink" title="(mark) minDistance (dc) O(mn)"></a>(mark) minDistance (dc) O(mn)</h1><img src="26.png" style="width:800px;"/><p>We define a recursive function: recursive(word1, word2, i, j, memo), which returns the amount of operations for word1[i:] and word2[j:], and we use memo to record the sub-problem. </p><p>For the word1[i] and word2[j], if they are equal, we make memo[i][j] = recursive(word1, word2, i+1, j+1, memo). Else, we define these three operations:</p><ul><li>insert = 1 + recursive(word1, word2, i, j+1, memo), we insert word2[j] before word1[i], so they are eaqual, then we compare word1[i] with word2[j+1].</li><li>delete = 1 + recursive(word1, word2, i+1, j, memo), we delete word2[j], then we compare word1[i+1] with word2[j].</li><li>replace = 1 + recursive(word1, word2, i+1, j+1, memo), we replace word1[i] with word2[j], so they are equal, so we continue to compare word1[i+1] with word2[j+1].</li></ul><p>and make memo[i][j] = min(insert, delete, replace).</p><p>For the termination of the recursion, when i=m, j=n, it means we’ve handled all words, so return 0, else, when i=m, we return n-j, when i=n, we return m-i, since if we’ve already handled one word, we only need to insert the remaining chars in another word.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(word1, word2)</span>:</span></span><br><span class="line">    memo = &#123;&#125;</span><br><span class="line">    n, m = len(word1), len(word2)</span><br><span class="line">    <span class="keyword">if</span> m &lt; n:</span><br><span class="line">        word1, word2, m, n = word2, word1, n, m</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">recursive</span><span class="params">(i, j)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> (i, j) <span class="keyword">not</span> <span class="keyword">in</span> memo:</span><br><span class="line">            <span class="keyword">if</span> i == n:</span><br><span class="line">                ans = m - j</span><br><span class="line">            <span class="keyword">elif</span> j == m:</span><br><span class="line">                ans = n - i</span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                <span class="keyword">if</span> word1[i] == word2[j]:</span><br><span class="line">                    ans = recursive(i + <span class="number">1</span>, j + <span class="number">1</span>)</span><br><span class="line">                <span class="keyword">else</span>:</span><br><span class="line">                    <span class="comment"># insert, delete, replace</span></span><br><span class="line">                    ans = min(recursive(i, j + <span class="number">1</span>), recursive(i + <span class="number">1</span>, j),</span><br><span class="line">                              recursive(i + <span class="number">1</span>, j + <span class="number">1</span>)) + <span class="number">1</span></span><br><span class="line">            memo[i, j] = ans</span><br><span class="line">        <span class="keyword">return</span> memo[i, j]</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> recursive(<span class="number">0</span>, <span class="number">0</span>)</span><br></pre></td></tr></table></figure><h1 id="sortColors-trick-O-n"><a href="#sortColors-trick-O-n" class="headerlink" title="sortColors (trick) O(n)"></a>sortColors (trick) O(n)</h1><img src="27.png" style="width:800px;"/><p>We use three indexes, i, j, k, the i and j indicate the range that we need to handle, and k indicates the value we are handling. For the nums[k], there are three cases:</p><ul><li>nums[k] == 0, we swap nums[i] and nums[k], then i+=1, k+=1.</li><li>nums[k] == 1, do nothing, just k+=1</li><li>nums[k] == 2, we swap nums[j] and nums[k], then j-=1.</li></ul><p>Actually, the i indicates the end of “0” values, the j for start of “2” values, each time, we encounter a “0”, we swap it to the end of “0” values, and for “2” values, we swap it to the start of “2” values, so at final, the middle are all “1” values.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(nums)</span>:</span></span><br><span class="line">    n = len(nums)</span><br><span class="line">    i, j, k = <span class="number">0</span>, <span class="number">0</span>, n<span class="number">-1</span></span><br><span class="line">    <span class="keyword">while</span> i &lt;= k:</span><br><span class="line">        <span class="keyword">if</span> nums[i] == <span class="number">0</span>:</span><br><span class="line">            <span class="keyword">if</span> i != j:</span><br><span class="line">                nums[i], nums[j] = nums[j], nums[i]</span><br><span class="line">                j += <span class="number">1</span></span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                j += <span class="number">1</span></span><br><span class="line">                i += <span class="number">1</span></span><br><span class="line">        <span class="keyword">elif</span> nums[i] == <span class="number">2</span>:</span><br><span class="line">            nums[i], nums[k] = nums[k], nums[i]</span><br><span class="line">            k -= <span class="number">1</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            i += <span class="number">1</span></span><br><span class="line">    <span class="keyword">return</span> nums</span><br></pre></td></tr></table></figure><h1 id="minWindow-trick-hash-O-n"><a href="#minWindow-trick-hash-O-n" class="headerlink" title="minWindow (trick hash) O(n)"></a>minWindow (trick hash) O(n)</h1><img src="28.png" style="width:800px;"/><p>We use two indexes i and j to indicate the start and end of the slide window. We also use a hash table to count amout of each char in T. The hash tbale m looks like a balance wallet, we first add chars of T into it, then once we encounter chars in S, we minus from m. So for m&gt;0 means there is still char we need to find, and m&lt;0 means we remove much more than we need in T. We also use a count=len(T) to identify whether we have encounter all chars within T. First, we move the i to right, and we minus one from hash table m[s[i]], and we only minus one from count if we found m[s[i]]&gt;0(means we remove a target char), which means there are still uncount chars within T(because we may encounter several same chars, so the m[s[i]] could be negative, but for that time, we won’t minus from count). Once count equals to zero, we move j to right, each time we add one to m[s[i]], and we add one to count if we found m[s[i]]&gt;0, which means there are still uncount chars within T again.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">minWindow</span><span class="params">(self, s, t)</span>:</span></span><br><span class="line">    <span class="string">"""</span></span><br><span class="line"><span class="string">    :type s: str</span></span><br><span class="line"><span class="string">    :type t: str</span></span><br><span class="line"><span class="string">    :rtype: str</span></span><br><span class="line"><span class="string">    """</span></span><br><span class="line">    <span class="keyword">import</span> sys</span><br><span class="line">    <span class="keyword">import</span> collections</span><br><span class="line">    m = collections.defaultdict(int)</span><br><span class="line">    <span class="comment"># m just like a balance wallet</span></span><br><span class="line">    <span class="comment"># we first add chars of T into it</span></span><br><span class="line">    <span class="comment"># then once we encounter chars in S</span></span><br><span class="line">    <span class="comment"># we minus from m</span></span><br><span class="line">    <span class="comment"># so for m&gt;0 menas there is still char we need to find</span></span><br><span class="line">    <span class="comment"># m&lt;0 means we remove much more than we need in T</span></span><br><span class="line">    <span class="keyword">for</span> c <span class="keyword">in</span> t:</span><br><span class="line">        m[c] += <span class="number">1</span></span><br><span class="line">    j = <span class="number">0</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># identify how many remaining chars in T we still need to find</span></span><br><span class="line">    count = len(t)</span><br><span class="line">    minLen = sys.maxsize</span><br><span class="line">    minStart = <span class="number">0</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(len(s)):</span><br><span class="line">        <span class="comment"># only when m[s[i]] &gt; 0, means remaining chars in t</span></span><br><span class="line">        <span class="keyword">if</span> m[s[i]] &gt; <span class="number">0</span>:</span><br><span class="line">            count -= <span class="number">1</span></span><br><span class="line">        m[s[i]] -= <span class="number">1</span></span><br><span class="line">        <span class="comment"># we have found all chars in T, we start to move j</span></span><br><span class="line">        <span class="keyword">while</span> count == <span class="number">0</span>:</span><br><span class="line">            <span class="keyword">if</span> (i - j + <span class="number">1</span>) &lt; minLen:</span><br><span class="line">                minLen = i-j+<span class="number">1</span></span><br><span class="line">                minStart = j</span><br><span class="line">            m[s[j]] += <span class="number">1</span></span><br><span class="line">            <span class="comment"># when m[s[i]] &gt; 0, means we remove too much target chars</span></span><br><span class="line">            <span class="comment"># we increase count to mark there are chars we need to find</span></span><br><span class="line">            <span class="keyword">if</span> m[s[j]] &gt; <span class="number">0</span>:</span><br><span class="line">                count += <span class="number">1</span></span><br><span class="line">            j += <span class="number">1</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> minLen &lt; sys.maxsize:</span><br><span class="line">        <span class="keyword">return</span> s[minStart:minStart+minLen]</span><br><span class="line">    <span class="keyword">return</span> <span class="string">""</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(s, t)</span>:</span></span><br><span class="line">    <span class="keyword">from</span> collections <span class="keyword">import</span> Counter</span><br><span class="line">    <span class="keyword">import</span> sys</span><br><span class="line"></span><br><span class="line">    n = len(s)</span><br><span class="line">    i, j = <span class="number">0</span>, <span class="number">0</span></span><br><span class="line">    c_t = Counter(t)</span><br><span class="line">    count = len(t)</span><br><span class="line">    b, l = <span class="number">0</span>, sys.maxsize</span><br><span class="line">    <span class="keyword">while</span> j &lt; n:</span><br><span class="line">        <span class="keyword">if</span> c_t[s[j]] &gt; <span class="number">0</span>:</span><br><span class="line">            count -= <span class="number">1</span></span><br><span class="line">        c_t[s[j]] -= <span class="number">1</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">while</span> <span class="keyword">not</span> count:</span><br><span class="line">            <span class="keyword">if</span> (j-i+<span class="number">1</span>) &lt; l:</span><br><span class="line">                l = j-i+<span class="number">1</span></span><br><span class="line">                b = i</span><br><span class="line">            c_t[s[i]] += <span class="number">1</span></span><br><span class="line">            <span class="keyword">if</span> c_t[s[i]] &gt; <span class="number">0</span>:</span><br><span class="line">                count += <span class="number">1</span></span><br><span class="line">            i += <span class="number">1</span></span><br><span class="line">        j += <span class="number">1</span></span><br><span class="line">    <span class="keyword">if</span> l &lt; sys.maxsize:</span><br><span class="line">        <span class="keyword">return</span> s[b:b+l]</span><br><span class="line">    <span class="keyword">return</span> <span class="string">""</span></span><br></pre></td></tr></table></figure><h1 id="Subsets-dfs-O-2-n"><a href="#Subsets-dfs-O-2-n" class="headerlink" title="Subsets (dfs) O(2^n)"></a>Subsets (dfs) O(2^n)</h1><img src="29.png" style="width:800px;"/><p>Same with permutation, but also output at intermediate nodes.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(nums)</span>:</span></span><br><span class="line">    n = len(nums)</span><br><span class="line">    ret = []</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">recursion</span><span class="params">(prefix, i)</span>:</span></span><br><span class="line">        ret.append(prefix)</span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> range(i, n):</span><br><span class="line">            recursion(prefix+[nums[j]], j+<span class="number">1</span>)</span><br><span class="line">    recursion([], <span class="number">0</span>)</span><br><span class="line">    <span class="keyword">return</span> ret</span><br></pre></td></tr></table></figure><h1 id="mark-Exist-dfs-O-mn-len-words"><a href="#mark-Exist-dfs-O-mn-len-words" class="headerlink" title="(mark) Exist (dfs) O(mn * len(words))"></a>(mark) Exist (dfs) O(mn * len(words))</h1><img src="30.png" style="width:800px;"/><p>We dedine a recursive function: dfs(row, col, idx), means from nums[row][col] starts finding word[idx:]. We iterative all cells, and only call dfs from the cell which equals to word[0].</p><p>Within the recursive function. If idx == len(word), return True, else we check four directions, if it equals to word[idx], we call recursive func, if all fail, return false.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(board, word)</span>:</span></span><br><span class="line">    m, n = len(board), len(board[<span class="number">0</span>])</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">recursion</span><span class="params">(i, j, idx)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> idx == len(word):</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line">        board[i][j] = <span class="string">"#"</span></span><br><span class="line">        <span class="keyword">if</span> i+<span class="number">1</span> &lt; m <span class="keyword">and</span> board[i+<span class="number">1</span>][j] == word[idx] <span class="keyword">and</span> recursion(i+<span class="number">1</span>, j, idx+<span class="number">1</span>):</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line">        <span class="keyword">if</span> j+<span class="number">1</span> &lt; n <span class="keyword">and</span> board[i][j+<span class="number">1</span>] == word[idx] <span class="keyword">and</span> recursion(i, j+<span class="number">1</span>, idx+<span class="number">1</span>):</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line">        <span class="keyword">if</span> i<span class="number">-1</span> &gt;= <span class="number">0</span> <span class="keyword">and</span> board[i<span class="number">-1</span>][j] == word[idx] <span class="keyword">and</span> recursion(i<span class="number">-1</span>, j, idx+<span class="number">1</span>):</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line">        <span class="keyword">if</span> j<span class="number">-1</span> &gt;= <span class="number">0</span> <span class="keyword">and</span> board[i][j<span class="number">-1</span>] == word[idx] <span class="keyword">and</span> recursion(i, j<span class="number">-1</span>, idx+<span class="number">1</span>):</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line">        board[i][j] = word[idx<span class="number">-1</span>]</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(m):</span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> range(n):</span><br><span class="line">            <span class="keyword">if</span> board[i][j] == word[<span class="number">0</span>] <span class="keyword">and</span> recursion(i, j, <span class="number">1</span>):</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">False</span></span><br></pre></td></tr></table></figure><h1 id="largestRectangleArea-left-and-right-dp-O-n"><a href="#largestRectangleArea-left-and-right-dp-O-n" class="headerlink" title="largestRectangleArea (left and right dp) O(n)"></a>largestRectangleArea (left and right dp) O(n)</h1><img src="31.png" style="width:800px;"/><p>We use two array, left_min and right_min to record the position of first smaller value from left and right, so each rectangle’s area is: height[i] * (right_min[i] - left_min[i] -1)</p><p>We set left_min[0]=-1, for left_min[i], we search from j=i-1, each time we found height[j] &gt;= height[i], we set j=left_min[j] to recursively find the first smaller value.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(heights)</span>:</span></span><br><span class="line">    heights = [<span class="number">0</span>] + heights + [<span class="number">0</span>]</span><br><span class="line">    n = len(heights)</span><br><span class="line">    min_left = [<span class="number">0</span>] * n</span><br><span class="line">    min_right = [<span class="number">0</span>] * n</span><br><span class="line">    ret = <span class="number">0</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>, n - <span class="number">1</span>):</span><br><span class="line">        tmp_i = i - <span class="number">1</span></span><br><span class="line">        <span class="keyword">while</span> tmp_i &gt; <span class="number">0</span> <span class="keyword">and</span> heights[tmp_i] &gt;= heights[i]:</span><br><span class="line">            tmp_i = min_left[tmp_i]</span><br><span class="line">        min_left[i] = tmp_i</span><br><span class="line">    <span class="keyword">for</span> j <span class="keyword">in</span> range(n - <span class="number">2</span>, <span class="number">0</span>, <span class="number">-1</span>):</span><br><span class="line">        tmp_j = j + <span class="number">1</span></span><br><span class="line">        <span class="keyword">while</span> tmp_j &lt; n - <span class="number">1</span> <span class="keyword">and</span> heights[tmp_j] &gt;= heights[j]:</span><br><span class="line">            tmp_j = min_right[tmp_j]</span><br><span class="line">        min_right[j] = tmp_j</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>, n - <span class="number">1</span>):</span><br><span class="line">        ret = max(ret, heights[i] * (min_right[i] - min_left[i] - <span class="number">1</span>))</span><br><span class="line">    <span class="keyword">return</span> ret</span><br></pre></td></tr></table></figure><h1 id="maximalRectangle-greedy-O-mn"><a href="#maximalRectangle-greedy-O-mn" class="headerlink" title="maximalRectangle (greedy) O(mn)"></a>maximalRectangle (greedy) O(mn)</h1><img src="32.png" style="width:800px;"/><p>We use three arrays to record the most far height, left boundary and right boundary.</p><p>For height:</p><ul><li>if matrix[i][j] == ‘1’: height[j] += 1, else height[j] = 0</li></ul><p>For left:</p><ul><li>if matrix[i][j] == ‘1’: left[j] = max(left[j], cur_left), else left[j] = 0, cur_left = j+1</li></ul><p>For right:</p><ul><li>if matrix[i][j] == ‘1’: right[j] = min(right[j], cur_right), else right[j] = n, cur_right = j.</li></ul><p>The precess is, firstly we determine the height of the rectangle, then we find the most far left boundary and right boundary of this rectangle. We use cur_left to record the far left boundary of this line, and left[j] to record the far boundary of all lines, so max(left[j], cur_left) gives us the most far left boundary of the rectangle in this line.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(matrix)</span>:</span></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> matrix:</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span></span><br><span class="line">    m, n = len(matrix), len(matrix[<span class="number">0</span>])</span><br><span class="line">    left, right, height = [<span class="number">0</span>] * n, [n] * n, [<span class="number">0</span>] * n</span><br><span class="line">    max_v = <span class="number">0</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(m):</span><br><span class="line">        cur_left, cur_right = <span class="number">0</span>, n</span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> range(n):</span><br><span class="line">            <span class="keyword">if</span> matrix[i][j] == <span class="string">'1'</span>:</span><br><span class="line">                height[j] += <span class="number">1</span></span><br><span class="line">                left[j] = max(left[j], cur_left)</span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                height[j] = <span class="number">0</span></span><br><span class="line">                left[j] = <span class="number">0</span></span><br><span class="line">                cur_left = j + <span class="number">1</span></span><br><span class="line">            j = n - j - <span class="number">1</span></span><br><span class="line">            <span class="keyword">if</span> matrix[i][j] == <span class="string">'1'</span>:</span><br><span class="line">                right[j] = min(right[j], cur_right)</span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                right[j] = n</span><br><span class="line">                cur_right = j</span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> range(n):</span><br><span class="line">            max_v = max(max_v, (right[j] - left[j]) * height[j])</span><br><span class="line">    <span class="keyword">return</span> max_v</span><br></pre></td></tr></table></figure><h1 id="inorderTraversal-iterative-O-n"><a href="#inorderTraversal-iterative-O-n" class="headerlink" title="inorderTraversal (iterative) O(n)"></a>inorderTraversal (iterative) O(n)</h1><img src="33.png" style="width:800px;"/><p>using a stack, each time pop a node, and push its children into the stack by this tuple (node.left, node.val, node.right), each time we found a value, we output it.</p><p>Another way: we firstly loop to push left node into the stack up to the most left leaf, each time we pop a node, ouput it, and add its left node recursively again.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(root)</span>:</span></span><br><span class="line">    ret = []</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">recursion</span><span class="params">(root)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> root:</span><br><span class="line">            <span class="keyword">return</span></span><br><span class="line">        recursion(root.left)</span><br><span class="line">        ret.append(root.val)</span><br><span class="line">        recursion(root.right)</span><br><span class="line">    recursion(root)</span><br><span class="line">    <span class="keyword">return</span> ret</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(root)</span>:</span></span><br><span class="line">    ret = []</span><br><span class="line">    stack = [root]</span><br><span class="line">    <span class="keyword">while</span> stack:</span><br><span class="line">        node = stack.pop()</span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> node:</span><br><span class="line">            <span class="keyword">continue</span></span><br><span class="line">        <span class="keyword">elif</span> isinstance(node, int):</span><br><span class="line">            ret.append(node)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            stack.extend([node.right, node.val, node.left])</span><br><span class="line">    <span class="keyword">return</span> ret</span><br></pre></td></tr></table></figure><h1 id="numTrees-dp-O-n-2"><a href="#numTrees-dp-O-n-2" class="headerlink" title="numTrees (dp) O(n^2)"></a>numTrees (dp) O(n^2)</h1><img src="34.png" style="width:800px;"/><p>We need to define two functions:</p><ul><li>G(n): the number of unique BST for a sequence of length n.</li><li>F(i, n), 1 &lt;= i &lt;= n: the number of unique BST, where the number i is the root of BST, and the sequence ranges from 1 to n.</li></ul><p>So, G(n) = F(1, n) + F(2, n) + … + F(n, n). And G(0)=1, G(1)=1.</p><p>We found that: F(i, n) = G(i-1) * G(n-i).</p><p>Combining the above two formulas, we obtain the recursive formula for G(n). i.e.</p><p>G(n) = G(0) * G(n-1) + G(1) * G(n-2) + … + G(n-1) * G(0).</p><p>So we start from G(2), G(2) = G(0) * G(1), G(3) = G(0) * G(2) + G(1) * G(1) + G(2) * G(0), …, finally we get G(n).</p><p>Firstly, I want to use 2D dp, but from the above solution, I found, we don’t care about the start point and end pint, we only care about the length of the sub-question. So we can only use 1D dp.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(n)</span>:</span></span><br><span class="line">    dp = [<span class="number">0</span>] * (n+<span class="number">1</span>)</span><br><span class="line">    dp[<span class="number">0</span>] = dp[<span class="number">1</span>] = <span class="number">1</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">2</span>, n+<span class="number">1</span>):</span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> range(<span class="number">1</span>, i+<span class="number">1</span>):</span><br><span class="line">            dp[i] += (dp[j<span class="number">-1</span>]*dp[i-j])</span><br><span class="line">    <span class="keyword">return</span> dp[<span class="number">-1</span>]</span><br></pre></td></tr></table></figure><h1 id="isValidBST-post-order-dfs-O-n"><a href="#isValidBST-post-order-dfs-O-n" class="headerlink" title="isValidBST (post-order, dfs) O(n)"></a>isValidBST (post-order, dfs) O(n)</h1><img src="35.png" style="width:800px;"/><p>The first solution, we compare from down to up, we get the max and min value of each subtree. So, for a valid node, its value must larger than max value of left subtree and less than min value of right subtree.</p><p>The second solution, we compare from up to down, we set a range by two value: low bound and upper bound, each time we enter a left tree, we constrict the upper bound, and when we enter the right tree, we constrict the right bound.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(root)</span>:</span></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> root:</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">recursion</span><span class="params">(root, min_v, max_v)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> root:</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line">        <span class="keyword">elif</span> root.val &lt;= min_v <span class="keyword">or</span> root.val &gt;= max_v:</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="keyword">return</span> recursion(root.left, min_v, root.val) <span class="keyword">and</span> recursion(root.right, root.val, max_v)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> recursion(root.left, -sys.maxsize, root.val) <span class="keyword">and</span> recursion(root.right, root.val, sys.maxsize)</span><br></pre></td></tr></table></figure><h1 id="mark-isSymmetric-dfs-or-bfs-O-n"><a href="#mark-isSymmetric-dfs-or-bfs-O-n" class="headerlink" title="(mark) isSymmetric (dfs or bfs) O(n)"></a>(mark) isSymmetric (dfs or bfs) O(n)</h1><img src="36.png" style="width:800px;"/><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(root)</span>:</span></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> root:</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">recursion</span><span class="params">(root1, root2)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> root1 <span class="keyword">and</span> <span class="keyword">not</span> root2:</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line">        <span class="keyword">elif</span> root1 <span class="keyword">and</span> root2:</span><br><span class="line">            <span class="keyword">if</span> root1.val == root2.val:</span><br><span class="line">                <span class="keyword">return</span> recursion(root1.left, root2.right) <span class="keyword">and</span> recursion(</span><br><span class="line">                    root1.right, root2.left)</span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> recursion(root.left, root.right)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(root)</span>:</span></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> root:</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line">    queue1 = [root.left, root.right]</span><br><span class="line">    queue2 = [root.right, root.left]</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> queue1 <span class="keyword">and</span> queue2:</span><br><span class="line">        node1 = queue1.pop(<span class="number">0</span>)</span><br><span class="line">        node2 = queue2.pop(<span class="number">0</span>)</span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> node1 <span class="keyword">and</span> <span class="keyword">not</span> node2:</span><br><span class="line">            <span class="keyword">continue</span></span><br><span class="line">        <span class="keyword">elif</span> node1 <span class="keyword">and</span> node2 <span class="keyword">and</span> node1.val == node2.val:</span><br><span class="line">            queue1.extend([node1.left, node1.right])</span><br><span class="line">            queue2.extend([node2.right, node2.left])</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> queue1 <span class="keyword">and</span> <span class="keyword">not</span> queue2:</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">False</span></span><br></pre></td></tr></table></figure><h1 id="mark-buildTree-recursive-O-n"><a href="#mark-buildTree-recursive-O-n" class="headerlink" title="(mark) buildTree (recursive) O(n)"></a>(mark) buildTree (recursive) O(n)</h1><img src="37.png" style="width:800px;"/><p>We found that the preorder indicates each root by the level order, and each root will recursively split the inorder into two part, its left and right subtree. </p><p>We use a dict to record the position of inorder in order to find the value quickly, then we define a function to construct the tree: def helper(start, end). Each time we give the function the range of its value in the inorder tree, then we recursively construct its left and right subtree as:</p><ul><li>root.left = helper(start, idx-1)</li><li>root.right = helper(idx+1, end)</li></ul><p>(if start &gt; end, return None)</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(preorder, inorder)</span>:</span></span><br><span class="line"></span><br><span class="line">    inorder_dict = &#123;v: k <span class="keyword">for</span> k, v <span class="keyword">in</span> enumerate(inorder)&#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">build</span><span class="params">(i, j)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> preorder <span class="keyword">or</span> i &gt; j:</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">None</span></span><br><span class="line">        val = preorder.pop(<span class="number">0</span>)</span><br><span class="line">        key = inorder_dict[val]</span><br><span class="line">        node = TreeNode(val)</span><br><span class="line">        node.left = build(i, key - <span class="number">1</span>)</span><br><span class="line">        node.right = build(key + <span class="number">1</span>, j)</span><br><span class="line">        <span class="keyword">return</span> node</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> build(<span class="number">0</span>, len(inorder) - <span class="number">1</span>)</span><br></pre></td></tr></table></figure><h1 id="mark-Flatten-iterative-pre-order-dfs-O-n"><a href="#mark-Flatten-iterative-pre-order-dfs-O-n" class="headerlink" title="(mark) Flatten (iterative pre-order dfs) O(n)"></a>(mark) Flatten (iterative pre-order dfs) O(n)</h1><img src="38.png" style="width:800px;"/><p>Each time we find the deepest left node, and insert it to its parent node’s first right node, then we enter the right node to do the same thing.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(root)</span>:</span></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> root:</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">None</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">flatten</span><span class="params">(node)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> node.left <span class="keyword">and</span> <span class="keyword">not</span> node.right:</span><br><span class="line">            <span class="keyword">return</span> node</span><br><span class="line">        <span class="keyword">if</span> node.left:</span><br><span class="line">            <span class="comment"># get the deepest left node to append to its first right node</span></span><br><span class="line">            tmp_node = flatten(node.left)</span><br><span class="line">            tmp_node.right = node.right <span class="comment"># append it right to the deepest node's right</span></span><br><span class="line">            node.right = node.left <span class="comment"># append the top left to its right</span></span><br><span class="line">            node.left = <span class="literal">None</span></span><br><span class="line">        <span class="keyword">if</span> node.right:</span><br><span class="line">            <span class="comment"># return deepest right node</span></span><br><span class="line">            <span class="keyword">return</span> flatten(node.right)</span><br><span class="line"></span><br><span class="line">    flatten(root)</span><br><span class="line">    <span class="keyword">return</span> root</span><br></pre></td></tr></table></figure><h1 id="maxProfit-trick-O-n"><a href="#maxProfit-trick-O-n" class="headerlink" title="maxProfit (trick) O(n)"></a>maxProfit (trick) O(n)</h1><img src="39.png" style="width:800px;"/><p>This problem is that, we want to find the tuple of [lowest, highest], and the highest value must appear after the lowest one. So we record the lowest, and each time we update:</p><ul><li>lowest = min(lowest, prices[i])</li><li>profit = max(profit, prices[i]-lowest)</li></ul><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(prices)</span>:</span></span><br><span class="line">    cur_min = sys.maxsize</span><br><span class="line">    max_profit = <span class="number">0</span></span><br><span class="line">    <span class="keyword">for</span> p <span class="keyword">in</span> prices:</span><br><span class="line">        cur_min = min(cur_min, p)</span><br><span class="line">        max_profit = max(max_profit, p - cur_min)</span><br><span class="line">    <span class="keyword">return</span> max_profit</span><br></pre></td></tr></table></figure><h1 id="maxPathSum-tree-dp-O-n"><a href="#maxPathSum-tree-dp-O-n" class="headerlink" title="maxPathSum (tree dp) O(n)"></a>maxPathSum (tree dp) O(n)</h1><img src="40.png" style="width:800px;"/><p>From the bottom to up, at each node, we calculate the max value from left and max value from right, we try to update the result = max(result, left_max+right_max+node.val), and for the return value, we return the max value from left_max+node.val and right_max+node.val, remember we need to compare with 0, since if the value less than 0, we actually delete it from the path. </p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(root)</span>:</span></span><br><span class="line">    max_v = -sys.maxsize</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">recursive</span><span class="params">(root)</span>:</span></span><br><span class="line">        <span class="keyword">nonlocal</span> max_v</span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> root:</span><br><span class="line">            <span class="keyword">return</span> <span class="number">0</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            left = recursive(root.left)</span><br><span class="line">            right = recursive(root.right)</span><br><span class="line">            max_v = max(max_v, left + right + root.val)</span><br><span class="line">            <span class="keyword">return</span> max(left + root.val, right + root.val, <span class="number">0</span>)</span><br><span class="line"></span><br><span class="line">    recursive(root)</span><br><span class="line">    <span class="keyword">return</span> max_v</span><br></pre></td></tr></table></figure><h1 id="longestConsecutive-hash-O-n"><a href="#longestConsecutive-hash-O-n" class="headerlink" title="longestConsecutive (hash) O(n)"></a>longestConsecutive (hash) O(n)</h1><img src="41.png" style="width:800px;"/><p>We convert the array to set(nums), when we iterate the array, we check whether the nums[i]-1 is in the array, if yes, skip. So once we found a smallest value, then try to check tmp_num+1 one by one, so we can get its consecutive length.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(nums)</span>:</span></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> nums:</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span></span><br><span class="line">    set_nums = set(nums)</span><br><span class="line">    max_v = <span class="number">0</span></span><br><span class="line">    <span class="keyword">for</span> num <span class="keyword">in</span> nums:</span><br><span class="line">        <span class="keyword">if</span> num - <span class="number">1</span> <span class="keyword">not</span> <span class="keyword">in</span> set_nums:</span><br><span class="line">            count = <span class="number">1</span></span><br><span class="line">            <span class="keyword">while</span> num + <span class="number">1</span> <span class="keyword">in</span> set_nums:</span><br><span class="line">                num += <span class="number">1</span></span><br><span class="line">                count += <span class="number">1</span></span><br><span class="line">            max_v = max(max_v, count)</span><br><span class="line">    <span class="keyword">return</span> max_v</span><br></pre></td></tr></table></figure><h1 id="copyRandomList-hash-O-n"><a href="#copyRandomList-hash-O-n" class="headerlink" title="copyRandomList (hash) O(n)"></a>copyRandomList (hash) O(n)</h1><img src="42.png" style="width:800px;"/><p>we using collections.defaultdict to store nodes which has been visited.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(head)</span>:</span></span><br><span class="line">    node_dict = defaultdict(Node)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">build</span><span class="params">(node)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> node:</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">None</span></span><br><span class="line">        _node = Node(node.val)</span><br><span class="line">        node_dict[node] = _node</span><br><span class="line">        <span class="keyword">if</span> node.random:</span><br><span class="line">            <span class="keyword">if</span> node.random <span class="keyword">in</span> node_dict:</span><br><span class="line">                _node.random = node_dict[node.random]</span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                _node.random = build(node.random)</span><br><span class="line">        <span class="keyword">if</span> node.next:</span><br><span class="line">            <span class="keyword">if</span> node.next <span class="keyword">in</span> node_dict:</span><br><span class="line">                _node.next = node_dict[node.next]</span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                _node.next = build(node.next)</span><br><span class="line">        <span class="keyword">return</span> _node</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> build(head)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(head)</span>:</span></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> head:</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">None</span></span><br><span class="line"></span><br><span class="line">    node_dict = defaultdict(Node)</span><br><span class="line"></span><br><span class="line">    _head = Node(head.val)</span><br><span class="line">    node_dict[head] = _head</span><br><span class="line">    ret_head = _head</span><br><span class="line">    <span class="keyword">while</span> head:</span><br><span class="line">        <span class="keyword">if</span> head.random:</span><br><span class="line">            <span class="keyword">if</span> head.random <span class="keyword">not</span> <span class="keyword">in</span> node_dict:</span><br><span class="line">                node_dict[head.random] = Node(head.random.val)</span><br><span class="line">            _head.random = node_dict[head.random]</span><br><span class="line">        <span class="keyword">if</span> head.next:</span><br><span class="line">            <span class="keyword">if</span> head.next <span class="keyword">not</span> <span class="keyword">in</span> node_dict:</span><br><span class="line">                node_dict[head.next] = Node(head.next.val)</span><br><span class="line">            _head.next = node_dict[head.next]</span><br><span class="line">        head = head.next</span><br><span class="line">        _head = _head.next</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> ret_head</span><br></pre></td></tr></table></figure><h1 id="wordbreak-dp-O-mn"><a href="#wordbreak-dp-O-mn" class="headerlink" title="wordbreak (dp) O(mn)"></a>wordbreak (dp) O(mn)</h1><img src="43.png" style="width:800px;"/><p>We set dp[i] as the result of s[0:i], and dp[i]=true if dp[i-len(w)] and s[i-len(w):i] == w.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(s, wordDict)</span>:</span></span><br><span class="line">    n = len(s)</span><br><span class="line">    dp = [<span class="literal">False</span>] * (n + <span class="number">1</span>)</span><br><span class="line">    dp[<span class="number">0</span>] = <span class="literal">True</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>, n + <span class="number">1</span>):</span><br><span class="line">        <span class="keyword">for</span> w <span class="keyword">in</span> wordDict:</span><br><span class="line">            <span class="keyword">if</span> i &gt;= len(w) <span class="keyword">and</span> dp[i - len(w)] <span class="keyword">and</span> s[i - len(w):i] == w:</span><br><span class="line">                dp[i] = <span class="literal">True</span></span><br><span class="line">    <span class="keyword">return</span> dp[<span class="number">-1</span>]</span><br></pre></td></tr></table></figure><h1 id="DetectCycle-trick-O-n"><a href="#DetectCycle-trick-O-n" class="headerlink" title="DetectCycle (trick) O(n)"></a>DetectCycle (trick) O(n)</h1><img src="44.png" style="width:800px;"/><p>Let the distance from the first node to the node where cycle begins be A, and let say the slow pointer travels A+B. The fast pointer must travel 2A+2B to catch up. The cycle size is N. Full cycle is also how much faster pointer has traveled than slow pointer at meeting point.</p><ul><li>A+B+N = 2A+2B</li><li>N=A+B</li></ul><p>So once meet, we make another pointer start from head, we move slow pointer and this pointer together, once they meet, we found the beginning node. (because B+A=N).</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(head)</span>:</span></span><br><span class="line">    p = head</span><br><span class="line">    q = head</span><br><span class="line">    t = head</span><br><span class="line">    <span class="keyword">while</span> p <span class="keyword">and</span> q <span class="keyword">and</span> p.next <span class="keyword">and</span> q.next <span class="keyword">and</span> q.next.next:</span><br><span class="line">        p = p.next.next</span><br><span class="line">        q = q.next</span><br><span class="line">        <span class="keyword">if</span> q == p:</span><br><span class="line">            <span class="keyword">while</span> q != t:</span><br><span class="line">                q = q.next</span><br><span class="line">                t = t.next</span><br><span class="line">            <span class="keyword">return</span> t</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">None</span></span><br></pre></td></tr></table></figure><h1 id="LRUCache-double-linked-list-O-1"><a href="#LRUCache-double-linked-list-O-1" class="headerlink" title="LRUCache (double linked list) O(1)"></a>LRUCache (double linked list) O(1)</h1><img src="45.png" style="width:800px;"/><p>We use double linked list here, and we make two basic operations, called remove and add. The remove is, remove a node by its id from the list, and add is add a node to the end of the tail.</p><ul><li>For the get operation, we firstly remove the node and then add.</li><li>For the put operation, we add it into the list and remove a node from the head.</li></ul><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Node</span>:</span></span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, k, x, next=None, pre=None)</span>:</span></span><br><span class="line">        self.key = k</span><br><span class="line">        self.val = x</span><br><span class="line">        self.next = next</span><br><span class="line">        self.pre = pre</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">LRUCache</span><span class="params">(object)</span>:</span></span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, capacity)</span>:</span></span><br><span class="line">        self.capacity = capacity</span><br><span class="line">        self.dict = &#123;&#125;</span><br><span class="line">        self.head = Node(<span class="number">0</span>, <span class="number">0</span>)</span><br><span class="line">        self.tail = Node(<span class="number">0</span>, <span class="number">0</span>)</span><br><span class="line">        self.head.next = self.tail</span><br><span class="line">        self.tail.pre = self.head</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">get</span><span class="params">(self, key)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> key <span class="keyword">in</span> self.dict:</span><br><span class="line">            node = self.dict[key]</span><br><span class="line">            self._remove(node)</span><br><span class="line">            self._add(node) </span><br><span class="line">            <span class="keyword">return</span> node.val</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="keyword">return</span> <span class="number">-1</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">put</span><span class="params">(self, key, value)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> key <span class="keyword">in</span> self.dict:</span><br><span class="line">            self._remove(self.dict[key])</span><br><span class="line">        node = Node(key, value)</span><br><span class="line">        self._add(node)</span><br><span class="line">        self.dict[key] = node</span><br><span class="line">        <span class="keyword">while</span> len(self.dict) &gt; self.capacity:</span><br><span class="line">            tmp_node = self.head.next</span><br><span class="line">            self._remove(tmp_node)</span><br><span class="line">            <span class="keyword">del</span> self.dict[tmp_node.key]</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">_remove</span><span class="params">(self, node)</span>:</span></span><br><span class="line">        p = node.pre</span><br><span class="line">        n = node.next</span><br><span class="line">        p.next = n</span><br><span class="line">        n.pre = p</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">_add</span><span class="params">(self, node)</span>:</span></span><br><span class="line">        p = self.tail.pre</span><br><span class="line">        p.next = node</span><br><span class="line">        node.pre = p</span><br><span class="line">        node.next = self.tail</span><br><span class="line">        self.tail.pre = node</span><br></pre></td></tr></table></figure><h1 id="SortList-merge-sort-or-quick-sort-O-nlogn"><a href="#SortList-merge-sort-or-quick-sort-O-nlogn" class="headerlink" title="SortList (merge sort or quick sort) O(nlogn)"></a>SortList (merge sort or quick sort) O(nlogn)</h1><img src="46.png" style="width:800px;"/><p>Using three list to store nodes, using head as the partition node, left_head stores the nodes whose value is less than partition node, right_head stores the nodes whose value is larger than partition node. partition_head stores the nodes whose value is equal with its value.</p><p>Then we recursive to call the sort function to sort left_head, right_head, at the final, we link these three lists together.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(head)</span>:</span></span><br><span class="line">    <span class="keyword">if</span> head <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">None</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">recursive</span><span class="params">(head)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> head <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">None</span>, <span class="literal">None</span></span><br><span class="line"></span><br><span class="line">        partition_node = head</span><br><span class="line">        middle_head = ListNode(<span class="literal">None</span>)</span><br><span class="line">        middle_cur = middle_head</span><br><span class="line">        left_head = ListNode(<span class="literal">None</span>)</span><br><span class="line">        left_cur = left_head</span><br><span class="line">        right_head = ListNode(<span class="literal">None</span>)</span><br><span class="line">        right_cur = right_head</span><br><span class="line"></span><br><span class="line">        <span class="keyword">while</span> head:</span><br><span class="line">            <span class="keyword">if</span> head.val &lt; partition_node.val:</span><br><span class="line">                left_cur.next = ListNode(head.val)</span><br><span class="line">                left_cur = left_cur.next</span><br><span class="line">            <span class="keyword">elif</span> head.val &gt; partition_node.val:</span><br><span class="line">                right_cur.next = ListNode(head.val)</span><br><span class="line">                right_cur = right_cur.next</span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                middle_cur.next = ListNode(head.val)</span><br><span class="line">                middle_cur = middle_cur.next</span><br><span class="line">            head = head.next</span><br><span class="line">        </span><br><span class="line">        left_head, left_tail = recursive(left_head.next)</span><br><span class="line">        right_head, right_tail = recursive(right_head.next)</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> left_head <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">None</span>:</span><br><span class="line">            left_tail.next = middle_head.next</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            left_head = middle_head</span><br><span class="line">        <span class="keyword">if</span> right_head <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">None</span>:</span><br><span class="line">            middle_cur.next = right_head.next</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            right_tail = middle_cur</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> left_head, right_tail</span><br><span class="line"></span><br><span class="line">    head, _ = recursive(head)</span><br><span class="line">    <span class="keyword">return</span> head.next</span><br></pre></td></tr></table></figure><h1 id="mark-maxProduct-greedy-O-n"><a href="#mark-maxProduct-greedy-O-n" class="headerlink" title="(mark) maxProduct (greedy) O(n)"></a>(mark) maxProduct (greedy) O(n)</h1><img src="47.png" style="width:800px;"/><p>we record the min and max value together, each time, we compare and update these two values.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(nums)</span>:</span></span><br><span class="line">    maximum = b = s = nums[<span class="number">0</span>]</span><br><span class="line">    <span class="keyword">for</span> num <span class="keyword">in</span> nums[<span class="number">1</span>:]:</span><br><span class="line">        b, s = max(num, b * num, s * num), min(num, b * num, s * num)</span><br><span class="line">        maximum = max(maximum, b)</span><br><span class="line">    <span class="keyword">return</span> maximum</span><br></pre></td></tr></table></figure><h1 id="MinStack-trick-O-n"><a href="#MinStack-trick-O-n" class="headerlink" title="MinStack (trick) O(n)"></a>MinStack (trick) O(n)</h1><img src="48.png" style="width:800px;"/><p>We use another stack to record the current min, each time we push a value, we compare it with the min[-1], if this valuer is larger, we duplicate min[-1], else we push value into the stack. And each time we need to pop a value, we also pop the min[-1].</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MinStack</span><span class="params">(object)</span>:</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="string">"""</span></span><br><span class="line"><span class="string">        initialize your data structure here.</span></span><br><span class="line"><span class="string">        """</span></span><br><span class="line">        self.min = []</span><br><span class="line">        self.stack = []</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">push</span><span class="params">(self, x)</span>:</span></span><br><span class="line">        <span class="string">"""</span></span><br><span class="line"><span class="string">        :type x: int</span></span><br><span class="line"><span class="string">        :rtype: None</span></span><br><span class="line"><span class="string">        """</span></span><br><span class="line">        self.stack.append(x)</span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> self.min:</span><br><span class="line">            self.min.append(x)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            self.min.append(min(self.min[<span class="number">-1</span>], x))</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">pop</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="string">"""</span></span><br><span class="line"><span class="string">        :rtype: None</span></span><br><span class="line"><span class="string">        """</span></span><br><span class="line">        self.stack.pop()</span><br><span class="line">        self.min.pop()</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">top</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="string">"""</span></span><br><span class="line"><span class="string">        :rtype: int</span></span><br><span class="line"><span class="string">        """</span></span><br><span class="line">        <span class="keyword">return</span> self.stack[<span class="number">-1</span>]</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">getMin</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="string">"""</span></span><br><span class="line"><span class="string">        :rtype: int</span></span><br><span class="line"><span class="string">        """</span></span><br><span class="line">        <span class="keyword">return</span> self.min[<span class="number">-1</span>]</span><br></pre></td></tr></table></figure><h1 id="GetIntersectionNode-trick-O-m-n"><a href="#GetIntersectionNode-trick-O-m-n" class="headerlink" title="GetIntersectionNode (trick) O(m+n)"></a>GetIntersectionNode (trick) O(m+n)</h1><img src="49.png" style="width:800px;"/><p>For two list p and q, we append the p-&gt;tail-&gt;q, and q-&gt;tail-&gt;p, so once we get <code>_p==_q</code>, we found the intersection.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(headA, headB)</span>:</span></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> headA <span class="keyword">or</span> <span class="keyword">not</span> headB:</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">None</span></span><br><span class="line">    nodeA = headA</span><br><span class="line">    nodeB = headB</span><br><span class="line">    <span class="keyword">while</span> nodeA <span class="keyword">is</span> <span class="keyword">not</span> nodeB:</span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> nodeA <span class="keyword">and</span> nodeB:</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">None</span></span><br><span class="line">        <span class="keyword">if</span> nodeA.next <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">            nodeA = headB</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            nodeA = nodeA.next</span><br><span class="line">        <span class="keyword">if</span> nodeB.next <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">            nodeB = headA</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            nodeB = nodeB.next</span><br><span class="line">    <span class="keyword">return</span> nodeA</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(headA, headB)</span>:</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">len</span><span class="params">(head)</span>:</span></span><br><span class="line">        ret = <span class="number">0</span></span><br><span class="line">        <span class="keyword">while</span> head:</span><br><span class="line">            ret += <span class="number">1</span></span><br><span class="line">            head = head.next</span><br><span class="line">        <span class="keyword">return</span> ret</span><br><span class="line"></span><br><span class="line">    lenA, lenB = len(headA), len(headB)</span><br><span class="line">    diff = abs(lenA - lenB)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> lenA &gt; lenB:</span><br><span class="line">        <span class="keyword">for</span> _ <span class="keyword">in</span> range(diff):</span><br><span class="line">            headA = headA.next</span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        <span class="keyword">for</span> _ <span class="keyword">in</span> range(diff):</span><br><span class="line">            headB = headB.next</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> headA != headB:</span><br><span class="line">        headA = headA.next</span><br><span class="line">        headB = headB.next</span><br><span class="line">    <span class="keyword">return</span> headA</span><br></pre></td></tr></table></figure><h1 id="MajorityElement-trick-O-n"><a href="#MajorityElement-trick-O-n" class="headerlink" title="MajorityElement (trick) O(n)"></a>MajorityElement (trick) O(n)</h1><img src="50.png" style="width:800px;"/><p> Make two variable, value and count, once count=0, we make value=nums[i], and count+=1, if nums[i]==value, make count+=1, else count-=1.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(nums)</span>:</span></span><br><span class="line">    cur_value = nums[<span class="number">0</span>]</span><br><span class="line">    cur_num = <span class="number">1</span></span><br><span class="line">    <span class="keyword">for</span> num <span class="keyword">in</span> nums[<span class="number">1</span>:]:</span><br><span class="line">        <span class="keyword">if</span> num != cur_value:</span><br><span class="line">            cur_num -= <span class="number">1</span></span><br><span class="line">            <span class="keyword">if</span> cur_num == <span class="number">-1</span>:</span><br><span class="line">                cur_value = num</span><br><span class="line">                cur_num = <span class="number">1</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            cur_num += <span class="number">1</span></span><br><span class="line">    <span class="keyword">return</span> cur_value</span><br></pre></td></tr></table></figure><h1 id="Rob-dp-O-n"><a href="#Rob-dp-O-n" class="headerlink" title="Rob (dp) O(n)"></a>Rob (dp) O(n)</h1><img src="51.png" style="width:800px;"/><p>Solution 1: using two arary, max_rob, max_not_rob. max_rob[i] means the max value when we rob nums[i], and max_not_rob means the max value when we don’t rob nums[i]. </p><ul><li>max_rob[i] = max_not_rob[i-1] + nums[i]</li><li>max_not_rob[i] = max(max_not_rob[i-1], max_rob[i-1])</li></ul><p>Solution 2:</p><ul><li>f(0) = nums[0]</li><li>f(1) = max(num[0], num[1])</li><li>f(k) = max(f(k-2) + nums[k], f(k-1))</li></ul><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(nums)</span>:</span></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> nums:</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span></span><br><span class="line">    dp = [[<span class="number">0</span>, <span class="number">0</span>] <span class="keyword">for</span> _ <span class="keyword">in</span> range(len(nums))]  <span class="comment"># (rob, not_rob) * n</span></span><br><span class="line">    dp[<span class="number">0</span>][<span class="number">0</span>], dp[<span class="number">0</span>][<span class="number">1</span>] = nums[<span class="number">0</span>], <span class="number">0</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>, len(nums)):</span><br><span class="line">        dp[i][<span class="number">0</span>] = dp[i - <span class="number">1</span>][<span class="number">1</span>] + nums[i]</span><br><span class="line">        dp[i][<span class="number">1</span>] = max(dp[i - <span class="number">1</span>])</span><br><span class="line">    <span class="keyword">return</span> max(dp[<span class="number">-1</span>])</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(nums)</span>:</span></span><br><span class="line">    last, now = <span class="number">0</span>, <span class="number">0</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> nums:</span><br><span class="line">        last, now = now, max(last + i, now)</span><br><span class="line">    <span class="keyword">return</span> now</span><br></pre></td></tr></table></figure><h1 id="numIslands-BFS-DFS-O-mn"><a href="#numIslands-BFS-DFS-O-mn" class="headerlink" title="numIslands (BFS DFS) O(mn)"></a>numIslands (BFS DFS) O(mn)</h1><img src="52.png" style="width:800px;"/><p>For loop all cells, once found a ‘1’, using stack to do BFS, and for each cell visited, change it to ‘0’</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(grid)</span>:</span></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> len(grid) <span class="keyword">or</span> <span class="keyword">not</span> len(grid[<span class="number">0</span>]):</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span></span><br><span class="line">    ret = <span class="number">0</span></span><br><span class="line">    n = len(grid)</span><br><span class="line">    m = len(grid[<span class="number">0</span>])</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(n):</span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> range(m):</span><br><span class="line">            <span class="keyword">if</span> grid[i][j] == <span class="string">"1"</span>:</span><br><span class="line">                grid[i][j] = <span class="string">"0"</span></span><br><span class="line">                ret += <span class="number">1</span></span><br><span class="line">                queue = [(i, j)]</span><br><span class="line">                <span class="keyword">while</span> queue:</span><br><span class="line">                    _i, _j = queue.pop(<span class="number">0</span>)</span><br><span class="line">                    <span class="keyword">if</span> _i &gt; <span class="number">0</span> <span class="keyword">and</span> grid[_i - <span class="number">1</span>][_j] == <span class="string">"1"</span>:</span><br><span class="line">                        grid[_i - <span class="number">1</span>][_j] = <span class="string">"0"</span></span><br><span class="line">                        queue.append((_i - <span class="number">1</span>, _j))</span><br><span class="line">                    <span class="keyword">if</span> _j &gt; <span class="number">0</span> <span class="keyword">and</span> grid[_i][_j - <span class="number">1</span>] == <span class="string">"1"</span>:</span><br><span class="line">                        grid[_i][_j - <span class="number">1</span>] = <span class="string">"0"</span></span><br><span class="line">                        queue.append((_i, _j - <span class="number">1</span>))</span><br><span class="line">                    <span class="keyword">if</span> _i &lt; n - <span class="number">1</span> <span class="keyword">and</span> grid[_i + <span class="number">1</span>][_j] == <span class="string">"1"</span>:</span><br><span class="line">                        grid[_i + <span class="number">1</span>][_j] = <span class="string">"0"</span></span><br><span class="line">                        queue.append((_i + <span class="number">1</span>, _j))</span><br><span class="line">                    <span class="keyword">if</span> _j &lt; m - <span class="number">1</span> <span class="keyword">and</span> grid[_i][_j + <span class="number">1</span>] == <span class="string">"1"</span>:</span><br><span class="line">                        grid[_i][_j + <span class="number">1</span>] = <span class="string">"0"</span></span><br><span class="line">                        queue.append((_i, _j + <span class="number">1</span>))</span><br><span class="line">    <span class="keyword">return</span> ret</span><br></pre></td></tr></table></figure><h1 id="mark-canFinish-dfs-O-n-3"><a href="#mark-canFinish-dfs-O-n-3" class="headerlink" title="(mark) canFinish (dfs) O(n^3)"></a>(mark) canFinish (dfs) O(n^3)</h1><img src="53.png" style="width:800px;"/><p>We set an array called visited to record which node has been visited. At the first, we set all visited[i]=0. When we recursively to search each node, we set its visited[i]=-1, once we’ve finished, we set it back to visited[i]=1. So, when the recursive function found a visited[i]=-1, return False, and visited[i]=1(avoid repeatedly search), return True. </p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(numCourses, prerequisites)</span>:</span></span><br><span class="line">    grid = [[] <span class="keyword">for</span> _ <span class="keyword">in</span> range(numCourses)]</span><br><span class="line">    visited = [<span class="number">0</span>] * numCourses</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> i, j <span class="keyword">in</span> prerequisites:</span><br><span class="line">        grid[i].append(j)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">recusive</span><span class="params">(i)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> visited[i] == <span class="number">-1</span>:</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line">        <span class="keyword">if</span> visited[i] == <span class="number">1</span>:</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line"></span><br><span class="line">        visited[i] = <span class="number">-1</span></span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> grid[i]:</span><br><span class="line">            <span class="keyword">if</span> <span class="keyword">not</span> recusive(j):</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line"></span><br><span class="line">        visited[i] = <span class="number">1</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(numCourses):</span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> recusive(i):</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">True</span></span><br></pre></td></tr></table></figure><h1 id="Trie-tree-O-n"><a href="#Trie-tree-O-n" class="headerlink" title="Trie (tree) O(n)"></a>Trie (tree) O(n)</h1><img src="54.png" style="width:800px;"/><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">TrieNode</span><span class="params">()</span>:</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, val)</span>:</span></span><br><span class="line">        self.next = [<span class="literal">None</span>] * <span class="number">26</span></span><br><span class="line">        self.val = val</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Trie</span><span class="params">(object)</span>:</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="string">"""</span></span><br><span class="line"><span class="string">        Initialize your data structure here.</span></span><br><span class="line"><span class="string">        """</span></span><br><span class="line">        self.root = TrieNode(<span class="literal">False</span>)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">insert</span><span class="params">(self, word)</span>:</span></span><br><span class="line">        <span class="string">"""</span></span><br><span class="line"><span class="string">        Inserts a word into the trie.</span></span><br><span class="line"><span class="string">        :type word: str</span></span><br><span class="line"><span class="string">        :rtype: None</span></span><br><span class="line"><span class="string">        """</span></span><br><span class="line">        node = self.root</span><br><span class="line">        <span class="keyword">for</span> w <span class="keyword">in</span> word:</span><br><span class="line">            idx = ord(w) - ord(<span class="string">'a'</span>)</span><br><span class="line">            <span class="keyword">if</span> <span class="keyword">not</span> node.next[idx]:</span><br><span class="line">                node.next[idx] = TrieNode(<span class="literal">False</span>)</span><br><span class="line">            node = node.next[idx]</span><br><span class="line">        node.val = <span class="literal">True</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">search</span><span class="params">(self, word)</span>:</span></span><br><span class="line">        <span class="string">"""</span></span><br><span class="line"><span class="string">        Returns if the word is in the trie.</span></span><br><span class="line"><span class="string">        :type word: str</span></span><br><span class="line"><span class="string">        :rtype: bool</span></span><br><span class="line"><span class="string">        """</span></span><br><span class="line">        node = self.root</span><br><span class="line">        <span class="keyword">for</span> w <span class="keyword">in</span> word:</span><br><span class="line">            idx = ord(w) - ord(<span class="string">'a'</span>)</span><br><span class="line">            <span class="keyword">if</span> <span class="keyword">not</span> node.next[idx]:</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line">            node = node.next[idx]</span><br><span class="line">        <span class="keyword">return</span> node.val</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">startsWith</span><span class="params">(self, prefix)</span>:</span></span><br><span class="line">        <span class="string">"""</span></span><br><span class="line"><span class="string">        Returns if there is any word in the trie that starts with the given prefix.</span></span><br><span class="line"><span class="string">        :type prefix: str</span></span><br><span class="line"><span class="string">        :rtype: bool</span></span><br><span class="line"><span class="string">        """</span></span><br><span class="line">        node = self.root</span><br><span class="line">        <span class="keyword">for</span> w <span class="keyword">in</span> prefix:</span><br><span class="line">            idx = ord(w) - ord(<span class="string">'a'</span>)</span><br><span class="line">            <span class="keyword">if</span> <span class="keyword">not</span> node.next[idx]:</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line">            node = node.next[idx]</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">True</span></span><br></pre></td></tr></table></figure><h1 id="mark-findKthLargest-heap-quickselect-O-nlogn"><a href="#mark-findKthLargest-heap-quickselect-O-nlogn" class="headerlink" title="(mark) findKthLargest (heap, quickselect) O(nlogn)"></a>(mark) findKthLargest (heap, quickselect) O(nlogn)</h1><img src="55.png" style="width:800px;"/><p>The heap is easy. The quickselect is similar with quicksort, each time we select a pivot, and partition the array to two parts, if the len(left_part)=k-1, we return this pivot, else if larger, we continue to find k-th in the left_part, else, we found the k-len(left_part)-1-th at the right part.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># O(klogk+2(n-k)logk)</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(nums, k)</span>:</span></span><br><span class="line">    <span class="keyword">import</span> heapq</span><br><span class="line">    k_heap = nums[<span class="number">0</span>:k]</span><br><span class="line">    heapq.heapify(k_heap)</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(k, len(nums)):</span><br><span class="line">        heapq.heappush(k_heap, nums[i])</span><br><span class="line">        heapq.heappop(k_heap)</span><br><span class="line">    <span class="keyword">return</span> heapq.heappop(k_heap)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># O(n)</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(nums, k)</span>:</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">recursive</span><span class="params">(nums, k)</span>:</span></span><br><span class="line">        pivot = nums.pop(len(nums) // <span class="number">2</span>)</span><br><span class="line">        right = [num <span class="keyword">for</span> num <span class="keyword">in</span> nums <span class="keyword">if</span> num &gt; pivot]</span><br><span class="line">        lr = len(right)</span><br><span class="line">        <span class="keyword">if</span> k == lr + <span class="number">1</span>:</span><br><span class="line">            <span class="keyword">return</span> pivot</span><br><span class="line">        <span class="keyword">elif</span> k &lt; lr + <span class="number">1</span>:</span><br><span class="line">            <span class="keyword">return</span> recursive(right, k)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            left = [num <span class="keyword">for</span> num <span class="keyword">in</span> nums <span class="keyword">if</span> num &lt;= pivot]</span><br><span class="line">            <span class="keyword">return</span> recursive(left, k - lr - <span class="number">1</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> recursive(nums, k)</span><br></pre></td></tr></table></figure><h1 id="maximalSquare-dp-O-n-2"><a href="#maximalSquare-dp-O-n-2" class="headerlink" title="maximalSquare (dp) O(n^2)"></a>maximalSquare (dp) O(n^2)</h1><img src="56.png" style="width:800px;"/><ul><li>dp[i][j] = min((dp[i-1][j-1]), (dp[i][j-1]), (dp[i-1][j])) + 1, if matrix[i][j]) == 1</li><li>max_v = max(max_v, dp[i][j])</li></ul><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(matrix)</span>:</span></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> matrix <span class="keyword">or</span> <span class="keyword">not</span> matrix[<span class="number">0</span>]:</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span></span><br><span class="line"></span><br><span class="line">    dp = [[int(x) <span class="keyword">for</span> x <span class="keyword">in</span> row] <span class="keyword">for</span> row <span class="keyword">in</span> matrix]</span><br><span class="line">    max_v = <span class="number">0</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(len(matrix)):</span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> range(len(matrix[<span class="number">0</span>])):</span><br><span class="line">            <span class="keyword">if</span> i == <span class="number">0</span> <span class="keyword">or</span> j == <span class="number">0</span>:</span><br><span class="line">                max_v = max(max_v, dp[i][j])</span><br><span class="line">            <span class="keyword">elif</span> matrix[i][j] == <span class="string">"1"</span>:</span><br><span class="line">                dp[i][j] = min(dp[i - <span class="number">1</span>][j], dp[i][j - <span class="number">1</span>], dp[i - <span class="number">1</span>][j - <span class="number">1</span>]) + <span class="number">1</span></span><br><span class="line">                max_v = max(max_v, dp[i][j])</span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                dp[i][j] == <span class="number">0</span></span><br><span class="line">    <span class="keyword">return</span> max_v**<span class="number">2</span></span><br></pre></td></tr></table></figure><h1 id="invertTree-recursive-O-n"><a href="#invertTree-recursive-O-n" class="headerlink" title="invertTree (recursive) O(n)"></a>invertTree (recursive) O(n)</h1><img src="57.png" style="width:800px;"/><p>root.left, root.right = recursive(root.right), recursive(root.left)</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(root)</span>:</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">recursive</span><span class="params">(root)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> root:</span><br><span class="line">            root.left, root.right = recursive(root.right), recursive(root.left)</span><br><span class="line">        <span class="keyword">return</span> root</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> recursive(root)</span><br></pre></td></tr></table></figure><h1 id="isPalindrome-trick-O-n"><a href="#isPalindrome-trick-O-n" class="headerlink" title="isPalindrome (trick) O(n)"></a>isPalindrome (trick) O(n)</h1><img src="58.png" style="width:800px;"/><p>We use pointers, faster and slower, faster=faster.next.next, slow=slow.next. once we found he mid of the array, we reverse the half end of the list.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(head)</span>:</span></span><br><span class="line">    rev = ListNode(<span class="literal">None</span>)</span><br><span class="line">    fast = slow = head</span><br><span class="line">    <span class="keyword">while</span> fast <span class="keyword">and</span> fast.next:</span><br><span class="line">        fast = fast.next.next</span><br><span class="line">        _slow = slow</span><br><span class="line">        slow = slow.next</span><br><span class="line">        _slow.next = rev.next</span><br><span class="line">        rev.next = _slow</span><br><span class="line">    <span class="keyword">if</span> fast:</span><br><span class="line">        slow = slow.next</span><br><span class="line">    rev = rev.next</span><br><span class="line">    <span class="keyword">while</span> rev <span class="keyword">and</span> slow:</span><br><span class="line">        <span class="keyword">if</span> rev.val != slow.val:</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line">        rev = rev.next</span><br><span class="line">        slow = slow.next</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">True</span></span><br></pre></td></tr></table></figure><h1 id="lowestCommonAncestor-recursive-O-n"><a href="#lowestCommonAncestor-recursive-O-n" class="headerlink" title="lowestCommonAncestor (recursive) O(n)"></a>lowestCommonAncestor (recursive) O(n)</h1><img src="59.png" style="width:800px;"/><p>use postorder recursive, count = recusive(root.left, p, q) + recusive(root.right, p, q), if root.val ==p or ==q: cur_count ==1 else 0. If count+cur_count ==2, return the result.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(root, p, q)</span>:</span></span><br><span class="line">    low_desc = <span class="literal">None</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">recursive</span><span class="params">(root)</span>:</span></span><br><span class="line">        <span class="keyword">nonlocal</span> low_desc</span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> root:</span><br><span class="line">            <span class="keyword">return</span> <span class="number">0</span></span><br><span class="line">        ret = recursive(root.left)</span><br><span class="line">        <span class="keyword">if</span> ret &lt; <span class="number">2</span>:</span><br><span class="line">            ret += recursive(root.right)</span><br><span class="line">        <span class="keyword">if</span> root.val <span class="keyword">in</span> (q.val, p.val):</span><br><span class="line">            ret += <span class="number">1</span></span><br><span class="line">        <span class="keyword">if</span> ret == <span class="number">2</span> <span class="keyword">and</span> <span class="keyword">not</span> low_desc:</span><br><span class="line">            low_desc = root</span><br><span class="line">        <span class="keyword">return</span> ret</span><br><span class="line"></span><br><span class="line">    recursive(root)</span><br><span class="line">    <span class="keyword">return</span> low_desc</span><br></pre></td></tr></table></figure><h1 id="productExceptSelf-two-pointers-reverse-list-O-n"><a href="#productExceptSelf-two-pointers-reverse-list-O-n" class="headerlink" title="productExceptSelf (two pointers, reverse list) O(n)"></a>productExceptSelf (two pointers, reverse list) O(n)</h1><img src="60.png" style="width:800px;"/><p>We use two arrays, the first to record the product of left values, and second to record the product of right values. Then result[i] = left[i] * right[i]</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(nums)</span>:</span></span><br><span class="line">    res = [<span class="number">1</span>] * len(nums)</span><br><span class="line">    p = <span class="number">1</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(len(nums)):</span><br><span class="line">        res[i] = p</span><br><span class="line">        p *= nums[i]</span><br><span class="line"></span><br><span class="line">    right = <span class="number">1</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(len(nums) - <span class="number">1</span>, <span class="number">-1</span>, <span class="number">-1</span>):</span><br><span class="line">        res[i] *= right</span><br><span class="line">        right *= nums[i]</span><br><span class="line">    <span class="keyword">return</span> res</span><br></pre></td></tr></table></figure><h1 id="maxSlidingWindow-trick-O-n"><a href="#maxSlidingWindow-trick-O-n" class="headerlink" title="maxSlidingWindow (trick) O(n)"></a>maxSlidingWindow (trick) O(n)</h1><img src="61.png" style="width:800px;"/><p>Actually, we don’t need to re-calculate the max value each time. We use an array called result to record the max values. Each time:</p><ul><li>If the new value is larger than current max value, we add it into the result array.</li><li>Else if the value will be deleted from the window is less than current max value, we add the current max value into the result again.</li><li>Else we re-calculate the max value within the window.</li></ul><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(nums, k)</span>:</span></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> nums:</span><br><span class="line">        <span class="keyword">return</span> nums</span><br><span class="line"></span><br><span class="line">    ret = [max(nums[:k])]</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>, len(nums) - k + <span class="number">1</span>):</span><br><span class="line">        <span class="keyword">if</span> nums[i + k - <span class="number">1</span>] &gt;= ret[<span class="number">-1</span>]:</span><br><span class="line">            ret.append(nums[i + k - <span class="number">1</span>])</span><br><span class="line">        <span class="keyword">elif</span> nums[i - <span class="number">1</span>] &lt; ret[<span class="number">-1</span>]:</span><br><span class="line">            ret.append(ret[<span class="number">-1</span>])</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            ret.append(max(nums[i:i + k]))</span><br><span class="line">    <span class="keyword">return</span> ret</span><br></pre></td></tr></table></figure><h1 id="mark-searchMatrix-trick-O-m-n"><a href="#mark-searchMatrix-trick-O-m-n" class="headerlink" title="(mark) searchMatrix (trick) O(m+n)"></a>(mark) searchMatrix (trick) O(m+n)</h1><img src="62.png" style="width:800px;"/><p>At the first row, we search from the end, exclude the values which are larger than target, and we record the index, then at the second row, we search from the index to exclude again. We return True once we found the target, else we return False.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(matrix, target)</span>:</span></span><br><span class="line">    <span class="keyword">if</span> len(matrix) == <span class="number">0</span> <span class="keyword">or</span> len(matrix[<span class="number">0</span>]) == <span class="number">0</span>:</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line">    j = len(matrix[<span class="number">0</span>]) - <span class="number">1</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(len(matrix)):</span><br><span class="line">        <span class="keyword">while</span> j &gt;= <span class="number">0</span> <span class="keyword">and</span> matrix[i][j] &gt; target:</span><br><span class="line">            j -= <span class="number">1</span></span><br><span class="line">        <span class="keyword">if</span> matrix[i][j] == target:</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">False</span></span><br></pre></td></tr></table></figure><h1 id="numSquares-dp-O-n-1-5"><a href="#numSquares-dp-O-n-1-5" class="headerlink" title="numSquares (dp) O(n^1.5)"></a>numSquares (dp) O(n^1.5)</h1><img src="63.png" style="width:800px;"/><p>first we set search range(nums) is [1, floor(n^0.5)], then for dp[i]=min(dp[i-range[j]^2]+1) over all j.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> math, sys</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(n)</span>:</span></span><br><span class="line">    dp = [<span class="number">0</span>] * (n + <span class="number">1</span>)</span><br><span class="line">    squares = [i**<span class="number">2</span> <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>, int(math.sqrt(n)) + <span class="number">1</span>)]</span><br><span class="line">    dp[<span class="number">1</span>] = <span class="number">1</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">2</span>, n + <span class="number">1</span>):</span><br><span class="line">        min_v = sys.maxsize</span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> squares:</span><br><span class="line">            <span class="keyword">if</span> i - j &lt; <span class="number">0</span>:</span><br><span class="line">                <span class="keyword">break</span></span><br><span class="line">            min_v = min(min_v, dp[i - j])</span><br><span class="line">        dp[i] = min_v + <span class="number">1</span></span><br><span class="line">    <span class="keyword">return</span> dp[<span class="number">-1</span>]</span><br></pre></td></tr></table></figure><h1 id="moveZeroes-trick-O-n"><a href="#moveZeroes-trick-O-n" class="headerlink" title="moveZeroes (trick) O(n)"></a>moveZeroes (trick) O(n)</h1><img src="64.png" style="width:800px;"/><p>Each time we encounter a non-zero value, we need to swap it with the first zero. So, we set an index to record the first zero’s position, if nums[i]!=0, we add one to this index, and swap nums[index] with nums[i], else we do nothing. </p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(nums)</span>:</span></span><br><span class="line">    n = len(nums)</span><br><span class="line">    <span class="keyword">if</span> n == <span class="number">0</span> <span class="keyword">or</span> n == <span class="number">1</span>:</span><br><span class="line">        <span class="keyword">return</span> nums</span><br><span class="line">    i = <span class="number">0</span></span><br><span class="line">    <span class="keyword">while</span> i &lt; n <span class="keyword">and</span> nums[i] != <span class="number">0</span>:</span><br><span class="line">        i += <span class="number">1</span></span><br><span class="line">    j = i</span><br><span class="line">    <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">        <span class="keyword">while</span> j &lt; n <span class="keyword">and</span> nums[j] == <span class="number">0</span>:</span><br><span class="line">            j += <span class="number">1</span></span><br><span class="line">        <span class="keyword">if</span> j &gt;= n:</span><br><span class="line">            <span class="keyword">break</span></span><br><span class="line">        nums[i], nums[j] = nums[j], nums[i]</span><br><span class="line">        i += <span class="number">1</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(nums)</span>:</span></span><br><span class="line">    n = len(nums)</span><br><span class="line">    <span class="keyword">if</span> n == <span class="number">0</span> <span class="keyword">or</span> n == <span class="number">1</span>:</span><br><span class="line">        <span class="keyword">return</span> nums</span><br><span class="line">    i = <span class="number">0</span></span><br><span class="line">    <span class="keyword">while</span> i &lt; n <span class="keyword">and</span> nums[i] != <span class="number">0</span>:</span><br><span class="line">        i += <span class="number">1</span></span><br><span class="line">    <span class="keyword">for</span> j <span class="keyword">in</span> range(i + <span class="number">1</span>, n):</span><br><span class="line">        <span class="keyword">if</span> nums[j] != <span class="number">0</span>:</span><br><span class="line">            nums[i], nums[j] = nums[j], nums[i]</span><br><span class="line">            i += <span class="number">1</span></span><br></pre></td></tr></table></figure><h1 id="findDuplicate-trick-O-n"><a href="#findDuplicate-trick-O-n" class="headerlink" title="findDuplicate (trick) O(n)"></a>findDuplicate (trick) O(n)</h1><img src="65.png" style="width:800px;"/><p>We take this question as detectCycle, the number i will point to the nums[i], because there are multiply number are same, it means this number will point to a same number. And this number must be the start of the cycle.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(nums)</span>:</span></span><br><span class="line">    slow = nums[<span class="number">0</span>]</span><br><span class="line">    fast = nums[nums[<span class="number">0</span>]]</span><br><span class="line">    <span class="keyword">while</span> fast != slow:</span><br><span class="line">        fast = nums[nums[fast]]</span><br><span class="line">        slow = nums[slow]</span><br><span class="line">    slow = <span class="number">0</span></span><br><span class="line">    <span class="keyword">while</span> slow != fast:</span><br><span class="line">        slow = nums[slow]</span><br><span class="line">        fast = nums[fast]</span><br><span class="line">    <span class="keyword">return</span> slow</span><br></pre></td></tr></table></figure><h1 id="MedianFinder-trick-O-nlogn"><a href="#MedianFinder-trick-O-nlogn" class="headerlink" title="MedianFinder (trick) O(nlogn)"></a>MedianFinder (trick) O(nlogn)</h1><img src="66.png" style="width:800px;"/><p>Solution1: we maintain a sorted list, each time we using binary search to find the right position to insert the upcoming value.</p><p>Solution2: we use two heaps, large heap and small heap. Each time, if the length of the two heaps is equal, then we firstly add the value into the large heap, then we pop the smallest value from the large heap, and then add it into the small heap. If length not equal, we add the value into the small heap and pop the largest value then add it into the large heap. Finally, if the length is equal, we return the average of the values from top of small and large heaps. If equal, we return the value from the top of large heap.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> heapq</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MedianFinder</span><span class="params">(object)</span>:</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="string">"""</span></span><br><span class="line"><span class="string">        initialize your data structure here.</span></span><br><span class="line"><span class="string">        """</span></span><br><span class="line">        self.left_heap = []</span><br><span class="line">        self.right_heap = []</span><br><span class="line">        heapq.heapify(self.left_heap)  <span class="comment"># big heap</span></span><br><span class="line">        heapq.heapify(self.right_heap)  <span class="comment"># small heap</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">addNum</span><span class="params">(self, num)</span>:</span></span><br><span class="line">        <span class="string">"""</span></span><br><span class="line"><span class="string">        :type num: int</span></span><br><span class="line"><span class="string">        :rtype: None</span></span><br><span class="line"><span class="string">        """</span></span><br><span class="line">        <span class="keyword">if</span> len(self.left_heap) == len(self.right_heap):</span><br><span class="line">            heapq.heappush(self.left_heap,</span><br><span class="line">                           -heapq.heappushpop(self.right_heap, num))</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            heapq.heappush(self.right_heap,</span><br><span class="line">                           -heapq.heappushpop(self.left_heap, -num))</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">findMedian</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="string">"""</span></span><br><span class="line"><span class="string">        :rtype: float</span></span><br><span class="line"><span class="string">        """</span></span><br><span class="line">        <span class="keyword">if</span> len(self.left_heap) == len(self.right_heap):</span><br><span class="line">            <span class="keyword">return</span> float(-self.left_heap[<span class="number">0</span>] + self.right_heap[<span class="number">0</span>]) / <span class="number">2.</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="keyword">return</span> float(-self.left_heap[<span class="number">0</span>])</span><br></pre></td></tr></table></figure><h1 id="Serialize-bfs-O-n"><a href="#Serialize-bfs-O-n" class="headerlink" title="Serialize (bfs) O(n)"></a>Serialize (bfs) O(n)</h1><img src="67.png" style="width:800px;"/><p>The children of each node[i] is node[2i] and node[2i+1]</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">serialize</span><span class="params">(self, root)</span>:</span></span><br><span class="line">    <span class="string">"""Encodes a tree to a single string.</span></span><br><span class="line"><span class="string">    </span></span><br><span class="line"><span class="string">    :type root: TreeNode</span></span><br><span class="line"><span class="string">    :rtype: str</span></span><br><span class="line"><span class="string">    """</span></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> root:</span><br><span class="line">        <span class="keyword">return</span> <span class="string">''</span></span><br><span class="line">    out = []</span><br><span class="line">    queue = collections.deque([root])</span><br><span class="line">    <span class="keyword">while</span> queue:</span><br><span class="line">        node = queue.popleft()</span><br><span class="line">        out.append(str(node.val) <span class="keyword">if</span> node <span class="keyword">else</span> <span class="string">'#'</span>)</span><br><span class="line">        <span class="keyword">if</span> node:</span><br><span class="line">            queue.append(node.left)</span><br><span class="line">            queue.append(node.right)</span><br><span class="line">    <span class="keyword">return</span> <span class="string">' '</span>.join(out)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">deserialize</span><span class="params">(self, data)</span>:</span></span><br><span class="line">    <span class="string">"""Decodes your encoded data to tree.</span></span><br><span class="line"><span class="string">    </span></span><br><span class="line"><span class="string">    :type data: str</span></span><br><span class="line"><span class="string">    :rtype: TreeNode</span></span><br><span class="line"><span class="string">    """</span></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> data:</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">None</span></span><br><span class="line">    out = data.split()</span><br><span class="line">    bfs = [TreeNode(int(i)) <span class="keyword">if</span> i != <span class="string">'#'</span> <span class="keyword">else</span> <span class="literal">None</span> <span class="keyword">for</span> i <span class="keyword">in</span> out]</span><br><span class="line">    slow_idx = <span class="number">0</span>  <span class="comment"># the id in array nodex</span></span><br><span class="line">    fast_idx = <span class="number">1</span>  <span class="comment"># the id in array bfs</span></span><br><span class="line">    root = bfs[<span class="number">0</span>]</span><br><span class="line">    nodes = [root]</span><br><span class="line">    <span class="keyword">while</span> slow_idx &lt; len(nodes):</span><br><span class="line">        node = nodes[slow_idx]</span><br><span class="line">        slow_idx += <span class="number">1</span>  <span class="comment"># each time we handle one node in nodes</span></span><br><span class="line">        node.left = bfs[fast_idx]</span><br><span class="line">        node.right = bfs[fast_idx + <span class="number">1</span>]</span><br><span class="line">        fast_idx += <span class="number">2</span>  <span class="comment"># each time we only handle two nodes in bfs</span></span><br><span class="line">        <span class="keyword">if</span> node.left:</span><br><span class="line">            nodes.append(node.left)</span><br><span class="line">        <span class="keyword">if</span> node.right:</span><br><span class="line">            nodes.append(node.right)</span><br><span class="line">    <span class="keyword">return</span> root</span><br></pre></td></tr></table></figure><h1 id="lengthOfLIS-dp"><a href="#lengthOfLIS-dp" class="headerlink" title="lengthOfLIS(dp)"></a>lengthOfLIS(dp)</h1><img src="68.png" style="width:800px;"/><h2 id="lengthOfLIS-dp-O-n-2"><a href="#lengthOfLIS-dp-O-n-2" class="headerlink" title="lengthOfLIS(dp) O(n^2)"></a>lengthOfLIS(dp) O(n^2)</h2><p>The dp[i] stores the longest length till current position, each time we compare the current value with all previous values, if the current value is larger than a previous value, we record its dp value:</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">lengthOfLIS</span><span class="params">(nums)</span>:</span></span><br><span class="line">    <span class="keyword">if</span> len(nums) == <span class="number">0</span>:</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span></span><br><span class="line">    dp = [<span class="number">0</span>] * len(nums)</span><br><span class="line">    dp[<span class="number">0</span>] = <span class="number">1</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>, len(nums)):</span><br><span class="line">        max_t = <span class="number">0</span></span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> range(<span class="number">0</span>, i):</span><br><span class="line">            <span class="keyword">if</span> nums[i] &gt; nums[j]:</span><br><span class="line">                max_t = max(max_t, dp[j]) <span class="comment"># # longest length till now plus current </span></span><br><span class="line">        dp[i] = max_t + <span class="number">1</span></span><br><span class="line">    <span class="keyword">return</span> max(dp)</span><br></pre></td></tr></table></figure><h2 id="mark-lengthOfLIS-dp-O-nlogn"><a href="#mark-lengthOfLIS-dp-O-nlogn" class="headerlink" title="(mark) lengthOfLIS(dp) O(nlogn)"></a>(mark) lengthOfLIS(dp) O(nlogn)</h2><p>The dp[i] stores the increasing subsequence formed by including the currently encountered element.</p><p>For example, for input: [0, 8, 4, 12, 2],</p><p>We iterate each value in the input, each time we find the position in dp which value is the first one larger than current value in the input. For example, for 0, we have dp: [0]; for 8, we have dp: [0, 8]; for 4, we have dp: [0, 4]; then for 12, all values is smaller than 12, so we have append it at the end of dp: [0, 4, 12]; for 2, we have dp: [0, 2, 12]. The final dp is not the longest increasing subsequence, but length of dp array results in length of Longest Increasing Subsequence.</p><p>The reason why we update the value from [0, 4, 12] to [0, 2, 12], is 1, the update doesn’t break the feature of length of LIS,  2, once we encounter a value which is larger than 2, we update 12 then, so this makes sure we can find a longer LIS then. </p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># o(n^2)</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(nums)</span>:</span></span><br><span class="line">    dp = [<span class="number">1</span>] * len(nums)</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>, len(nums)):</span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> range(i):</span><br><span class="line">            <span class="keyword">if</span> nums[j] &lt; nums[i]:</span><br><span class="line">                dp[i] = max(dp[i], dp[j] + <span class="number">1</span>)</span><br><span class="line">    <span class="keyword">return</span> max(dp)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># O(n * logn)</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(nums)</span>:</span></span><br><span class="line">    dp = [<span class="number">0</span>] * len(nums)</span><br><span class="line">    size = <span class="number">0</span></span><br><span class="line">    <span class="keyword">for</span> n <span class="keyword">in</span> nums:</span><br><span class="line">        i, j = <span class="number">0</span>, size</span><br><span class="line">        <span class="keyword">while</span> i != j:</span><br><span class="line">            m = (i + j) // <span class="number">2</span></span><br><span class="line">            <span class="keyword">if</span> dp[m] &lt; n:</span><br><span class="line">                i = m + <span class="number">1</span></span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                j = m</span><br><span class="line">        dp[i] = n</span><br><span class="line">        size = max(size, i + <span class="number">1</span>)</span><br><span class="line">    <span class="keyword">return</span> size</span><br></pre></td></tr></table></figure><h1 id="removeInvalidParentheses-recursive-O-n-2"><a href="#removeInvalidParentheses-recursive-O-n-2" class="headerlink" title="removeInvalidParentheses(recursive) O(n^2)"></a>removeInvalidParentheses(recursive) O(n^2)</h1><img src="69.png" style="width:800px;"/><p>We use a counter to check a string of parentheses is valid. The counter will increase when it is ‘(‘ and decrease when it is ‘)’. Whenever the counter is negative, we have more ‘)’ than ‘(‘ in the prefix.</p><p>To make the prefix valid, we need to remove a ‘)’. The problem is: which one? The answer is any one in the prefix. However, if we remove any one, we will generate duplicate results, for example: s = ()), we can remove s[1] or s[2] but the result is the same (). Thus, we restrict ourselves to remove the first ‘)’ in a series of consecutive ‘)’s.</p><p>After the removal, the prefix is then valid. We then call the function recursively to solve the rest of the string. However, we need to keep another information: the last removal position. If we do not have this position, we will generate duplicate by removing two ‘)’ in two steps only with a different order.</p><p>For this, we keep tracking the last removal position and only remove ‘)’ after that. So, we need two flags, the first i means till i the string is valid, and j means we should start removing ‘)’ from j. And for ‘)’ between i and j, we call recursive function to remove every ‘)’.</p><p>Now one may ask. What about ‘(‘? What if s = ‘(()(()’ in which we need remove ‘(‘?</p><p>The answer is: do the same from right to left.</p><p>However, a cleverer idea is: reverse the string and reuse the code!</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">removeInvalidParentheses</span><span class="params">(self, s)</span>:</span></span><br><span class="line">    <span class="string">"""</span></span><br><span class="line"><span class="string">    :type s: str</span></span><br><span class="line"><span class="string">    :rtype: List[str]</span></span><br><span class="line"><span class="string">    """</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">removeHelper</span><span class="params">(s, output, iStart, jStart, openParen, closedParen)</span>:</span></span><br><span class="line">        numOpenParen, numClosedParen = <span class="number">0</span>, <span class="number">0</span></span><br><span class="line">        <span class="keyword">for</span> i <span class="keyword">in</span> range(iStart, len(s)):</span><br><span class="line">            <span class="keyword">if</span> s[i] == openParen:</span><br><span class="line">                numOpenParen += <span class="number">1</span></span><br><span class="line">            <span class="keyword">if</span> s[i] == closedParen:</span><br><span class="line">                numClosedParen += <span class="number">1</span></span><br><span class="line">            <span class="comment"># We have only ONE extra closed paren we need to remove</span></span><br><span class="line">            <span class="keyword">if</span> numClosedParen &gt; numOpenParen:</span><br><span class="line">                print(s)</span><br><span class="line">                <span class="keyword">if</span> openParen == <span class="string">'('</span>:</span><br><span class="line">                    print(<span class="string">"positive"</span>)</span><br><span class="line">                <span class="keyword">elif</span> openParen == <span class="string">')'</span>:</span><br><span class="line">                    print(<span class="string">'negative'</span>)</span><br><span class="line">                print(<span class="string">"i="</span>, i, numOpenParen, numClosedParen)</span><br><span class="line">                <span class="comment"># Try removing this ONE closed paren at each position, skipping duplicates</span></span><br><span class="line">                print(jStart, i)</span><br><span class="line">                <span class="comment"># jStart: always to search the first closedParen which can be removed</span></span><br><span class="line">                <span class="keyword">for</span> j <span class="keyword">in</span> range(jStart, i+<span class="number">1</span>):  <span class="comment"># &lt;=</span></span><br><span class="line">                    <span class="comment"># can be the first char,</span></span><br><span class="line">                    <span class="comment"># or the char which previous char isn't closedParen, removing each one of the consequtive closedParen actually is the same</span></span><br><span class="line">                    <span class="keyword">if</span> s[j] == closedParen <span class="keyword">and</span> (j == jStart <span class="keyword">or</span> s[j<span class="number">-1</span>] != closedParen): <span class="comment"># since when j == jStart, there isn't j-1</span></span><br><span class="line">                        <span class="comment"># Recursion: iStart = i since we now have valid # closed parenthesis thru i. jStart = j prevents duplicates</span></span><br><span class="line">                        <span class="comment"># at the next recursion, we only need to search the iStart from the current i, and search the jStart from the current j</span></span><br><span class="line">                        <span class="comment"># since we found the first invalid closedParen at the i, and we have removed a closedParen at j</span></span><br><span class="line">                        print(<span class="string">"remove, j="</span>, j, s[j])</span><br><span class="line">                        removeHelper(s[:j]+s[j+<span class="number">1</span>:], output,</span><br><span class="line">                                     i, j, openParen, closedParen)</span><br><span class="line">                <span class="keyword">return</span></span><br><span class="line">        reversed = s[::<span class="number">-1</span>]</span><br><span class="line">        print(reversed, <span class="string">"now out of the loop"</span>)</span><br><span class="line">        <span class="keyword">if</span> openParen == <span class="string">'('</span>:</span><br><span class="line">            removeHelper(reversed, output, <span class="number">0</span>, <span class="number">0</span>, <span class="string">')'</span>, <span class="string">'('</span>)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            output.append(reversed)</span><br><span class="line"></span><br><span class="line">    output = []</span><br><span class="line">    removeHelper(s, output, <span class="number">0</span>, <span class="number">0</span>, <span class="string">'('</span>, <span class="string">')'</span>)</span><br><span class="line">    <span class="keyword">return</span> output</span><br></pre></td></tr></table></figure><h1 id="maxProfit-dp-O-3n"><a href="#maxProfit-dp-O-3n" class="headerlink" title="maxProfit(dp) O(3n)"></a>maxProfit(dp) O(3n)</h1><img src="70.png" style="width:800px;"/><p>We use stack [n][3], stack[i][0] records colldown action, stack[i][1] records buy action, stack[i][0] records sell action. </p><ul><li>For cooldown, we keep the largest profit from the previous day: stack[i][0] = max(stack[i-1]).</li><li>For buy, we only consider the previous cooldown action, stack[i][1] = stack[i-1][0] - prices[i]. The [buy, cooldown, buy] action will not occur, since the previous cooldown is got from the largest actions(stack[i][0] = max(stack[i-1])).</li><li>For sell, stack[i][2] = bought + prices[i]. And we record a bought value each time, bought = max(bought, stack[i][1]), so each time we keep the max bought which means the samllest cost bought.</li><li>we record the bought to avoid the [sell, rest, sell], each time we keep the max bought which means the samllest cost bought</li></ul><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(prices)</span>:</span></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> prices:</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span></span><br><span class="line">    n = len(prices)</span><br><span class="line">    buy, sell, cooldown = [<span class="number">0</span>] * n, [<span class="number">0</span>] * n, [<span class="number">0</span>] * n</span><br><span class="line">    bought = buy[<span class="number">0</span>] = -prices[<span class="number">0</span>]</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>, n):</span><br><span class="line">        buy[i] = cooldown[i - <span class="number">1</span>] - prices[i]</span><br><span class="line">        sell[i] = bought + prices[i]</span><br><span class="line">        cooldown[i] = max(buy[i - <span class="number">1</span>], sell[i - <span class="number">1</span>], cooldown[i - <span class="number">1</span>])</span><br><span class="line">        bought = max(bought, buy[i])</span><br><span class="line">    <span class="keyword">return</span> max(buy[<span class="number">-1</span>], sell[<span class="number">-1</span>], cooldown[<span class="number">-1</span>])</span><br></pre></td></tr></table></figure><h1 id="maxCoins-dp-O-n-3"><a href="#maxCoins-dp-O-n-3" class="headerlink" title="maxCoins(dp) O(n^3)"></a>maxCoins(dp) O(n^3)</h1><img src="71.png" style="width:800px;"/><p>First of all, dp[i][j] means, the maximum coins we get after we burst all the balloons between i and j in the original array. So dp[i][j] means we already burst all balloons between i and j. For the transition function:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">for k in range(i+1, j):</span><br><span class="line">    coins &#x3D; max(coins, nums[i]*nums[k]*nums[j] + recursion(i, k) + recursion(k, j))</span><br></pre></td></tr></table></figure><p>This transition function basically says in order to get the maximum value we can get for bursting all the balloons between [ i , j] , we just loop through each balloon between these two indexes and make them to be the last balloon to be burst, which means we already burst all balloons from left to k-1, and from k+1 to right.<br>72<br>Notes: we append 1 to the start and end of input, each time we handle the solution from i+1 to j. we actually skep the first and last items. This trick assures the equation is correct: nums[i]<em>nums[k]</em>nums[j].</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(nums)</span>:</span></span><br><span class="line">    nums = [<span class="number">1</span>] + nums + [<span class="number">1</span>]</span><br><span class="line">    n = len(nums)</span><br><span class="line">    dp = [[<span class="number">0</span>] * n <span class="keyword">for</span> _ <span class="keyword">in</span> range(n)]</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">recursive</span><span class="params">(i, j)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> dp[i][j] <span class="keyword">or</span> j == i + <span class="number">1</span>:</span><br><span class="line">            <span class="keyword">return</span> dp[i][j]</span><br><span class="line">        coins = <span class="number">0</span></span><br><span class="line">        <span class="keyword">for</span> k <span class="keyword">in</span> range(i + <span class="number">1</span>, j):</span><br><span class="line">            <span class="comment"># we firstly burst (i,k) and (k,j), only leave (i,k,j) three ballons</span></span><br><span class="line">            coins = max(</span><br><span class="line">                coins,</span><br><span class="line">                nums[i] * nums[k] * nums[j] + recursive(i, k) + recursive(k, j))</span><br><span class="line">        dp[i][j] = coins</span><br><span class="line">        <span class="keyword">return</span> coins</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> recursive(<span class="number">0</span>, n - <span class="number">1</span>)</span><br></pre></td></tr></table></figure><h1 id="coinChange-dp-O-nm"><a href="#coinChange-dp-O-nm" class="headerlink" title="coinChange(dp) O(nm)"></a>coinChange(dp) O(nm)</h1><img src="72.png" style="width:800px;"/><p>dp[i] = min(dp[i-coin] + 1) for coin in coins.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(coins, amount)</span>:</span></span><br><span class="line">    <span class="keyword">if</span> len(coins) == <span class="number">0</span>:</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span></span><br><span class="line">    <span class="keyword">if</span> amount == <span class="number">0</span>:</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span></span><br><span class="line"></span><br><span class="line">    coins = sorted(set(coins), reverse=<span class="literal">True</span>)</span><br><span class="line">    dp = [amount + <span class="number">1</span>] * (amount + <span class="number">1</span>)</span><br><span class="line">    dp[<span class="number">0</span>] = <span class="number">0</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>, amount + <span class="number">1</span>):</span><br><span class="line">        <span class="keyword">for</span> c <span class="keyword">in</span> coins:</span><br><span class="line">            <span class="keyword">if</span> i == c:</span><br><span class="line">                dp[i] = <span class="number">1</span></span><br><span class="line">                <span class="keyword">break</span></span><br><span class="line">            <span class="keyword">elif</span> i &gt; c <span class="keyword">and</span> dp[i - c] &gt; <span class="number">0</span>:</span><br><span class="line">                dp[i] = min(dp[i], dp[i - c] + <span class="number">1</span>)</span><br><span class="line">    <span class="keyword">return</span> <span class="number">-1</span> <span class="keyword">if</span> dp[<span class="number">-1</span>] &gt; amount <span class="keyword">else</span> dp[<span class="number">-1</span>]</span><br></pre></td></tr></table></figure><h1 id="Rob-dp-in-a-tree-O-n"><a href="#Rob-dp-in-a-tree-O-n" class="headerlink" title="Rob(dp in a tree), O(n)"></a>Rob(dp in a tree), O(n)</h1><img src="73.png" style="width:800px;"/><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(root)</span>:</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">recursive</span><span class="params">(root)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> root:</span><br><span class="line">            <span class="keyword">return</span> <span class="number">0</span>, <span class="number">0</span></span><br><span class="line"></span><br><span class="line">        l_rob, l_not_rob = recursive(root.left)</span><br><span class="line">        r_rob, r_not_rob = recursive(root.right)</span><br><span class="line">        rob = l_not_rob + r_not_rob + root.val</span><br><span class="line">        not_rob = max(l_rob, l_not_rob) + max(r_rob, r_not_rob)</span><br><span class="line">        <span class="keyword">return</span> rob, not_rob</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> max(recursive(root))</span><br></pre></td></tr></table></figure><h1 id="countBits-dp-O-n"><a href="#countBits-dp-O-n" class="headerlink" title="countBits(dp) O(n)"></a>countBits(dp) O(n)</h1><img src="74.png" style="width:800px;"/><p><code>f[i]=f[i // 2] + i % 2</code></p><p>just like shift one bit to left and add the remainder.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(num)</span>:</span></span><br><span class="line">    dp = [<span class="number">0</span>] * (num + <span class="number">1</span>)</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>, num + <span class="number">1</span>):</span><br><span class="line">        dp[i] = dp[i // <span class="number">2</span>] + i % <span class="number">2</span></span><br><span class="line">    <span class="keyword">return</span> dp</span><br></pre></td></tr></table></figure><h1 id="topKFrequent-heap-O-nlogn"><a href="#topKFrequent-heap-O-nlogn" class="headerlink" title="topKFrequent(heap) O(nlogn)"></a>topKFrequent(heap) O(nlogn)</h1><img src="75.png" style="width:800px;"/><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> collections <span class="keyword">import</span> defaultdict</span><br><span class="line"><span class="keyword">import</span> heapq</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(nums, k)</span>:</span></span><br><span class="line">    hash_map = defaultdict(int)</span><br><span class="line">    <span class="keyword">for</span> num <span class="keyword">in</span> nums:</span><br><span class="line">        hash_map[num] += <span class="number">1</span></span><br><span class="line">    ret = []</span><br><span class="line">    i = <span class="number">0</span></span><br><span class="line">    <span class="keyword">for</span> key, val <span class="keyword">in</span> hash_map.items():</span><br><span class="line">        <span class="keyword">if</span> i &lt; k:</span><br><span class="line">            heapq.heappush(ret, (val, key))</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            heapq.heappushpop(ret, (val, key))</span><br><span class="line">        i += <span class="number">1</span></span><br><span class="line">    <span class="keyword">return</span> [v <span class="keyword">for</span> k, v <span class="keyword">in</span> ret]</span><br></pre></td></tr></table></figure><h1 id="decodeString-stack-O-n"><a href="#decodeString-stack-O-n" class="headerlink" title="decodeString(stack) O(n)"></a>decodeString(stack) O(n)</h1><img src="76.png" style="width:800px;"/><p>We use a stack to record chars. Each time we have a ‘]’, we pop and record the chars until the ‘[’, then we continue to find all digital chars, and get handle then to get a 10-based value to repeat the chars we recorded between ‘[’ and ‘]’.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(s)</span>:</span></span><br><span class="line">    stack = [<span class="string">""</span>]</span><br><span class="line">    num = <span class="string">""</span></span><br><span class="line">    <span class="keyword">for</span> c <span class="keyword">in</span> s:</span><br><span class="line">        <span class="keyword">if</span> c.isdigit():</span><br><span class="line">            num += c</span><br><span class="line">        <span class="keyword">elif</span> c == <span class="string">"["</span>:</span><br><span class="line">            stack.extend([num, <span class="string">""</span>])</span><br><span class="line">            num = <span class="string">""</span></span><br><span class="line">        <span class="keyword">elif</span> c == <span class="string">"]"</span>:</span><br><span class="line">            chars = stack.pop()</span><br><span class="line">            n_chars = stack.pop()</span><br><span class="line">            stack[<span class="number">-1</span>] += chars * int(n_chars)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            stack[<span class="number">-1</span>] += c</span><br><span class="line">    <span class="keyword">return</span> stack.pop()</span><br></pre></td></tr></table></figure><h1 id="reconstructQueue-trick-O-nlog"><a href="#reconstructQueue-trick-O-nlog" class="headerlink" title="reconstructQueue(trick) O(nlog)"></a>reconstructQueue(trick) O(nlog)</h1><img src="77.png" style="width:800px;"/><p>First we order the list by, people = sorted(people, key=lambda x: (-x[0], x[1])), the descending order of the first element and ascending value by the second element. </p><p>Then we inset the tuple by the index of second element: result.insert(p[1], p).</p><p>The reason behind this is, the tuple’s order only be affected by the tuple which is larger than it. So we first to handle the larger tuple. Then, for the second element, we insert it by ascending order to keep the result valid. </p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(people)</span>:</span></span><br><span class="line">    people = sorted(people, key=<span class="keyword">lambda</span> x: (-x[<span class="number">0</span>], x[<span class="number">1</span>]))</span><br><span class="line">    ret = []</span><br><span class="line">    <span class="keyword">for</span> p <span class="keyword">in</span> people:</span><br><span class="line">        ret.insert(p[<span class="number">1</span>], p)</span><br><span class="line">    <span class="keyword">return</span> ret</span><br></pre></td></tr></table></figure><h1 id="canPartition"><a href="#canPartition" class="headerlink" title="canPartition"></a>canPartition</h1><img src="78.png" style="width:800px;"/><h2 id="canPartition-dp-O-n-s-n-lt-200-s-lt-10000"><a href="#canPartition-dp-O-n-s-n-lt-200-s-lt-10000" class="headerlink" title="canPartition(dp) O(n*s), n&lt;=200, s&lt;=10000"></a>canPartition(dp) O(n*s), n&lt;=200, s&lt;=10000</h2><p>We make a matrix with n*s, n is the number of the input, and s is the sum(input)//2. For a cell in the matrix, for example, for i-th row and j-th column, it mean with input[:i], can we get sum value of j. so, the process as following:</p><ul><li>We make all dp[i][nums[i]-1] = 1 for i in range(m). it means other values are not selected, only the i-th value’s sum.</li><li>And when we iterate fo i from 1 to m and j from 0 to s. dp[i-1][j] == dp[i][j] means when current value is not selected, the sum we get.. And dp[i][j] = dp[i-1][j-nums[i]] means when current value is selected, the sum we get.</li></ul><h2 id="Partition-dp-O-2-n-with-cutting-branch"><a href="#Partition-dp-O-2-n-with-cutting-branch" class="headerlink" title="Partition(dp) O(2^n with cutting branch)"></a>Partition(dp) O(2^n with cutting branch)</h2><p>just reclusively call helper(nums[i+1:], target-num) for i, num in enumerate(nums)</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># O(N * S)</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(nums)</span>:</span></span><br><span class="line">    sum_v = sum(nums)</span><br><span class="line">    <span class="keyword">if</span> sum_v % <span class="number">2</span> == <span class="number">1</span>:</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line">    <span class="keyword">if</span> max(nums) &gt; sum_v // <span class="number">2</span>:</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line">    scale = sum_v // <span class="number">2</span> + <span class="number">1</span></span><br><span class="line">    dp = [[<span class="literal">False</span>] * scale <span class="keyword">for</span> _ <span class="keyword">in</span> range(len(nums))]</span><br><span class="line"></span><br><span class="line">    nums = sorted(nums, reverse=<span class="literal">True</span>)</span><br><span class="line">    dp[<span class="number">0</span>][nums[<span class="number">0</span>]] = <span class="literal">True</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>, len(nums)):</span><br><span class="line">        dp[i][nums[i]] = <span class="literal">True</span></span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> range(scale - <span class="number">1</span>, <span class="number">0</span>, <span class="number">-1</span>):</span><br><span class="line">            <span class="keyword">if</span> dp[i - <span class="number">1</span>][j]:</span><br><span class="line">                dp[i][j] = <span class="literal">True</span></span><br><span class="line">            <span class="keyword">if</span> j &gt; nums[i] <span class="keyword">and</span> dp[i - <span class="number">1</span>][j - nums[i]]:</span><br><span class="line">                dp[i][j] = <span class="literal">True</span></span><br><span class="line">            <span class="keyword">if</span> dp[i][<span class="number">-1</span>]:</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line">    <span class="keyword">return</span> dp[<span class="number">-1</span>][<span class="number">-1</span>]</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(nums)</span>:</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">recursive</span><span class="params">(nums, target)</span>:</span></span><br><span class="line">        <span class="keyword">for</span> i, num <span class="keyword">in</span> enumerate(nums):</span><br><span class="line">            <span class="keyword">if</span> num &gt; target:</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line">            <span class="keyword">elif</span> num == target:</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line">            <span class="keyword">elif</span> recursive(nums[i + <span class="number">1</span>:], target - nums[i]):</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line"></span><br><span class="line">    sum_v = sum(nums)</span><br><span class="line">    <span class="keyword">if</span> sum_v % <span class="number">2</span> == <span class="number">1</span>:</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line">    nums = sorted(nums, reverse=<span class="literal">True</span>)</span><br><span class="line">    <span class="keyword">return</span> recursive(nums, sum_v // <span class="number">2</span>)</span><br></pre></td></tr></table></figure><h1 id="pathSum-dp-O-n"><a href="#pathSum-dp-O-n" class="headerlink" title="pathSum(dp) O(n)"></a>pathSum(dp) O(n)</h1><img src="79.png" style="width:800px;"/><p>We assume all paths are from root, then there is an old path called oldPathsum, when we continue to go down the tree, we have a new path called currPathSum, once oldPathsum = currPathSum – target, we found a path, so we result += cache.get(oldPathsum, 0). </p><p>Note, when we enter a node, we make cache[currPathSum] = cache.get(currPathSum, 0) + 1, and then we iterate its children, and once we leave this node, we make cache[currPathSum] -= 1.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(root, target)</span>:</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">recursive</span><span class="params">(root, cur_sum)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> root:</span><br><span class="line">            <span class="keyword">nonlocal</span> ret</span><br><span class="line">            cur_sum += root.val</span><br><span class="line">            ret += cache.get(cur_sum - target, <span class="number">0</span>)</span><br><span class="line">            cache[cur_sum] = cache.get(cur_sum, <span class="number">0</span>) + <span class="number">1</span></span><br><span class="line">            recursive(root.left, cur_sum)</span><br><span class="line">            recursive(root.right, cur_sum)</span><br><span class="line">            cache[cur_sum] -= <span class="number">1</span></span><br><span class="line"></span><br><span class="line">    ret = <span class="number">0</span></span><br><span class="line">    cache = &#123;<span class="number">0</span>: <span class="number">1</span>&#125;</span><br><span class="line">    recursive(root, <span class="number">0</span>)</span><br><span class="line">    <span class="keyword">return</span> ret</span><br></pre></td></tr></table></figure><h1 id="findAnagrams-cache-O-n"><a href="#findAnagrams-cache-O-n" class="headerlink" title="findAnagrams(cache) O(n)"></a>findAnagrams(cache) O(n)</h1><img src="80.png" style="width:800px;"/><p>Using a 26 sized cache to stores the current frequency of substring. Each time we slide the window to update the cache and compare it with the cache of target. </p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(s, p)</span>:</span></span><br><span class="line">    <span class="keyword">import</span> collections</span><br><span class="line">    ret = []</span><br><span class="line">    p_counter = collections.Counter(p)</span><br><span class="line">    s_counter = collections.Counter(s[:len(p) - <span class="number">1</span>])</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(len(p) - <span class="number">1</span>, len(s)):</span><br><span class="line">        s_counter[s[i]] += <span class="number">1</span></span><br><span class="line">        pre_i = i - len(p) + <span class="number">1</span></span><br><span class="line">        <span class="keyword">if</span> s_counter == p_counter:</span><br><span class="line">            ret.append(pre_i)</span><br><span class="line">        s_counter[s[pre_i]] -= <span class="number">1</span></span><br><span class="line">        <span class="keyword">if</span> s_counter[s[pre_i]] == <span class="number">0</span>:</span><br><span class="line">            <span class="keyword">del</span> s_counter[s[pre_i]]</span><br><span class="line">    <span class="keyword">return</span> ret</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(s, p)</span>:</span></span><br><span class="line">    l = len(p)</span><br><span class="line">    p_frequency = [<span class="number">0</span>] * <span class="number">26</span></span><br><span class="line">    ss_frequency = [<span class="number">0</span>] * <span class="number">26</span></span><br><span class="line">    res = []</span><br><span class="line">    <span class="keyword">for</span> c <span class="keyword">in</span> p:</span><br><span class="line">        p_frequency[ord(c) - <span class="number">97</span>] += <span class="number">1</span></span><br><span class="line">    <span class="keyword">for</span> c <span class="keyword">in</span> s[<span class="number">0</span>:l]:</span><br><span class="line">        ss_frequency[ord(c) - <span class="number">97</span>] += <span class="number">1</span></span><br><span class="line">    <span class="keyword">if</span> p_frequency == ss_frequency:</span><br><span class="line">        res.append(<span class="number">0</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>, len(s) - l + <span class="number">1</span>):</span><br><span class="line">        ss_frequency[ord(s[i - <span class="number">1</span>]) - <span class="number">97</span>] -= <span class="number">1</span></span><br><span class="line">        ss_frequency[ord(s[i + l - <span class="number">1</span>]) - <span class="number">97</span>] += <span class="number">1</span></span><br><span class="line">        <span class="keyword">if</span> p_frequency == ss_frequency:</span><br><span class="line">            res.append(i)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> res</span><br></pre></td></tr></table></figure><h1 id="findDisappearedNumbers-trick-n"><a href="#findDisappearedNumbers-trick-n" class="headerlink" title="findDisappearedNumbers(trick) (n)"></a>findDisappearedNumbers(trick) (n)</h1><img src="81.png" style="width:800px;"/><p>We iterate the array, to mark the seen position as negative.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(nums)</span>:</span></span><br><span class="line">    n = len(nums)</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(n):</span><br><span class="line">        _num = abs(nums[i])</span><br><span class="line">        <span class="keyword">if</span> _num &lt;= n:</span><br><span class="line">            nums[_num - <span class="number">1</span>] = -abs(nums[_num - <span class="number">1</span>])</span><br><span class="line"></span><br><span class="line">    ret = []</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(n):</span><br><span class="line">        <span class="keyword">if</span> nums[i] &gt; <span class="number">0</span>:</span><br><span class="line">            ret.append(i + <span class="number">1</span>)</span><br><span class="line">    <span class="keyword">return</span> ret</span><br></pre></td></tr></table></figure><h1 id="findTargetSumWays-dp-O-n-2s-s-sum-nums"><a href="#findTargetSumWays-dp-O-n-2s-s-sum-nums" class="headerlink" title="findTargetSumWays(dp) O(n*2s) s=sum(nums)"></a>findTargetSumWays(dp) O(n*2s) s=sum(nums)</h1><p>the matrix of dp is dp[0:n][-s:s], for row is 0, we take dp[0][+-nums[0]] =1, and then for later rows, dp[i][j] = dp[i][j-nums[i]] + dp[i][j+nums[i]].</p><img src="82.png" style="width:800px;"/><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(nums, S)</span>:</span></span><br><span class="line">    sum_v = sum(nums)</span><br><span class="line">    <span class="keyword">if</span> sum_v &lt; S: </span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span></span><br><span class="line">    n = len(nums)</span><br><span class="line">    dp = [<span class="number">0</span>] * (<span class="number">2</span> * sum_v + <span class="number">1</span>)</span><br><span class="line">    dp[nums[<span class="number">0</span>]] += <span class="number">1</span></span><br><span class="line">    dp[-nums[<span class="number">0</span>]] += <span class="number">1</span></span><br><span class="line">    <span class="keyword">for</span> n <span class="keyword">in</span> nums[<span class="number">1</span>:]:</span><br><span class="line">        _dp = [<span class="number">0</span>] * (<span class="number">2</span> * sum_v + <span class="number">1</span>)</span><br><span class="line">        <span class="keyword">for</span> i <span class="keyword">in</span> range(-sum_v, sum_v + <span class="number">1</span>):</span><br><span class="line">            <span class="keyword">if</span> i + n &lt;= sum_v:</span><br><span class="line">                _dp[i] += dp[i + n]</span><br><span class="line">            <span class="keyword">if</span> i - n &gt;= -sum_v:</span><br><span class="line">                _dp[i] += dp[i - n]</span><br><span class="line">        dp = _dp</span><br><span class="line">    <span class="keyword">return</span> dp[S]</span><br></pre></td></tr></table></figure><h1 id="diameterOfBinaryTree-tree-dp-n"><a href="#diameterOfBinaryTree-tree-dp-n" class="headerlink" title="diameterOfBinaryTree(tree dp) (n)"></a>diameterOfBinaryTree(tree dp) (n)</h1><img src="83.png" style="width:800px;"/><p>From the leave, at each parent, we update the result = max(result, a+b+1), a is the longest length from left, b is from right. Then we transfer max(a, b) to this parent as its longest length.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(root)</span>:</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">recusive</span><span class="params">(root)</span>:</span></span><br><span class="line">        <span class="keyword">nonlocal</span> ret</span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> root:</span><br><span class="line">            <span class="keyword">return</span> <span class="number">0</span>, <span class="number">0</span></span><br><span class="line"></span><br><span class="line">        l = recusive(root.left)</span><br><span class="line">        r = recusive(root.right)</span><br><span class="line">        ret = max(ret, max(l) + max(r))</span><br><span class="line">        <span class="keyword">return</span> max(l) + <span class="number">1</span>, max(r) + <span class="number">1</span></span><br><span class="line"></span><br><span class="line">    ret = <span class="number">0</span></span><br><span class="line">    recusive(root)</span><br><span class="line">    <span class="keyword">return</span> ret</span><br></pre></td></tr></table></figure><h1 id="subarraySum-hash-trick-O-n"><a href="#subarraySum-hash-trick-O-n" class="headerlink" title="subarraySum(hash trick) O(n)"></a>subarraySum(hash trick) O(n)</h1><img src="84.png" style="width:800px;"/><p>similar with findTargetSumWays</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># O(n^2)</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(nums, k)</span>:</span></span><br><span class="line">    n = len(nums)</span><br><span class="line">    ret = <span class="number">0</span></span><br><span class="line">    dp = [[<span class="number">0</span>] * (n + <span class="number">1</span>) <span class="keyword">for</span> _ <span class="keyword">in</span> range(n)]  <span class="comment"># (start_pos, len)</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>, n + <span class="number">1</span>):</span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> range(<span class="number">0</span>, n - i + <span class="number">1</span>):</span><br><span class="line">            <span class="keyword">if</span> i == <span class="number">1</span>:</span><br><span class="line">                dp[j][i] = nums[j]</span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                dp[j][i] = nums[j] + dp[j + <span class="number">1</span>][i - <span class="number">1</span>]</span><br><span class="line">            <span class="keyword">if</span> dp[j][i] == k:</span><br><span class="line">                ret += <span class="number">1</span></span><br><span class="line">    <span class="keyword">return</span> ret</span><br><span class="line"></span><br><span class="line"><span class="comment"># O(n)</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(nums, k)</span>:</span></span><br><span class="line">    cache = &#123;<span class="number">0</span>: <span class="number">1</span>&#125;</span><br><span class="line">    cur_sum = <span class="number">0</span></span><br><span class="line">    ret = <span class="number">0</span></span><br><span class="line">    <span class="keyword">for</span> n <span class="keyword">in</span> nums:</span><br><span class="line">        cur_sum += n</span><br><span class="line">        ret += cache.get(cur_sum - k, <span class="number">0</span>)</span><br><span class="line">        cache[cur_sum] = cache.get(cur_sum, <span class="number">0</span>) + <span class="number">1</span></span><br><span class="line">    <span class="keyword">return</span> ret</span><br></pre></td></tr></table></figure><h1 id="findUnsortedSubarray-trick-O-n"><a href="#findUnsortedSubarray-trick-O-n" class="headerlink" title="findUnsortedSubarray(trick) O(n)"></a>findUnsortedSubarray(trick) O(n)</h1><img src="85.png" style="width:800px;"/><p>We firstly iterate from left, each time, we store the current max value, and once we found the current value is smaller than the current max value, we mark it position. Then we iterate from right using min value to do again. Finally the result is max(right-left+1, 0).</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(nums)</span>:</span></span><br><span class="line">    n = len(nums)</span><br><span class="line">    <span class="keyword">if</span> n &lt;= <span class="number">1</span>:</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span></span><br><span class="line">    right = <span class="number">0</span></span><br><span class="line">    cur_max = nums[<span class="number">0</span>]</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>, n):</span><br><span class="line">        <span class="keyword">if</span> nums[i] &lt; cur_max:</span><br><span class="line">            right = i</span><br><span class="line">        cur_max = max(cur_max, nums[i])</span><br><span class="line">        </span><br><span class="line"></span><br><span class="line">    cur_min = nums[<span class="number">-1</span>]</span><br><span class="line">    left = len(nums)<span class="number">-1</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(n - <span class="number">2</span>, <span class="number">-1</span>, <span class="number">-1</span>):</span><br><span class="line">        <span class="keyword">if</span> nums[i] &gt; cur_min:</span><br><span class="line">            left = i</span><br><span class="line">        cur_min = min(cur_min, nums[i])</span><br><span class="line">    <span class="keyword">return</span> max(right - left + <span class="number">1</span>, <span class="number">0</span>)</span><br></pre></td></tr></table></figure><h1 id="mergeTrees-recursive-O-n"><a href="#mergeTrees-recursive-O-n" class="headerlink" title="mergeTrees(recursive) O(n)"></a>mergeTrees(recursive) O(n)</h1><img src="86.png" style="width:800px;"/><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(t1, t2)</span>:</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">recursive</span><span class="params">(t1, t2)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> t1 <span class="keyword">and</span> <span class="keyword">not</span> t2:</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">None</span></span><br><span class="line">        <span class="keyword">elif</span> <span class="keyword">not</span> t1:</span><br><span class="line">            <span class="keyword">return</span> t2</span><br><span class="line">        <span class="keyword">elif</span> <span class="keyword">not</span> t2:</span><br><span class="line">            <span class="keyword">return</span> t1</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            t1.left = recursive(t1.left, t2.left)</span><br><span class="line">            t1.right = recursive(t1.right, t2.right)</span><br><span class="line">            t1.val = t1.val + t2.val</span><br><span class="line">            <span class="keyword">return</span> t1</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> recursive(t1, t2)</span><br></pre></td></tr></table></figure><h1 id="leastInterval-trick-O-n"><a href="#leastInterval-trick-O-n" class="headerlink" title="leastInterval(trick) O(n)"></a>leastInterval(trick) O(n)</h1><img src="87.png" style="width:800px;"/><img src="87.5.png" style="width:800px;"/><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(tasks, n)</span>:</span></span><br><span class="line">    cache = [<span class="number">0</span>] * <span class="number">26</span></span><br><span class="line">    <span class="keyword">for</span> c <span class="keyword">in</span> tasks:</span><br><span class="line">        cache[ord(c) - ord(<span class="string">'A'</span>)] += <span class="number">1</span></span><br><span class="line">    cache.sort(reverse=<span class="literal">True</span>)</span><br><span class="line">    max_v = cache[<span class="number">0</span>] - <span class="number">1</span></span><br><span class="line">    idle_slots = max_v * n</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>, len(cache)):</span><br><span class="line">        <span class="keyword">if</span> cache[i] == <span class="number">0</span>:</span><br><span class="line">            <span class="keyword">break</span></span><br><span class="line">        idle_slots -= min(cache[i], max_v)</span><br><span class="line">    <span class="keyword">return</span> idle_slots + len(tasks) <span class="keyword">if</span> idle_slots &gt; <span class="number">0</span> <span class="keyword">else</span> len(tasks)</span><br></pre></td></tr></table></figure><h1 id="countSubstrings"><a href="#countSubstrings" class="headerlink" title="countSubstrings"></a>countSubstrings</h1><img src="88.png" style="width:800px;"/><h2 id="countSubstrings-O-n-2"><a href="#countSubstrings-O-n-2" class="headerlink" title="countSubstrings() O(n^2)"></a>countSubstrings() O(n^2)</h2><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">n = len(s)</span><br><span class="line"><span class="keyword">if</span> n &lt;= <span class="number">1</span>:</span><br><span class="line">    <span class="keyword">return</span> n</span><br><span class="line">dp = [[<span class="number">0</span>] * (n + <span class="number">1</span>) <span class="keyword">for</span> _ <span class="keyword">in</span> range(n)]  <span class="comment"># (start_pos, len)</span></span><br><span class="line">ret = <span class="number">0</span></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>, n + <span class="number">1</span>):</span><br><span class="line">    <span class="keyword">for</span> j <span class="keyword">in</span> range(<span class="number">0</span>, n - i + <span class="number">1</span>):</span><br><span class="line">        <span class="keyword">if</span> i == <span class="number">1</span> <span class="keyword">or</span> (i == <span class="number">2</span> <span class="keyword">and</span></span><br><span class="line">                      s[j] == s[j + <span class="number">1</span>]) <span class="keyword">or</span> (s[j] == s[j + i - <span class="number">1</span>] <span class="keyword">and</span></span><br><span class="line">                                            dp[j + <span class="number">1</span>][i - <span class="number">2</span>]):</span><br><span class="line">            dp[j][i] = <span class="number">1</span></span><br><span class="line">            ret += <span class="number">1</span></span><br><span class="line"><span class="keyword">return</span> ret</span><br></pre></td></tr></table></figure><h2 id="countSubstrings-Manacher’s-Algorithm-O-n"><a href="#countSubstrings-Manacher’s-Algorithm-O-n" class="headerlink" title="countSubstrings(Manacher’s Algorithm?) O(n)"></a>countSubstrings(Manacher’s Algorithm?) O(n)</h2><h1 id="dailyTemperatures-stack-O-n"><a href="#dailyTemperatures-stack-O-n" class="headerlink" title="dailyTemperatures(stack) O(n)"></a>dailyTemperatures(stack) O(n)</h1><img src="89.png" style="width:800px;"/><p>We use a stack to store the values. We push the value which is smaller than then top item of stack. Once we have a value which is larger than top item, we pop all values which are smaller than the current value and mark the result as [current position – item position].</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">solution</span><span class="params">(T)</span>:</span></span><br><span class="line">    ret = [<span class="number">0</span>] * len(T)</span><br><span class="line">    stack = [(T[<span class="number">0</span>], <span class="number">0</span>)]</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>, len(T)):</span><br><span class="line">        top = stack[<span class="number">-1</span>]</span><br><span class="line">        <span class="keyword">if</span> T[i] &gt; top[<span class="number">0</span>]:</span><br><span class="line">            <span class="keyword">while</span> stack <span class="keyword">and</span> T[i] &gt; stack[<span class="number">-1</span>][<span class="number">0</span>]:</span><br><span class="line">                _t = stack.pop()</span><br><span class="line">                ret[_t[<span class="number">1</span>]] = i - _t[<span class="number">1</span>]</span><br><span class="line">        stack.append((T[i], i))</span><br><span class="line">    <span class="keyword">return</span> ret</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      Summary of Leetcode Top 100
    
    </summary>
    
    
      <category term="Algorithm" scheme="https://joddiy.github.io/categories/Algorithm/"/>
    
    
      <category term="Leetcode" scheme="https://joddiy.github.io/tags/Leetcode/"/>
    
  </entry>
  
  <entry>
    <title>Week Reports</title>
    <link href="https://joddiy.github.io/2020/05/26/Week-Reports/"/>
    <id>https://joddiy.github.io/2020/05/26/Week-Reports/</id>
    <published>2020-05-26T08:43:21.000Z</published>
    <updated>2020-07-03T05:45:19.000Z</updated>
    
    <content type="html"><![CDATA[<h1 id="2020-05-24"><a href="#2020-05-24" class="headerlink" title="2020-05-24"></a>2020-05-24</h1><h2 id="what-have-been-done"><a href="#what-have-been-done" class="headerlink" title="what have been done"></a>what have been done</h2><ul><li>Refactored the <code>SONNX</code> of SINGA,</li><li>and its test cases</li><li>fixed some issues at <code>Layers</code> of SINGA.</li></ul><h2 id="What-have-you-been-doing"><a href="#What-have-you-been-doing" class="headerlink" title="What have you been doing"></a>What have you been doing</h2><ul><li>Refactor the examples of SONNX including model zoos.</li></ul><h2 id="Any-issue-encounter"><a href="#Any-issue-encounter" class="headerlink" title="Any issue encounter"></a>Any issue encounter</h2><p>Just like I declared at this <a href="https://github.com/apache/singa/issues/706" target="_blank" rel="noopener">issue</a></p><ul><li>ONNX prefers to use tensors as input instead of attributes, which may incurs some issues when we create SINGA operators(or layers). There are two cases: SINGA params &lt;- ONNX Initializer and NGA params &lt;- ONNX operator(dynamic graph). We can support first one, but the seconde is hard to support.</li><li>For some layers in SINGA, there are some bugs needed to fix.</li><li>We discussed last time to use Metaclass in SONNX, but I check it carefully, we don’t need to use it. </li></ul><p>And all these issues has been fixed.</p><h2 id="What-are-you-going-to-work"><a href="#What-are-you-going-to-work" class="headerlink" title="What are you going to work"></a>What are you going to work</h2><ul><li>continue to finish the model zoos of SONNX, (1 day)</li><li>debug each part (2 day)</li><li>(if time is enough) try to sppourt more models: super_resolution, ResNet101_DUC_HDC, ShuffleNet_V2, ShuffleNet_V1, inception_v2, squeezenet.</li></ul><h1 id="2020-05-31"><a href="#2020-05-31" class="headerlink" title="2020-05-31"></a>2020-05-31</h1><h2 id="what-have-been-done-1"><a href="#what-have-been-done-1" class="headerlink" title="what have been done"></a>what have been done</h2><ul><li>Debug test cases of autograd and fix issues,</li><li>Refactor and debug test cases of <code>SONNX</code>,</li><li>Refactor and debug the <code>SONNX</code> model zoos,</li><li>Add re-training examples for <code>SONNX</code>,</li><li>Review several PRs of a new intern.</li></ul><h2 id="What-have-you-been-doing-1"><a href="#What-have-you-been-doing-1" class="headerlink" title="What have you been doing"></a>What have you been doing</h2><ul><li>Try to add more models to <code>SONNX</code> model zoos.</li></ul><h2 id="Any-issue-encounter-1"><a href="#Any-issue-encounter-1" class="headerlink" title="Any issue encounter"></a>Any issue encounter</h2><p>when I test autograd opertors, there are four types of issues, I have list it <a href="https://github.com/apache/singa/issues/710" target="_blank" rel="noopener">here</a>:</p><ul><li>Zero-value tensors</li><li>CURAND_STATUS_LAUNCH_FAILURE</li><li>CUDNN_STATUS_EXECUTION_FAILED</li><li>cannot support conv1d and pool1d</li></ul><p>All these issues have been fixed.</p><h2 id="What-are-you-going-to-work-1"><a href="#What-are-you-going-to-work-1" class="headerlink" title="What are you going to work"></a>What are you going to work</h2><ul><li>continue to add more models to <code>SONNX</code> model zoos.</li></ul><h1 id="2020-06-14"><a href="#2020-06-14" class="headerlink" title="2020-06-14"></a>2020-06-14</h1><h2 id="what-have-been-done-2"><a href="#what-have-been-done-2" class="headerlink" title="what have been done"></a>what have been done</h2><ul><li>Fix issues in SONNX frontend</li><li>Add operators to SINGA: Pad and Expand</li><li>Assign works for intern and review their PRs.</li></ul><h2 id="What-have-you-been-doing-2"><a href="#What-have-you-been-doing-2" class="headerlink" title="What have you been doing"></a>What have you been doing</h2><ul><li>Add operators to SINGA, Upsample</li><li>Add more models to <code>SONNX</code> model zoos.</li></ul><h2 id="Any-issue-encounter-2"><a href="#Any-issue-encounter-2" class="headerlink" title="Any issue encounter"></a>Any issue encounter</h2><p>When fix the issues in frontend of SONNX, I found all weights in SINGA is same, and I check the code, found the issue takes place because their layer doesn’t have a name. For example, if I have two conv layers, and I don’t have a top layer to organize them, just append them one by one, the weights of these two conv have a same name: ‘.W’.</p><p>All these issues have been fixed.</p><h2 id="What-are-you-going-to-work-2"><a href="#What-are-you-going-to-work-2" class="headerlink" title="What are you going to work"></a>What are you going to work</h2><ul><li>Add more operators to SINGA</li><li>Continue to add more models to <code>SONNX</code> model zoos.</li></ul><h1 id="2020-06-22"><a href="#2020-06-22" class="headerlink" title="2020-06-22"></a>2020-06-22</h1><h2 id="What-have-been-done"><a href="#What-have-been-done" class="headerlink" title="What have been done"></a>What have been done</h2><ul><li>Fix conflict and merge several PRs</li><li>Fix issues in autograd.py</li><li>Add operators to SINGA: Exp, Round, Rounde.</li><li>Assign works for intern and review their PRs.</li></ul><h2 id="What-have-you-been-doing-3"><a href="#What-have-you-been-doing-3" class="headerlink" title="What have you been doing"></a>What have you been doing</h2><ul><li>Add operators to SINGA, SpaceToDepth.</li><li>Add more models to SONNX model zoos.</li></ul><h2 id="Any-issue-encounter-3"><a href="#Any-issue-encounter-3" class="headerlink" title="Any issue encounter"></a>Any issue encounter</h2><p>There are two issues this week, one for pool operator and one for equal operator.</p><ul><li><p>pool<br>In SINGA, we pad the input with zero before we do the max pool, and when the zero is larger than every element in the recpetive field, the final output will be zero, then the erro occurs.<br>So, for the conv2d, I still pad with zero values, but for maxpool and avgpool, I pad the input with the edge values which can solve above problem.</p></li><li><p>equal<br>For equal operator, the result with int32 input has some problems, we are still checking it.</p></li></ul><h2 id="What-are-you-going-to-work-3"><a href="#What-are-you-going-to-work-3" class="headerlink" title="What are you going to work"></a>What are you going to work</h2><ul><li>Add more operators to SINGA, ScatterElements, RoiAlign, TopK, ReduceMin, NonMaxSuppression, Resize.</li><li>Continue to add more models to SONNX model zoos.</li></ul><h1 id="2020-06-29"><a href="#2020-06-29" class="headerlink" title="2020-06-29"></a>2020-06-29</h1><h2 id="What-have-been-done-1"><a href="#What-have-been-done-1" class="headerlink" title="What have been done"></a>What have been done</h2><ul><li>Add operators to SINGA: SpaceToDepth and DepthToSpace</li><li>Assign works for intern and review their PRs.</li></ul><h2 id="What-have-you-been-doing-4"><a href="#What-have-you-been-doing-4" class="headerlink" title="What have you been doing"></a>What have you been doing</h2><ul><li>Add operators to SINGA, RoiAlign. </li><li>Add more models to SONNX model zoos.</li></ul><h2 id="Any-issue-encounter-4"><a href="#Any-issue-encounter-4" class="headerlink" title="Any issue encounter"></a>Any issue encounter</h2><p>We cannot support <code>Transpose</code> with more than 4 dims. This may be fixed later.</p><h2 id="What-are-you-going-to-work-4"><a href="#What-are-you-going-to-work-4" class="headerlink" title="What are you going to work"></a>What are you going to work</h2><ul><li>Add more operators to SINGA, NonMaxSuppression, ConvTranspose, Resize.</li><li>Continue to add more models to SONNX model zoos.</li></ul>]]></content>
    
    <summary type="html">
    
      The summary of the week reports
    
    </summary>
    
    
      <category term="Work" scheme="https://joddiy.github.io/categories/Work/"/>
    
    
      <category term="Week Reports" scheme="https://joddiy.github.io/tags/Week-Reports/"/>
    
  </entry>
  
  <entry>
    <title>Magazine Review May 16th-22nd 2020</title>
    <link href="https://joddiy.github.io/2020/05/21/magazine-review/"/>
    <id>https://joddiy.github.io/2020/05/21/magazine-review/</id>
    <published>2020-05-21T11:12:54.000Z</published>
    <updated>2020-05-25T15:09:35.000Z</updated>
    
    <content type="html"><![CDATA[<h1 id="On-the-blink"><a href="#On-the-blink" class="headerlink" title="On the blink"></a>On the blink</h1><blockquote><p>By failing to face up to its difficulties, the European Union only makes them worse</p></blockquote><p>Seventy years ago this month Robert Schuman, the French foreign minister, proposed a European “coal and steel community”.</p><p>Somehow, the eu muddled through. The pandemic in Europe is fast becoming a political and constitutional crisis. This is solvable in principle, but the eu’s members cannot agree on what is needed to make their union more resilient, nor on how to bring about reform.</p><p>Instead the pandemic is testing the bonds of membership, </p><ul><li>One example is the single market. This is governed by strict rules limiting subsidies, but they have been suspended as governments pour €2trn ($2.2trn) into saving businesses from collapse. </li><li>Another example is the single currency. As countries cushion the effects of lockdowns, their debts are rising sharply. </li><li>A third example is the status of eu law. The euis built on law. If the stresses of the pandemic weaken the ecj’s foundations, the entire union will shake.</li></ul><p>All these problems can be solved with vision, compromise and reform. But such sentiments crumble before countries’ different views of what the eu should be for. Even before the first death from covid-19, they struggled to forge common policies on defence, Russia, migration and much more besides.</p><p>Ominously, the mechanism of reform is also broken. Leaders have not dared to put through a significant amendment since 2007.</p><p>If the eu is to thrive, it will have to be a lot more ambitious than the northerners admit. Successful treaty change entails a broader acknowledgment that different countries want different things from the union and that such a “multi-speed Europe” can be more resilient than today’s unmet aspirations.</p><p>So long as the eu remains a conduit for spreading crises, the risk of collapse will be high. Bigger transfers and significant debt mutualisation would be hard, but as a down payment to avert catastrophe and to set the eu on the path to stability, they would be worth it.</p><p><strong>Words</strong>:</p><p>bumpy, beacon, exemplar, loggerheads, fortify, crumble, mutualise, ominously, one-off, stagnate, taboo, unmet, doomster, wobbly, conduit.</p><h1 id="Goodbye-globalisation-The-world-economy"><a href="#Goodbye-globalisation-The-world-economy" class="headerlink" title="Goodbye globalisation - The world economy"></a>Goodbye globalisation - The world economy</h1><blockquote><p>A more nationalistic and self-sufficient era beckons. It won’t be richer—or safer</p></blockquote><p>Even before the pandemic, globalisation was in trouble. Now it is reeling from its third bodyblow in a dozen years as lockdowns have sealed borders and disrupted commerce. As economies reopen, activity will recover, but don’t expect a quick return to a carefree world of unfettered movement and free trade.</p><p>Since January a new wave of disruption has spread westward from Asia. Factory, shop and office closures have caused demand to tumble and prevented suppliers from reaching customers.</p><p>The underlying anarchy of global governance is being exposed. Around the world, public opinion is shifting away from globalisation.</p><p>This is just the start. Although the flow of information is largely free outside China, the movement of people, goods and capital is not. </p><p>Trade will suffer as countries abandon the idea that firms and goods are treated equally regardless of where they come from. Governments and central banks are asking taxpayers to underwrite national firms through their stimulus packages. And the push to bring supply chains back home in the name of resilience is accelerating.</p><p>The flow of capital is also suffering, as longterm investment sinks. </p><p>Poorer countries will find it harder to catch up and, in the rich world, life will be more expensive and less free. Moreover, a fractured world will make solving global problems harder, including finding a vaccine and securing an economic recovery.</p><p>Wave goodbye to the greatest era of globalisation—and worry about what is going to take its place.</p><p><strong>Words</strong>:</p><p>Sino-American trade war, reel, carefree, unfettered, lurch, enfeeble, autocratic, chauvinism, contempt, savage, anarchy, squabble, prestige, curtail, drum up, moot, underwrite, repatriate, venture-capital, domesticate, forfeit, fractured</p><h1 id="First-do-no-harm-Escaping-the-lockdown"><a href="#First-do-no-harm-Escaping-the-lockdown" class="headerlink" title="First, do no harm - Escaping the lockdown"></a>First, do no harm - Escaping the lockdown</h1><blockquote><p>Governments pinning their hopes on contact-tracing apps should tread carefully</p></blockquote><p>Medicine’s history is full of promising treatments that, when tested, turned out not to work or even to cause harm.</p><p>Many governments hope salvation can come sooner, with contact-tracing apps on smartphones.</p><p>Yet contact-tracing apps are also an untested medical invention. Inaccurate information can mislead health offcials and citizens in ways that can be as harmful as any failed drug:</p><ul><li>Coverage is one complication. Epidemiologists reckon that apps might be useful if around 60% of people use them.</li><li>Accuracy is an issue, too. Yet the strength of the radio signals used to do this is affected by all sorts of things besides distance.</li><li>That could make it hard to calibrate the system. Too sensitive, and you risk a deluge of “false positives”. Too forgiving, and genuine cases of viral transmission will go undetected.</li><li>Moreover, the apps themselves might change behaviour: spur people to go out before it is safe; Privacy must be weighed against transparency. </li></ul><p>The efforts of some governments have been complicated by Apple and Google. The firms have made privacy<br>a priority</p><p>In a pandemic, experimenting with novel public-health responses such as mass surveillance should be done carefully, in case it subsequently turns out to have nasty side-effects.</p><p><strong>Words</strong>:<br>agonise, salvation, keep tabs, impede, deluge, homophobic invective, infuriate, surveillance, pills and potions. </p><h1 id="Reopen-and-shut-Unemployment"><a href="#Reopen-and-shut-Unemployment" class="headerlink" title="Reopen and shut - Unemployment"></a>Reopen and shut - Unemployment</h1><blockquote><p>Freezing labour markets for too long will cost too much and impede the recovery</p></blockquote><p>Never before have governments erected safety-nets as generous as those they have created during the pandemic.</p><p>Yet governments need to prepare an exit strategy not just from lockdowns, but also from their emergency policies.  </p><p>Yet it seems increasingly likely that economies will instead be permanently changed. Consumers may emerge from lockdown with new habits and fears about mixing, spending less on restaurants, cinemas and travel, and spending more on deliveries, home-improvement and video-streaming. Four in ten American jobs lost in the pandemic will not return, according to one estimate based on surveys, historical patterns and stockmarket signals. </p><p>In America roughly three-quarters of recipients of unemployment insurance are receiving more than they did in work. That blunts the incentive to seek new jobs.</p><p>Governments thus face a difficult balancing act: withdraw support too readily, and many people will suffer; withdraw it too late, and the economy will ossify. </p><p>Schemes should also encourage flexibility. Idled workers should be allowed to return to their companies part-time.</p><p>Finally, governments should help people find new jobs. That means boosting support for training, tearing down barriers to opportunity such as unnecessary licensing rules, and cutting payroll taxes to encourage hiring.</p><p><strong>Words</strong>:<br>trebling, so claims, indispensable, avert, revive, subside, elbow-to-elbow, starve, entice, blunt, fend, ossify, lavish, forcibly, stand in the way, grease.</p><h1 id="Water-torture-Hydropower-in-Asia"><a href="#Water-torture-Hydropower-in-Asia" class="headerlink" title="Water torture - Hydropower in Asia"></a>Water torture - Hydropower in Asia</h1><blockquote><p>China should tell its neighbours what it is doing on the rivers they share</p></blockquote><p>They have built big dams on the Brahmaputra and the Mekong, which pass through several more countries on their way to the sea. </p><p>If the countries nearest the source suck up too much of the flow, or even simply stop silt flowing down or fish swimming up by building dams, the consequences in the lower reaches of the river can be grim: parched crops, collapsed fisheries, salty farmland. </p><p>Tension and recrimination have been the order of the day for China and its neighbours, alas:</p><ul><li>Last year, during a drought, the river ran so low that Cambodia had to turn off a big hydropower plant. </li><li>Even when rainfall is normal, the altered flow and diminished siltation are causing saltwater to intrude into the Mekong delta, which is the bread-</li></ul><p>China has long resisted any formal commitment to curb its construction of dams or to guarantee downstream countries a minimum allocation of water:</p><ul><li>The problem is not just that China gets huffy about anything that could be construed as foreign interference in its “internal affairs”. </li><li>The country’s leaders are also mesmerised by big engineering projects and seldom show much concern for the people displaced or disadvantaged by them, even when the victims are their fellow citizens. </li></ul><p>They could do plenty more to reassure their neighbours. Sharing data on water levels routinely, without interruption, would be a good start. By the same token, downstream countries would love to know when Chinese hydro- power plants plan to retain or discharge water, to allow farmers and fishermen in the lower reaches time to prepare. </p><p><strong>Words</strong>:<br>cascade, slosh, twitchy, grim, parched, riparian, bicker, recrimination, tributary, contemplate, diminish, siltation, deplete, huffy, mesmerised, tantalising, fetish, alleviate, gratitude.</p><h1 id="Free-at-last-France-leaves-lockdown"><a href="#Free-at-last-France-leaves-lockdown" class="headerlink" title="Free at last - France leaves lockdown"></a>Free at last - France leaves lockdown</h1><p>After eight weeks of confinement, France was supposed to resume work this week.</p><p>Much of the complexity of organising this is not unique to France.</p><p>One is the adversarial nature of labour relations, combined with the French state’s enduring<br> appetite for bureaucracy.<br>In preparing déconfinement, bosses report vastly more such consultations.</p><p>Furthermore, both company bosses and elected o!cials are criminally liable while in their jobs.</p><p>Still, many people are itching to go back to work.</p><p>Yet there is an underlying fearfulness, links to a “particular lack of trust in French society”<br>towards institutions, employers and government.</p><p>The balance between safety and prosperity is perilous.</p><p><strong>Words</strong>:<br>estuary, hand gel, scrub, acquit, itch, anxiety, layoff, assuage.</p><h1 id="The-Swedish-way-Controlling-covid-19"><a href="#The-Swedish-way-Controlling-covid-19" class="headerlink" title="The Swedish way - Controlling covid-19"></a>The Swedish way - Controlling covid-19</h1><blockquote><p>Sweden shunned a hard lockdown. Was that wise?</p></blockquote><p>Sweden chose this path because it looked at the longer term, Full lockdowns are stop-gap measures, and European governments rushed to put them in place without plans for what would replace them. </p><p>Swedes have been sensible. </p><p>On first glance, Sweden seems to have paid a heavy price for choosing less stringentmeasures to keep people apart. Even so, Sweden’s mortality rate has been much lower.</p><p>Time will tell whether Sweden chose a better strategy than other countries,</p><ul><li>because the costs of lockdowns—interms not only of economic damage but also harm to people’s mental health—are yet to be tallied. </li><li>Stockholm will reach “herd immunity”, the 40-60% rate of infection needed to halt the spread of the coronavirus, by June. When European countries count deaths a year from now their figures will be similar, regardless of the measures taken and the numbers now. The economic damage in Sweden, however, may be smaller.</li></ul><p><strong>Words</strong>:</p><p>bleary-eyed, squint, stop-gap</p><h1 id="Northern-fights-Naval-strategy"><a href="#Northern-fights-Naval-strategy" class="headerlink" title="Northern fights - Naval strategy"></a>Northern fights - Naval strategy</h1><blockquote><p>America and Britain mount a show of force in the Arctic</p></blockquote><p>On May 1st a flotilla of two American destroyers, a nuclear submarine, a support ship and a long-range maritime-patrol aircraft, plus a British frigate, practised their sub-hunting skills in the Norwegian Sea.</p><p>The decision to dispatch destroyerswas a bold one. One aim was to show that covid- 19 has not blunted swords, Another was to assert freedom of navigation in the face of Russia’s imposition of rules on the Northern Sea Route (nsr).</p><p>More broadly, the Arctic is a growing factor in nato defence plans. </p><p>The main task of Russian subs is defensive: to protect a “bastion”. But nato admirals worry that, in a conflict, some might pose a wider threat to the alliance.</p><p>For much of the cold war, nato allies sought to bottle up the Soviet fleet in the Arctic by establishing a picket across the so-called giuk gap, But defence in depth may not su”ce.The challenge is a familiar one. ce. A new generation of Russian ship-based missiles, capable of striking nato ships or territory from far north of the giuk gap, represents “a dramatically new and challenging threat”, concludes the iiss.</p><p><strong>Words</strong>:<br>blot out, snoop, beastliness, flotilla, destroyer, patrol, frigate, blunt, think-tank, bottle up, picket, bastion, lair.</p><h1 id="The-jobless-market-Welfare-and-work"><a href="#The-jobless-market-Welfare-and-work" class="headerlink" title="The jobless market - Welfare and work"></a>The jobless market - Welfare and work</h1><blockquote><p>Unemployment is at levels last seen during the Great Depression.<br>The welfare system is creaking</p></blockquote><p>Millions have found themselves in a similar position, pushing America’s unemployment rate to an 80-year high. As elsewhere, New York’s computer systems could not handle the rush of applications for unemployment insurance (UI).</p><p>So rapid a rise in unemployment would stress any country’s UI system. America’s long economic expansion left its system woefully underfunded. So although the weekly $600 top-up that Congress approved in late March is nominally generous, millions of people have not had their money.</p><p>UI is intended to replace a share of lost wages while a recipient looks for work. But overall, prepandemic America had perhaps the rich world’s stingiest system. The average payout in 2019 was equivalent to about 40% of previous earnings.</p><p>Now America may have the world’s most generous system. Goldman Sachs, a bank, estimates that three-quarters of laid-o! workers are in line to receive benefits that exceed their former wage.</p><p>Giving some workers more money than they had earned in a job may not be the best use of public resources, but the people who benefit most are likely to be among America’s poorest.</p><p>Regulations are designed to foil people who quit a job in order to pick up an unemployment cheque. </p><p>Some economists want the government to do more to encourage employers to reduce working hours rather than laying people off.</p><p>Another option would be to prolong the more generous unemployment system past July. But at some point such payouts will hinder the economic recovery. </p><p>Most probably, the unemployment system will return to its pre-pandemic ways sooner rather than later. The economy is reopening and bosses are looking for workers.</p><p><strong>Words</strong>:</p><p>woefully, nomimally, loom, stingy, hefty, archaic, fiscal, foil</p><h1 id="Craics-in-the-business-model-Irish-pubs"><a href="#Craics-in-the-business-model-Irish-pubs" class="headerlink" title="Craics in the business model - Irish pubs"></a>Craics in the business model - Irish pubs</h1><blockquote><p>NEW YORK<br>Surviving covid could be harder than surviving Prohibition</p></blockquote><p>On the eve of one of the most profitable weeks of the pub year, in an attempt to stop covid-19 from spreading, City Hall closed down Irish pubs, along with other bars and restaurants. Some will never reopen.</p><p>Even before the lockdown, the structural problems causing the decline of the Irish pub were already in place. Increases in rents and the minimum wage were eating into profits. And drinking habits have changed.</p><p>Some neighbourhood bars have a chance of surviving the lockdown. It helps that they often own the building. They have lower overheads than those catering to tourists and the corporate-worker crowd. Their customers tend to be regulars. But it won’t be easy.</p><p>Still, many owners are confident that customers will be back. Mr Dolan thinks it will be impossible to stop young people socialising.</p><p>Historically, the Irish pub was the place where business was done, jobs were found and favours were traded, a very important social function.</p><p><strong>Words</strong>:</p><p>boozy, overtly, coveted, catering, bustling, rejig. </p><h1 id="Mike-Pompeo’s-followership"><a href="#Mike-Pompeo’s-followership" class="headerlink" title="Mike Pompeo’s followership"></a>Mike Pompeo’s followership</h1><blockquote><p>The secretary of state is confusing global leadership with barking for his master</p></blockquote><p>For a man acknowledged to be highly intelligent, Mike Pompeo has a long history of talking nonsense. His recent insistence that covid- 19 probably emerged from a Chinese laboratory—a conclusion American spies appear not to share—was of this pattern.</p><p>Hardly anyone else outside the Republican base seems to have taken his allegation all that seriously. The other half of America discounted it on the basis that Mr Pompeo said it.</p><p>His China-baiting, an obvious effort to distract from Mr Trump’s struggles with the pandemic, was a case in point.</p><p>Two particular reasons seem to explain Mr Pompeo’s flexibility.</p><p>One is personal. Yet his subsequent climb is mainly due to Mr Trump’s need for fresh faces for an administration that many Republicans were unwilling to join or, because of past criticism of the president, unwelcome in.  No one in the administration owes Mr Trump more than he does.</p><p>The other explanation is that Mr Pompeo represents a broader politicisation of foreign policy. Yet it has accelerated under Mr Trump, in part because blaming the other side is the easiest way for establishment Republicans to justify his protectionism and other o”ences against conservative orthodoxy.</p><p>He will be remembered for undercutting the world’s reasonable case against China’s handling of the virus by throwing mud for his boss in the midst of a pandemic. That is not American leadership.</p><p><strong>Words</strong>:</p><p>complicit, evangelical, allegation, mediocrity, articulation, sceptically, grudge, tsar, nonentity, harangue, annexation, capitulation, hasty, nuanced, bullish, baiting, abhor, seepage, partisanship, orthodoxy.</p>]]></content>
    
    <summary type="html">
    
      Summaries and reviews for The Economist
    
    </summary>
    
    
      <category term="Reading" scheme="https://joddiy.github.io/categories/Reading/"/>
    
    
      <category term="Reading" scheme="https://joddiy.github.io/tags/Reading/"/>
    
  </entry>
  
  <entry>
    <title>SINGA-ONNX</title>
    <link href="https://joddiy.github.io/2020/05/14/SINGA-ONNX/"/>
    <id>https://joddiy.github.io/2020/05/14/SINGA-ONNX/</id>
    <published>2020-05-14T08:54:44.000Z</published>
    <updated>2020-07-03T05:45:22.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="Conslusion-first"><a href="#Conslusion-first" class="headerlink" title="Conslusion first"></a>Conslusion first</h2><p>Good news: </p><blockquote><p>The ONNX can defines the loss and optimizer now within its format. However, current loss only have <code>NegativeLogLikelihoodLoss</code> and <code>SoftmaxCrossEntropyLoss</code>. Also, it only can store optimizers, only have <code>Adagrad</code>, <code>Adam</code>, <code>Momentum</code>(SGD with standard momentum). </p></blockquote><p>Bad news:</p><blockquote><p>we need to update the onnx to 1.7, which is released last week, may not be so stable. In this release, ONNX defines a comlicated node called <code>GraphCall</code> to specify which gradients should be computed and how to update the tensors by using these gradients. Since we will update the weights following the backward, so this part may not be useful for us.</p></blockquote><h2 id="ONNX-Training-Preview-TrainingInfoProto"><a href="#ONNX-Training-Preview-TrainingInfoProto" class="headerlink" title="ONNX Training Preview (TrainingInfoProto)"></a>ONNX Training Preview (TrainingInfoProto)</h2><p>In last week, the ONNX team has released a new version <a href="https://github.com/onnx/onnx/releases/tag/v1.7.0" target="_blank" rel="noopener">1.7.0</a> which upgrade its opset version to 12. In this new rleases, they add a new feature called <a href="https://github.com/onnx/onnx/blob/3368834cf0b1f0ab9838cf6bdf78a27299d08187/onnx/onnx.proto3#L211-L316" target="_blank" rel="noopener"><code>TrainingInfoProto</code></a>. </p><p>This new feature defines something about training information. There are two main parts in it, <code>initialization-step</code> and <code>training-algorithm-step</code>.</p><h3 id="initialization-step"><a href="#initialization-step" class="headerlink" title="initialization-step"></a>initialization-step</h3><p><code>initialization-step</code> means the developer can defines a <code>initialization</code>. For its type, the <code>initialization</code> is a formal ONNX graph. It doesn’t have input but seveal outputs. The developer can defines some nodes in this graph, such as <code>RandomNormal</code> or <code>RandomUniform</code>, and in another field called <code>initialization_binding</code>, the developer can assign these outputs to the specific tensors in the inference graph.</p><p>The current supported ramdom methods are: <code>RandomNormal</code> or <code>RandomUniform</code>.</p><h3 id="training-algorithm-step"><a href="#training-algorithm-step" class="headerlink" title="training-algorithm-step"></a>training-algorithm-step</h3><p><code>training-algorithm-step</code> defines a field called <code>algorithm</code>. It defines a inference graph which represents a training algorithm’s step. Given required inputs, it computes outputs to update tensors in its own or in the main computaton graph. <code>update_binding</code> contains a key-value pair of strings to assign the outputs to some specific tensors.</p><p>In general, this graph contains loss node, gradient node, optimizer node, increment of iteration count, and some calls to the inference graph. The field algorithm.node is the only place the user can use GraphCall operator. </p><h4 id="Loss-node"><a href="#Loss-node" class="headerlink" title="Loss node"></a>Loss node</h4><ul><li><code>NegativeLogLikelihoodLoss</code></li><li><code>SoftmaxCrossEntropyLoss</code></li></ul><h4 id="Optimizer-node"><a href="#Optimizer-node" class="headerlink" title="Optimizer node"></a>Optimizer node</h4><ul><li><code>Adagrad</code></li><li><code>Adam</code></li><li><code>Momentum</code>: SG with standard momentum</li></ul><h4 id="Gradient-node"><a href="#Gradient-node" class="headerlink" title="Gradient node"></a>Gradient node</h4><p>The gradient node actually only defines the necessary information to compute the gradient for all graph, for example, at the following graph, the gradient defines its inputs containing the <code>xs</code>(intermidate weights) and <code>zs</code>(input of the graph), and <code>y</code>(the output of the graph), and its outputs having <code>dY/dW</code>, <code>dY/dZ</code> whose order corresponds to the inputs in <code>xs</code>. </p><p>It doesn’t defines any logic about how to compute the <code>dY/dW</code>, <code>dY/dZ</code>.</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">W --&gt; Conv --&gt; H --&gt; Gemm --&gt; Y</span><br><span class="line">|      ^              ^</span><br><span class="line">|      |              |</span><br><span class="line">|      X              Z</span><br><span class="line">|      |              |</span><br><span class="line">|      |   .----------&#39;</span><br><span class="line">|      |   |  (W&#x2F;Z&#x2F;X is the 1st&#x2F;2nd&#x2F;3rd input of Gradient as shown in</span><br><span class="line">|      |   |   &quot;xs&quot; followed by &quot;zs&quot;)</span><br><span class="line">|      v   v</span><br><span class="line">&#39;---&gt; Gradient(xs&#x3D;[&quot;W&quot;, &quot;Z&quot;], zs&#x3D;[&quot;X&quot;], y&#x3D;&quot;Y&quot;)</span><br><span class="line">       |   |</span><br><span class="line">       |   &#39;-----------------------------------&gt; dY&#x2F;dW (1st output of Gradient)</span><br><span class="line">       |</span><br><span class="line">       &#39;---------------------------------------&gt; dY&#x2F;dZ (2nd output of Gradient)</span><br></pre></td></tr></table></figure><h4 id="GraphCall-node"><a href="#GraphCall-node" class="headerlink" title="GraphCall node"></a>GraphCall node</h4><p>The GraphCall operator invokes a graph inside TrainingInfoProto’s algorithm field. The GraphCall inputs and outputs are bound to those of invoked graph by position.</p><p>Based on the above inference graph, the GraphCall can use like this:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line">.-------- W (a global and mutable variable from</span><br><span class="line">|         |  the inference graph)</span><br><span class="line">|         |</span><br><span class="line">|   .-----&#39;-----------.</span><br><span class="line">|   |                 |</span><br><span class="line">|   |                 v</span><br><span class="line">|   | .-- X_1 --&gt; GraphCall(graph_name&#x3D;&quot;MyInferenceGraph&quot;)</span><br><span class="line">|   | |            |  |</span><br><span class="line">|   | |            |  |</span><br><span class="line">|   | |   Z_1 -----&#39;  |</span><br><span class="line">|   | |    |          V</span><br><span class="line">|   | |    |         Y_1 ---&gt; Loss ---&gt; O</span><br><span class="line">|   | |    |                    ^</span><br><span class="line">|   | |    |                    |</span><br><span class="line">|   | &#96;--. |                    C</span><br><span class="line">|   |    | |                    |</span><br><span class="line">|   |    | |   .----------------&#39;</span><br><span class="line">|   |    | |   |</span><br><span class="line">|   |    v v   v</span><br><span class="line">|   &#96;--&gt; Gradient(xs&#x3D;[&quot;W&quot;], zs&#x3D;[&quot;X_1&quot;, &quot;Z_1&quot;, &quot;C&quot;], y&#x3D;&quot;O&quot;)</span><br><span class="line">|        |</span><br><span class="line">|        v</span><br><span class="line">|      dO_dW (gradient of W)      1 (a scalar one)</span><br><span class="line">|        |                        |</span><br><span class="line">|        V                        v</span><br><span class="line">|       Div &lt;--- T ------------&gt; Add ---&gt; T_new</span><br><span class="line">|        |    (T is the number of training iterations.</span><br><span class="line">|        |     T is also globally visible and mutable.)</span><br><span class="line">|        v</span><br><span class="line">&#96;-----&gt; Sub ----&gt; W_new</span><br></pre></td></tr></table></figure><p>The previous section’s inference graph is called by <code>GraphCall(graph_name=&quot;MyInferenceGraph&quot;)</code>, and it uses a new batch of inputs (<code>X_1</code>, <code>Z_1</code>) to compute <code>Y_1</code>. </p><p><code>Gradient</code> defines the graidents the graph should compute, finally, it gets <code>W_new</code> amd <code>T_new</code>.</p><p>The it uses the following <code>update_binding</code> to udpate the tensors:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">update_binding: &#123;&quot;W&quot;: &quot;W_new&quot;, &quot;T&quot;: &quot;T_new&quot;&#125;</span><br></pre></td></tr></table></figure><h2 id="API"><a href="#API" class="headerlink" title="API"></a>API</h2><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># handle ONNX </span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">to_onnx</span><span class="params">(model)</span>:</span></span><br><span class="line">    <span class="keyword">return</span> a onnx model </span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SONNXModel</span><span class="params">(Module)</span>:</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, onnx_model)</span>:</span></span><br><span class="line">singa_rep = sonnx.prepare(onnx_model, device=dev, batchsize=<span class="number">1</span>)</span><br><span class="line"><span class="keyword">for</span> layer_name, layer <span class="keyword">in</span> singa_rep.layers:</span><br><span class="line">self.__dict__[layer_name] = layer</span><br><span class="line"><span class="comment"># store weights here as numpy</span></span><br><span class="line"><span class="keyword">for</span> weith_name, weight <span class="keyword">in</span> singa_rep.weights:</span><br><span class="line">self.weights[weith_name] = weight</span><br><span class="line"><span class="comment"># store layer info such as input and output name(only weights)</span></span><br><span class="line"><span class="keyword">for</span> layer_name, layer_info <span class="keyword">in</span> singa_rep.layer_infos:</span><br><span class="line">self.layer_infos[layer_name] = layer_info</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">forward</span><span class="params">(self, aux_output)</span>:</span></span><br><span class="line">        <span class="comment"># run forward according to onnx graph </span></span><br><span class="line">        <span class="keyword">return</span> the last output + aux_output</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">compile</span><span class="params">(self)</span></span></span><br><span class="line"><span class="function">    # <span class="title">init</span> <span class="title">weights</span></span></span><br><span class="line"><span class="function">    <span class="title">super</span>.<span class="title">compile</span><span class="params">(self)</span></span></span><br><span class="line"><span class="function">    # <span class="title">set</span> <span class="title">weights</span>' <span class="title">value</span></span></span><br><span class="line">for layer_name, layer in self.__dict__:</span><br><span class="line">input_info, output_info = self.layer_infos[layer_name]</span><br><span class="line"><span class="keyword">for</span> input_name <span class="keyword">in</span> input_info:</span><br><span class="line">layer.set_weight(self.weights[input_name])</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyModel</span><span class="params">(SONNXModel)</span>:</span></span><br><span class="line">     <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, onnx)</span>:</span></span><br><span class="line">          super.__init__(onnx)</span><br><span class="line">          self.layer1 = Conv()</span><br><span class="line">          self.layer2 = Conv()</span><br><span class="line"></span><br><span class="line">     <span class="function"><span class="keyword">def</span> <span class="title">forward</span><span class="params">(self, x)</span>:</span></span><br><span class="line">           x1, x2 = super.forward(x, aux_output)</span><br><span class="line">           x = self.layer1.forward(x2)</span><br><span class="line">           <span class="keyword">return</span> self.layer2.forward(x1) + x</span><br><span class="line"></span><br><span class="line">      <span class="function"><span class="keyword">def</span> <span class="title">train_one_batch</span><span class="params">(self, x, y)</span>:</span></span><br><span class="line">           y_ = self.forward(x)</span><br><span class="line">           ....</span><br></pre></td></tr></table></figure><hr><p>save and load: how to load onnx model, and how to save loss and sgd</p>]]></content>
    
    <summary type="html">
    
      some upgrade about SINGA ONNX
    
    </summary>
    
    
      <category term="Work Notes" scheme="https://joddiy.github.io/categories/Work-Notes/"/>
    
    
      <category term="Deep Learning" scheme="https://joddiy.github.io/tags/Deep-Learning/"/>
    
      <category term="Work" scheme="https://joddiy.github.io/tags/Work/"/>
    
      <category term="SINGA" scheme="https://joddiy.github.io/tags/SINGA/"/>
    
  </entry>
  
  <entry>
    <title>Notes for Distributed Tensorflow 2.0</title>
    <link href="https://joddiy.github.io/2020/05/12/TF2-0-Distributed/"/>
    <id>https://joddiy.github.io/2020/05/12/TF2-0-Distributed/</id>
    <published>2020-05-11T16:00:00.000Z</published>
    <updated>2020-08-12T02:26:00.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="Overview"><a href="#Overview" class="headerlink" title="Overview"></a>Overview</h2><ul><li>Parallelism<ul><li>Model parallelism</li><li>Data parallelism<ol><li>Synchronous vs asynchronous training</li><li>TensorFlow Strategy</li></ol></li><li>Model Computation Pipelining</li></ul></li><li>Graph optimization with Grappler<ul><li>MetaOptimizer</li><li>Pruning optimizer</li><li>Function optimizer</li><li>Common subgraph elimination</li><li>Debug stripper</li><li>Constant folding optimizer</li><li>Shape optimizer</li><li>Auto mixed precision optimizer</li><li>Pin to host optimizer</li><li>Arithmetic optimizer </li><li>Layout optimizer</li><li>Remapper optimizer</li><li>Loop optimizer</li><li>Dependency optimizer</li><li>Memory optimizer</li><li>Autoparallel optimizer</li><li>Scoped allocator optimizer</li></ul></li><li>Distributed architecture (unfinished)<ul><li>Parameter server</li><li>Ring-allreduce</li><li>Horovod</li></ul></li></ul><h2 id="1-Parallelism"><a href="#1-Parallelism" class="headerlink" title="1 Parallelism"></a>1 Parallelism</h2><p>TensorFlow’s basic dataflow graph model can be used in a variety of ways for machine learning applications. However, some neural networks models are so large they cannot fit in memory of a single device (GPU). Google’s Neural Machine Translation system is an example of such a network. </p><p>Such models need to be split over many devices, carrying out the training in parallel on the devices. There are three method to train a model in parallel on the devices, <code>Model parallelism</code>, <code>Data parallelism</code>, and <code>Model Computation Pipelining</code>. </p><p><code>Model parallelism</code> uses same data for every device but partitions the model among the devices. The graph is split as several sub-graphs, and assigns these sub-graphs to feasible devices to training. All devices use a same mini-batch to train.</p><p><code>Data parallelism</code> uses the same model for every device, but train the model in each device using different training samples. Each device holds a entire model, but trains with partial samples from the mini-batch.</p><p><code>Model Computation Pipelining</code> pipelines the computation of seveal same models within one device by running a small number of concurrent steps. </p><h3 id="1-1-Model-parallelism"><a href="#1-1-Model-parallelism" class="headerlink" title="1.1 Model parallelism"></a>1.1 Model parallelism</h3><p>Model parallel training, where different portions of the model computation are done on different computational devices simultaneously for the same batch of examples, as the following figure:</p><p><img src="1.png" alt="1"></p><p>It is challenging to get good performance, because some layers may depend on previous layers which leads to a long waiting time. However, if a model has some components which can run in parallel, it can use this method to improve the efficiency.</p><h3 id="1-2-Data-parallelism"><a href="#1-2-Data-parallelism" class="headerlink" title="1.2 Data parallelism"></a>1.2 Data parallelism</h3><p>In modern deep learning, because the dataset is too big to be fit into the memory, we could only do stochastic gradient descent(SGD) for batches. The shortcoming of SGD is that the estimate of the gradients might not accurately represent the true gradients of using the full dataset. Therefore, it may take much longer to converge.</p><p>Data parallelism is a simple technique for speeding up SGD is to parallelize the computation of the gradient for a mini-batch across devices.</p><p>Each device will independently compute the loss the gradients of small batches, the final estimate of the gradients is the weighted average of the gradients calculated from all the small batches(require communication).</p><p>By using data parallelism, the model can train on a large batch size. For example, the folloing figure shows a typical data parallelism, distributing 32 different images to each of the 256 GPUs running a single model. Together, the total mini-batch size for an iteration is 8,092 images (32 x 256) (Facebook: Training ImageNet in 1 Hour).</p><p><img src="2.png" alt="2"></p><p>Mathematically, data parallelism is valid because:</p><p><img src="3.png" alt="3"></p><p>* m1+m2+⋯+mk=n.</p><p>When m1=m2=⋯=mk=nk, we could further have:</p><p><img src="4.png" alt="4"></p><h4 id="1-2-1-Synchronous-vs-asynchronous-training"><a href="#1-2-1-Synchronous-vs-asynchronous-training" class="headerlink" title="1.2.1 Synchronous vs asynchronous training"></a>1.2.1 Synchronous vs asynchronous training</h4><p>In synchronous training(as following figure), all of the devices train their local model using different parts of data from a single (large) mini-batch. They then communicate their locally calculated gradients (directly or indirectly) to all devices. </p><p>Only after all devices have successfully computed and sent their gradients is the model updated. The updated model is then sent to all nodes along with splits from the next mini-batch. That is, devices train on non-overlapping splits (subset) of the mini-batch.</p><p><img src="5.png" alt="5"></p><p>In asynchronous training, no device waits for updates to the model from any other device. The devices can run independently and share results as peers, or communicate through one or more central servers known as “parameter” servers. </p><p><img src="6.png" alt="6"></p><p>In synchronous training, the parameter servers compute the latest up-to-date version of the model, and send it back to devices. In asynchronous training, parameter servers send gradients to devices that locally compute the new model.</p><h4 id="1-2-2-TensorFlow-Strategy"><a href="#1-2-2-TensorFlow-Strategy" class="headerlink" title="1.2.2 TensorFlow Strategy"></a>1.2.2 TensorFlow Strategy</h4><p><code>tf.distribute.Strategy</code> is a TensorFlow API to distribute training across multiple GPUs, multiple machines or TPUs. Using this API. </p><p>It intends to cover a number of use cases along different axes, including:</p><ul><li>synchronous vs asynchronous training, </li><li>hardware platform(multiple GPUs on one machine, or multiple machines in a network, or on Cloud TPUs).</li></ul><h5 id="MirroredStrategy"><a href="#MirroredStrategy" class="headerlink" title="MirroredStrategy"></a>MirroredStrategy</h5><p>tf.distribute.MirroredStrategy supports synchronous distributed training on <strong>multiple GPUs on one machine.</strong> </p><p>It creates one replica per GPU device. During training, one mini-batch is split into N parts and each part feed to one GPU device. </p><p>Efficient <strong>all-reduce</strong> algorithms are used to communicate the variable updates across the devices. By default, it uses NVIDIA NCCL as the all-reduce implementation. Currently, tf.distribute.HierarchicalCopyAllReduce and tf.distribute.ReductionToOneDevice are two options other than tf.distribute.NcclAllReduce which is the default.</p><h5 id="CentralStorageStrategy"><a href="#CentralStorageStrategy" class="headerlink" title="CentralStorageStrategy"></a>CentralStorageStrategy</h5><p>tf.distribute.experimental.CentralStorageStrategy does synchronous training as well. <strong>Variables are not mirrored, instead they are placed on the CPU</strong> and operations are replicated across all local GPUs. If there is only one GPU, all variables and operations will be placed on that GPU.</p><h5 id="MultiWorkerMirroredStrategy"><a href="#MultiWorkerMirroredStrategy" class="headerlink" title="MultiWorkerMirroredStrategy"></a>MultiWorkerMirroredStrategy</h5><p>tf.distribute.experimental.MultiWorkerMirroredStrategy is very similar to MirroredStrategy. It implements <strong>synchronous distributed training across multiple workers, each with potentially multiple GPUs</strong>. Similar to MirroredStrategy, it creates copies of all variables in the model on each device across all workers.</p><p>It uses <strong>CollectiveOps</strong> as the multi-worker all-reduce communication method used to keep variables in sync. </p><p>A collective op is a single op in the TensorFlow graph which can automatically choose an all-reduce algorithm in the TensorFlow runtime according to hardware, network topology and tensor sizes.</p><p>MultiWorkerMirroredStrategy currently allows two different implementations of collective ops：</p><ul><li>CollectiveCommunication.RING, ring algorithms for all-reduce and all-gather.</li><li>CollectiveCommunication.NCCL, ncclAllReduce for all-reduce, and ring algorithms for all-gather.</li></ul><h5 id="TPUStrategy"><a href="#TPUStrategy" class="headerlink" title="TPUStrategy"></a>TPUStrategy</h5><p>tf.distribute.experimental.TPUStrategy lets you run your TensorFlow training on Tensor Processing Units (TPUs). </p><h5 id="ParameterServerStrategy"><a href="#ParameterServerStrategy" class="headerlink" title="ParameterServerStrategy"></a>ParameterServerStrategy</h5><p>tf.distribute.experimental.ParameterServerStrategy supports parameter servers training on multiple machines. In this setup, some machines are designated as workers and some as parameter servers. <strong>Each variable of the model is placed on one parameter server. Computation is replicated across all GPUs of all the workers.</strong></p><h5 id="OneDeviceStrategy"><a href="#OneDeviceStrategy" class="headerlink" title="OneDeviceStrategy"></a>OneDeviceStrategy</h5><p>tf.distribute.OneDeviceStrategy runs on a <strong>single device</strong>. This strategy will place any variables created in its scope on the specified device. Input distributed through this strategy will be prefetched to the specified device. </p><p>You can use this strategy to test your code before switching to other strategies which actually distributes to multiple devices/machines.</p><h3 id="1-3-Model-Computation-Pipelining"><a href="#1-3-Model-Computation-Pipelining" class="headerlink" title="1.3 Model Computation Pipelining"></a>1.3 Model Computation Pipelining</h3><p>Another common way to get better utilization for training deep neural networks is to pipeline the computation of the model within the same devices, by running a small number of concurrent steps within the same set of devices.</p><p>It is somewhat similar to asynchronous data parallelism, except that the parallelism occurs within the same device(s), rather than replicating the computation graph on different devices. </p><p><img src="7.png" alt="7"></p><p>This allows “filling in the gaps” where computation of a single batch of examples might not be able to fully utilize the full parallelism on all devices at all times during a single step.</p><hr><h2 id="2-Graph-optimization-with-Grappler"><a href="#2-Graph-optimization-with-Grappler" class="headerlink" title="2 Graph optimization with Grappler"></a>2 Graph optimization with Grappler</h2><p>Graph is the default graph optimization system in the TF runtime to:</p><ul><li><strong>Automatically improve TF performance</strong> through graph simplifications &amp; high-level optimizations</li><li><strong>Reduce device peak memory usage</strong> to enable larger models to run</li><li><strong>Improve hardware utilization</strong> by optimizing the mapping of graph nodes to compute resources</li></ul><p><img src="8.png" alt="8"></p><h3 id="2-1-MetaOptimizer"><a href="#2-1-MetaOptimizer" class="headerlink" title="2.1 MetaOptimizer"></a>2.1 MetaOptimizer</h3><p>MetaOptimizer is the top-level driver invoked by runtime or standalone tool, it Runs multiple sub-optimizers in a loop: (* = not on by default):</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">i = <span class="number">0</span></span><br><span class="line"><span class="keyword">while</span> i &lt; <span class="built_in">config</span>.meta_optimizer_iterations (<span class="keyword">default</span>=<span class="number">2</span>):</span><br><span class="line">Pruning() # Remove nodes <span class="keyword">not</span> in fanin of outputs, unused functions</span><br><span class="line">Function() # Function specialization &amp; inlining, symbolic gradient inlining</span><br><span class="line">CommonSubgraphElimination() # dedup Subgraph</span><br><span class="line">DebugStripper ()* # Remove assert, <span class="built_in">print</span>, check_numerics</span><br><span class="line">ConstFold () # Constant folding <span class="keyword">and</span> materialization</span><br><span class="line">Shape() # Symbolic shape arithmetic</span><br><span class="line">AutoMixedPrecision() # Converts data types to float16 where applicable to improve performance</span><br><span class="line">PinToHost()* # Swaps small operations onto the CPU</span><br><span class="line">Arithmetic() # Node deduping (CSE) &amp; arithmetic simplification</span><br><span class="line"><span class="keyword">if</span> i==<span class="number">0</span>: Layout() # Layout optimization <span class="keyword">for</span> GPU</span><br><span class="line">Remapper() # Op fusion</span><br><span class="line">Loop() # Loop Invariant Node Motion*, Stack Push &amp; Dead Node Elimination</span><br><span class="line">Dependency () # Prune/optimize control edges, NoOp/Identity node pruning</span><br><span class="line"><span class="keyword">if</span> i==<span class="number">0</span>: Memory() # Swap-out/Swap-in, Recompute*, split large nodes</span><br><span class="line">Custom() # Run registered custom optimizers (e.g. TensorRT)</span><br><span class="line">AutoParallel()* # Automatically parallelizes graphs by splitting along the batch dimension.</span><br><span class="line">Scopedallocator() # must <span class="built_in">run</span> last: reduce data movement <span class="keyword">and</span> to consolidate some operations.</span><br><span class="line">i += <span class="number">1</span></span><br></pre></td></tr></table></figure><h3 id="2-2-Pruning-optimizer"><a href="#2-2-Pruning-optimizer" class="headerlink" title="2.2 Pruning optimizer"></a>2.2 Pruning optimizer</h3><p>Prunes nodes that have no effect on the output from the graph. It is usually run first to reduce the size of the graph and speed up processing in other Grappler passes.</p><p>Typically, this optimizer removes some <code>StopGradient</code> nodes and <code>Identity</code> nodes. For example, as the folloing figure, the Identity node is moved to a new branch.</p><table><thead><tr><th></th><th></th></tr></thead><tbody><tr><td><img src="9.png" alt="9"></td><td><img src="10.png" alt="10"></td></tr></tbody></table><h3 id="2-3-Function-optimizer"><a href="#2-3-Function-optimizer" class="headerlink" title="2.3 Function optimizer"></a>2.3 Function optimizer</h3><p>Optimizes the function library of a TensorFlow program and inlines function bodies to enable other inter-procedural optimizations.</p><h3 id="2-4-Common-subgraph-elimination"><a href="#2-4-Common-subgraph-elimination" class="headerlink" title="2.4 Common subgraph elimination"></a>2.4 Common subgraph elimination</h3><p>This optimizer travels the entire graph to find and dedup same subgraphs.</p><h3 id="2-5-Debug-stripper"><a href="#2-5-Debug-stripper" class="headerlink" title="2.5 Debug stripper"></a>2.5 Debug stripper</h3><p>Strips nodes related to debugging operations such as tf.debugging.Assert, tf.debugging.check_numerics, and tf.print from the graph. </p><p>This optimizer is turned OFF by default.</p><h3 id="2-6-Constant-folding-optimizer"><a href="#2-6-Constant-folding-optimizer" class="headerlink" title="2.6 Constant folding optimizer"></a>2.6 Constant folding optimizer</h3><p>Statically infers the value of tensors when possible by folding constant nodes in the graph and materializes the result using constants.</p><p>This optimizer has three methods: <code>MaterializeShapes</code>, <code>FoldGraph</code>, and <code>SimplifyGraph</code>.</p><p><code>MaterializeShapes</code> handles three nodes: Shape, Size, and Rank. Because these three nodes depend on the shape of input tensor, so it doesn’t have relationship with the value of the tensor. <code>MaterializeShapes</code> replaces these three node with <code>Const</code> node.</p><p><code>FoldGraph</code> folds the node whose all inputs are <code>Const</code> node, because its output can be pre-computed.</p><p><code>SimplifyGraph</code> handles: </p><ul><li>Constant push-down:<ul><li>Add(c1, Add(x, c2)) =&gt; Add(x, c1 + c2)</li><li>ConvND(c1 * x, c2) =&gt; ConvND(x, c1 * c2)</li></ul></li><li>Partial constfold:<ul><li>AddN(c1, x, c2, y) =&gt; AddN(c1 + c2, x, y),</li><li>Concat([x, c1, c2, y]) = Concat([x, Concat([c1, c2]), y)</li></ul></li><li>Operations with neutral &amp; absorbing elements:<ul><li>x * Ones(s) =&gt; Identity(x), if shape(x) == output_shape</li><li>x * Ones(s) =&gt; BroadcastTo(x, Shape(s)), if shape(s) == output_shape</li><li>Same for x + Zeros(s) , x / Ones(s), x * Zeros(s) etc.</li><li>Zeros(s) - y =&gt; Neg(y), if shape(y) == output_shape</li><li>Ones(s) / y =&gt; Recip(y) if shape(y) == output_shape</li></ul></li></ul><h3 id="2-7-Shape-optimizer"><a href="#2-7-Shape-optimizer" class="headerlink" title="2.7 Shape optimizer"></a>2.7 Shape optimizer</h3><p>Optimizes subgraphs that operate on shape and shape related information.</p><h3 id="2-8-Auto-mixed-precision-optimizer"><a href="#2-8-Auto-mixed-precision-optimizer" class="headerlink" title="2.8 Auto mixed precision optimizer"></a>2.8 Auto mixed precision optimizer</h3><p>Converts data types to float16 where applicable to improve performance. Currently applies only to GPUs.</p><h3 id="2-9-Pin-to-host-optimizer"><a href="#2-9-Pin-to-host-optimizer" class="headerlink" title="2.9 Pin to host optimizer"></a>2.9 Pin to host optimizer</h3><p>Swaps small operations onto the CPU. </p><p>This optimizer is turned OFF by default.</p><h3 id="2-10-Arithmetic-optimizer"><a href="#2-10-Arithmetic-optimizer" class="headerlink" title="2.10 Arithmetic optimizer"></a>2.10 Arithmetic optimizer</h3><p>Simplifies arithmetic operations by eliminating common subexpressions and simplifying arithmetic statements.</p><ul><li>Arithmetic simplifications<ul><li>Flattening: a+b+c+d =&gt; AddN(a, b, c, d)</li><li>Hoisting: AddN(x * a, b * x, x * c) =&gt; x * AddN(a+b+c)</li><li>Simplification to reduce number of nodes:<ul><li>Numeric: x+x+x =&gt; 3*x</li><li>Logic: !(x &gt; y) =&gt; x &lt;= y</li></ul></li></ul></li><li>Broadcast minimization<ul><li>Example: (matrix1 + scalar1) + (matrix2 + scalar2) =&gt; (matrix1 + matrix2) + (scalar1 + scalar2)</li></ul></li><li>Better use of intrinsics<ul><li>Matmul(Transpose(x), y) =&gt; Matmul(x, y, transpose_x=True)</li></ul></li><li>Remove redundant ops or op pairs<ul><li>Transpose(Transpose(x, perm), inverse_perm)</li><li>BitCast(BitCast(x, dtype1), dtype2) =&gt; BitCast(x, dtype2)</li><li>Pairs of elementwise involutions f(f(x)) =&gt; x (Neg, Conj, Reciprocal, LogicalNot)</li><li>Repeated Idempotent ops f(f(x)) =&gt; f(x) (DeepCopy, Identity, CheckNumerics…)</li></ul></li><li>Hoist chains of unary ops at Concat/Split/SplitV<ul><li>Concat([Exp(Cos(x)), Exp(Cos(y)), Exp(Cos(z))]) =&gt; Exp(Cos(Concat([x, y, z])))</li><li>[Exp(Cos(y)) for y in Split(x)] =&gt; Split(Exp(Cos(x), num_splits)</li></ul></li></ul><h3 id="2-11-Layout-optimizer"><a href="#2-11-Layout-optimizer" class="headerlink" title="2.11 Layout optimizer"></a>2.11 Layout optimizer</h3><p>Optimizes tensor layouts to execute data format dependent operations such as convolutions more efficiently.</p><p>For some nodes including AvgPool, Conv2D, etc, they supports two types of input format, <code>NHWC</code> and <code>NCHW</code>. But at the GPU runtime kernel, the <code>NCHW</code> data foramt is more efficient. This optimize adds a node to transfer the data format before these nodes.</p><p>For example, the following original graph with all ops in NHWC format<br><img src="11.png" alt="11"></p><p>Phase 1, expand by inserting conversion pairs:<br><img src="12.png" alt="12"></p><p>Phase 2, collapse adjacent conversion pairs:<br><img src="13.png" alt="13"></p><p>This optimizer only runs at the first iteration.</p><h3 id="2-12-Remapper-optimizer"><a href="#2-12-Remapper-optimizer" class="headerlink" title="2.12 Remapper optimizer"></a>2.12 Remapper optimizer</h3><p>Remaps subgraphs onto more efficient implementations by replacing commonly occuring subgraphs with optimized fused monolithic kernels.</p><p>Replaces commonly occurring subgraphs with optimized fused “monolithic” kernels:</p><ul><li>Conv2D + BiasAdd + &lt;Activation&gt;</li><li>Conv2D + FusedBatchNorm + &lt;Activation&gt;</li><li>Conv2D + Squeeze + BiasAdd</li><li>MatMul + BiasAdd + &lt;Activation&gt;</li></ul><p>Several performance advantages:</p><ul><li>Completely eliminates Op scheduling overhead</li><li>Improves temporal and spatial locality of data access<ul><li>E.g. MatMul is computed block-wise and bias and activation function can be<br>applied while data is still “hot” in cache</li></ul></li></ul><h3 id="2-13-Loop-optimizer"><a href="#2-13-Loop-optimizer" class="headerlink" title="2.13 Loop optimizer"></a>2.13 Loop optimizer</h3><p>Optimizes the graph control flow by hoisting loop-invariant subgraphs out of loops and by removing redundant stack operations in loops. Also optimizes loops with statically known trip counts and removes statically known dead branches in conditionals.</p><ul><li>Loop Invariant Node Motion<figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">    x = y + z;</span><br><span class="line">    a[i] = <span class="number">6</span> * i + x * x;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// Motion the y+z and x*x</span></span><br><span class="line">x = y + z;</span><br><span class="line">t1 = x * x;</span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">    a[i] = <span class="number">6</span> * i + t1;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li>StackPush removal<ul><li>Remove StackPushes without consumers</li></ul></li><li>Dead Branch Elimination</li><li>Deduce loop trip count statically<ul><li>Remove loop for zero trip count</li><li>Remove control flow nodes for trip count == 1</li></ul></li></ul><h3 id="2-14-Dependency-optimizer"><a href="#2-14-Dependency-optimizer" class="headerlink" title="2.14 Dependency optimizer"></a>2.14 Dependency optimizer</h3><p>Removes or rearranges <strong>control dependencies</strong> to shorten the critical path for a model step or enables other optimizations. Also removes nodes that are effectively no-ops such as Identity.</p><p>A control edge is redundant iff there exists a path of length &gt; 1 from source to control target:<br><img src="17.png" alt="17"></p><h3 id="2-15-Memory-optimizer"><a href="#2-15-Memory-optimizer" class="headerlink" title="2.15 Memory optimizer"></a>2.15 Memory optimizer</h3><p>Analyzes the graph to inspect the peak memory usage for each operation and inserts CPU-GPU memory copy operations for swapping GPU memory to CPU to reduce the peak memory usage.</p><p>Memory optimization based on abstract interpretation</p><ul><li>Swap-out / Swap-in optimization<ul><li>Reduces device memory usage by swapping to host memory</li><li>Uses memory cost model to estimate peak memory</li><li>Uses op cost model to schedule Swap-In at (roughly) the right time</li></ul></li><li>Recomputation optimization (not on by default)</li></ul><p>Peak Memory Characterization:<br><img src="14.png" alt="14"></p><p>Swapping (start early):<br><img src="15.png" alt="15"></p><p>Recomputation:<br><img src="16.png" alt="16"></p><p>This optimizer only runs at the first iteration.</p><h3 id="2-16-Autoparallel-optimizer"><a href="#2-16-Autoparallel-optimizer" class="headerlink" title="2.16 Autoparallel optimizer"></a>2.16 Autoparallel optimizer</h3><p>Automatically parallelizes graphs by splitting along the batch dimension. </p><p>AutoParallel is similar to the MirroredStrategy, however, the AutoParallel implements the parallel training by modifying the graph, instead of using replicated mulitiply models.</p><p>For example, the following graph shows a graph, the Dequeue node fetches some samples from the FIFO node, add with Const node and as the input of ApplyGradient node whose logic is <code>var −= add * learning_rate</code>.<br><img src="18.png" alt="18"></p><p>After applying AutoParallel with Replica=2, the following graph shows, some nodes keep same(FIFO), some nodes are duplicated(add, Dequeue), and some new nodes are added(Div).<br><img src="19.png" alt="19"></p><p>These two ApplyGradientDescent nodes can run parallelly to compute:</p><p><code>var −= add(replica−0)/2 * learning_rate(replica−0)</code><br>​<br><code>var −= add(replica−1)/2 * learning_rate(replica−1)</code></p><p>This optimizer is turned OFF by default.</p><h3 id="2-17-Scoped-allocator-optimizer"><a href="#2-17-Scoped-allocator-optimizer" class="headerlink" title="2.17 Scoped allocator optimizer"></a>2.17 Scoped allocator optimizer</h3><p>Introduces scoped allocators to reduce data movement and to consolidate some operations.</p><p>This optimizer only runs at the last iteration.</p><hr><h2 id="3-Distributed-architecture-unfinished"><a href="#3-Distributed-architecture-unfinished" class="headerlink" title="3 Distributed architecture (unfinished)"></a>3 Distributed architecture (unfinished)</h2><hr><h2 id="Reference"><a href="#Reference" class="headerlink" title="Reference"></a>Reference</h2><ol><li><a href="https://www.oreilly.com/content/distributed-tensorflow/" target="_blank" rel="noopener">https://www.oreilly.com/content/distributed-tensorflow/</a></li><li><a href="https://www.tensorflow.org/guide/distributed_training" target="_blank" rel="noopener">https://www.tensorflow.org/guide/distributed_training</a></li><li><a href="https://www.tensorflow.org/guide/graph_optimization" target="_blank" rel="noopener">https://www.tensorflow.org/guide/graph_optimization</a></li><li><a href="https://web.stanford.edu/class/cs245/slides/TFGraphOptimizationsStanford.pdf" target="_blank" rel="noopener">https://web.stanford.edu/class/cs245/slides/TFGraphOptimizationsStanford.pdf</a></li><li>Abadi, M., Agarwal, A., Barham, P., Brevdo, E., Chen, Z., Citro, C., … &amp; Ghemawat, S. (2016). Tensorflow: Large-scale machine learning on heterogeneous distributed systems. arXiv preprint arXiv:1603.04467.</li><li>Sergeev, A., &amp; Del Balso, M. (2018). Horovod: fast and easy distributed deep learning in TensorFlow. arXiv preprint arXiv:1802.05799.</li><li>Abadi, M., Barham, P., Chen, J., Chen, Z., Davis, A., Dean, J., … &amp; Kudlur, M. (2016). Tensorflow: A system for large-scale machine learning. In 12th {USENIX} Symposium on Operating Systems Design and Implementation ({OSDI} 16) (pp. 265-283).</li><li>Mirhoseini, A., Pham, H., Le, Q. V., Steiner, B., Larsen, R., Zhou, Y., … &amp; Dean, J. (2017, August). Device placement optimization with reinforcement learning. In Proceedings of the 34th International Conference on Machine Learning-Volume 70 (pp. 2430-2439). JMLR. org.</li><li><a href="https://leimao.github.io/blog/Data-Parallelism-vs-Model-Paralelism/" target="_blank" rel="noopener">https://leimao.github.io/blog/Data-Parallelism-vs-Model-Paralelism/</a></li><li>Goyal, P., Dollár, P., Girshick, R., Noordhuis, P., Wesolowski, L., Kyrola, A., … &amp; He, K. (2017). Accurate, large minibatch sgd: Training imagenet in 1 hour. arXiv preprint arXiv:1706.02677.</li><li><a href="https://github.com/tensorflow/tensorflow/blob/master/tensorflow/core/grappler/optimizers/meta_optimizer.cc" target="_blank" rel="noopener">https://github.com/tensorflow/tensorflow/blob/master/tensorflow/core/grappler/optimizers/meta_optimizer.cc</a></li></ol>]]></content>
    
    <summary type="html">
    
      Distributed Tensorflow
    
    </summary>
    
    
      <category term="Study Notes" scheme="https://joddiy.github.io/categories/Study-Notes/"/>
    
    
      <category term="Tools" scheme="https://joddiy.github.io/tags/Tools/"/>
    
      <category term="Deep Learning" scheme="https://joddiy.github.io/tags/Deep-Learning/"/>
    
      <category term="Tensorflow" scheme="https://joddiy.github.io/tags/Tensorflow/"/>
    
  </entry>
  
  <entry>
    <title>C++ Notes</title>
    <link href="https://joddiy.github.io/2020/05/11/C-Plus-Plus/"/>
    <id>https://joddiy.github.io/2020/05/11/C-Plus-Plus/</id>
    <published>2020-05-10T18:16:54.000Z</published>
    <updated>2020-06-30T18:14:03.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="C-数据类型"><a href="#C-数据类型" class="headerlink" title="C++ 数据类型"></a>C++ 数据类型</h2><h3 id="基本的内置类型"><a href="#基本的内置类型" class="headerlink" title="基本的内置类型"></a>基本的内置类型</h3><p>bool char int float double void wchar_t</p><h3 id="枚举类型"><a href="#枚举类型" class="headerlink" title="枚举类型"></a>枚举类型</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">enum</span> color &#123; red, green, blue &#125; c;</span><br><span class="line"></span><br><span class="line"><span class="keyword">enum</span> color &#123; red, green=<span class="number">5</span>, blue &#125;;</span><br></pre></td></tr></table></figure><hr><h2 id="C-变量类型"><a href="#C-变量类型" class="headerlink" title="C++ 变量类型"></a>C++ 变量类型</h2><h3 id="C-中的变量声明"><a href="#C-中的变量声明" class="headerlink" title="C++ 中的变量声明"></a>C++ 中的变量声明</h3><p>您可以使用 extern 关键字在任何地方声明一个变量</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"> </span><br><span class="line"><span class="comment">// 变量声明</span></span><br><span class="line"><span class="keyword">extern</span> <span class="keyword">int</span> a, b;</span><br><span class="line"><span class="keyword">extern</span> <span class="keyword">int</span> c;</span><br><span class="line"><span class="keyword">extern</span> <span class="keyword">float</span> f;</span><br><span class="line">  </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span> <span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="comment">// 变量定义</span></span><br><span class="line">  <span class="keyword">int</span> a, b;</span><br><span class="line">  <span class="keyword">int</span> c;</span><br><span class="line">  <span class="keyword">float</span> f;</span><br><span class="line"> </span><br><span class="line">  <span class="comment">// 实际初始化</span></span><br><span class="line">  a = <span class="number">10</span>;</span><br><span class="line">  b = <span class="number">20</span>;</span><br><span class="line">  c = a + b;</span><br><span class="line"> </span><br><span class="line">  <span class="built_in">cout</span> &lt;&lt; c &lt;&lt; <span class="built_in">endl</span> ;</span><br><span class="line"> </span><br><span class="line">  f = <span class="number">70.0</span>/<span class="number">3.0</span>;</span><br><span class="line">  <span class="built_in">cout</span> &lt;&lt; f &lt;&lt; <span class="built_in">endl</span> ;</span><br><span class="line"> </span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>同样的，在函数声明时，提供一个函数名，而函数的实际定义则可以在任何地方进行。例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 函数声明</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">func</span><span class="params">()</span></span>;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// 函数调用</span></span><br><span class="line">    <span class="keyword">int</span> i = func();</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="comment">// 函数定义</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">func</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h2 id="C-存储类"><a href="#C-存储类" class="headerlink" title="C++ 存储类"></a>C++ 存储类</h2><p>下面列出 C++ 程序中可用的存储类：</p><ul><li>auto</li><li>register</li><li>static</li><li>extern</li><li>mutable</li><li>thread_local (C++11)</li></ul><h3 id="auto-存储类"><a href="#auto-存储类" class="headerlink" title="auto 存储类"></a>auto 存储类</h3><p>自 C++ 11 以来，auto 关键字用于两种情况：声明变量时根据初始化表达式自动推断该变量的类型、声明函数时函数返回值的占位符。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">auto</span> f=<span class="number">3.14</span>;      <span class="comment">//double</span></span><br><span class="line"><span class="function"><span class="keyword">auto</span> <span class="title">s</span><span class="params">(<span class="string">"hello"</span>)</span></span>;  <span class="comment">//const char*</span></span><br><span class="line"><span class="keyword">auto</span> z = <span class="keyword">new</span> <span class="keyword">auto</span>(<span class="number">9</span>); <span class="comment">// int*</span></span><br><span class="line"><span class="keyword">auto</span> x1 = <span class="number">5</span>, x2 = <span class="number">5.0</span>, x3=<span class="string">'r'</span>;<span class="comment">//错误，必须是初始化为同一类型</span></span><br></pre></td></tr></table></figure><h3 id="register-存储类"><a href="#register-存储类" class="headerlink" title="register 存储类"></a>register 存储类</h3><p>register 存储类用于定义存储在寄存器中而不是 RAM 中的局部变量。这意味着变量的最大尺寸等于寄存器的大小（通常是一个词），且不能对它应用一元的 ‘&amp;’ 运算符（因为它没有内存位置）。</p><p>定义 ‘register’ 并不意味着变量将被存储在寄存器中，它意味着变量可能存储在寄存器中，这取决于硬件和实现的限制。</p><h3 id="static-存储类"><a href="#static-存储类" class="headerlink" title="static 存储类"></a>static 存储类</h3><p>static 存储类指示编译器在程序的生命周期内保持局部变量的存在，而不需要在每次它进入和离开作用域时进行创建和销毁。因此，使用 static 修饰局部变量可以在函数调用之间保持局部变量的值。</p><p>static 修饰符也可以应用于全局变量。当 static 修饰全局变量时，会使变量的作用域限制在声明它的文件内。</p><p>在 C++ 中，当 static 用在类数据成员上时，会导致仅有一个该成员的副本被类的所有对象共享。</p><h3 id="extern-存储类"><a href="#extern-存储类" class="headerlink" title="extern 存储类"></a>extern 存储类</h3><h3 id="mutable-存储类"><a href="#mutable-存储类" class="headerlink" title="mutable 存储类"></a>mutable 存储类</h3><h3 id="thread-local-存储类"><a href="#thread-local-存储类" class="headerlink" title="thread_local 存储类"></a>thread_local 存储类</h3><p>使用 thread_local 说明符声明的变量仅可在它在其上创建的线程上访问。 变量在创建线程时创建，并在销毁线程时销毁。 每个线程都有其自己的变量副本。</p><p>可以将 thread_local 仅应用于数据声明和定义，thread_local 不能用于函数声明或定义。</p><hr><h2 id="C-运算符"><a href="#C-运算符" class="headerlink" title="C++ 运算符"></a>C++ 运算符</h2><p>运算符是一种告诉编译器执行特定的数学或逻辑操作的符号。C++ 内置了丰富的运算符，并提供了以下类型的运算符：</p><ul><li>算术运算符</li><li>关系运算符</li><li>逻辑运算符</li><li>位运算符</li><li>赋值运算符</li><li>杂项运算符</li></ul><h3 id="杂项运算符"><a href="#杂项运算符" class="headerlink" title="杂项运算符"></a>杂项运算符</h3><p>下表列出了 C++ 支持的其他一些重要的运算符。</p><table><thead><tr><th>运算符</th><th>描述</th></tr></thead><tbody><tr><td>sizeof</td><td>sizeof 运算符返回变量的大小。例如，sizeof(a) 将返回 4，其中 a 是整数。</td></tr><tr><td>Condition ? X : Y</td><td>条件运算符。如果 Condition 为真 ? 则值为 X : 否则值为 Y。</td></tr><tr><td>,</td><td>逗号运算符会顺序执行一系列运算。整个逗号表达式的值是以逗号分隔的列表中的最后一个表达式的值。</td></tr><tr><td>.（点）和 -&gt;（箭头）</td><td>成员运算符用于引用类、结构和共用体的成员。</td></tr><tr><td>Cast</td><td>强制转换运算符把一种数据类型转换为另一种数据类型。例如，int(2.2000) 将返回 2。</td></tr><tr><td>&amp;</td><td>指针运算符 &amp; 返回变量的地址。例如 &a; 将给出变量的实际地址。</td></tr><tr><td>*</td><td>指针运算符 * 指向一个变量。例如，*var; 将指向变量 var。</td></tr></tbody></table><h3 id="C-中的运算符优先级"><a href="#C-中的运算符优先级" class="headerlink" title="C++ 中的运算符优先级"></a>C++ 中的运算符优先级</h3><table><thead><tr><th>类别</th><th>运算符</th><th>结合性</th></tr></thead><tbody><tr><td>后缀</td><td>() [] -&gt; . ++ - -</td><td>从左到右</td></tr><tr><td>一元</td><td>+ - ! ~ ++ - - (type)* &amp; sizeof</td><td>从右到左</td></tr><tr><td>乘除</td><td>* / %</td><td>从左到右</td></tr><tr><td>加减</td><td>+ -</td><td>从左到右</td></tr><tr><td>移位</td><td>&lt;&lt; &gt;&gt;</td><td>从左到右</td></tr><tr><td>关系</td><td>&lt; &lt;= &gt; &gt;=</td><td>从左到右</td></tr><tr><td>相等</td><td>== !=</td><td>从左到右</td></tr><tr><td>位与 AND</td><td>&amp;</td><td>从左到右</td></tr><tr><td>位异或 XOR</td><td>^</td><td>从左到右</td></tr><tr><td>位或 OR</td><td></td><td></td></tr><tr><td>逻辑与 AND</td><td>&amp;&amp;</td><td>从左到右</td></tr><tr><td>逻辑或 OR</td><td></td><td></td></tr><tr><td>条件</td><td>?:</td><td>从右到左</td></tr><tr><td>赋值</td><td>= += -= *= /= %=&gt;&gt;= &lt;&lt;= &amp;= ^=</td><td>=</td></tr><tr><td>逗号</td><td>,</td><td>从左到右</td></tr></tbody></table><hr><h2 id="C-函数"><a href="#C-函数" class="headerlink" title="C++ 函数"></a>C++ 函数</h2><h3 id="函数参数"><a href="#函数参数" class="headerlink" title="函数参数"></a>函数参数</h3><p>如果函数要使用参数，则必须声明接受参数值的变量。这些变量称为函数的形式参数。</p><p>形式参数就像函数内的其他局部变量，在进入函数时被创建，退出函数时被销毁。</p><p>当调用函数时，有三种向函数传递参数的方式：</p><table><thead><tr><th>调用类型</th><th>描述</th></tr></thead><tbody><tr><td>传值调用</td><td>该方法把参数的实际值赋值给函数的形式参数。在这种情况下，修改函数内的形式参数对实际参数没有影响。</td></tr><tr><td>指针调用</td><td>该方法把参数的地址赋值给形式参数。在函数内，该地址用于访问调用中要用到的实际参数。这意味着，修改形式参数会影响实际参数。</td></tr><tr><td>引用调用</td><td>该方法把参数的引用赋值给形式参数。在函数内，该引用用于访问调用中要用到的实际参数。这意味着，修改形式参数会影响实际参数。</td></tr></tbody></table><h4 id="C-引用调用"><a href="#C-引用调用" class="headerlink" title="C++ 引用调用"></a>C++ 引用调用</h4><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 函数定义</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">swap</span><span class="params">(<span class="keyword">int</span> &amp;x, <span class="keyword">int</span> &amp;y)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">   <span class="keyword">int</span> temp;</span><br><span class="line">   temp = x; <span class="comment">/* 保存地址 x 的值 */</span></span><br><span class="line">   x = y;    <span class="comment">/* 把 y 赋值给 x */</span></span><br><span class="line">   y = temp; <span class="comment">/* 把 x 赋值给 y  */</span></span><br><span class="line">  </span><br><span class="line">   <span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="C-指针调用"><a href="#C-指针调用" class="headerlink" title="C++ 指针调用"></a>C++ 指针调用</h4><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 函数定义</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">swap</span><span class="params">(<span class="keyword">int</span> *x, <span class="keyword">int</span> *y)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">   <span class="keyword">int</span> temp;</span><br><span class="line">   temp = *x;    <span class="comment">/* 保存地址 x 的值 */</span></span><br><span class="line">   *x = *y;        <span class="comment">/* 把 y 赋值给 x */</span></span><br><span class="line">   *y = temp;    <span class="comment">/* 把 x 赋值给 y */</span></span><br><span class="line">  </span><br><span class="line">   <span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="参数的默认值"><a href="#参数的默认值" class="headerlink" title="参数的默认值"></a>参数的默认值</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sum</span><span class="params">(<span class="keyword">int</span> a, <span class="keyword">int</span> b=<span class="number">20</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="keyword">int</span> result;</span><br><span class="line"> </span><br><span class="line">  result = a + b;</span><br><span class="line">  </span><br><span class="line">  <span class="keyword">return</span> (result);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="Lambda-函数与表达式"><a href="#Lambda-函数与表达式" class="headerlink" title="Lambda 函数与表达式"></a>Lambda 函数与表达式</h3>]]></content>
    
    <summary type="html">
    
      Study notes of C++
    
    </summary>
    
    
      <category term="Study Notes" scheme="https://joddiy.github.io/categories/Study-Notes/"/>
    
    
      <category term="Program language" scheme="https://joddiy.github.io/tags/Program-language/"/>
    
      <category term="C++" scheme="https://joddiy.github.io/tags/C/"/>
    
  </entry>
  
  <entry>
    <title>Distributed training with TensorFlow</title>
    <link href="https://joddiy.github.io/2020/05/09/TF2-0-Accelerators/"/>
    <id>https://joddiy.github.io/2020/05/09/TF2-0-Accelerators/</id>
    <published>2020-05-09T09:22:15.000Z</published>
    <updated>2020-05-12T19:52:43.000Z</updated>
    
    <content type="html"><![CDATA[<h1 id="Distributed-training-with-TensorFlow"><a href="#Distributed-training-with-TensorFlow" class="headerlink" title="Distributed training with TensorFlow"></a>Distributed training with TensorFlow</h1><p><a href="https://www.tensorflow.org/guide/distributed_training" target="_blank" rel="noopener">doc link</a></p><h2 id="Overview"><a href="#Overview" class="headerlink" title="Overview"></a>Overview</h2><p><code>tf.distribute.Strategy</code> is a TensorFlow API to distribute training across multiple GPUs, multiple machines or TPUs. Using this API, you can distribute your existing models and training code with minimal code changes.</p><hr><h2 id="Types-of-strategies"><a href="#Types-of-strategies" class="headerlink" title="Types of strategies"></a>Types of strategies</h2><p>tf.distribute.Strategy intends to cover a number of use cases along different axes. Some of these combinations are currently supported and others will be added in the future. Some of these axes are:</p><ul><li><p>Synchronous vs asynchronous training: These are two common ways of distributing training with data parallelism. In sync training, all workers train over different slices of input data in sync, and aggregating gradients at each step. In async training, all workers are independently training over the input data and updating variables asynchronously. Typically sync training is supported via all-reduce and async through parameter server architecture.</p></li><li><p>Hardware platform: You may want to scale your training onto multiple GPUs on one machine, or multiple machines in a network (with 0 or more GPUs each), or on Cloud TPUs.</p></li></ul><table><thead><tr><th>Training API</th><th>MirroredStrategy</th><th>TPUStrategy</th><th>MultiWorkerMirroredStrategy</th></tr></thead><tbody><tr><td>Keras API</td><td>Supported</td><td>Experimental support</td><td>Experimental support</td></tr><tr><td>Custom training loop</td><td>Experimental support</td><td>Experimental support</td><td>Support planned post 2.0</td></tr><tr><td>Estimator API</td><td>Limited Support</td><td>Not supported</td><td>Limited Support</td></tr></tbody></table><table><thead><tr><th>CentralStorageStrategy</th><th>ParameterServerStrategy</th><th>OneDeviceStrategy</th></tr></thead><tbody><tr><td>Experimental support</td><td>Supported planned post 2.0</td><td>Supported</td></tr><tr><td>Support</td><td>planned post 2.0</td><td>No support yet</td></tr><tr><td>Limited Support</td><td>Limited Support</td><td>Limited Support</td></tr></tbody></table><h3 id="MirroredStrategy"><a href="#MirroredStrategy" class="headerlink" title="MirroredStrategy"></a>MirroredStrategy</h3><p>tf.distribute.MirroredStrategy supports synchronous distributed training on multiple GPUs on one machine. It creates one replica per GPU device. Each variable in the model is mirrored across all the replicas. Together, these variables form a single conceptual variable called MirroredVariable. These variables are kept in sync with each other by applying identical updates.</p><p>Efficient <strong>all-reduce</strong> algorithms are used to communicate the variable updates across the devices.</p><p>All-reduce aggregates tensors across all the devices by adding them up, and makes them available on each device. It’s a fused algorithm that is very efficient and can reduce the overhead of synchronization significantly. </p><p>There are many all-reduce algorithms and implementations available, depending on the type of communication available between devices. By default, it uses NVIDIA NCCL as the all-reduce implementation. You can choose from a few other options we provide, or write your own.</p><p>Here is the simplest way of creating MirroredStrategy:</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">mirrored_strategy = tf.distribute.MirroredStrategy()</span><br></pre></td></tr></table></figure><p>This will create a MirroredStrategy instance which will use all the GPUs that are visible to TensorFlow, and use NCCL as the cross device communication.</p><p>If you wish to use only some of the GPUs on your machine, you can do so like this:</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">mirrored_strategy = tf.distribute.MirroredStrategy(devices=[<span class="string">"/gpu:0"</span>, <span class="string">"/gpu:1"</span>])</span><br></pre></td></tr></table></figure><p>Currently, tf.distribute.HierarchicalCopyAllReduce and tf.distribute.ReductionToOneDevice are two options other than tf.distribute.NcclAllReduce which is the default.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">mirrored_strategy = tf.distribute.MirroredStrategy(</span><br><span class="line">    cross_device_ops=tf.distribute.HierarchicalCopyAllReduce())</span><br></pre></td></tr></table></figure><h3 id="CentralStorageStrategy"><a href="#CentralStorageStrategy" class="headerlink" title="CentralStorageStrategy"></a>CentralStorageStrategy</h3><p>tf.distribute.experimental.CentralStorageStrategy does synchronous training as well. Variables are not mirrored, instead they are placed on the CPU and operations are replicated across all local GPUs. If there is only one GPU, all variables and operations will be placed on that GPU.</p><p>Create an instance of CentralStorageStrategy by:</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">central_storage_strategy = tf.distribute.experimental.CentralStorageStrategy()</span><br></pre></td></tr></table></figure><h3 id="MultiWorkerMirroredStrategy"><a href="#MultiWorkerMirroredStrategy" class="headerlink" title="MultiWorkerMirroredStrategy"></a>MultiWorkerMirroredStrategy</h3><p>tf.distribute.experimental.MultiWorkerMirroredStrategy is very similar to MirroredStrategy. It implements synchronous distributed training across multiple workers, each with potentially multiple GPUs. Similar to MirroredStrategy, it creates copies of all variables in the model on each device across all workers.</p><p>It uses CollectiveOps as the multi-worker all-reduce communication method used to keep variables in sync. </p><p>A collective op is a single op in the TensorFlow graph which can automatically choose an all-reduce algorithm in the TensorFlow runtime according to hardware, network topology and tensor sizes.</p><p>It also implements additional performance optimizations. For example, it includes a static optimization that converts multiple all-reductions on small tensors into fewer all-reductions on larger tensors.</p><div class="alert warning"><p>How?</p></div><p>MultiWorkerMirroredStrategy currently allows you to choose between two different implementations of collective ops. CollectiveCommunication.RING implements ring-based collectives using gRPC as the communication layer. CollectiveCommunication.NCCL uses Nvidia’s NCCL to implement collectives.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">multiworker_strategy = tf.distribute.experimental.MultiWorkerMirroredStrategy(</span><br><span class="line">    tf.distribute.experimental.CollectiveCommunication.NCCL)</span><br></pre></td></tr></table></figure><h3 id="TPUStrategy"><a href="#TPUStrategy" class="headerlink" title="TPUStrategy"></a>TPUStrategy</h3><p>tf.distribute.experimental.TPUStrategy lets you run your TensorFlow training on Tensor Processing Units (TPUs). </p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">cluster_resolver = tf.distribute.cluster_resolver.TPUClusterResolver(</span><br><span class="line">    tpu=tpu_address)</span><br><span class="line">tf.config.experimental_connect_to_cluster(cluster_resolver)</span><br><span class="line">tf.tpu.experimental.initialize_tpu_system(cluster_resolver)</span><br><span class="line">tpu_strategy = tf.distribute.experimental.TPUStrategy(cluster_resolver)</span><br></pre></td></tr></table></figure><h3 id="ParameterServerStrategy"><a href="#ParameterServerStrategy" class="headerlink" title="ParameterServerStrategy"></a>ParameterServerStrategy</h3><p>tf.distribute.experimental.ParameterServerStrategy supports parameter servers training on multiple machines. In this setup, some machines are designated as workers and some as parameter servers. Each variable of the model is placed on one parameter server. Computation is replicated across all GPUs of all the workers.</p><h3 id="OneDeviceStrategy"><a href="#OneDeviceStrategy" class="headerlink" title="OneDeviceStrategy"></a>OneDeviceStrategy</h3><p>tf.distribute.OneDeviceStrategy runs on a single device. This strategy will place any variables created in its scope on the specified device. Input distributed through this strategy will be prefetched to the specified device. Moreover, any functions called via strategy.run will also be placed on the specified device.</p><p>You can use this strategy to test your code before switching to other strategies which actually distributes to multiple devices/machines.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">strategy = tf.distribute.OneDeviceStrategy(device=<span class="string">"/gpu:0"</span>)</span><br></pre></td></tr></table></figure><hr><h2 id="Using-tf-distribute-Strategy-with-Keras"><a href="#Using-tf-distribute-Strategy-with-Keras" class="headerlink" title="Using tf.distribute.Strategy with Keras"></a>Using tf.distribute.Strategy with Keras</h2><p>By integrating into tf.keras backend, we’ve made it seamless for you to distribute your training written in the Keras training framework.</p><p>Here’s what you need to change in your code:</p><ul><li>Create an instance of the appropriate tf.distribute.Strategy</li><li>Move the creation and compiling of Keras model inside strategy.scope.</li></ul><p>We support all types of Keras models - sequential, functional and subclassed.</p><p>Here is a snippet of code to do this for a very simple Keras model with one dense layer:</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">mirrored_strategy = tf.distribute.MirroredStrategy()</span><br><span class="line"><span class="keyword">with</span> mirrored_strategy.scope():</span><br><span class="line">  model = tf.keras.Sequential([tf.keras.layers.Dense(<span class="number">1</span>, input_shape=(<span class="number">1</span>,))])</span><br><span class="line">  model.compile(loss=<span class="string">'mse'</span>, optimizer=<span class="string">'sgd'</span>)</span><br></pre></td></tr></table></figure><p>In this example we used MirroredStrategy so we can run this on a machine with multiple GPUs. </p><p>strategy.scope() indicated which parts of the code to run distributed. Creating a model inside this scope allows us to create mirrored variables instead of regular variables. Compiling under the scope allows us to know that the user intends to train this model using this strategy. Once this is set up, you can fit your model like you would normally. </p><p>MirroredStrategy takes care of replicating the model’s training on the available GPUs, aggregating gradients, and more.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># tf.data.Dataset as dataset</span></span><br><span class="line">dataset = tf.data.Dataset.from_tensors(([<span class="number">1.</span>], [<span class="number">1.</span>])).repeat(<span class="number">100</span>).batch(<span class="number">10</span>)</span><br><span class="line">model.fit(dataset, epochs=<span class="number">2</span>)</span><br><span class="line">model.evaluate(dataset)</span><br><span class="line"></span><br><span class="line"><span class="comment"># np array as dataset</span></span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line">inputs, targets = np.ones((<span class="number">100</span>, <span class="number">1</span>)), np.ones((<span class="number">100</span>, <span class="number">1</span>))</span><br><span class="line">model.fit(inputs, targets, epochs=<span class="number">2</span>, batch_size=<span class="number">10</span>)</span><br></pre></td></tr></table></figure><p>In both cases (dataset or numpy), each batch of the given input is divided equally among the multiple replicas. For instance, if using MirroredStrategy with 2 GPUs, each batch of size 10 will get divided among the 2 GPUs, with each receiving 5 input examples in each step. </p><p>Each epoch will then train faster as you add more GPUs. Typically, you would want to increase your batch size as you add more accelerators so as to make effective use of the extra computing power. You will also need to re-tune your learning rate, depending on the model. </p><div class="alert warning"><p>How to merge these replicated models?</p></div><p>You can use strategy.num_replicas_in_sync to get the number of replicas.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Compute global batch size using number of replicas.</span></span><br><span class="line">BATCH_SIZE_PER_REPLICA = <span class="number">5</span></span><br><span class="line">global_batch_size = (BATCH_SIZE_PER_REPLICA *</span><br><span class="line">                     mirrored_strategy.num_replicas_in_sync)</span><br><span class="line">dataset = tf.data.Dataset.from_tensors(([<span class="number">1.</span>], [<span class="number">1.</span>])).repeat(<span class="number">100</span>)</span><br><span class="line">dataset = dataset.batch(global_batch_size)</span><br><span class="line"></span><br><span class="line">LEARNING_RATES_BY_BATCH_SIZE = &#123;<span class="number">5</span>: <span class="number">0.1</span>, <span class="number">10</span>: <span class="number">0.15</span>&#125;</span><br><span class="line">learning_rate = LEARNING_RATES_BY_BATCH_SIZE[global_batch_size]</span><br></pre></td></tr></table></figure><hr><h2 id="Using-tf-distribute-Strategy-with-custom-training-loops"><a href="#Using-tf-distribute-Strategy-with-custom-training-loops" class="headerlink" title="Using tf.distribute.Strategy with custom training loops"></a>Using tf.distribute.Strategy with custom training loops</h2><p>If you need more flexibility and control over your training loops than is possible with Estimator or Keras, you can write custom training loops. </p><p>For instance, when using a GAN, you may want to take a different number of generator or discriminator steps each round. Similarly, the high level frameworks are not very suitable for Reinforcement Learning training.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">with</span> mirrored_strategy.scope():</span><br><span class="line">  model = tf.keras.Sequential([tf.keras.layers.Dense(<span class="number">1</span>, input_shape=(<span class="number">1</span>,))])</span><br><span class="line">  optimizer = tf.keras.optimizers.SGD()</span><br><span class="line"></span><br><span class="line">dataset = tf.data.Dataset.from_tensors(([<span class="number">1.</span>], [<span class="number">1.</span>])).repeat(<span class="number">1000</span>).batch(</span><br><span class="line">    global_batch_size)</span><br><span class="line">dist_dataset = mirrored_strategy.experimental_distribute_dataset(dataset)</span><br><span class="line"></span><br><span class="line"><span class="meta">@tf.function</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">train_step</span><span class="params">(dist_inputs)</span>:</span></span><br><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">step_fn</span><span class="params">(inputs)</span>:</span></span><br><span class="line">    features, labels = inputs</span><br><span class="line"></span><br><span class="line">    <span class="keyword">with</span> tf.GradientTape() <span class="keyword">as</span> tape:</span><br><span class="line">      <span class="comment"># training=True is only needed if there are layers with different</span></span><br><span class="line">      <span class="comment"># behavior during training versus inference (e.g. Dropout).</span></span><br><span class="line">      logits = model(features, training=<span class="literal">True</span>)</span><br><span class="line">      cross_entropy = tf.nn.softmax_cross_entropy_with_logits(</span><br><span class="line">          logits=logits, labels=labels)</span><br><span class="line">      <span class="comment"># scaled the total loss by the global batch size</span></span><br><span class="line">      loss = tf.reduce_sum(cross_entropy) * (<span class="number">1.0</span> / global_batch_size)</span><br><span class="line"></span><br><span class="line">    grads = tape.gradient(loss, model.trainable_variables)</span><br><span class="line">    optimizer.apply_gradients(list(zip(grads, model.trainable_variables)))</span><br><span class="line">    <span class="keyword">return</span> cross_entropy</span><br><span class="line"></span><br><span class="line">  per_example_losses = mirrored_strategy.run(step_fn, args=(dist_inputs,))</span><br><span class="line">  mean_loss = mirrored_strategy.reduce(</span><br><span class="line">      tf.distribute.ReduceOp.MEAN, per_example_losses, axis=<span class="number">0</span>)</span><br><span class="line">  <span class="keyword">return</span> mean_loss</span><br><span class="line"></span><br><span class="line"> <span class="keyword">with</span> mirrored_strategy.scope():</span><br><span class="line">  <span class="keyword">for</span> inputs <span class="keyword">in</span> dist_dataset:</span><br><span class="line">    print(train_step(inputs))</span><br></pre></td></tr></table></figure><hr><h2 id="Using-tf-distribute-Strategy-with-Estimator-Limited-support"><a href="#Using-tf-distribute-Strategy-with-Estimator-Limited-support" class="headerlink" title="Using tf.distribute.Strategy with Estimator (Limited support)"></a>Using tf.distribute.Strategy with Estimator (Limited support)</h2><p>tf.estimator is a distributed training TensorFlow API that originally supported the async parameter server approach. </p><p>We pass the strategy object into the RunConfig for the Estimator.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">mirrored_strategy = tf.distribute.MirroredStrategy()</span><br><span class="line">config = tf.estimator.RunConfig(</span><br><span class="line">    train_distribute=mirrored_strategy, eval_distribute=mirrored_strategy)</span><br><span class="line">regressor = tf.estimator.LinearRegressor(</span><br><span class="line">    feature_columns=[tf.feature_column.numeric_column(<span class="string">'feats'</span>)],</span><br><span class="line">    optimizer=<span class="string">'SGD'</span>,</span><br><span class="line">    config=config)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">input_fn</span><span class="params">()</span>:</span></span><br><span class="line">  dataset = tf.data.Dataset.from_tensors((&#123;<span class="string">"feats"</span>:[<span class="number">1.</span>]&#125;, [<span class="number">1.</span>]))</span><br><span class="line">  <span class="keyword">return</span> dataset.repeat(<span class="number">1000</span>).batch(<span class="number">10</span>)</span><br><span class="line">regressor.train(input_fn=input_fn, steps=<span class="number">10</span>)</span><br><span class="line">regressor.evaluate(input_fn=input_fn, steps=<span class="number">10</span>)</span><br></pre></td></tr></table></figure><hr><p>Another difference to highlight here between Estimator and Keras is the input handling. In Estimator, we do not do automatic splitting of batch, nor automatically shard the data across different workers. You have full control over how you want your data to be distributed across workers and devices, and you must provide an input_fn to specify how to distribute your data.</p><p>Your input_fn is called once per worker, thus giving one dataset per worker. Then one batch from that dataset is fed to one replica on that worker, thereby consuming N batches for N replicas on 1 worker. In other words, the dataset returned by the input_fn should provide batches of size PER_REPLICA_BATCH_SIZE. And the global batch size for a step can be obtained as PER_REPLICA_BATCH_SIZE * strategy.num_replicas_in_sync.</p><h2 id="Use-a-GPU"><a href="#Use-a-GPU" class="headerlink" title="Use a GPU"></a>Use a GPU</h2><p>This guide is for users who have tried above approaches and found that they need fine-grained control of how TensorFlow uses the GPU.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> tensorflow <span class="keyword">as</span> tf</span><br><span class="line">print(<span class="string">"Num GPUs Available: "</span>, len(tf.config.experimental.list_physical_devices(<span class="string">'GPU'</span>)))</span><br></pre></td></tr></table></figure><h3 id="Logging-device-placement"><a href="#Logging-device-placement" class="headerlink" title="Logging device placement"></a>Logging device placement</h3><p>To find out which devices your operations and tensors are assigned to, put tf.debugging.set_log_device_placement(True) as the first statement of your program. Enabling device placement logging causes any Tensor allocations or operations to be printed.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">tf.debugging.set_log_device_placement(<span class="literal">True</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># Create some tensors</span></span><br><span class="line">a = tf.constant([[<span class="number">1.0</span>, <span class="number">2.0</span>, <span class="number">3.0</span>], [<span class="number">4.0</span>, <span class="number">5.0</span>, <span class="number">6.0</span>]])</span><br><span class="line">b = tf.constant([[<span class="number">1.0</span>, <span class="number">2.0</span>], [<span class="number">3.0</span>, <span class="number">4.0</span>], [<span class="number">5.0</span>, <span class="number">6.0</span>]])</span><br><span class="line">c = tf.matmul(a, b)</span><br><span class="line"></span><br><span class="line">print(c)</span><br></pre></td></tr></table></figure><h3 id="Manual-device-placement"><a href="#Manual-device-placement" class="headerlink" title="Manual device placement"></a>Manual device placement</h3><p>If you would like a particular operation to run on a device of your choice instead of what’s automatically selected for you, you can use with tf.device to create a device context, and all the operations within that context will run on the same designated device.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">tf.debugging.set_log_device_placement(<span class="literal">True</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># Place tensors on the CPU</span></span><br><span class="line"><span class="keyword">with</span> tf.device(<span class="string">'/CPU:0'</span>):</span><br><span class="line">  a = tf.constant([[<span class="number">1.0</span>, <span class="number">2.0</span>, <span class="number">3.0</span>], [<span class="number">4.0</span>, <span class="number">5.0</span>, <span class="number">6.0</span>]])</span><br><span class="line">  b = tf.constant([[<span class="number">1.0</span>, <span class="number">2.0</span>], [<span class="number">3.0</span>, <span class="number">4.0</span>], [<span class="number">5.0</span>, <span class="number">6.0</span>]])</span><br><span class="line"></span><br><span class="line">c = tf.matmul(a, b)</span><br><span class="line">print(c)</span><br></pre></td></tr></table></figure><h3 id="Limiting-GPU-memory-growth"><a href="#Limiting-GPU-memory-growth" class="headerlink" title="Limiting GPU memory growth"></a>Limiting GPU memory growth</h3><p>By default, TensorFlow maps nearly all of the GPU memory of all GPUs (subject to CUDA_VISIBLE_DEVICES) visible to the process. This is done to more efficiently use the relatively precious GPU memory resources on the devices by reducing memory fragmentation. To limit TensorFlow to a specific set of GPUs we use the tf.config.experimental.set_visible_devices method.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">gpus = tf.config.experimental.list_physical_devices(<span class="string">'GPU'</span>)</span><br><span class="line"><span class="keyword">if</span> gpus:</span><br><span class="line">  <span class="comment"># Restrict TensorFlow to only use the first GPU</span></span><br><span class="line">  <span class="keyword">try</span>:</span><br><span class="line">    tf.config.experimental.set_visible_devices(gpus[<span class="number">0</span>], <span class="string">'GPU'</span>)</span><br><span class="line">    logical_gpus = tf.config.experimental.list_logical_devices(<span class="string">'GPU'</span>)</span><br><span class="line">    print(len(gpus), <span class="string">"Physical GPUs,"</span>, len(logical_gpus), <span class="string">"Logical GPU"</span>)</span><br><span class="line">  <span class="keyword">except</span> RuntimeError <span class="keyword">as</span> e:</span><br><span class="line">    <span class="comment"># Visible devices must be set before GPUs have been initialized</span></span><br><span class="line">    print(e)</span><br></pre></td></tr></table></figure><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">gpus = tf.config.experimental.list_physical_devices(<span class="string">'GPU'</span>)</span><br><span class="line"><span class="keyword">if</span> gpus:</span><br><span class="line">  <span class="keyword">try</span>:</span><br><span class="line">    <span class="comment"># Currently, memory growth needs to be the same across GPUs</span></span><br><span class="line">    <span class="keyword">for</span> gpu <span class="keyword">in</span> gpus:</span><br><span class="line">      tf.config.experimental.set_memory_growth(gpu, <span class="literal">True</span>)</span><br><span class="line">    logical_gpus = tf.config.experimental.list_logical_devices(<span class="string">'GPU'</span>)</span><br><span class="line">    print(len(gpus), <span class="string">"Physical GPUs,"</span>, len(logical_gpus), <span class="string">"Logical GPUs"</span>)</span><br><span class="line">  <span class="keyword">except</span> RuntimeError <span class="keyword">as</span> e:</span><br><span class="line">    <span class="comment"># Memory growth must be set before GPUs have been initialized</span></span><br><span class="line">    print(e)</span><br></pre></td></tr></table></figure><p>The second method is to configure a virtual GPU device with tf.config.experimental.set_virtual_device_configuration and set a hard limit on the total memory to allocate on the GPU.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">gpus = tf.config.experimental.list_physical_devices(<span class="string">'GPU'</span>)</span><br><span class="line"><span class="keyword">if</span> gpus:</span><br><span class="line">  <span class="comment"># Restrict TensorFlow to only allocate 1GB of memory on the first GPU</span></span><br><span class="line">  <span class="keyword">try</span>:</span><br><span class="line">    tf.config.experimental.set_virtual_device_configuration(</span><br><span class="line">        gpus[<span class="number">0</span>],</span><br><span class="line">        [tf.config.experimental.VirtualDeviceConfiguration(memory_limit=<span class="number">1024</span>)])</span><br><span class="line">    logical_gpus = tf.config.experimental.list_logical_devices(<span class="string">'GPU'</span>)</span><br><span class="line">    print(len(gpus), <span class="string">"Physical GPUs,"</span>, len(logical_gpus), <span class="string">"Logical GPUs"</span>)</span><br><span class="line">  <span class="keyword">except</span> RuntimeError <span class="keyword">as</span> e:</span><br><span class="line">    <span class="comment"># Virtual devices must be set before GPUs have been initialized</span></span><br><span class="line">    print(e)</span><br></pre></td></tr></table></figure><h3 id="Using-a-single-GPU-on-a-multi-GPU-system"><a href="#Using-a-single-GPU-on-a-multi-GPU-system" class="headerlink" title="Using a single GPU on a multi-GPU system"></a>Using a single GPU on a multi-GPU system</h3><p>If you have more than one GPU in your system, the GPU with the lowest ID will be selected by default. If you would like to run on a different GPU, you will need to specify the preference explicitly:</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">tf.debugging.set_log_device_placement(<span class="literal">True</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">try</span>:</span><br><span class="line">  <span class="comment"># Specify an invalid GPU device</span></span><br><span class="line">  <span class="keyword">with</span> tf.device(<span class="string">'/device:GPU:2'</span>):</span><br><span class="line">    a = tf.constant([[<span class="number">1.0</span>, <span class="number">2.0</span>, <span class="number">3.0</span>], [<span class="number">4.0</span>, <span class="number">5.0</span>, <span class="number">6.0</span>]])</span><br><span class="line">    b = tf.constant([[<span class="number">1.0</span>, <span class="number">2.0</span>], [<span class="number">3.0</span>, <span class="number">4.0</span>], [<span class="number">5.0</span>, <span class="number">6.0</span>]])</span><br><span class="line">    c = tf.matmul(a, b)</span><br><span class="line"><span class="keyword">except</span> RuntimeError <span class="keyword">as</span> e:</span><br><span class="line">  print(e)</span><br></pre></td></tr></table></figure><p>If you would like TensorFlow to automatically choose an existing and supported device to run the operations in case the specified one doesn’t exist, you can call tf.config.set_soft_device_placement(True).</p><h3 id="Using-multiple-GPUs"><a href="#Using-multiple-GPUs" class="headerlink" title="Using multiple GPUs"></a>Using multiple GPUs</h3><p>Developing for multiple GPUs will allow a model to scale with the additional resources. If developing on a system with a single GPU, we can simulate multiple GPUs with virtual devices. This enables easy testing of multi-GPU setups without requiring additional resources.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">gpus = tf.config.experimental.list_physical_devices(<span class="string">'GPU'</span>)</span><br><span class="line"><span class="keyword">if</span> gpus:</span><br><span class="line">  <span class="comment"># Create 2 virtual GPUs with 1GB memory each</span></span><br><span class="line">  <span class="keyword">try</span>:</span><br><span class="line">    tf.config.experimental.set_virtual_device_configuration(</span><br><span class="line">        gpus[<span class="number">0</span>],</span><br><span class="line">        [tf.config.experimental.VirtualDeviceConfiguration(memory_limit=<span class="number">1024</span>),</span><br><span class="line">         tf.config.experimental.VirtualDeviceConfiguration(memory_limit=<span class="number">1024</span>)])</span><br><span class="line">    logical_gpus = tf.config.experimental.list_logical_devices(<span class="string">'GPU'</span>)</span><br><span class="line">    print(len(gpus), <span class="string">"Physical GPU,"</span>, len(logical_gpus), <span class="string">"Logical GPUs"</span>)</span><br><span class="line">  <span class="keyword">except</span> RuntimeError <span class="keyword">as</span> e:</span><br><span class="line">    <span class="comment"># Virtual devices must be set before GPUs have been initialized</span></span><br><span class="line">    print(e)</span><br></pre></td></tr></table></figure><p>The best practice for using multiple GPUs is to use tf.distribute.Strategy. Here is a simple example:</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">tf.debugging.set_log_device_placement(<span class="literal">True</span>)</span><br><span class="line"></span><br><span class="line">strategy = tf.distribute.MirroredStrategy()</span><br><span class="line"><span class="keyword">with</span> strategy.scope():</span><br><span class="line">  inputs = tf.keras.layers.Input(shape=(<span class="number">1</span>,))</span><br><span class="line">  predictions = tf.keras.layers.Dense(<span class="number">1</span>)(inputs)</span><br><span class="line">  model = tf.keras.models.Model(inputs=inputs, outputs=predictions)</span><br><span class="line">  model.compile(loss=<span class="string">'mse'</span>,</span><br><span class="line">                optimizer=tf.keras.optimizers.SGD(learning_rate=<span class="number">0.2</span>))</span><br></pre></td></tr></table></figure><div class="alert danger"><p>Error info: NCCL cannot support logical gpus</p></div>]]></content>
    
    <summary type="html">
    
      Distributed training strategies and how to use multi GPUs
    
    </summary>
    
    
      <category term="Study Notes" scheme="https://joddiy.github.io/categories/Study-Notes/"/>
    
    
      <category term="Tools" scheme="https://joddiy.github.io/tags/Tools/"/>
    
      <category term="Deep Learning" scheme="https://joddiy.github.io/tags/Deep-Learning/"/>
    
      <category term="Tensorflow" scheme="https://joddiy.github.io/tags/Tensorflow/"/>
    
  </entry>
  
  <entry>
    <title>TensorFlow 2.0 Functions, not Sessions.</title>
    <link href="https://joddiy.github.io/2020/05/07/TF2-0-Function/"/>
    <id>https://joddiy.github.io/2020/05/07/TF2-0-Function/</id>
    <published>2020-05-07T13:03:33.000Z</published>
    <updated>2020-05-09T21:32:56.000Z</updated>
    
    <content type="html"><![CDATA[<h1 id="TensorFlow-2-0-Functions-not-Sessions"><a href="#TensorFlow-2-0-Functions-not-Sessions" class="headerlink" title="TensorFlow 2.0: Functions, not Sessions."></a>TensorFlow 2.0: Functions, not Sessions.</h1><p><a href="https://github.com/tensorflow/community/blob/49c9f13500ddedddf60144690b9be744a3654b11/rfcs/20180918-functions-not-sessions-20.md" target="_blank" rel="noopener">doc link</a></p><h2 id="Design-Proposal"><a href="#Design-Proposal" class="headerlink" title="Design Proposal"></a>Design Proposal</h2><h3 id="Basic-idea-Python-functions-as-Graphs"><a href="#Basic-idea-Python-functions-as-Graphs" class="headerlink" title="Basic idea: Python functions as Graphs"></a>Basic idea: Python functions as Graphs</h3><p>Where tf.function is a decorator that “defines a TensorFlow function”. A “TensorFlow function” defines a computation as a graph of TensorFlow operations, with named arguments and explicit return values. </p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> tensorflow <span class="keyword">as</span> tf</span><br><span class="line"></span><br><span class="line"><span class="meta">@tf.function</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">compute_z1</span><span class="params">(x, y)</span>:</span></span><br><span class="line">  <span class="keyword">return</span> tf.add(x, y)</span><br><span class="line"></span><br><span class="line"><span class="meta">@tf.function</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">compute_z0</span><span class="params">(x)</span>:</span></span><br><span class="line">  <span class="keyword">return</span> compute_z1(x, tf.square(x))</span><br><span class="line"></span><br><span class="line">z0 = compute_z0(<span class="number">2.</span>)</span><br><span class="line">z1 = compute_z1(<span class="number">2.</span>, <span class="number">2.</span>)</span><br></pre></td></tr></table></figure><p>Having the Python function correspond to what the runtime will execute reduces conceptual complexity in translating between the two domains. </p><h3 id="Referencing-state-Variables-tables-etc"><a href="#Referencing-state-Variables-tables-etc" class="headerlink" title="Referencing state: Variables, tables etc."></a>Referencing state: Variables, tables etc.</h3><p>A function decorated Python function encapsulates a graph and its execution. The Python function may reference stateful objects (i.e., state backed by DT_RESOURCE tensors in the runtime, e.g., tf.Variable) by referencing the corresponding Python object, and these will be captured as implicit inputs to the function.</p><p>Comparing TensorFlow code today with how we propose it looks in 2.x:</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># TF 1.x</span></span><br><span class="line">W = tf.Variable(</span><br><span class="line">  tf.glorot_uniform_initializer()(</span><br><span class="line">    (<span class="number">10</span>, <span class="number">10</span>)))</span><br><span class="line">b = tf.Variable(tf.zeros(<span class="number">10</span>))</span><br><span class="line">c = tf.Variable(<span class="number">0</span>)</span><br><span class="line"></span><br><span class="line">x = tf.placeholder(tf.float32)</span><br><span class="line">ctr = c.assign_add(<span class="number">1</span>)</span><br><span class="line"><span class="keyword">with</span> tf.control_dependencies([ctr]):</span><br><span class="line">  y = tf.matmul(x, W) + b</span><br><span class="line">init = </span><br><span class="line">  tf.global_variables_initializer()</span><br><span class="line"></span><br><span class="line"><span class="keyword">with</span> tf.Session() <span class="keyword">as</span> sess:</span><br><span class="line">  sess.run(init)</span><br><span class="line">  print(sess.run(y,</span><br><span class="line">  feed_dict=&#123;x: make_input_value()&#125;))</span><br><span class="line">  <span class="keyword">assert</span> int(sess.run(c)) == <span class="number">1</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># TF 2.0</span></span><br><span class="line">W = tf.Variable(</span><br><span class="line">  tf.glorot_uniform_initializer()(</span><br><span class="line">    (<span class="number">10</span>, <span class="number">10</span>)))</span><br><span class="line">b = tf.Variable(tf.zeros(<span class="number">10</span>))</span><br><span class="line">c = tf.Variable(<span class="number">0</span>)</span><br><span class="line"></span><br><span class="line"><span class="meta">@tf.function</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">f</span><span class="params">(x)</span>:</span></span><br><span class="line">  c.assign_add(<span class="number">1</span>)</span><br><span class="line">  <span class="keyword">return</span> tf.matmul(x, W) + b</span><br><span class="line"></span><br><span class="line">print(f(make_input_value())</span><br><span class="line"><span class="keyword">assert</span> int(c) == <span class="number">1</span></span><br></pre></td></tr></table></figure><p>Worthy of note here - in TensorFlow 1.x, the memory underlying the variables W and b in the runtime lives for the lifetime of the Session - unrelated to the lifetime of the Python objects. In 2.x, the lifetime of the Python objects and the runtime state are tied together.</p><h3 id="Control-dependencies"><a href="#Control-dependencies" class="headerlink" title="Control dependencies"></a>Control dependencies</h3><p>In TensorFlow graphs today, control dependencies are sometimes needed to ensure correct evaluation order. </p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># TF 1.x</span></span><br><span class="line">v = tf.Variable(<span class="number">1.0</span>)</span><br><span class="line">init_op = tf.global_variables_initializer()</span><br><span class="line">assign_op = v.assign(<span class="number">2.0</span>)</span><br><span class="line">read = v.read_value()</span><br><span class="line"></span><br><span class="line"><span class="keyword">with</span> tf.Session() <span class="keyword">as</span> sess:</span><br><span class="line">  sess.run(init_op)</span><br><span class="line">  val = sess.run(read)</span><br><span class="line">  print(val) <span class="comment"># Will print 1.0, the assign is ignored</span></span><br><span class="line">  val = sess.run([read, assign_op])[<span class="number">0</span>]</span><br><span class="line">  print(val)   <span class="comment"># Non-deterministically prints 1.0 or 2.0,</span></span><br><span class="line"></span><br><span class="line"> <span class="comment"># TF2.0</span></span><br><span class="line"> v = tf.Variable(<span class="number">1.0</span>)</span><br><span class="line"><span class="meta">@tf.function</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">f</span><span class="params">()</span>:</span></span><br><span class="line">  v.assign(<span class="number">2.0</span>)</span><br><span class="line">  <span class="keyword">return</span> v.read_value()</span><br><span class="line"></span><br><span class="line">print(f()) <span class="comment"># Always prints 2.0.</span></span><br></pre></td></tr></table></figure><p>Note that the intention here is to avoid observable differences from program order. For example:</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">a = tf.Variable(<span class="number">1.0</span>)</span><br><span class="line">b = tf.Variable(<span class="number">1.0</span>)</span><br><span class="line"><span class="meta">@tf.function</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">f</span><span class="params">()</span>:</span></span><br><span class="line">  a.assign(<span class="number">2.0</span>)</span><br><span class="line">  b.assign(<span class="number">3.0</span>)</span><br><span class="line">  <span class="keyword">return</span> a + b</span><br><span class="line">print(f())</span><br></pre></td></tr></table></figure><p>Will always print 5.0 since the assignments will occur before the read. However, there is no guaranteed ordering between the assignment of a and b (as any difference in that is not observable).</p><h3 id="Functions-that-create-state"><a href="#Functions-that-create-state" class="headerlink" title="Functions that create state"></a>Functions that create state</h3><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">v = <span class="literal">None</span></span><br><span class="line"></span><br><span class="line"><span class="meta">@tf.function</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">f</span><span class="params">(x)</span>:</span></span><br><span class="line">  <span class="keyword">global</span> v</span><br><span class="line">  <span class="keyword">if</span> v <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">    v = tf.Variable(<span class="number">1.0</span>)</span><br><span class="line">  <span class="keyword">return</span> tf.cast(x, tf.float32) + v</span><br><span class="line"></span><br><span class="line">f(tf.constant(<span class="number">1</span>, dtype=tf.float32)) <span class="comment"># Creates the variable, returns 2.0</span></span><br><span class="line">f(tf.constant(<span class="number">2</span>, dtype=tf.int32))   <span class="comment"># Reuses the variable, returns 3.0</span></span><br></pre></td></tr></table></figure><ol><li>State (like tf.Variable objects) are only created the first time the function f is called. If any variables are created in the first execution of f, then @tf.function will trace f again the second time it is invoked in order to record the behavior that will be used from then on. </li><li>The caller must make sure that any variable referenced by the function still exists whenever the function is evaluated. @tf.function itself will keep only weak references to these created variables. </li></ol><h3 id="Trace-Caches"><a href="#Trace-Caches" class="headerlink" title="Trace Caches"></a>Trace Caches</h3><p>Since new graphs are traced when new input signatures are encountered, a function can encapsulate multiple graphs. For example, considering the following, there are two graphs created here:</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@tf.function</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">f</span><span class="params">(x)</span>:</span></span><br><span class="line">  <span class="keyword">return</span> tf.square(x)</span><br><span class="line"></span><br><span class="line">f(tf.constant(<span class="number">1</span>, dtype=tf.int32))</span><br><span class="line">f(tf.constant(<span class="number">1.0</span>, dtype=tf.float32))</span><br></pre></td></tr></table></figure><p>Note the use of tf.constant to ensure that the argument is a Tensor. If the argument were a Python value, then additional graphs will be traced for each such value. For example, the following two calls will result in two additional graphs being traced:</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">f(<span class="number">1.0</span>)</span><br><span class="line">f(<span class="number">2.0</span>)</span><br></pre></td></tr></table></figure><p>Where arguments are not Tensors, the “value” of the argument is used to compute the trace_cache_key. For example:</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@tf.function</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">f</span><span class="params">(x, use_multiply)</span>:</span></span><br><span class="line">  <span class="keyword">return</span> tf.multiply(x, x) <span class="keyword">if</span> use_multiply <span class="keyword">else</span> tf.square(x)</span><br><span class="line"></span><br><span class="line">f(tf.constant(<span class="number">2.0</span>), <span class="literal">True</span>)</span><br><span class="line">f(tf.constant(<span class="number">2.0</span>), <span class="literal">False</span>)</span><br></pre></td></tr></table></figure><p>will result in 2 graphs being created, since the two calls result in two different cache keys because the value of the Python object (the second argument) changes between the two.</p><p>Note that the “type” of Tensor inputs to the function also incorporates the shape. For example:</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@tf.function</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">f</span><span class="params">(x)</span>:</span> </span><br><span class="line"><span class="keyword">return</span> tf.add(x, <span class="number">1.</span>)</span><br><span class="line"></span><br><span class="line">f(tf.constant([<span class="number">2.0</span>]))</span><br><span class="line">f(tf.constant([<span class="number">2.0</span>, <span class="number">3.0</span>]))</span><br><span class="line">f(tf.constant([[<span class="number">2.0</span>]]))</span><br><span class="line">f(tf.constant([<span class="number">3.0</span>]))</span><br><span class="line">f(tf.constant([<span class="number">4.0</span>, <span class="number">5.0</span>]))</span><br></pre></td></tr></table></figure><p>will result in 3 graphs being created.</p><p>The trace_cache_key also incorporates the “context” in which the call was made. For example:</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@tf.function</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">f</span><span class="params">(x)</span>:</span> <span class="keyword">return</span> tf.add(x, <span class="number">1.</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">with</span> tf.device(<span class="string">"/device:CPU:0"</span>):</span><br><span class="line">  f(tf.constant(<span class="number">2.0</span>))</span><br><span class="line"><span class="keyword">with</span> tf.device(<span class="string">"/device:GPU:0"</span>):</span><br><span class="line">  f(tf.constant(<span class="number">2.0</span>))</span><br></pre></td></tr></table></figure><p>Will create 2 graphs.</p><p><strong>CAUTION: Too many traces</strong></p><p><strong>CAUTION: Mutable non-Tensor arguments</strong></p><p>The trace_cache_key includes the Python object for non-Tensor arguments. Mutations of these arguments might not be detected. For example:</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># non-Tensor object</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Params</span><span class="params">(object)</span>:</span></span><br><span class="line">  multiply = <span class="literal">True</span></span><br><span class="line"></span><br><span class="line">p = Params()</span><br><span class="line"><span class="meta">@tf.function</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">f</span><span class="params">(x, y)</span>:</span></span><br><span class="line">  <span class="keyword">return</span> tf.multiply(x, <span class="number">2.</span>) <span class="keyword">if</span> y.multiply <span class="keyword">else</span> tf.add(x, <span class="number">2.</span>)</span><br><span class="line"></span><br><span class="line">f(<span class="number">3.</span>, p) <span class="comment"># Returns 6.0</span></span><br><span class="line">p.multiply = <span class="literal">False</span></span><br><span class="line">f(<span class="number">3.</span>, p)  <span class="comment"># Mutations to `p` may not trigger a retrace, so might still return 6.0</span></span><br></pre></td></tr></table></figure><h3 id="Input-Signatures"><a href="#Input-Signatures" class="headerlink" title="Input Signatures"></a>Input Signatures</h3><p>An “input signature” can be explicitly specified to control the trace_cache_key computation based on the type and shape of Tensor (and list of Tensor) arguments to f.</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@tf.function(input_signature=((tf.float32, [None]))</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">f</span><span class="params">(x)</span>:</span> </span><br><span class="line"><span class="keyword">return</span> tf.add(x, <span class="number">1.</span>)</span><br><span class="line"></span><br><span class="line">f(tf.constant([<span class="number">2.0</span>]))      <span class="comment"># Returns [3.0]</span></span><br><span class="line">f(tf.constant([<span class="number">2.0</span>, <span class="number">3.0</span>])) <span class="comment"># Matches the input signature as [None]</span></span><br><span class="line">                           <span class="comment"># matches the actual shape [2]</span></span><br><span class="line">f(tf.constant([[<span class="number">2.0</span>]]))    <span class="comment"># Raises an error as the arguments don't match the</span></span><br><span class="line">                           <span class="comment"># input signature.</span></span><br><span class="line">f(tf.constant([<span class="number">2</span>], dtype=tf.int32)) <span class="comment"># Raises an error as the dtype of the argument</span></span><br><span class="line">                                    <span class="comment"># does not match the input signature</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># f is backed by a single Graph since the input signature specification allowed</span></span><br><span class="line"><span class="comment"># for the same graph to be used when the input shape is (1,) or (2,).</span></span><br></pre></td></tr></table></figure><ol><li>For a Tensor argument, it specifies a (dtype, shape pattern).<ul><li>(tf.float32, [None]) means the argument must be a float32 vector (with any number of elements).</li><li>(tf.int32, []) means that the argument must be an int32 scalar.</li></ul></li><li>For a list of Tensor objects, it specifies an optional list length and the signature for elements in the list (i.e., the dtype and shape pattern for all elements in the list).</li><li>For non-Tensor arguments: tf.PYTHON_VALUE</li></ol><p>You can use the tf.TRACE_ON_NEW_VALUE to release the restriction of dtype:</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@tf.function(input_signature=((tf.TRACE_ON_NEW_VALUE, [None]))</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">f</span><span class="params">(x)</span>:</span> </span><br><span class="line"><span class="keyword">return</span> tf.square(x)</span><br><span class="line"></span><br><span class="line">f(tf.constant([<span class="number">2.0</span>])) <span class="comment"># Returns 4.0</span></span><br><span class="line">f(tf.constant([<span class="number">2</span>, <span class="number">2</span>], dtype=tf.int32) <span class="comment"># Returns [4, 4] after tracing a new graph</span></span><br></pre></td></tr></table></figure><h3 id="Classes"><a href="#Classes" class="headerlink" title="Classes"></a>Classes</h3><p>If a member function of a class does not create variables, it may be decorated with @tf.function and it will work:</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">AnyShapeModel</span><span class="params">(object)</span>:</span></span><br><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self)</span>:</span></span><br><span class="line">    self.v = <span class="literal">None</span></span><br><span class="line"></span><br><span class="line"><span class="meta">  @tf.function</span></span><br><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">increment</span><span class="params">(self, amount)</span>:</span></span><br><span class="line">    <span class="keyword">if</span> self.v <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">      self.v = tf.Variable(tf.zeros_like(amount))</span><br><span class="line">    self.v.assign_add(amount)</span><br><span class="line"></span><br><span class="line">model1 = AnyShapeModel()</span><br><span class="line">model1.increment(tf.constant(<span class="number">3</span>))</span><br><span class="line"><span class="keyword">assert</span> int(model1.v) == <span class="number">3</span></span><br><span class="line">model1.increment(tf.constant(<span class="number">4</span>))</span><br><span class="line"><span class="keyword">assert</span> int(model1.v) == <span class="number">7</span></span><br><span class="line">model2 = AnyShapeModel()</span><br><span class="line">model2.increment(tf.constant([<span class="number">4</span>, <span class="number">5</span>]))</span><br><span class="line"><span class="keyword">assert</span> model2.v.numpy() == [<span class="number">4</span>, <span class="number">5</span>]</span><br></pre></td></tr></table></figure><p>The semantics here are that each new instance is allowed to create variables in each @tf.function once.</p><h3 id="function-ing-Python-control-flow"><a href="#function-ing-Python-control-flow" class="headerlink" title="function-ing Python control flow"></a>function-ing Python control flow</h3><p>If the function has <strong>data-dependent control flow</strong> then though the function will execute fine with eager execution enabled, function decorating it will fail. For example:</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">f</span><span class="params">(x, y)</span>:</span></span><br><span class="line">  <span class="keyword">if</span> tf.equal(y, <span class="number">0.0</span>):</span><br><span class="line">    <span class="keyword">return</span> y</span><br><span class="line">  <span class="keyword">return</span> x / y</span><br><span class="line"></span><br><span class="line">x = tf.constant(<span class="number">2.0</span>)</span><br><span class="line">y = tf.constant(<span class="number">2.0</span>)</span><br><span class="line"></span><br><span class="line">f(x, y) <span class="comment"># Will be 1.0</span></span><br><span class="line"></span><br><span class="line">df = tf.function(f)</span><br><span class="line">df(x, y)  <span class="comment"># Will raise an error complaining about the data-dependent control flow</span></span><br></pre></td></tr></table></figure><p>To fix this, one would have to use the graph construction APIs for control flow (tf.cond, tf.while_loop):</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">f</span><span class="params">(x, y)</span>:</span></span><br><span class="line">  <span class="keyword">return</span> tf.cond(tf.equal(y, <span class="number">0.0</span>), <span class="keyword">lambda</span>: y, <span class="keyword">lambda</span>: x/y)</span><br><span class="line"></span><br><span class="line">x = tf.constant(<span class="number">2.0</span>)</span><br><span class="line">y = tf.constant(<span class="number">2.0</span>)</span><br><span class="line"></span><br><span class="line">f(x, y) <span class="comment"># Will be 1.0</span></span><br><span class="line"></span><br><span class="line">df = tf.function(f)</span><br><span class="line">df(x, y)  <span class="comment"># Will be 1.0</span></span><br></pre></td></tr></table></figure><p>This situation can be improved with the help of autograph to allow expression of control flow in Python. </p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">df = tf.function(autograph=<span class="literal">True</span>)(f)</span><br><span class="line">f(x, y) <span class="comment"># Will be 1.0</span></span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      Design Proposal of function in Tensorflow 2.0
    
    </summary>
    
    
      <category term="Study Notes" scheme="https://joddiy.github.io/categories/Study-Notes/"/>
    
    
      <category term="Tools" scheme="https://joddiy.github.io/tags/Tools/"/>
    
      <category term="Deep Learning" scheme="https://joddiy.github.io/tags/Deep-Learning/"/>
    
      <category term="Tensorflow" scheme="https://joddiy.github.io/tags/Tensorflow/"/>
    
  </entry>
  
  <entry>
    <title>Understanding and Optimizing Packed Neural Network Training for Hyper-Parameter Tuning</title>
    <link href="https://joddiy.github.io/2020/05/01/Packed-Neural-Network-Training/"/>
    <id>https://joddiy.github.io/2020/05/01/Packed-Neural-Network-Training/</id>
    <published>2020-04-30T16:00:00.000Z</published>
    <updated>2020-05-11T08:34:49.000Z</updated>
    
    <content type="html"><![CDATA[<h1 id="Understanding-and-Optimizing-Packed-Neural-Network-Training-for-Hyper-Parameter-Tuning"><a href="#Understanding-and-Optimizing-Packed-Neural-Network-Training-for-Hyper-Parameter-Tuning" class="headerlink" title="Understanding and Optimizing Packed Neural Network Training for Hyper-Parameter Tuning"></a>Understanding and Optimizing Packed Neural Network Training for Hyper-Parameter Tuning</h1><h2 id="Subject"><a href="#Subject" class="headerlink" title="Subject"></a>Subject</h2><p>This paper studies jointly training multiple neural network models on a single GPU. Our research prototype is in TensorFlow, and we evaluate performance across different models (ResNet, MobileNet, DenseNet, and MLP) and training scenarios. </p><h2 id="Contribution"><a href="#Contribution" class="headerlink" title="Contribution"></a>Contribution</h2><ol><li>packing two models can bring up to 40% performance improvement over unpacked setups for a single training step and the improvement increases when packing more models; </li><li>the benefit of a pack primitive largely depends on a number of factors including memory capacity, chip architecture, neural network structure, and batch size; </li><li>there exists a trade-off between packing and unpacking when training multiple neural network models on limited resources; (4) a pack-based Hyperband is up to 2.7× faster than the original Hyperband training method in our experiment setting, with this improvement growing as memory size increases and subsequently the density of models packed. </li></ol><h2 id="Introduction"><a href="#Introduction" class="headerlink" title="Introduction"></a>Introduction</h2><h3 id="Background"><a href="#Background" class="headerlink" title="Background"></a>Background</h3><ol><li>It is increasingly the case that a diverse set of model training tasks share limited training resources. The long-running nature of these tasks and the large variation in their size and complexity makes efficient resource sharing a crucial concern. </li><li>These concerns are compounded by an extensive trial-and-error development process where parameters are tuned and architectures are tweaked that result in a large number of trial models to train. </li></ol><h3 id="Multi-tenant-training"><a href="#Multi-tenant-training" class="headerlink" title="Multi-tenant training"></a>Multi-tenant training</h3><h4 id="granularity-of-full-devices"><a href="#granularity-of-full-devices" class="headerlink" title="granularity of full devices"></a>granularity of full devices</h4><p>Such a policy can result in poor resource utilization due to its coarse granularity. If a training workload consists of a large number of small neural networks, allocating entire devices to these training tasks is wasteful and significantly delays any large model training.</p><h4 id="fine-grained-resource-sharing"><a href="#fine-grained-resource-sharing" class="headerlink" title="fine-grained resource sharing"></a>fine-grained resource sharing</h4><p>Unlike CPUs, the full virtualization of GPU resources is very nascent. In this setting, those parallel kernels would transfer and store multiple copies of the same training data on the device. </p><h3 id="Pack-models"><a href="#Pack-models" class="headerlink" title="Pack models"></a>Pack models</h3><p>A multiple static neural network architectures (e.g., ones that are typically used in Computer Vision) can be rewritten as a single concatenated network that preserves the input, output, and backpropagation semantics.</p><ol><li>facilitate partitioning of a single device</li><li>synchronize data processing threads on GPUs</li><li>collapse common variables in the computation graph.</li></ol><p>It is often the case during hyperparameter or neural architecture search that multiple similar networks are trained, and packed configurations can feed a single I/O stream of training examples to each variant of the network. In contrast, an isolated sharing configuration may lead to duplicated work and wasted resources.</p><h3 id="Conclusion"><a href="#Conclusion" class="headerlink" title="Conclusion"></a>Conclusion</h3><ol><li>packing models together is not strictly beneficial, that packing models that are too different can result in excessive blocking and wasted work.</li><li>this paper studies the range of possible improvements (and/or overheads) for using packed configurations. </li><li>design an algorithm that heuristically finds performant packing configurations</li></ol><h2 id="Background-1"><a href="#Background-1" class="headerlink" title="Background"></a>Background</h2><h3 id="Motivation"><a href="#Motivation" class="headerlink" title="Motivation"></a>Motivation</h3><p><img src="https://res.cloudinary.com/dzarsr3se/image/upload/v1588964038/blog/Understanding-and-Optimizing-Packed-Neural-Network-Training-for-Hyper-Parameter-Tuning/1_mfq4g7.png" alt="P1"></p><p>Suppose, we are training two models on the same dataset to test if a small tweak in network architecture will improve performance. If the system could pack together models when compatible in size, then these redundant data streams can be fused together to improve performance.</p><h3 id="Basic-Framework-API"><a href="#Basic-Framework-API" class="headerlink" title="Basic Framework API"></a>Basic Framework API</h3><p>We desire a framework that can pack models together when possible and jointly optimize their respective computation graphs to reduce redundant work. </p><p>Each training task is identified by four inputs.</p><ol><li>Model.</li><li>Optimizer.</li><li>Batch Size.</li><li>Steps. </li></ol><p>Such a system requires three basic primitives load, pack, and free. </p><ol><li>load<figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">load(model, device)</span><br></pre></td></tr></table></figure></li><li>free<figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">checkpt &#x3D; free(model, device)</span><br></pre></td></tr></table></figure></li><li>pack<figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">output1 &#x3D; nn1(input1)</span><br><span class="line">output2 &#x3D; nn2(input2)</span><br><span class="line">&#x3D;&#x3D;&gt;</span><br><span class="line">[output1 output2] &#x3D; nn12([input1, input2])</span><br></pre></td></tr></table></figure></li></ol><p>This pack operation is fully differentiable and preserves the correctness semantics of the two original networks. The models can be jointly trained.</p><p>If the models are too different the device may waste effort stalling on one model.</p><h2 id="Implementation"><a href="#Implementation" class="headerlink" title="Implementation"></a>Implementation</h2><h3 id="Basic-Packing"><a href="#Basic-Packing" class="headerlink" title="Basic Packing"></a>Basic Packing</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">resnet_out &#x3D; ... #reference to resnet output </span><br><span class="line">mobilenet_out &#x3D; ..#reference to mobilenet output </span><br><span class="line">packed_output &#x3D; stack([resnet_out, mobilenet_out])</span><br></pre></td></tr></table></figure><p><img src="https://res.cloudinary.com/dzarsr3se/image/upload/v1588964039/blog/Understanding-and-Optimizing-Packed-Neural-Network-Training-for-Hyper-Parameter-Tuning/2_yjjdme.png" alt="P2"></p><p>If one of the models is significantly more complex than the others, it will block progress.</p><ol><li>Misaligned Batch Sizes</li></ol><p>Our method is to rewrite the packed model to include a dummy operation that pads models with the smaller batch size to match the larger ones in dimension. </p><p><img src="https://res.cloudinary.com/dzarsr3se/image/upload/v1588964038/blog/Understanding-and-Optimizing-Packed-Neural-Network-Training-for-Hyper-Parameter-Tuning/3_mozcet.png" alt="P3"></p><ol start="2"><li>Misaligned Step Counts</li></ol><p>when the earliest model finishes, the packed model has to be check-pointed and reconfigured to only include the remaining models. This introduces a switching overhead of loading and freeing the device memory.</p><h3 id="Eliminating-Redundancy"><a href="#Eliminating-Redundancy" class="headerlink" title="Eliminating Redundancy"></a>Eliminating Redundancy</h3><p>Dimensional differences between the models or training differences between the models can lead to wasted work. </p><p>In this case, input1 and input2 refer to the same dataset. We can avoid transferring the batch multiple times by symbolically rewriting the network description to refer to the same input:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">output1 &#x3D; nn_relu(input1)</span><br><span class="line">output2 &#x3D; nn_sigmoid(input2)</span><br><span class="line">&#x3D;&#x3D;&gt;</span><br><span class="line">[output1 output2] &#x3D; nn12([input, input])</span><br></pre></td></tr></table></figure><p>When packing models that use the same preprocessing tasks, the pack primitive fuse the steam processing accordingly.</p><h2 id="Profiling-Model-Packing"><a href="#Profiling-Model-Packing" class="headerlink" title="Profiling Model Packing"></a>Profiling Model Packing</h2><p>As it stands, model packing leads to the following tradeoffs. Potential performance improvements include: </p><ol><li>performing computations (inference and backpropagation) in parallel if and when possible, </li><li>eliminating redundant data transfers when models are trained on the same dataset, </li><li>combining redudant computations including preprocessing. </li></ol><p>On the other hand, the potential overheads include: </p><ol><li>models that dominate the device resources and block the progress of the others, </li><li>padding misaligned batch sizes,</li><li>loading and unloading models with a differing number of training steps.</li></ol><p>In particular, we find that packing is most beneficial when the models jointly trained are similar and train on the same dataset.</p><h3 id="Metrics"><a href="#Metrics" class="headerlink" title="Metrics"></a>Metrics</h3><p>we define the improvement metric as follows:</p><p>[IMPV]</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">Ts(Seq) &#x3D; Ts(Model 1)+···+Ts(Model n)</span><br><span class="line">Ts(Pack) &#x3D; Ts(Pack(Model 1,··· ,n))</span><br><span class="line"></span><br><span class="line">IMPV &#x3D; (Ts(Seq)−Ts(Pack))&#x2F;Ts(Seq)</span><br></pre></td></tr></table></figure><p>The switching overhead of training n models is defined as:</p><p>[SwOH]</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">SwOH &#x3D; Te(Seq)−Te(Model 1)···−Te(Model n)</span><br></pre></td></tr></table></figure><h3 id="Micro-Benchmarks"><a href="#Micro-Benchmarks" class="headerlink" title="Micro-Benchmarks"></a>Micro-Benchmarks</h3><p>Improvement of packing models when increasing number of models and batch size on GPU.</p><p><img src="https://res.cloudinary.com/dzarsr3se/image/upload/v1588964038/blog/Understanding-and-Optimizing-Packed-Neural-Network-Training-for-Hyper-Parameter-Tuning/4_aeswjs.png" alt="P4"></p><h3 id="Ablation-Study"><a href="#Ablation-Study" class="headerlink" title="Ablation Study"></a>Ablation Study</h3><p>We test more cases based on the five factors:</p><p><img src="https://res.cloudinary.com/dzarsr3se/image/upload/v1588964038/blog/Understanding-and-Optimizing-Packed-Neural-Network-Training-for-Hyper-Parameter-Tuning/T1_u9m0tn.png" alt="T1"></p><p>The best scenarios are where the same training data and same batch size are used. Over all the configurations, the pack primitive always brings benefits when we training models with same data or same batch size since it will reduces the data transfer. </p><p><img src="https://res.cloudinary.com/dzarsr3se/image/upload/v1588964038/blog/Understanding-and-Optimizing-Packed-Neural-Network-Training-for-Hyper-Parameter-Tuning/5_a4nf4v.png" alt="P5"></p><h3 id="Memory-Usage"><a href="#Memory-Usage" class="headerlink" title="Memory Usage"></a>Memory Usage</h3><p>MLP-3 model maintains the same as batch size goes up. For convolutional neural networks like ResNet, MobileNet and DenseNet, the GPU memory usage is proportional to the batch size.</p><p><img src="https://res.cloudinary.com/dzarsr3se/image/upload/v1588964038/blog/Understanding-and-Optimizing-Packed-Neural-Network-Training-for-Hyper-Parameter-Tuning/6_otfjyk.png" alt="P6"></p><h3 id="Switching-Overheads"><a href="#Switching-Overheads" class="headerlink" title="Switching Overheads"></a>Switching Overheads</h3><p>The switching overhead is minor compared to the overall training time.</p><p><img src="https://res.cloudinary.com/dzarsr3se/image/upload/v1588964037/blog/Understanding-and-Optimizing-Packed-Neural-Network-Training-for-Hyper-Parameter-Tuning/T2_yivti7.png" alt="T2"></p><h3 id="Packing-vs-CUDA-Parallelism"><a href="#Packing-vs-CUDA-Parallelism" class="headerlink" title="Packing vs CUDA Parallelism"></a>Packing vs CUDA Parallelism</h3><p>Although CUDA supports it, our results show that it is an inefficient technique. We find that in all but the simplest cases lead to an out-of-memory error: “failed to allocateXXXfromdevice:CUDA_ERROR_OUT_OF_MEMORY: out of memory”. </p><h2 id="Packing-Aware-Hyperparameter-Search"><a href="#Packing-Aware-Hyperparameter-Search" class="headerlink" title="Packing-Aware Hyperparameter Search"></a>Packing-Aware Hyperparameter Search</h2><h3 id="Hyperband-for-Hyperparameter-Search"><a href="#Hyperband-for-Hyperparameter-Search" class="headerlink" title="Hyperband for Hyperparameter Search"></a>Hyperband for Hyperparameter Search</h3><p>Hyperband works by repeatedly sampling random parameter configurations, partially training models with those configurations and discarding those configurations that do not seem promising. </p><p>Hyperband is very effective for parallel hyperparameter search in comparison to sequential algorithms such as Bayesian Optimization.</p><p>The algorithm is long-running since it consists of a large number of trial models/configurations to run and each of them will occupy the entire GPU resource when running.</p><h3 id="Packed-Hyperband"><a href="#Packed-Hyperband" class="headerlink" title="Packed Hyperband"></a>Packed Hyperband</h3><p>Our pack primitive allows Hyperband to jointly train configurations when possible thereby reducing the overall training time. </p><p>The optimization problem is to search over all packable partitions to find the best possible configuratio. We call this primitive pack_opt(), which solves the search problem to produce feasible packing configuration. </p><h3 id="Heuristic-Solution"><a href="#Heuristic-Solution" class="headerlink" title="Heuristic Solution"></a>Heuristic Solution</h3><p>We design a nearest-neighbor based heuristic. The method randomly selects a single configuration as the centroid and packs all other configurations similar to it until the device runs out of memory. </p><p>For calculating the similarity, we map hyperparameter configurations to multidimensional feature space and measure the pairwise Euclid distance among all the configurations. </p><p>We take standard distance unit as 1, and compute the distance between any two configurations. For categorical hyperparameters like optimizer and activation, the distance is 0 if same and 1 if different, for numeric hyperparameters, we use the index to compute distance. </p><p><img src="https://res.cloudinary.com/dzarsr3se/image/upload/v1588964037/blog/Understanding-and-Optimizing-Packed-Neural-Network-Training-for-Hyper-Parameter-Tuning/T3_efcra4.png" alt="T3"></p><h3 id="Evaluation"><a href="#Evaluation" class="headerlink" title="Evaluation"></a>Evaluation</h3><p>A Random Pack Hyperband can save more time than Batch-size Pack Hyperband since it achieves a better GPU resource utilization. Our kNN strategy gets the best of both worlds: it finds the most beneficial packing opportunities while completely utilizing the available resources, and benefits are scalable when deployed in an environment with larger GPU resource.</p><p><img src="https://res.cloudinary.com/dzarsr3se/image/upload/v1588964037/blog/Understanding-and-Optimizing-Packed-Neural-Network-Training-for-Hyper-Parameter-Tuning/T4_gzjwyr.png" alt="T4"></p><h2 id="Related-Work"><a href="#Related-Work" class="headerlink" title="Related Work"></a>Related Work</h2><p>We focus on related work that studies placement in machine learning and hyperparameter tuning systems.</p><h3 id="Systems-for-Hyperparameter-Tuning"><a href="#Systems-for-Hyperparameter-Tuning" class="headerlink" title="Systems for Hyperparameter Tuning"></a>Systems for Hyperparameter Tuning</h3><ol><li>Google Vizier [3] exposes hyperparamter search as a service to its organization’s data scientists. Aggressive “scale-out” has been the main design principle of Vizier and similar systems.</li><li>A more controlled resource usage. CHOPT proposes a novel stop-and-go scheme that can take advantage of transient cloud based resources. </li><li>HyperSched, proposes a scheduling framework for hyperparameter tuning tasks when there are contended specialized resources. And, some work has been done on resource management [33] and pipeline re-use [18] in the non-deep learning setting. </li></ol><h3 id="Coarse-grained-Multi-tenancy"><a href="#Coarse-grained-Multi-tenancy" class="headerlink" title="Coarse-grained Multi-tenancy"></a>Coarse-grained Multi-tenancy</h3><p>dividing resources at the granularity of full devices</p><ol><li>One of the earliest works in this space was the TetriSched project, which considered how to specify and optimize locality constraints during process scheduling.</li><li>Project Philly analyzed a trace of machine learning workloads run on a large cluster of GPUs in Microsoft and indicated the trade-off between locality and GPU utilization. </li></ol><h3 id="Fine-grained-Multi-tenancy"><a href="#Fine-grained-Multi-tenancy" class="headerlink" title="Fine-grained Multi-tenancy"></a>Fine-grained Multi-tenancy</h3><p>There are a number of projects which attempt to achieve the fine granularity. This idea called packing.</p><ol><li>Gandiva is a cluster scheduling framework for deep learning jobs that provides primitives such as time-slicing and migration to schedule different jobs, it supports packing as well, implementing packing arrangements using a greedy algorithm without decent optimizations.</li><li>Narayanan et al. [26] discuss a packing idea that tries to combine various models and trains them together.</li></ol><p>However, this proposed packing method of layer fusion has limited application scenarios as it only focuses on models with the same batch size.</p><h3 id="Multi-tenancy-Systems"><a href="#Multi-tenancy-Systems" class="headerlink" title="Multi-tenancy Systems"></a>Multi-tenancy Systems</h3><ol><li>CROSSBOW</li><li>PipeDream</li><li>Ease.ml</li></ol><h2 id="Discussion-and-Limitations"><a href="#Discussion-and-Limitations" class="headerlink" title="Discussion and Limitations"></a>Discussion and Limitations</h2><p>Any distributed training and the regarding optimization is out of the scope of the paper. </p>]]></content>
    
    <summary type="html">
    
      Study notes of paper https://arxiv.org/abs/2002.02885
    
    </summary>
    
    
      <category term="Research" scheme="https://joddiy.github.io/categories/Research/"/>
    
    
      <category term="Deep Learning" scheme="https://joddiy.github.io/tags/Deep-Learning/"/>
    
      <category term="Hyper-Parameter Tuning" scheme="https://joddiy.github.io/tags/Hyper-Parameter-Tuning/"/>
    
      <category term="Graph Optimization" scheme="https://joddiy.github.io/tags/Graph-Optimization/"/>
    
  </entry>
  
  <entry>
    <title>Dynamic Malware Detection Research</title>
    <link href="https://joddiy.github.io/2018/12/18/dynamic-malware-research/"/>
    <id>https://joddiy.github.io/2018/12/18/dynamic-malware-research/</id>
    <published>2018-12-18T03:20:01.000Z</published>
    <updated>2020-05-11T08:34:44.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="Using-Spatio-Temporal-Information-in-API-Calls-with-Machine-Learning-Algorithms-for-Malware-Detection（2009）"><a href="#Using-Spatio-Temporal-Information-in-API-Calls-with-Machine-Learning-Algorithms-for-Malware-Detection（2009）" class="headerlink" title="Using Spatio-Temporal Information in API Calls with Machine Learning Algorithms for Malware Detection（2009）"></a>Using Spatio-Temporal Information in API Calls with Machine Learning Algorithms for Malware Detection（2009）</h2><p>This paper uses statistical features which are extracted from both spatial (arguments) and temporal (sequences) information available in Windows API calls. </p><p>The Windows API provides thousands of distinct API calls serving diverse functionalities. However, this paper chooses short-listed 237 API calls which are widely used by both benign Windows applications and malware programs. They refer to the short-listed calls as the core-functionality API calls. They further divide the core-functionality API calls into seven different categories: (1) registry, (2) network management, (3) memory management, (4) file I/O, (5) socket, (6) processor and threads, and (7) dynamically linked libraries (DLLs). </p><p><img src="1.png" alt="Figure1"></p><h3 id="spatial-information"><a href="#spatial-information" class="headerlink" title="spatial information"></a>spatial information</h3><p>The spatial information refers to the set of arguments which always are generally address pointers or variables that hold crisp numerical values. They use fundamental statistical and information theoretic measures to quantify the spatial information. These measures include mean, variance, entropy, minimum, and maximum of given values. For example, they have observed that malware writers suffice on lean and mean implementations of malware programs with low memory usage so that they can remain unnoticed for as long as possible.</p><h3 id="Temporal-information"><a href="#Temporal-information" class="headerlink" title="Temporal information"></a>Temporal information</h3><p>Temporal information refers to represents the order of invoked API calls where call sequences and control flow graphs are the most popular techniques. By some auto-correlation analysis, this paper finds that 3rd order dependence has a high correlation for both benign and malware programs, so it finally uses 3-gram API call sequence. Then they model API call sequence using a discrete time Markov chain. The order of a Markov chain represents the extent to which past states determine the present state. They uses 3rd order Markov chain to represent the probability of transitions, which means the next possible API call depends on previous three API calls. The transition matrix T looks like:</p><p><img src="2.png" alt="Figure2"></p><p>Each element in this matrix indicates the probability of a new API call when they have observed the first 3 API calls. Then the paper information gain to select the most discriminative features:</p><p><img src="3.png" alt="Figure3"></p><p>Specifically, for each element in the matrix, the paper uses a to count its appearance times in benign training samples and b to count in malware samples. Then this element’s information gain can be computed by:</p><p><img src="4.png" alt="Figure4"></p><h3 id="Experiment"><a href="#Experiment" class="headerlink" title="Experiment"></a>Experiment</h3><p>The paper designs an experiment using spatio-temporal features’ set over standalone spatial or temporal features set.  </p><p><img src="5.png" alt="Figure5"></p><p>The table shows that that spatial and temporal features alone can provide on the average detection accuracies of approximately 0.898 and 0.952 respectively. However, the combined spatio-temporal features’ set provide an average detection accuracy of 0.963 – approximately 5% improvement over standalone features’ set.</p><p>NB with spatio-temporal features’ set provides the best detection accuracy of 0.980. It is closely followed by SMO which provides the detection accuracy of 0.968. On the other hand, J48 provides the lowest accuracy of 0.947.</p><h2 id="Discovering-Similar-Malware-Samples-Using-API-Call-Topics（2015）"><a href="#Discovering-Similar-Malware-Samples-Using-API-Call-Topics（2015）" class="headerlink" title="Discovering Similar Malware Samples Using API Call Topics（2015）"></a>Discovering Similar Malware Samples Using API Call Topics（2015）</h2><p>This paper aims to extract similar malware samples automatically using the concept of “API call topics,” which represents a set of API calls that are intrinsic to a specific group of malware samples. They present an unsupervised approach to extract API call topics from a large corpus of API calls. Through analysis of the API call logs collected from thousands of malware samples, they demonstrate that the extracted API call topics can detect similar malware samples. The proposed approach is expected to be useful for automating the process of analyzing a huge volume of logs collected from dynamic malware analysis systems.</p><p>The key idea of this paper is that they present an unsupervised approach to extract API call topics which can detect similar malware samples. </p><h3 id="Dataset"><a href="#Dataset" class="headerlink" title="Dataset"></a>Dataset</h3><p>The data of this paper consists of Cuckoo Sandbox logs of 2,641 malware samples collected from September 2012 to March 2013. The format of all malware samples was the Portable Executable (PE) format used in Windows operating systems. These malware samples were executed on Cuckoo Sandbox in 90 seconds and generated 1.7 GB of dynamic analysis logs in JSON format.</p><p>Some interesting they found:</p><ul><li>The majority of the samples had more than 400 API calls, a few samples had only a small number of API calls.</li><li>Malware samples that exhibited a very low number of API calls likely failed to execute correctly because of the anti-VM techniques. Therefore, they pruned malware samples with fewer than 10 API calls. </li></ul><h3 id="Methodologies"><a href="#Methodologies" class="headerlink" title="Methodologies"></a>Methodologies</h3><h4 id="API-words-features"><a href="#API-words-features" class="headerlink" title="API words features"></a>API words features</h4><p>This paper parsed the JSON formatted Cuckoo Sandbox log, then they used three variables(API function names, arguments names, argument values) to convert an API call into an “API word”， and they omitted return values and thread IDs.</p><p>They used level-3 approach to compress the API logs. Its definition is <em>api:n1:v1:n2:v2:…</em>, for example, <em>“LdrGetDllHandle”:”FileName”:”C:nnWINDOWSnnsystem32nnrpcss.dll”:”ModuleHandle”:0x00000000</em>. Using the API words, they generated a feature vector for each malware sample (By BoW approach with TF-IDF word weighting). </p><h4 id="Feature-selection"><a href="#Feature-selection" class="headerlink" title="Feature selection"></a>Feature selection</h4><p>The paper applied DF-thresholding to eliminate very popular API words that appear in a majority of the malware samples. These API words are too generic to characterize a cluster of malware samples. </p><h4 id="Non-negative-Matrix-Factorization-NMF"><a href="#Non-negative-Matrix-Factorization-NMF" class="headerlink" title="Non-negative Matrix Factorization (NMF)"></a>Non-negative Matrix Factorization (NMF)</h4><p>NMF is an algorithm that factorizes a matrix into two matrices with the constraint that all three matrices have no negative elements. The advantage of NMF is that it can extract topics that can then be used as signatures for the task. </p><p>The key idea of NMF is to factorize the feature matrix X, i.e., X = TV.</p><p><img src="6.png" alt="Figure6"></p><p>K is a parameter that controls the number of basis, which corresponds to clusters. The matrices T and V can be interpreted as the topic (basis) and the clustering results, respectively. This factorization can be obtained by minimizing the distance between the matrices X and TV: </p><p><img src="7.png" alt="Figure7"></p><p>The paper adopt Kullback-Leibler(KL) divergence to compute the distance d:</p><p><img src="8.png" alt="Figure8"></p><h3 id="Result"><a href="#Result" class="headerlink" title="Result"></a>Result</h3><p>The API call topics successfully extracted malware samples that had the similar intrinsic API calls. In addition, the detected malware samples were actually similar to each other because the majority of the detected samples have a single primary label in each cluster.</p><p><img src="9.png" alt="Figure9"></p><h2 id="DeepSign-Deep-Learning-for-Automatic-Malware-Signature-Generation-and-Classification（2015）"><a href="#DeepSign-Deep-Learning-for-Automatic-Malware-Signature-Generation-and-Classification（2015）" class="headerlink" title="DeepSign: Deep Learning for Automatic Malware Signature Generation and Classification（2015）"></a>DeepSign: Deep Learning for Automatic Malware Signature Generation and Classification（2015）</h2><p>The method uses a deep belief network (DBN), implemented with a deep stack of denoising autoencoders, generating an invariant compact representation of the malware behavior.</p><h3 id="Feature"><a href="#Feature" class="headerlink" title="Feature"></a>Feature</h3><p>This paper used one of the simplest method of NLP to approach the sandbox files, that is, using unigram(1-gram) extraction. Furthermore, this paper in order to remain as domain agnostic as possible, they propose to treat the sandbox file as a simple text file, and extract unigrams without any preprocessing. That means, they omits all meaningless symbols and only pay attention to the words, for example, given “api”: “CreateFileW”, the terms extracted are “api”: and “CreateFileW”, completely ignoring what each part means. </p><p>In order to get the fixed size features, they proposed that:</p><ol><li>For each sandbox file in the dataset, extract all unigrams.</li><li>Remove the unigrams which appear in all files (contain no information).</li><li>For each unigram count the number of files in which it appears.</li><li>Select top 20,000 with highest frequency.</li><li>Convert each sandbox file to a 20,000 sized bit string, by checking whether each of the 20,000 unigrams appeared in it.</li></ol><p>In their approach, they trained a deep denoising autoencoder consisting of eight layer: 20,000–5,000–2,500–1,000–500–250–100–30. At each step only one layer is trained, then the weights are “frozen”, and the subsequent layer is trained. </p><p>At the end of this training phase, they have a deep network which is capable of converting the 20,000 input vector into 30 floating point values. They regard these 30-sized vector as the “signature” of the program.</p><p>The process of DBN training looks like:</p><p><img src="10.png" alt="Figure10"></p><p>(I) Train the first autoencoder layer. (II) Use the weights from the previous layer, and build a new autoencoder on top of them. The weights taken from the first autoencoder are frozen. (III) Similarly, take the weights from the previous layer and add a new layer. (IV) Proceed with layer-wise training until training all the eight layers for the final DBN.</p><p>The all steps of this paper:</p><p><img src="11.png" alt="Figure11"></p><p>Some configuration:</p><ul><li>Each layer-wise training with a 0.5 dropout.</li><li>Using ReLU as the activation function.</li><li>0.2 noise ratio, 0.001 learning rate then linearly decays to 0.000001, batch size of 20, and no momentum.</li></ul><h3 id="Dataset-1"><a href="#Dataset-1" class="headerlink" title="Dataset"></a>Dataset</h3><p>Their dataset consists of six major categories of malware, and 300 variants in each category, for a total of 1,800 samples. The six malware categories used are Zeus, Carberp, Spy-Eye, Cidox, Andromeda, and DarkComet.</p><h3 id="Experimental-Results"><a href="#Experimental-Results" class="headerlink" title="Experimental Results"></a>Experimental Results</h3><ul><li>SVM with 96.4% accuracy.</li><li>KNN with 95.3% accuracy.</li><li>This paper’s DBN with 98.6% accuracy.</li></ul><h2 id="A-Novel-Approach-to-Detect-Malware-Based-on-API-Call-Sequence-Analysis（2015"><a href="#A-Novel-Approach-to-Detect-Malware-Based-on-API-Call-Sequence-Analysis（2015" class="headerlink" title="A Novel Approach to Detect Malware Based on API Call Sequence Analysis（2015)"></a>A Novel Approach to Detect Malware Based on API Call Sequence Analysis（2015)</h2><p>This paper adopted DNA sequence alignment algorithms and extracted common API call sequence patterns of malicious function from malware in different categories. They indicated that two major approaches in dynamic analysis are control flow analysis and API call analysis. However, malware authors try to circumvent those techniques through inserting meaningless codes or shuffling the sequence of programs.</p><h3 id="DNA-Sequence-Alignment"><a href="#DNA-Sequence-Alignment" class="headerlink" title="DNA Sequence Alignment"></a>DNA Sequence Alignment</h3><p>Sequence alignment algorithms are most widely used in the area of biometrics to calculate the similarity between two or more DNA sequences or to find certain DNA subsequences from full DNA.</p><p>Sequence alignment algorithms fall into two categories, which are global and local alignment. Global alignment aligns entire sequence. It is useful when they attempt to find the sequence of most similar length and strings. Local alignment finds the highly similar subsequences between given sequences. </p><p>Pairwise sequence alignment methods find global alignments or the locally similar subsequences of two sequences. Multiple sequence alignment algorithm extends pairwise alignment to handle multiple sequences at the same time. This paper applied multiple sequence alignment algorithm.</p><h3 id="Environment-Setup"><a href="#Environment-Setup" class="headerlink" title="Environment Setup"></a>Environment Setup</h3><p>In this study, they used the Detours hooking library supported byMicrosoft to trace API call sequences from Windows executable programs. The Detours hooking process proceeds as shown in the following. Unlike the original process without Detours, the hooking process goes through the Detour function and the Trampoline function. This allows them to trace API call sequence.</p><p><img src="12.png" alt="Figure12"></p><p>For DNA sequence alignment, they used the ClustalX. ClustalX is widely used freeware in genome sequence analysis, such as DNA, RNA, or protein sequences. The program supports multiple sequence alignment (MSA) and also provides visualization.</p><p>In their dataset, 2,727 kinds of API were found; they categorized them into 26 groups according to MSDN library:<br>In our dataset, 2,727 kinds of API were found; they categorized them into 26 groups according to MSDN library:</p><p><img src="13.png" alt="Figure13"></p><p>To extract the common API call sequence pattern among malware, the longest common subsequences (LCSs) were used. LCS shows the longest malware API call sequence pattern, it can be treated as a malware’s signature. For example, the LCSs of ABCD and ACB are AB and AC:</p><p>Furthermore, they excluded these typical LCSs reduce false positive error since some benign programs share same API calls sequence. They employed an additional feature. They assumed that there exists a specific API call sequence pattern that matches specific malicious activity. If such an API call sequence pattern exists, they can detect unknown attacks by checking whether a critical API call sequence pattern exists. For example:</p><p><img src="14.png" alt="Figure14"></p><p>It should be noted that round brackets represent the OR relation. For example, in the case of IAT hooking, one possible critical API call sequence pattern is [LoadLibrary, strcmp, VirtualProtect]. They believe that this behavior-based malware classification can be effective in the dynamic analysis of API call sequences.</p><p>The overall process as following:</p><p><img src="15.png" alt="Figure15"></p><p>In the analysis process, the extracted API call sequence of a newly inserted program is compared with the API signatures in the database. If any LCS ismatched with the signatures, the newly inserted program is classified as malware.</p><h3 id="Experiment-result"><a href="#Experiment-result" class="headerlink" title="Experiment result"></a>Experiment result</h3><p>To summarize, the precision is 1, FPR is 0, and recall is 0.998. The 𝐹1 score is 0.999.</p><p><img src="16.png" alt="Figure16"></p><h2 id="MALWARE-CLASSIFICATION-WITH-RECURRENT-NETWORKS-2015"><a href="#MALWARE-CLASSIFICATION-WITH-RECURRENT-NETWORKS-2015" class="headerlink" title="MALWARE CLASSIFICATION WITH RECURRENT NETWORKS(2015)"></a>MALWARE CLASSIFICATION WITH RECURRENT NETWORKS(2015)</h2><p>This paper proposed a different approach, which, similar to natural language modeling, learns the language of malware spoken through the executed instructions and extracts robust, time domain features. Echo state networks (ESNs) and recurrent neural networks (RNNs) are used for the projection stage that extracts the features.</p><p>Specifically, using the recurrent model to directly classify the files is not efficient (there is only a single bit of information for every sequence). Instead, they use a recurrent model trained to predict next API call, and use the hidden state of the model (that encodes the history of past events) as the fixed-length feature vector that is given to a separate classifier (logistic regression or MLP).</p><p>The overall process as following:</p><p><img src="18.png" alt="Figure18"></p><p>ut and ht demote the input and state vectors. At each state, they apply a recurrent neural network operation to the input and get a hidden vector ht. Then they apply a max-pooling to get a feature vector, then, applying a MLP or logistic regression to do the classification on the feature vector</p><p>Finally, they got TPR 71.71% at FPR 0.1%.</p><h2 id="Malware-Characterization-using-Windows-API-Call-Sequences-2016"><a href="#Malware-Characterization-using-Windows-API-Call-Sequences-2016" class="headerlink" title="Malware Characterization using Windows API Call Sequences(2016)"></a>Malware Characterization using Windows API Call Sequences(2016)</h2><p>This paper proposed a hashed method to identify the category of malware. First, they chose a total set of 534 important Win-APIs have been hooked and mapped to 26categories (A…Z). Then they used Fuzzy hashing algorithm ssdeep to generate fuzzy hash based signature. These signatures were matched to quantify the API call sequence homologies between test samples and training samples.</p><p>The overall process as following:</p><p><img src="19.png" alt="Figure19"></p><h3 id="Categorization-of-Win-API-Calls"><a href="#Categorization-of-Win-API-Calls" class="headerlink" title="Categorization of Win-API Calls"></a>Categorization of Win-API Calls</h3><p>This paper have categorized the total set of 534 Win-API calls into 26 Categories based upon the function these APIs are performing.</p><p><img src="20.png" alt="Figure20"></p><h3 id="Fuzzy-Hash-Signatures"><a href="#Fuzzy-Hash-Signatures" class="headerlink" title="Fuzzy Hash Signatures"></a>Fuzzy Hash Signatures</h3><p>This paper applied ssdeep program to compute Context Triggered Piecewise Hash (CTPH), also called fuzzy hash, on the categorized API call sequences.</p><p>The concept of fuzzy hashing has been used as it has the capability to compare two different samples and determine the level of similarity between them. Instead of generating a single hash for a file, piecewise hashing generates many hashes for a file based on different sections of the file. CTPH algorithm uses the rolling hash to determine the start and stop of each segment. CTPH Signature generation algorithm combines these section hashes in a particular way to generate the fuzzy hash of the file.</p><p>They selected this technique because CTPH can match inputs that have homologies and these sequences may be different in both content and length.</p><p><img src="21.png" alt="Figure21"></p><p>Ssdeep matching algorithm calculates the matching between fuzzy hashes of two different samples. This score is based on the edit distance algorithm: Damerau Levenshtein Distance between two strings. After matching a comparison score is generated between 0 and 100. they have used this matching score as malware classification criteria.</p><h3 id="Result-1"><a href="#Result-1" class="headerlink" title="Result"></a>Result</h3><p>Following table gives the classification accuracy and FPR for these five 2-class problems.</p><p><img src="22.png" alt="Figure22"></p><p>In addition, this table gives the accuracy &amp; FPR for 2 class classifier problems (Malware vs Benign).</p><p><img src="23.png" alt="Figure23"></p><h2 id="Deep-Learning-LSTM-based-Ransomeware-Detection-2017"><a href="#Deep-Learning-LSTM-based-Ransomeware-Detection-2017" class="headerlink" title="Deep Learning LSTM based Ransomeware Detection(2017)"></a>Deep Learning LSTM based Ransomeware Detection(2017)</h2><p>This paper aims to detect ransomware behavior by employing Long-Short Term Memory (LSTM) networks for binary sequence classification of API calls. They present an automated approach to extract API calls from the log of modified sandbox environment and detect ransomware behavior.</p><p>Ransomware are malicious software designed to deny access to a computer system or computer resource. There are two categories of ransomware: Crypto ransomware and locker ransomware. Locker ransomware try to lock access to the system user interface. Crypto ransomware on the other hand, deny access to user ata using strong encryption algorithms.</p><h3 id="API-call-extraction"><a href="#API-call-extraction" class="headerlink" title="API call extraction"></a>API call extraction</h3><p>This paper used a cuckoo-modified since it has provided more anti-anti sandbox features than the standard version. Also, the timeout value for analysis has been extended to 20 minutes to defeat delayed execution strategies employed by some malwares authors.</p><p>The overall process as following:</p><p><img src="17.png" alt="Figure17"></p><h3 id="LSTM"><a href="#LSTM" class="headerlink" title="LSTM"></a>LSTM</h3><p>This paper picked up 38 APIs which were common in all the samples. And then labeled them as an integer between 1 and 39. Each process had a different length of API sequence. After the conversion to integer, 0s were appended to the sequences to match the length of the longest sequence of length 779980.</p><p>Finally, they received an experimental accuracy of 96.67%.</p><h2 id="NEURAL-SEQUENTIAL-MALWARE-DETECTION-WITH-PARAMETERS-2018"><a href="#NEURAL-SEQUENTIAL-MALWARE-DETECTION-WITH-PARAMETERS-2018" class="headerlink" title="NEURAL SEQUENTIAL MALWARE DETECTION WITH PARAMETERS(2018)"></a>NEURAL SEQUENTIAL MALWARE DETECTION WITH PARAMETERS(2018)</h2><p>This paper extend previous work, they said the previous LSTM model only considered the API calls sequence but ignored its parameters. So they extend this model to include each system API call’s two most input parameters.</p><p>Due to the variation in the structure and size of the vocabulary, this paper used these parameters in a limited character space. They used these parameters by learning character grams from them. They built n-grams over these characters in the corpus, and only extract the top-K by the frequency distribution.</p><p>The process of constructing character grams as following:</p><p><img src="24.png" alt="Figure24"></p><p>Then by using the following two algorithms, they converted the n-grams to indexed vector.</p><p><img src="25.png" alt="Figure25"></p><p><img src="26.png" alt="Figure26"></p><p>Then they applied tanh to each vectored parameter, finally concatenate each API calls name with its parameters together.</p><p>The overall process as following:</p><p><img src="28.png" alt="Figure28"></p><p>The LSTM process looks like the above paper, which used a maxpooling to extract feature from each stage of LSTM, then applied a logistic regression followed by a ReLU and sigmoid.</p><h3 id="Result-2"><a href="#Result-2" class="headerlink" title="Result"></a>Result</h3><p>They collected the system API calls and their parameters from 75,000 files, which were evenly split between malware and benign files, in September 2017. The data from all of the files is unique, and there is a total of 158 event types in our data.</p><p>Comparison of the ROC curves for two versions of the Events Only LSTM+NN model, the Events Only CNN+LSTM model, and the proposed Events + Parameters model as following:</p><p><img src="29.png" alt="Figure29"></p>]]></content>
    
    <summary type="html">
    
      This post summaries 8 papers on dynamic malware detection domain.
    
    </summary>
    
    
      <category term="Research" scheme="https://joddiy.github.io/categories/Research/"/>
    
    
      <category term="Malware Detection" scheme="https://joddiy.github.io/tags/Malware-Detection/"/>
    
  </entry>
  
  <entry>
    <title>利用子查询优化SQL</title>
    <link href="https://joddiy.github.io/2017/10/13/%E5%88%A9%E7%94%A8%E5%AD%90%E6%9F%A5%E8%AF%A2%E4%BC%98%E5%8C%96SQL/"/>
    <id>https://joddiy.github.io/2017/10/13/%E5%88%A9%E7%94%A8%E5%AD%90%E6%9F%A5%E8%AF%A2%E4%BC%98%E5%8C%96SQL/</id>
    <published>2017-10-13T09:23:36.000Z</published>
    <updated>2020-05-11T07:10:57.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="场景"><a href="#场景" class="headerlink" title="场景"></a>场景</h2><p>A 表中大约有 50 万行数据，其中有一个 a 字段，其上未加索引，不同的 a 有大约 3000 个。</p><h2 id="要求"><a href="#要求" class="headerlink" title="要求"></a>要求</h2><p>指定了 250 个 a ，要求判断其中多少个在 A 表中出现过。</p><h2 id="第一版-SQL"><a href="#第一版-SQL" class="headerlink" title="第一版 SQL"></a>第一版 SQL</h2><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="keyword">DISTINCT</span> a</span><br><span class="line"><span class="keyword">FROM</span> <span class="string">`A`</span></span><br><span class="line"><span class="keyword">WHERE</span> a <span class="keyword">IN</span> (<span class="number">250</span>个指定 a)</span><br></pre></td></tr></table></figure><p>执行速度约 40秒</p><h2 id="第二版-SQL"><a href="#第二版-SQL" class="headerlink" title="第二版 SQL"></a>第二版 SQL</h2><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">FROM</span></span><br><span class="line">    (<span class="keyword">SELECT</span> <span class="keyword">DISTINCT</span> a</span><br><span class="line">        <span class="keyword">FROM</span> <span class="string">`A`</span>) <span class="keyword">AS</span> t</span><br><span class="line"><span class="keyword">WHERE</span> t.a <span class="keyword">IN</span> (<span class="number">250</span>个指定 a)</span><br></pre></td></tr></table></figure><p>执行速度约 0.7秒</p><h2 id="分析原因"><a href="#分析原因" class="headerlink" title="分析原因"></a>分析原因</h2><ol><li>因为 a 上未加索引，直接 where 条件 in 的话会对 50 万行原始数据依次比较是否在 250 个指定 a 中，比较次数为 50万*250 = 1.25 亿次</li><li>如果先 distinct 全部，再子查询 in，只会在 distinct 的 3000 个结果中依次比较 250 个指定 a，比较次数为 50万+3000*250 = 80 万次</li></ol><h2 id="结论"><a href="#结论" class="headerlink" title="结论"></a>结论</h2><p>在未加索引的字段上面进行大量 in 操作，使用子查询先预筛选，可以达到近似索引的效果。</p>]]></content>
    
    <summary type="html">
    
      Some tips for mysql
    
    </summary>
    
    
      <category term="Engineering" scheme="https://joddiy.github.io/categories/Engineering/"/>
    
    
      <category term="研究向" scheme="https://joddiy.github.io/tags/%E7%A0%94%E7%A9%B6%E5%90%91/"/>
    
      <category term="SQL" scheme="https://joddiy.github.io/tags/SQL/"/>
    
  </entry>
  
  <entry>
    <title>PHP下的几种多进程模型</title>
    <link href="https://joddiy.github.io/2017/01/11/PHP%E4%B8%8B%E7%9A%84%E5%87%A0%E7%A7%8D%E5%A4%9A%E8%BF%9B%E7%A8%8B%E6%A8%A1%E5%9E%8B/"/>
    <id>https://joddiy.github.io/2017/01/11/PHP%E4%B8%8B%E7%9A%84%E5%87%A0%E7%A7%8D%E5%A4%9A%E8%BF%9B%E7%A8%8B%E6%A8%A1%E5%9E%8B/</id>
    <published>2017-01-11T09:02:13.000Z</published>
    <updated>2020-05-11T07:03:37.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p><em>此部分为设计背景，可不看</em><br>从16年8月至今我一直都在开发 XiaoMi 内部通用 BI 系统的 ETL 模块，第一个版本在10月初上线，目前还在不断的迭代开发中。整个 ETL 模块大致分成三个部分： </p><ul><li>前端部分：记录 ETL 的配置信息，包括数据源，脚本，执行时参数等</li><li>多进程调度部分：包括触发器和调度器，触发器筛选可被执行的作业并封装成任务，调度器读取任务执行</li><li>ETL 执行模块：拿到任务之后根据相应参数实例化对应的数据抓取模块 E 和存储模块 L，并处理两个模块之间的数据格式转化 T</li></ul><p>ETL 任务分成两种：实时任务和历史任务。设计方案需要满足的要求是：</p><ul><li>实时任务秒级触发</li><li>历史任务实时性要求不高，但同一时间执行的历史任务数目有上限</li><li>实时任务和历史任务出现异常时不应该影响触发器和调度器的运行</li></ul><p>因此，我研究和设计了几种 PHP 多进程模型来满足要求，设计方案为：</p><ul><li>实时任务由触发器直接分裂进程执行，且分裂后触发器应该继续触发其他任务，不应等待进程回收</li><li>历史任务由触发器封装成任务，塞入 Redis 队列，调度器读取 Redis 队列分裂进程执行，当分裂进程达到限制数目后，等待直到有进程被回收。</li></ul><h2 id="设计目的"><a href="#设计目的" class="headerlink" title="设计目的"></a>设计目的</h2><p>两种 PHP 多进程模型：</p><ol><li>父进程不等待子进程退出</li><li>父进程记录子进程个数并及时回收，超过最大限制个数时等待</li></ol><h2 id="一些概念"><a href="#一些概念" class="headerlink" title="一些概念"></a>一些概念</h2><p>在多进程模型中，如果子进程没有及时被父进程显式地回收，会产生僵尸进程问题，导致大量进程ID 和内存被占用。这里介绍一些基本概念：</p><blockquote><p>僵尸进程</p></blockquote><p>如果子进程退出后一直不被父进程显式地获取退出状态，那么就会一直占用进程号，资源也不会被释放</p><blockquote><p>孤儿进程</p></blockquote><p>父进程退出后，子进程还在运行，此时子进程就会被 init 进程（进程号为1）收养，并由 init 进程完成回收工作</p><h2 id="一些函数"><a href="#一些函数" class="headerlink" title="一些函数"></a>一些函数</h2><blockquote><p>int pcntl_fork ( void )</p></blockquote><p>在当前进程当前位置产生分支（子进程），并返回 int 值，父子进程都从 fork 位置继续执行，父进程返回值为子进程ID号，子进程返回值为0，返回值小于0时异常。</p><blockquote><p>int pcntl_wait ( int &amp;$status [, int $options = 0 ] )</p></blockquote><p>wait 函数挂起当前进程的执行直到一个子进程退出或接收到一个信号要求中断当前进程或调用一个信号处理函数。 如果一个子进程在调用此函数时已经退出（俗称僵尸进程），此函数立刻返回。子进程使用的所有系统资源将 被释放。wait3 级别下提供 options 选项：</p><ul><li>WNOHANG -&gt; 如果没有子进程退出立刻返回。</li><li>WUNTRACED -&gt; 子进程已经退出并且其状态未报告时返回。</li></ul><h2 id="模型1"><a href="#模型1" class="headerlink" title="模型1"></a>模型1</h2><p>父进程不等待子进程退出</p><h4 id="方案"><a href="#方案" class="headerlink" title="方案"></a>方案</h4><p>采用两层 fork 模型，首先父进程分裂出一代子进程后，一代子进程再次分裂出二代子进程，一代子进程直接退出，使二代子进程成为孤儿进程，从而被 init 进程收养。此时父进程继续执行其他逻辑不需要关心回收问题，二代子进程结束后会由 init 进程回收。</p><h4 id="结构图"><a href="#结构图" class="headerlink" title="结构图"></a>结构图</h4><p><img src="image1.png" alt="流程图"></p><h4 id="代码"><a href="#代码" class="headerlink" title="代码"></a>代码</h4><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">$pid = pcntl_fork();</span><br><span class="line"><span class="keyword">if</span> ($pid &lt; <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="keyword">die</span>(<span class="string">'Fork the process failed.'</span>);</span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> ($pid == <span class="number">0</span>) &#123;</span><br><span class="line">    closeAll();<span class="comment">//父进程关闭所有连接</span></span><br><span class="line">    $pid = pcntl_fork();</span><br><span class="line">    <span class="keyword">if</span> ($pid &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">die</span>(<span class="string">'Fork the process failed.'</span>);</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> ($pid &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="comment">// 二代父进程直接退出，使二代子进程成为孤儿进程，从而被 init 收养</span></span><br><span class="line">        <span class="keyword">exit</span>(<span class="number">0</span>);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        openAll();<span class="comment">//子进程打开所有连接</span></span><br><span class="line">        run();<span class="comment">// 子进程执行统计</span></span><br><span class="line">        <span class="keyword">exit</span>(<span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    openAll();<span class="comment">//父进程打开所有连接</span></span><br><span class="line">    pcntl_wait($status); <span class="comment">//一代父进程回收二代父进程后继续运行</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="模型2"><a href="#模型2" class="headerlink" title="模型2"></a>模型2</h2><p>父进程记录子进程个数并及时回收，超过最大限制个数时等待</p><h4 id="方案-1"><a href="#方案-1" class="headerlink" title="方案"></a>方案</h4><p>每轮循环先回收已经报告退出状态的子进程（非挂起模式，即如果没有子进程可回收则继续执行），然后判断子进程数目是否超过限制，如果超过则等待1s后继续循环，否则分裂子进程执行统计。</p><h4 id="代码-1"><a href="#代码-1" class="headerlink" title="代码"></a>代码</h4><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">$count = <span class="number">0</span>;</span><br><span class="line">$maxProcesses = <span class="number">20</span>;<span class="comment">//最大子进程数</span></span><br><span class="line"><span class="keyword">while</span> (<span class="keyword">true</span>) &#123;</span><br><span class="line">    <span class="comment">// 非挂起模式下回收已经退出的子进程</span></span><br><span class="line">    <span class="keyword">while</span> (pcntl_wait($status, WNOHANG) &gt; <span class="number">0</span>)</span><br><span class="line">        $count--;</span><br><span class="line">    <span class="comment">// 子进程数目超过限制时等待</span></span><br><span class="line">    <span class="keyword">if</span> ($count &gt;= $maxProcesses) &#123;</span><br><span class="line">        sleep(<span class="number">1</span>);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        $count++;</span><br><span class="line">        closeAll();<span class="comment">//父进程关闭所有连接</span></span><br><span class="line">        $pid = pcntl_fork();</span><br><span class="line">        <span class="keyword">if</span> ($pid &lt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">die</span>(<span class="string">'Fork the process failed.'</span>);</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> ($pid == <span class="number">0</span>) &#123;</span><br><span class="line">            openAll();<span class="comment">//子进程打开所有连接</span></span><br><span class="line">            run();<span class="comment">// 子进程执行统计</span></span><br><span class="line">            <span class="keyword">exit</span>(<span class="number">0</span>);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">             openAll();<span class="comment">//父进程打开所有连接</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="一些关于进程信号通讯的补充"><a href="#一些关于进程信号通讯的补充" class="headerlink" title="一些关于进程信号通讯的补充"></a>一些关于进程信号通讯的补充</h2><blockquote><p>bool pcntl_signal ( int $signo , callable|int $handler [, bool $restart_syscalls = true ] )</p></blockquote><p>为信号 signo 安装一个信号处理器。指明接收到某个特定信号时，执行某个方法。</p><blockquote><p>bool pcntl_signal_dispatch ( void )</p></blockquote><p>检查发送给自己的信号并调用先前 pcntl_signal 安装的信号处理器。</p><blockquote><p>bool posix_kill ( int $pid , int $sig )</p></blockquote><p>向某个进程发送某个特定信号。</p><h4 id="在进程模型中的使用"><a href="#在进程模型中的使用" class="headerlink" title="在进程模型中的使用"></a>在进程模型中的使用</h4><p><em>待补充</em></p><h2 id="备注"><a href="#备注" class="headerlink" title="备注"></a>备注</h2><p>数据库和 Redis 连接在多进程下会产生串流的情况，所以在代码中，父进程分裂前会先关闭所有连接，子进程产生后，父子进程各自再打开新的连接。</p><h2 id="后记"><a href="#后记" class="headerlink" title="后记"></a>后记</h2><p>暂时没想到</p>]]></content>
    
    <summary type="html">
    
      A customed PHP multi-process model
    
    </summary>
    
    
      <category term="Tool" scheme="https://joddiy.github.io/categories/Tool/"/>
    
    
      <category term="PHP" scheme="https://joddiy.github.io/tags/PHP/"/>
    
      <category term="工作向" scheme="https://joddiy.github.io/tags/%E5%B7%A5%E4%BD%9C%E5%90%91/"/>
    
      <category term="模型设计" scheme="https://joddiy.github.io/tags/%E6%A8%A1%E5%9E%8B%E8%AE%BE%E8%AE%A1/"/>
    
  </entry>
  
  <entry>
    <title>PHP下的一种多计划任务守护模型</title>
    <link href="https://joddiy.github.io/2017/01/11/PHP%E4%B8%8B%E7%9A%84%E4%B8%80%E7%A7%8D%E5%A4%9A%E8%AE%A1%E5%88%92%E4%BB%BB%E5%8A%A1%E5%AE%88%E6%8A%A4%E6%A8%A1%E5%9E%8B/"/>
    <id>https://joddiy.github.io/2017/01/11/PHP%E4%B8%8B%E7%9A%84%E4%B8%80%E7%A7%8D%E5%A4%9A%E8%AE%A1%E5%88%92%E4%BB%BB%E5%8A%A1%E5%AE%88%E6%8A%A4%E6%A8%A1%E5%9E%8B/</id>
    <published>2017-01-11T02:18:37.000Z</published>
    <updated>2020-05-11T07:03:06.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p><em>此部分为设计背景，可不看</em><br>从16年8月至今我一直都在开发 XiaoMi 内部通用 BI 系统的 ETL 模块，第一个版本在10月初上线，目前还在不断的迭代开发中。整个 ETL 模块大致分成三个部分： </p><ul><li>前端部分：记录 ETL 的配置信息，包括数据源，脚本，执行时参数等</li><li>多进程调度部分：包括触发器和调度器等，触发器筛选可被执行的作业并封装成任务，调度器读取任务执行</li><li>ETL 执行模块：拿到任务之后根据相应参数实例化对应的数据抓取模块 E 和存储模块 L，并处理两个模块之间的数据格式转化 T</li></ul><p>其中多进程调度模块主要由以下五个子模块组成：</p><ul><li>Trigger：触发器，筛选作业封装任务，对于实时任务直接开启子进程启动 ETL 模块</li><li>Processor：调度器，接收任务（历史任务）指令，开启子进程启动 ETL 模块</li><li>Scavenger：清道夫，清除无用数据</li><li>Sentry：哨兵，接收指令杀死对应进程</li><li>Inspector：质检员，定期抽检数据准确性</li></ul><p>这些子模块都需要进程常驻，所以需要一种合适的守护模型的设计方案。</p><h2 id="设计目的"><a href="#设计目的" class="headerlink" title="设计目的"></a>设计目的</h2><p>设计一种守护模型的方案，保证进程常驻，同时避免单一进程长时间存活带来的一些问题（如 Yii 框架的配置信息无法及时更新）</p><h2 id="设计方案"><a href="#设计方案" class="headerlink" title="设计方案"></a>设计方案</h2><p>一种多个计划任务交替获得执行权的守护模型</p><h2 id="简单的流程图"><a href="#简单的流程图" class="headerlink" title="简单的流程图"></a>简单的流程图</h2><p><img src="flow_chart.png" alt="流程图"></p><ul><li>流程图中有两个计划任务，odd 和 even，假设 odd 已经在执行，even 需要获得执行权</li><li>PID 文件存储了当前正在执行的进程 ID ，对此文件的写入操作需要加文件锁</li><li>odd 进程首先从 PID 文件中获取当前正在执行的 PID ，检查其是否存活，如果不存活则直接写入 PID，并启动任务，否则下一步</li><li>通知读取到的 PID 退出，间隔 1s 循环检查是否已经退出，如果已经退出则写入新的 PID 并启动任务。</li><li>采用平滑退出机制，odd 进程收到退出信号后会在当前正在执行的任务结束后退出。</li></ul><h2 id="源码及分析"><a href="#源码及分析" class="headerlink" title="源码及分析"></a>源码及分析</h2><h4 id="ETLCronCommand-类"><a href="#ETLCronCommand-类" class="headerlink" title="ETLCronCommand 类"></a>ETLCronCommand 类</h4><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * ETL Commend 入口命令</span></span><br><span class="line"><span class="comment"> * Class ETLCronCommand</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@package</span> app\services\etl</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ETLCronCommand</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 启动</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> $pidFile</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> ETLSchedule $schedule</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="function"><span class="keyword">function</span> <span class="title">start</span><span class="params">($pidFile, $schedule)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        $shouldRun = <span class="keyword">true</span>;</span><br><span class="line">        <span class="comment">// 注册信号处理函数，传入 $shouldRun 的地址</span></span><br><span class="line">        pcntl_signal(SIGHUP, <span class="function"><span class="keyword">function</span> <span class="params">($signo)</span> <span class="title">use</span> <span class="params">(&amp;$shouldRun)</span> </span>&#123;</span><br><span class="line">            $shouldRun = <span class="keyword">false</span>;</span><br><span class="line">        &#125;);</span><br><span class="line">        <span class="keyword">if</span> (<span class="keyword">$this</span>-&gt;writePID($pidFile)) &#123;</span><br><span class="line">            <span class="comment">// 收到信号后的下一轮退出</span></span><br><span class="line">            <span class="keyword">while</span> ($shouldRun) &#123;</span><br><span class="line">                <span class="comment">// 检查信号</span></span><br><span class="line">                pcntl_signal_dispatch();</span><br><span class="line">                $schedule-&gt;run();</span><br><span class="line">                sleep(<span class="number">1</span>);</span><br><span class="line">            &#125;</span><br><span class="line">            ULog::stdout(<span class="string">"Schedule has exited."</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 平滑重启</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> $pidFile</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> ETLSchedule $schedule</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">restart</span><span class="params">($pidFile, $schedule)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;stop($pidFile);</span><br><span class="line">        <span class="keyword">while</span> (<span class="keyword">$this</span>-&gt;isAlive($pidFile)) &#123;</span><br><span class="line">            sleep(<span class="number">1</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;start($pidFile, $schedule);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 向文件中写入 PID</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> $pidFile</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> bool</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="function"><span class="keyword">function</span> <span class="title">writePID</span><span class="params">($pidFile)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="comment">// 进程存活</span></span><br><span class="line">        <span class="keyword">if</span> (<span class="keyword">$this</span>-&gt;isAlive($pidFile)) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 否则加锁并写入最新进程号</span></span><br><span class="line">        $fp = fopen($pidFile, <span class="string">'w+'</span>);</span><br><span class="line">        <span class="keyword">if</span> (flock($fp, LOCK_EX | LOCK_NB)) &#123;</span><br><span class="line">            fwrite($fp, posix_getpid());</span><br><span class="line">            fflush($fp);</span><br><span class="line">            flock($fp, LOCK_UN);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            ULog::stdout(<span class="string">"Some other Daemon is running, return for it."</span>);</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        fclose($fp);</span><br><span class="line">        ULog::stdout(<span class="string">"The Schedule is dead, starting a new one."</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 进程是否存活</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> $pidFile</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> bool</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="function"><span class="keyword">function</span> <span class="title">isAlive</span><span class="params">($pidFile)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="comment">// 文件存在 且 进程存活</span></span><br><span class="line">        <span class="keyword">if</span> (file_exists($pidFile)) &#123;</span><br><span class="line">            $pid = file_get_contents($pidFile);</span><br><span class="line">            <span class="keyword">if</span> (!<span class="keyword">empty</span>($pid) &amp;&amp; file_exists(<span class="string">"/proc/$pid"</span>)) &#123;</span><br><span class="line">                ULog::stdout(<span class="string">"The Schedule is still alive, sleep."</span>);</span><br><span class="line">                <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 发送信号通知进程结束</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> $pidFile</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">stop</span><span class="params">($pidFile)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (file_exists($pidFile)) &#123;</span><br><span class="line">            $filePid = file_get_contents($pidFile);</span><br><span class="line">            <span class="keyword">if</span> (file_exists(<span class="string">"/proc/$filePid"</span>)) &#123;</span><br><span class="line">                posix_kill($filePid, SIGHUP);</span><br><span class="line">                ULog::stdout(<span class="string">"Send stop signal successfully."</span>);</span><br><span class="line">                <span class="keyword">return</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        ULog::stdout(<span class="string">"The Schedule is not alive."</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="命令行函数（以-Sentry-为例）"><a href="#命令行函数（以-Sentry-为例）" class="headerlink" title="命令行函数（以 Sentry 为例）"></a>命令行函数（以 Sentry 为例）</h4><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Class EtlController</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@package</span> app\commands</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">EtlController</span> <span class="keyword">extends</span> <span class="title">CommandController</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * restart sentry</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> string $pidFile</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">actionRestartSentry</span><span class="params">($pidFile = <span class="string">'/tmp/sentry.pid'</span>)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        (<span class="keyword">new</span> ETLCronCommand())-&gt;restart($pidFile, <span class="keyword">new</span> ETLSentry());</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * stop sentry</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> string $pidFile</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">actionStopSentry</span><span class="params">($pidFile = <span class="string">'/tmp/sentry.pid'</span>)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        (<span class="keyword">new</span> ETLCronCommand())-&gt;stop($pidFile);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="备注"><a href="#备注" class="headerlink" title="备注"></a>备注</h4><ul><li>所有待守护的子模块需要实现 ETLSchedule 接口，该接口只有一个 run 函数。</li><li>计划任务的配置参数：<ul><li>odd: 1-59/2 * * * * 每奇数分钟启动</li><li>even: */2 * * * * 每偶数分钟启动</li></ul></li></ul><h2 id="后记"><a href="#后记" class="headerlink" title="后记"></a>后记</h2><p>暂时没想到</p>]]></content>
    
    <summary type="html">
    
      A customed PHP crontab model
    
    </summary>
    
    
      <category term="Tool" scheme="https://joddiy.github.io/categories/Tool/"/>
    
    
      <category term="PHP" scheme="https://joddiy.github.io/tags/PHP/"/>
    
      <category term="工作向" scheme="https://joddiy.github.io/tags/%E5%B7%A5%E4%BD%9C%E5%90%91/"/>
    
      <category term="模型设计" scheme="https://joddiy.github.io/tags/%E6%A8%A1%E5%9E%8B%E8%AE%BE%E8%AE%A1/"/>
    
  </entry>
  
</feed>
